# Phase 6 — Native Telegram Web acceptance

## Session and provenance

Date: 2026-09-17, approximately 01:55–02:05 Europe/Kyiv (UTC+03:00).
Actor: A, Валерій Погорєлов / @greensmilemind. Chrome Telegram Web K, actual commands, replies and inline buttons. Browser DOM observations and a rendered screenshot establish native evidence; no callbacks were fabricated and no hidden Telegram state or direct Telegram requests were used.

The existing UAT was resumed without resetting either group. GSD `init.verify-work 6` found two pending groups; the verify-pre API coverage gate passed. Overall remains 0/2 fully accepted groups with partial native evidence and no reproduced product defect. Subjective wording acceptance remains human-owned.

## Runtime preparation

The in-app browser showed a login screen. Chrome had an authenticated Telegram session, confirmed through the account menu as actor A. No new sign-in or account switch was needed.

Existing `gsm_bot-bot-1` and `gsm_bot-postgres-1` were stopped. Started the existing PostgreSQL container and confirmed healthy status. The old bot image lacked `dist/shared/i18n/index.js`. Built the current working tree's bot and migrate images; an initial missing Docker credential-helper PATH failure was corrected by adding the installed Docker/Git directories to the process PATH. Runtime build succeeded. Compose applied `20260916180000_chat_language_preferences`, verified application catalog/queue readiness, and started exactly one bot process. PostgreSQL volume and history were preserved. Existing unrelated working-tree modifications were not changed or committed; this is evidence against the built working tree, not a claim of a clean revision.

## Primary group: GSM_bot_test_group

Verified Web peer `-4358185686` and group title. At 01:59, `/settings` showed English; Europe/Kyiv; Wednesday 14:00; 120 minutes; daily 10:00–21:00; reminders 10:00/16:00; admins only. `/plan_status` reported nobody planning. Roster baseline was B (Valeriii Phorielov / @greenvaleratest) and A.

| Subcase | Native action and observed result |
| --- | --- |
| Default and language navigation | `/settings` showed `Language: English` and `Мова / Language`. Choosing Українська returned the same settings values with Ukrainian text and controls. Switching back returned English. |
| Long access labels | Opened `Змінити доступ до планування`. Screenshot confirmed full readable labels: `Лише адміністратори`, `Учасники попереднього планування`, `Усі в чаті`, and prompt `Хто може запропонувати репетицію?`. |
| Open settings confirmation across switch | Selected the existing admins-only value to open Ukrainian review. Opened a separate `/settings` screen, switched to English, then used the still-open `Залишити поточне значення`. It completed and rendered an English settings dashboard without a stale-action rejection. |
| Old-language prompt and correction | Opened English `Duration`, changed language via the other settings screen to Ukrainian, then sent `abc`. Received `Ой, не вдалося розібрати тривалість. Надішли цілу кількість хвилин, наприклад: 120.` The valid answer `121` produced Ukrainian review showing 120 хвилин → 121 хвилина. |
| Save and restore setting | `Зберегти зміну` persisted 121 and rendered the Ukrainian dashboard. Reopened duration, sent 120, saved, and observed restoration of all baseline values. |
| Roster wording and old confirmation | `/roster` showed `Склад гурту` and A/B. Opened removal for A; target name/username and Ukrainian consequence were correct. Switched to English using settings, then clicked the existing Ukrainian `Видалити учасника`. Result was English `Roster updated` with the expected consequence. |
| Roster add and restoration | Used Telegram's actual Reply action on A's `/roster` message; verified `Reply to Валерій Погорєлов`, then sent `/roster_add`. Bot confirmed A added. Fresh `/roster` at 02:03 showed original A/B identities. |
| Same-language selection | Reopened language chooser and selected already-active English. Returned to English settings without a new chat confirmation message. No notification was observed. This does not claim inspection of Telegram protocol acknowledgement counts. |

Primary group restoration: original English presentation, all original settings and A/B roster verified. No rehearsal created, no role/profile modifications, and no messages deleted. Switching created an explicit English preference where the original presentation used the default; no database deletion was attempted to erase that harmless test history.

## Onboarding group: GSM_bot_test_group_1

The user explicitly authorized this second group during the run. Verified title and Web peer `-5514562341`. Historical messages included an old configuration, so a fresh `/settings` was used to establish current state instead of assuming it was unconfigured. At 02:03 it returned only `Language: English`, `Мова / Language`, and `Continue setup`, confirming the incomplete-configuration surface.

Sent `/setup` and observed initial language choice. Chose Українська before any configuration save. The same card advanced to Ukrainian `Налаштування триває / Крок 1 із 8` requesting a location reply; `Мова / Language` remained available. A later native DOM snapshot captured the toast `Мову змінено на українську.`

The actual attachment menu exposed Photo or Video, Document, Poll and Checklist; no Location control. No direct API or fake location was substituted. Requested a mobile Telegram reply from actor A with a public Kyiv location, not the user's private current location. Full setup, review/save, remaining setup copy and final wording acceptance remain pending.

## Handoff and restoration

The primary group is restored. The second group intentionally retains Ukrainian and actor A's timezone setup draft for continuation; it is still unconfigured. The local PostgreSQL and one Phase 6 bot are running. Chrome test tab is retained for continuation, but reacquire it from a fresh listing if the browser runtime changes.

Next action: inspect the user-sent location reply and current bot step, then complete the remaining Ukrainian setup with native controls. If the draft expires, `/setup` should resume/restart in the saved language. Do not reset existing evidence or infer a pass for unobserved setup steps. After the full wizard, resolve the second group's desired retained configuration and obtain the user's wording acceptance before closing Phase 6.

## Evening continuation — 18:32–18:40 Europe/Kyiv

The first user-sent Dnipro location was in the primary group. Requested the correct secondary destination. The 18:32 `/setup` reported the 30-minute draft expiry in Ukrainian; repeating `/setup` restarted at timezone input in the persisted Ukrainian language. This provides native expiry/restart language-continuity evidence.

At 18:37 the user supplied the location in GSM_bot_test_group_1. The bot proposed Europe/Athens, which was accepted with the native button. The production resolver imports `geo-tz/dist/find-now`; its installed README describes merged zones sharing present/future timekeeping rules. This explains the representative name without claiming a comprehensive geographical timezone audit or classifying it as a localization defect. Precise coordinates are intentionally omitted from this report.

Completed native wizard: step 2 selected Ср; step 3 entered 14:00; step 4 entered 120; step 5 entered 10:00; step 6 entered 21:00. At step 7 an extra text answer `10:00, 16:00` re-rendered the current choice instead of advancing; the latest `Залишити типовий час` button then advanced normally. Step 8 selected `Лише адміністратори`. Every prompt and control was Ukrainian; later steps did not offer an inline language entry. The rendered review screenshot showed readable fields and both save/cancel buttons.

Clicked `Зберегти налаштування` and observed `Налаштування чату збережено` with all chosen values. Fresh `/settings` at 18:39 confirmed the same persisted configuration and Ukrainian navigation. `/roster_add` without a reply produced the expected Ukrainian instruction to reply to a member's message.

Current disposition: native behavioral group H2 passes across the authorized two groups; H1 awaits the user's subjective wording acceptance. Counts: 1/2 groups passed, 1 pending, 0 reported product defects. No additional test rehearsal, roster mutation or profile/role change occurred this evening.

Restoration disclosure: primary fixtures remain restored. Secondary group now retains the newly saved Ukrainian test configuration (Europe/Athens, Wednesday 14:00, 120 minutes, daily 10:00–21:00, reminders 10:00/16:00, admins only), whereas its initial state was unconfigured. No reset/deletion was performed to erase this successful onboarding. User retention disposition is still requested before closure. The earlier location handoff is complete and superseded by this section.
