---
phase: 06-localization-foundation-and-ukrainian-onboarding
plan: "03"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/shared/i18n/index.ts
  - src/shared/i18n/en.ts
  - src/shared/i18n/uk.ts
  - src/telegram/renderers.ts
  - tests/unit/i18n.test.ts
  - src/telegram/keyboards.ts
  - src/telegram/setup-handlers.ts
  - tests/unit/setup.test.ts
  - tests/unit/timezone-prompt-copy.test.ts
  - tests/unit/language-navigation.test.ts
autonomous: true
requirements: ["LANG-01","LANG-03","LANG-04","TEXT-01","L10N-01"]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-04: Final setup review includes a localized language row, and cancelling or expiring setup retains the saved preference."
    - "D-09: Ukrainian setup instructions use informal singular Обери/Надішли."
    - "D-11: The planning-access prompt is Хто може запропонувати репетицію? with Лише адміністратори, Учасники попереднього планування, and Усі в чаті mapped to unchanged policy values."
    - "D-12: Invalid onboarding input receives conversational corrective feedback with an example such as Ой, не вдалося розібрати час. Спробуй так: 19:30."
    - "D-13: An answer to an older-language setup prompt remains valid and the next step uses the current group language."
    - "TEXT-01 empty edge: incomplete setup renders only the current prompt, and valid empty optional collections retain their existing semantic meaning."
    - "L10N-01 empty edge: every declared message key has a nonempty en and uk entry with the same parameter contract; missing required parameters fail type checking."
    - "L10N-01 encoding edge: Ukrainian catalog text round-trips in compiled source and dynamic Unicode parameters preserve their original identity."
    - "Representative background rendering invokes renderMessage with an explicit locale and ordinary data without constructing a Telegram context."
  artifacts:
    - path: src/shared/i18n/index.ts
      provides: "Complete typed bilingual setup projections and wizard responses"
    - path: src/shared/i18n/en.ts
      provides: "Complete typed bilingual setup projections and wizard responses"
    - path: src/shared/i18n/uk.ts
      provides: "Complete typed bilingual setup projections and wizard responses"
    - path: src/telegram/renderers.ts
      provides: "Complete typed bilingual setup projections and wizard responses"
    - path: src/telegram/keyboards.ts
      provides: "Complete typed bilingual setup projections and wizard responses"
    - path: src/telegram/setup-handlers.ts
      provides: "Complete typed bilingual setup projections and wizard responses"
  key_links:
    - from: src/telegram/setup-handlers.ts
      to: src/telegram/renderers.ts
      via: "Current locale passed after awaited setup transition."
    - from: src/shared/i18n/index.ts
      to: src/shared/i18n/uk.ts
      via: "MessageCatalog checks the same keys and parameters as English."
---

<objective>
Complete typed bilingual setup projections and wizard responses.

Purpose: Deliver the scoped onboarding localization behaviors above with existing authorization, scheduling and durable identity guarantees.
Output: src/shared/i18n/en.ts: en catalog; src/shared/i18n/uk.ts: uk catalog; src/shared/i18n/index.ts: expanded MessageParameters, locale formatting helpers; tests/unit/i18n.test.ts; explicit-locale setup/settings renderer and keyboard signatures.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-PATTERNS.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-VALIDATION.md
</context>

## Artifacts this phase produces

- src/shared/i18n/en.ts: en catalog
- src/shared/i18n/uk.ts: uk catalog
- src/shared/i18n/index.ts: expanded MessageParameters, locale formatting helpers
- tests/unit/i18n.test.ts
- explicit-locale setup/settings renderer and keyboard signatures
- tests/unit/i18n.test.ts
- tests/unit/setup.test.ts
- tests/unit/timezone-prompt-copy.test.ts
- tests/unit/language-navigation.test.ts

## Execution contract

Research was explicitly skipped. Use current source/PATTERNS as Level-0 discovery; no new package installs. Preserve unrelated workspace edits. Write English documentation and Ukrainian interface copy. The leading phase tracer is 06-01 task 1; all other tasks expand that verified production path. Run a failing behavioral test before behavior changes, then make it pass. No full reminder delivery, lifecycle translation or historical-message rewriting is included. Run targeted checks after each task and typecheck at the plan boundary; record real outputs, never inferred passes. Integration fixtures deploy committed migrations to disposable PostgreSQL before schema-dependent queries. Per-task file ownership is bounded to five paths.

Scope rationale: the ten listed paths belong to one onboarding localization concern, split into two sequential tasks of exactly five files each. Task 1 establishes catalogs and compatible pure renderer contracts; its unit tests and intermediate typecheck must pass while existing callers remain unchanged. Task 2 migrates setup callers and keyboard contracts, followed by focused tests and typecheck. Keeping these bounded tasks together preserves the catalog-to-setup verification boundary without introducing an additional dependency-only plan.

Staged signature contract: setup/settings renderers and locale formatting helpers introduced in task 1 retain their existing call shapes through backward-compatible overloads or optional trailing locale arguments with English defaults. This includes existing Phase 6 onboarding callers until their owning migration task, as well as untouched Phase 7 callers. Task 2 applies the same compatible contract to settingsDashboardKeyboard, settingsReviewKeyboard, and planningAccessKeyboard, preserving existing arguments and callback behavior. Every migrated caller must explicitly pass the response-time locale: setup in task 2, settings in 06-04, and remaining Phase 6 surfaces in their owning plans. By 06-07, no Phase 6 production caller may rely on a compatibility default; untouched Phase 7 callers may retain that contract.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Render complete onboarding projections from typed catalogs</name>
  <reversibility rating="costly">Shared key/parameter signatures become the localization contract for later phases; changing them requires coordinated callers.</reversibility>
  <files>src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, src/telegram/renderers.ts, tests/unit/i18n.test.ts</files>
  <read_first>src/telegram/renderers.ts; tests/unit/i18n.test.ts; src/telegram/keyboards.ts; src/telegram/setup-handlers.ts; src/telegram/settings-handlers.ts; src/telegram/roster-renderers.ts; src/telegram/roster-handlers.ts; .planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md. Read existing targets before editing; for new paths read the named analogs.</read_first>
  <behavior>Both catalogs typecheck against the common parameter map, setup review shows localized language, every setup step renders English/Ukrainian without Telegram context, and typed misuse is a compile error. Existing setup/settings caller shapes still compile at this task boundary; explicit uk/en renderer inputs produce the selected language.</behavior>
  <action>Apply the staged signature contract above to setup/settings renderers and locale helpers. Compatibility defaults remain temporarily available to unmigrated Phase 6 callers until their owning task, in addition to untouched Phase 7 callers; this staging rule governs the compatibility wording below. Preserve all existing arguments and make every caller migrated in this task pass locale explicitly. Prove both legacy call-shape compatibility and explicit locale behavior before the intermediate typecheck. Extract the existing language catalog entries into en.ts and uk.ts without changing renderMessage's signature. Inventory all setup/settings/roster phrases from source and define complete phrase keys, typed parameter names and values for these phase surfaces, including feedback and button labels. Include language names and shared helpers for onboarding weekdays, 24-hour time, duration/count phrases only where used; retain authoritative IANA/civil time values. Use mapped MessageCatalog contracts for both catalogs, with no silent missing-key English fallback. Extend pure renderers with explicit locale inputs, retaining compatible English defaults for unmigrated Phase 6 callers until their owning task and untouched Phase 7 callers under the staged contract; migrated onboarding calls must pass locale. Per D-04 include language in review and committed summaries where appropriate, with simple localized wording; per D-09/D-11/D-12 use approved Ukrainian text and retain operational recovery meaning. Add one representative background-style render using the same renderMessage function, explicit uk/en and ordinary payload data, without ctx or worker delivery changes. Test @ts-expect-error cases for unknown keys/missing or wrong parameters through the existing typecheck command and runtime nonempty/key equality. Keep output escaping owned exactly once by the projection layer and distinguish safe dynamic values from trusted phrase templates.</action>
  <acceptance_criteria>Typed en/uk catalogs cover onboarding phrases and pure projections render both languages with explicit context-free locale inputs. Both catalogs typecheck against the common parameter map, setup review shows localized language, every setup step renders English/Ukrainian without Telegram context, and typed misuse is a compile error.</acceptance_criteria>
  <verify><automated>npm run test:unit -- tests/unit/i18n.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <done>Typed en/uk catalogs cover onboarding phrases and pure projections render both languages with explicit context-free locale inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete localized setup buttons, input and recovery routes</name>
  <files>src/telegram/keyboards.ts, src/telegram/setup-handlers.ts, tests/unit/setup.test.ts, tests/unit/timezone-prompt-copy.test.ts, tests/unit/language-navigation.test.ts</files>
  <read_first>src/telegram/keyboards.ts; src/telegram/setup-handlers.ts; tests/unit/setup.test.ts; tests/unit/timezone-prompt-copy.test.ts; src/shared/i18n/index.ts; src/shared/i18n/en.ts; src/shared/i18n/uk.ts; src/domain/chat/setup-service.ts; src/telegram/renderers.ts; .planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md. Read existing targets before editing; for new paths read the named analogs.</read_first>
  <behavior>Every setup step and timezone candidate renders selected-language text and controls; invalid time, resolver failure, stale/expired draft, save/cancel outcomes retain their meaning; older-language responses progress in current language. settingsDashboardKeyboard, settingsReviewKeyboard, and planningAccessKeyboard accept explicit locale while their not-yet-migrated settings call shapes still typecheck.</behavior>
  <action>Stage settingsDashboardKeyboard, settingsReviewKeyboard, and planningAccessKeyboard using the compatible locale signatures defined above. Migrate every setup renderer/helper/keyboard call to pass response-time locale explicitly; retain English-default compatibility for settings callers until 06-04 migrates them. Verify that both task boundaries compile without editing settings-handlers.ts outside its owning plan. Replace English-only button constants with locale-aware factories while preserving exported compatibility where untouched callers require it. Translate weekday/reminder/access/save/cancel labels using catalog entries; retain action values, long access-policy one-choice-per-row layout and callback sizes. Resolve LanguageService.resolve(chatId) at each setup send/answer after awaited state transitions, including text and location candidates, so per D-13 an older-language prompt can advance under normal validity rules. Translate timezone privacy-mode instructions, candidate selection, all eight setup steps, invalid-input examples, duplicate/success/failure/cancel/expiry recovery. Per D-02/D-04 keep language independent of cancellation/expiry; per D-03 show language navigation only on timezone, and per D-09/D-11/D-12 use catalog wording. Test both supported locales, old-prompt/new-language replies, saved values, exact approved labels, English regression and location privacy instructions. Update existing focused test fake preferences locally so absent preference is represented explicitly, not by skipping assertions.</action>
  <acceptance_criteria>A user can finish, correct, cancel or resume the entire setup flow in Ukrainian, preserving existing scheduling and authorization behavior. Every setup step and timezone candidate renders selected-language text and controls; invalid time, resolver failure, stale/expired draft, save/cancel outcomes retain their meaning; older-language responses progress in current language.</acceptance_criteria>
  <verify><automated>npm run test:unit -- tests/unit/setup.test.ts tests/unit/timezone-prompt-copy.test.ts tests/unit/language-navigation.test.ts tests/unit/i18n.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <done>A user can finish, correct, cancel or resume the entire setup flow in Ukrainian, preserving existing scheduling and authorization behavior.</done>
</task>
</tasks>

<threat_model>
ASVS level 1; block on unresolved high or critical threats.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update → controller | Actor, chat and opaque action are untrusted; current role and stored validity remain authoritative. |
| Controller → PostgreSQL | Chat-keyed preference and domain records have separate ownership and mutation paths. |
| Catalog/data → Telegram output | Dynamic member values retain existing escaping and output budgets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-03-01 | Spoofing/Elevation | Onboarding controls | high | mitigate | Retain fresh role, chat/actor binding, expiry/consumption checks; assert denied writes and exactly one ack in affected route tests. |
| T-06-03-02 | Tampering | Preference/domain state | high | mitigate | Resolve by bigint chat identity, validate en/uk, isolate locale writes from revisions and schedule generation, verify state equality around switching. |
| T-06-03-03 | Information disclosure | Rendered dynamic data | medium | mitigate | Escape once at projection boundary, retain Unicode/UTF-16 budgets and scoped logs; assert hostile-name payloads. |
| T-06-03-04 | Denial of service | Telegram callback output | medium | mitigate | Preserve callback fallback and bounds; reject unsupported targets through normal controlled outcomes. |
</threat_model>

<verification>
npm run test:unit -- tests/unit/i18n.test.ts && npm run typecheck
npm run test:unit -- tests/unit/setup.test.ts tests/unit/timezone-prompt-copy.test.ts tests/unit/language-navigation.test.ts tests/unit/i18n.test.ts
npm run typecheck
Use 06-VALIDATION.md for source coverage and phase-level checks. PostgreSQL suites may exceed 60 seconds during container startup; stream progress rather than replace real database proof with mocks.
</verification>

<success_criteria>
All must-have truths have direct behavioral evidence; task commands pass, English behavior remains supported, and no high/critical threat is left open within scope.
</success_criteria>

<output>
Create `.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-03-SUMMARY.md` with implementation and verification evidence.
</output>
