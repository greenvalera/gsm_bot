---
phase: 06-localization-foundation-and-ukrainian-onboarding
plan: "04"
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - src/telegram/settings-handlers.ts
  - tests/unit/settings-localization.test.ts
  - tests/unit/settings-dashboard-keyboard.test.ts
  - src/shared/i18n/en.ts
  - src/shared/i18n/uk.ts
  - tests/unit/settings.test.ts
  - tests/unit/language-navigation.test.ts
autonomous: true
requirements: ["LANG-02","LANG-04","TEXT-01"]
estimate:
  tokens: 21000
  raw_tokens: 21000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-15: Language-only changes leave an already-open schedule confirmation valid; it commits under existing revision rules and responds in current language."
    - "Settings prompts, dashboard labels, summaries, buttons and feedback render Ukrainian for a Ukrainian group and retain English behavior."
    - "D-05/D-08: Changed selection confirms in the new language; same explicit locale returns silently with exactly one acknowledgement."
    - "Locale-only writes leave configuration revision, setup/settings draft contents, reminder generation and due times unchanged."
  artifacts:
    - path: src/telegram/settings-handlers.ts
      provides: "Complete Ukrainian settings with safe open edits"
    - path: src/shared/i18n/en.ts
      provides: "Complete Ukrainian settings with safe open edits"
    - path: src/shared/i18n/uk.ts
      provides: "Complete Ukrainian settings with safe open edits"
  key_links:
    - from: src/telegram/settings-handlers.ts
      to: src/telegram/renderers.ts
      via: "Current locale passed for dashboard/edit/review outcomes."
    - from: src/telegram/settings-handlers.ts
      to: src/domain/chat/settings-service.ts
      via: "Existing draft expectedRevision retained across locale-only changes."
---

<objective>
Complete Ukrainian settings with safe open edits.

Purpose: Deliver the scoped onboarding localization behaviors above with existing authorization, scheduling and durable identity guarantees.
Output: locale-aware settings controller responses; tests/unit/settings-localization.test.ts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-PATTERNS.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-VALIDATION.md
</context>

## Artifacts this phase produces

- locale-aware settings controller responses
- tests/unit/settings-localization.test.ts
- tests/unit/settings-localization.test.ts
- tests/unit/settings-dashboard-keyboard.test.ts
- tests/unit/settings.test.ts
- tests/unit/language-navigation.test.ts

## Execution contract

Research was explicitly skipped. Use current source/PATTERNS as Level-0 discovery; no new package installs. Preserve unrelated workspace edits. Write English documentation and Ukrainian interface copy. The leading phase tracer is 06-01 task 1; all other tasks expand that verified production path. Run a failing behavioral test before behavior changes, then make it pass. No full reminder delivery, lifecycle translation or historical-message rewriting is included. Run targeted checks after each task and typecheck at the plan boundary; record real outputs, never inferred passes. Integration fixtures deploy committed migrations to disposable PostgreSQL before schema-dependent queries. Per-task file ownership is bounded to five paths.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Localize all schedule settings interactions</name>
  <files>src/telegram/settings-handlers.ts, tests/unit/settings-localization.test.ts, tests/unit/settings-dashboard-keyboard.test.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts</files>
  <read_first>src/telegram/settings-handlers.ts; tests/unit/settings-localization.test.ts; tests/unit/settings-dashboard-keyboard.test.ts; src/domain/chat/settings-service.ts; src/telegram/renderers.ts; src/telegram/keyboards.ts; src/domain/chat/language-service.ts; tests/unit/schedule-settings.test.ts; .planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md. Read existing targets before editing; for new paths read the named analogs.</read_first>
  <behavior>Every editable settings field renders Ukrainian prompts/reviews and controls; success, keep, duplicate, invalid values and timezone failure feedback use current locale; language-only settings remain usable before configuration.</behavior>
  <action>Thread explicit current locale through dashboard, edit, timezone candidates and review projections on command/callback/text/location paths. Replace all settings-owned English feedback with typed catalogs, adding full phrases to both catalog implementations when a source branch was missed. Keep D-05/D-06/D-07/D-08 language behavior separate from SettingsEditDraft save/keep and expectedRevision. Retain current schedule validation, warnings, unavailable/expired recovery destinations and row layouts. Use informal instructions and corrective examples per D-09/D-12. Add a table-driven test across SettingsField variants, both locales and operational outcomes. Ensure each send resolves current preference after awaited service operations.</action>
  <acceptance_criteria>Configured and incomplete settings are fully usable in Ukrainian without partial schedule fabrication or changed business rules. Every editable settings field renders Ukrainian prompts/reviews and controls; success, keep, duplicate, invalid values and timezone failure feedback use current locale; language-only settings remain usable before configuration.</acceptance_criteria>
  <verify><automated>npm run test:unit -- tests/unit/settings-localization.test.ts tests/unit/settings-dashboard-keyboard.test.ts tests/unit/schedule-settings.test.ts</automated></verify>
  <done>Configured and incomplete settings are fully usable in Ukrainian without partial schedule fabrication or changed business rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify switching while a settings edit is open</name>
  <files>tests/unit/settings.test.ts, tests/unit/settings-localization.test.ts, tests/unit/language-navigation.test.ts</files>
  <read_first>tests/unit/settings.test.ts; tests/unit/settings-localization.test.ts; src/telegram/settings-handlers.ts; src/domain/chat/settings-service.ts; src/domain/chat/language-service.ts; src/telegram/language-handlers.ts; .planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md. Read existing targets before editing; for new paths read the named analogs.</read_first>
  <behavior>An English edit can be answered/confirmed after switching to Ukrainian; language-only changes preserve expectedRevision, draft values, expiry and ownership, while genuinely stale schedule revisions still reject.</behavior>
  <action>Extend the actual controller harness with an English opened edit followed by another valid language choice and then text/location/select/save/keep replies. Assert D-13 next-step language and D-15 original confirmation validity; compare schedule revision, draft identity and values immediately around the language mutation. Include reverse switching, expired edit, another actor's Continue setup and same-language no-op. Keep existing conflict/expiry tests meaningful: only language changes preserve validity; genuine domain conflicts still reject. Record English-regression expectations for unchanged settings semantics rather than broadly replacing snapshots.</action>
  <acceptance_criteria>Open edits survive language-only changes and genuine domain conflicts remain protected. An English edit can be answered/confirmed after switching to Ukrainian; language-only changes preserve expectedRevision, draft values, expiry and ownership, while genuinely stale schedule revisions still reject.</acceptance_criteria>
  <verify><automated>npm run test:unit -- tests/unit/settings.test.ts tests/unit/settings-localization.test.ts tests/unit/language-navigation.test.ts</automated></verify>
  <done>Open edits survive language-only changes and genuine domain conflicts remain protected.</done>
</task>
</tasks>

<threat_model>
ASVS level 1; block on unresolved high or critical threats.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update → controller | Actor, chat and opaque action are untrusted; current role and stored validity remain authoritative. |
| Controller → PostgreSQL | Chat-keyed preference and domain records have separate ownership and mutation paths. |
| Catalog/data → Telegram output | Dynamic member values retain existing escaping and output budgets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-04-01 | Spoofing/Elevation | Onboarding controls | high | mitigate | Retain fresh role, chat/actor binding, expiry/consumption checks; assert denied writes and exactly one ack in affected route tests. |
| T-06-04-02 | Tampering | Preference/domain state | high | mitigate | Resolve by bigint chat identity, validate en/uk, isolate locale writes from revisions and schedule generation, verify state equality around switching. |
| T-06-04-03 | Information disclosure | Rendered dynamic data | medium | mitigate | Escape once at projection boundary, retain Unicode/UTF-16 budgets and scoped logs; assert hostile-name payloads. |
| T-06-04-04 | Denial of service | Telegram callback output | medium | mitigate | Preserve callback fallback and bounds; reject unsupported targets through normal controlled outcomes. |
</threat_model>

<verification>
npm run test:unit -- tests/unit/settings-localization.test.ts tests/unit/settings-dashboard-keyboard.test.ts tests/unit/schedule-settings.test.ts
npm run test:unit -- tests/unit/settings.test.ts tests/unit/settings-localization.test.ts tests/unit/language-navigation.test.ts
npm run typecheck
Use 06-VALIDATION.md for source coverage and phase-level checks. PostgreSQL suites may exceed 60 seconds during container startup; stream progress rather than replace real database proof with mocks.
</verification>

<success_criteria>
All must-have truths have direct behavioral evidence; task commands pass, English behavior remains supported, and no high/critical threat is left open within scope.
</success_criteria>

<output>
Create `.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-04-SUMMARY.md` with implementation and verification evidence.
</output>
