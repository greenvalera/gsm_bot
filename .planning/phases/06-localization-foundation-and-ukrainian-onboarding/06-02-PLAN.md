---
phase: 06-localization-foundation-and-ukrainian-onboarding
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/domain/chat/language-service.ts
  - src/telegram/language-handlers.ts
  - src/shared/callback-schema.ts
  - src/telegram/callbacks.ts
  - tests/unit/language-selection.test.ts
  - src/telegram/setup-handlers.ts
  - src/telegram/settings-handlers.ts
  - tests/unit/language-navigation.test.ts
  - tests/integration/localization-tracer.test.ts
autonomous: true
requirements: ["LANG-01","LANG-02","LANG-03","LANG-04","LANG-05"]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: First unconfigured /setup shows English / Українська in an English language screen; choosing persists immediately and advances directly to timezone."
    - "D-02: Repeated /setup uses the persisted language and resumes a valid actor-owned draft."
    - "D-03: Timezone setup offers language change, and /settings supports changing language before setup completes."
    - "D-05: A different choice applies immediately, returns to settings and confirms in the newly selected language."
    - "D-06: Incomplete /settings presents current language, language change and Continue setup without a schedule dashboard."
    - "D-07: The navigation entry is always Мова / Language; options are always English / Українська."
    - "D-08: Selecting the current language returns to settings with one callback acknowledgement and no extra confirmation or repeated preference mutation."
    - "D-14: The last explicit still-valid selection wins across administrators; ordinary authorization, actor, chat, expiry and consumption checks remain effective."
    - "D-16: Continue setup preserves the acting administrator's valid draft values and uses current language; expiration restarts setup with saved language."
    - "LANG-02 adjacency edge: selecting an already explicit current locale is a successful no-op with stable updatedAt."
    - "LANG-02 empty edge: an unconfigured chat can change language through settings without creating configuration."
    - "LANG-02 ordering edge: independent valid screens apply in accepted mutation order."
    - "LANG-04 ordering edge: a group's latest accepted choice affects subsequent rendering only in that group."
  artifacts:
    - path: src/domain/chat/language-service.ts
      provides: "Make administrator language choices durable and reachable"
    - path: src/telegram/language-handlers.ts
      provides: "Make administrator language choices durable and reachable"
    - path: src/shared/callback-schema.ts
      provides: "Make administrator language choices durable and reachable"
    - path: src/telegram/callbacks.ts
      provides: "Make administrator language choices durable and reachable"
    - path: src/telegram/setup-handlers.ts
      provides: "Make administrator language choices durable and reachable"
    - path: src/telegram/settings-handlers.ts
      provides: "Make administrator language choices durable and reachable"
  key_links:
    - from: src/telegram/callbacks.ts
      to: src/telegram/language-handlers.ts
      via: "SETTINGS_EDIT language targets dispatch after current-admin and ordinary token checks."
    - from: src/telegram/language-handlers.ts
      to: src/domain/chat/language-service.ts
      via: "Atomic select followed by current-locale outcome."
---

<objective>
Make administrator language choices durable and reachable.

Purpose: Deliver the scoped onboarding localization behaviors above with existing authorization, scheduling and durable identity guarantees.
Output: src/domain/chat/language-service.ts: LanguageService.resolve, LanguageService.select; src/telegram/language-handlers.ts: renderLanguageSelection, dispatchLanguageCallback; src/shared/callback-schema.ts: createLanguageTarget, parseLanguageTarget, LanguageTarget; tests/unit/language-selection.test.ts; tests/unit/language-navigation.test.ts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-PATTERNS.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-VALIDATION.md
</context>

## Artifacts this phase produces

- src/domain/chat/language-service.ts: LanguageService.resolve, LanguageService.select
- src/telegram/language-handlers.ts: renderLanguageSelection, dispatchLanguageCallback
- src/shared/callback-schema.ts: createLanguageTarget, parseLanguageTarget, LanguageTarget
- tests/unit/language-selection.test.ts
- tests/unit/language-navigation.test.ts
- tests/unit/language-selection.test.ts
- tests/unit/language-navigation.test.ts
- tests/integration/localization-tracer.test.ts

## Execution contract

Research was explicitly skipped. Use current source/PATTERNS as Level-0 discovery; no new package installs. Preserve unrelated workspace edits. Write English documentation and Ukrainian interface copy. The leading phase tracer is 06-01 task 1; all other tasks expand that verified production path. Run a failing behavioral test before behavior changes, then make it pass. No full reminder delivery, lifecycle translation or historical-message rewriting is included. Run targeted checks after each task and typecheck at the plan boundary; record real outputs, never inferred passes. Integration fixtures deploy committed migrations to disposable PostgreSQL before schema-dependent queries. Per-task file ownership is bounded to five paths.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Accept durable language callbacks through existing authority boundary</name>
  <files>src/domain/chat/language-service.ts, src/telegram/language-handlers.ts, src/shared/callback-schema.ts, src/telegram/callbacks.ts, tests/unit/language-selection.test.ts</files>
  <read_first>src/domain/chat/language-service.ts; src/telegram/language-handlers.ts; src/shared/callback-schema.ts; src/telegram/callbacks.ts; src/shared/i18n/index.ts; src/domain/chat/setup-service.ts; src/telegram/handlers.ts; src/shared/chat-coordinator.ts; .planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md. Read existing targets before editing; for new paths read the named analogs.</read_first>
  <behavior>Fresh administrator choices persist en/uk; two independently valid screens can change it sequentially; already-explicit same-language selection leaves preference timestamps unchanged. Unsupported target, foreign actor/chat, expired/consumed token and lost admin role fail closed with exactly one acknowledgement.</behavior>
  <action>Create LanguageService(prisma) with resolve(chatId) returning locale and explicit-selection state, and select(chatId, locale, now) returning changed/unchanged and the persisted locale. An implicit English→explicit English first choice must persist the selection marker once; subsequently selecting the current explicit locale is a no-write no-op. Use existing coordinator/transaction conventions to order accepted writes without coupling to configuration revision. Define strict stored LanguageTarget variants for open/select/continue-setup, fixed locale en/uk and return destination setup/settings. Reuse existing SETTINGS_EDIT callback kind with disjoint language action names, validated by parseLanguageTarget before normal settings dispatch; no enum/schema expansion. Keep opaque v1 tokens and current-admin/strict actor routes. Put dispatch and token minting in language-handlers.ts, constructing LanguageService from already-injected prisma so focused registrations need no global state. Resolve locale again after writes and immediately before rendering feedback. Language tokens must not compare a language revision or invalidate another screen. Token consumption and preference mutation are atomic/idempotent; rejected writes produce localized recovery feedback. Use initial typed catalog keys from plan 01, and preserve the branch-owned ack plus fallback per D-08/D-14. open/continue targets delegate to existing setup/settings entry functions through narrow callbacks to avoid cyclic imports.</action>
  <acceptance_criteria>Supported language choices use durable opaque actions with fresh authorization, idempotence and last-valid-selection behavior. Fresh administrator choices persist en/uk; two independently valid screens can change it sequentially; already-explicit same-language selection leaves preference timestamps unchanged. Unsupported target, foreign actor/chat, expired/consumed token and lost admin role fail closed with exactly one acknowledgement.</acceptance_criteria>
  <verify><automated>npm run test:unit -- tests/unit/language-selection.test.ts</automated></verify>
  <done>Supported language choices use durable opaque actions with fresh authorization, idempotence and last-valid-selection behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connect first setup, timezone navigation and incomplete settings</name>
  <files>src/telegram/setup-handlers.ts, src/telegram/settings-handlers.ts, src/telegram/language-handlers.ts, tests/unit/language-navigation.test.ts, tests/integration/localization-tracer.test.ts</files>
  <read_first>src/telegram/setup-handlers.ts; src/telegram/settings-handlers.ts; src/telegram/language-handlers.ts; tests/integration/localization-tracer.test.ts; src/domain/chat/setup-service.ts; src/telegram/handlers.ts; tests/unit/setup.test.ts; tests/unit/settings.test.ts; .planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md. Read existing targets before editing; for new paths read the named analogs.</read_first>
  <behavior>First /setup asks once, selecting advances to timezone, repeated /setup and Continue setup retain values, expired drafts restart in saved language, configured legacy groups remain English. Both complete/incomplete settings expose the bilingual entry.</behavior>
  <action>Wire the language handler factories to existing setup/settings entry points using their current injected dependencies. Per D-01/D-02 gate only unconfigured chats lacking an explicit selection, without creating or treating preferences as configuration. Per D-03 add the bilingual entry on timezone only; keep later setup steps unchanged until their normal settings route. Per D-05/D-06/D-07 render language-only settings with current-language row, Мова / Language and localized Continue setup before configuration; append the row/control to a configured dashboard without partial schedule casting. On selection invoke the correct return destination immediately, and emit changed confirmation only for an actual locale difference, in the new locale. Per D-08 silence means no extra feedback, not missing acknowledgement. Per D-16 Continue setup calls beginOrResume for the acting administrator; never adopt another actor's draft. Enrich the composed PostgreSQL tracer to choose Ukrainian via real minted callback, then /settings, service reconstruction and repeated /setup. First English selection is recorded even though its displayed default matches, with no spurious changed confirmation.</action>
  <acceptance_criteria>First setup and both settings states have usable persisted administrator language controls, with draft ownership and no repeated mutation. First /setup asks once, selecting advances to timezone, repeated /setup and Continue setup retain values, expired drafts restart in saved language, configured legacy groups remain English. Both complete/incomplete settings expose the bilingual entry.</acceptance_criteria>
  <verify><automated>npm run test:unit -- tests/unit/language-navigation.test.ts tests/unit/language-selection.test.ts &amp;&amp; npm run test:integration -- tests/integration/localization-tracer.test.ts</automated></verify>
  <done>First setup and both settings states have usable persisted administrator language controls, with draft ownership and no repeated mutation.</done>
</task>
</tasks>

<threat_model>
ASVS level 1; block on unresolved high or critical threats.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update → controller | Actor, chat and opaque action are untrusted; current role and stored validity remain authoritative. |
| Controller → PostgreSQL | Chat-keyed preference and domain records have separate ownership and mutation paths. |
| Catalog/data → Telegram output | Dynamic member values retain existing escaping and output budgets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-02-01 | Spoofing/Elevation | Onboarding controls | high | mitigate | Retain fresh role, chat/actor binding, expiry/consumption checks; assert denied writes and exactly one ack in affected route tests. |
| T-06-02-02 | Tampering | Preference/domain state | high | mitigate | Resolve by bigint chat identity, validate en/uk, isolate locale writes from revisions and schedule generation, verify state equality around switching. |
| T-06-02-03 | Information disclosure | Rendered dynamic data | medium | mitigate | Escape once at projection boundary, retain Unicode/UTF-16 budgets and scoped logs; assert hostile-name payloads. |
| T-06-02-04 | Denial of service | Telegram callback output | medium | mitigate | Preserve callback fallback and bounds; reject unsupported targets through normal controlled outcomes. |
</threat_model>

<verification>
npm run test:unit -- tests/unit/language-selection.test.ts
npm run test:unit -- tests/unit/language-navigation.test.ts tests/unit/language-selection.test.ts && npm run test:integration -- tests/integration/localization-tracer.test.ts
npm run typecheck
Use 06-VALIDATION.md for source coverage and phase-level checks. PostgreSQL suites may exceed 60 seconds during container startup; stream progress rather than replace real database proof with mocks.
</verification>

<success_criteria>
All must-have truths have direct behavioral evidence; task commands pass, English behavior remains supported, and no high/critical threat is left open within scope.
</success_criteria>

<output>
Create `.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-02-SUMMARY.md` with implementation and verification evidence.
</output>
