---
phase: 05-proactive-reliable-reminders
plan: "09"
type: execute
wave: 9
depends_on: ["05-08"]
files_modified: ["src/domain/chat/migration-service.ts","src/domain/reminders/reminder-service.ts","tests/integration/chat-migration.test.ts","tests/integration/reminder-migration.test.ts","src/app/main.ts","src/infrastructure/jobs/reminder-queue.ts","tests/integration/reminder-runtime.test.ts"]
autonomous: true
requirements: ["REM-05","RELI-02","RELI-03"]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Migration carries durable reminder suppression and attempt spacing to the canonical chat; old-chat payloads cannot send to either a stale chat or the wrong successor round."
    - "Shutdown stops new claims, safely drains or records in-flight attempts, and closes queue before Prisma; partial startup closes acquired resources."
    - "Reconciliation repairs lost wakeups and processes bounded batches without requiring runtime schema changes."
  artifacts:
    - path: "src/domain/chat/migration-service.ts"
      provides: "Preserve reminder state through group migration and runtime shutdown"
    - path: "src/domain/reminders/reminder-service.ts"
      provides: "Preserve reminder state through group migration and runtime shutdown"
    - path: "tests/integration/chat-migration.test.ts"
      provides: "Preserve reminder state through group migration and runtime shutdown"
    - path: "tests/integration/reminder-migration.test.ts"
      provides: "Preserve reminder state through group migration and runtime shutdown"
  key_links:
    - from: "src/domain/chat/migration-service.ts"
      to: "src/domain/reminders/reminder-service.ts"
      via: "canonical chat move preserves terminal history and invalidates old-generation work"
---

<objective>
Preserve reminder state through group migration and runtime shutdown.
Purpose: Deliver the Phase 5 goal without weakening D-01–D-18 or the completed lifecycle.
Output: src/domain/chat/migration-service.ts, src/domain/reminders/reminder-service.ts, tests/integration/chat-migration.test.ts, tests/integration/reminder-migration.test.ts, src/app/main.ts, src/infrastructure/jobs/reminder-queue.ts, tests/integration/reminder-runtime.test.ts
</objective>

<execution_context>
@C:/dev/gsm_bot/.codex/gsd-core/workflows/execute-plan.md
@C:/dev/gsm_bot/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-proactive-reliable-reminders/05-CONTEXT.md
@.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md
@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md
@.planning/phases/05-proactive-reliable-reminders/05-COVERAGE.md
@.planning/phases/05-proactive-reliable-reminders/05-08-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols/contracts: Migration moves for ChatReminderState/ReminderOccurrence; runtime start/stop admission and drain contract.

- `src/domain/chat/migration-service.ts`
- `src/domain/reminders/reminder-service.ts`
- `tests/integration/chat-migration.test.ts`
- `tests/integration/reminder-migration.test.ts`
- `src/app/main.ts`
- `src/infrastructure/jobs/reminder-queue.ts`
- `tests/integration/reminder-runtime.test.ts`

<tasks>
<task type="auto" tdd="true">
<name>05-09-01: Migrate reminder identities with the existing chat transaction</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/domain/chat/migration-service.ts (if present); @src/domain/reminders/reminder-service.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/domain/chat/migration-service.ts, src/domain/reminders/reminder-service.ts, tests/integration/chat-migration.test.ts, tests/integration/reminder-migration.test.ts</files>
<behavior>Migrated chats preserve quiet periods and idempotency while stale queued work is harmless. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Extend the existing migration transaction's explicit table moves to ChatReminderState and ReminderOccurrence; preserve quiet boundaries, terminal dispositions and round spacing. Resolve source/destination row and occurrence collisions deterministically under both chat keys: consume duplicate terminal identities conservatively, retain maximum no-earlier-than quiet/spacing boundaries and generate a new future-only schedule generation; do not reset potential-send history. Invalidate old pending queue work and callback capability through existing tombstone semantics. Resolve queued old-chat payload to canonical identity before reload, but never reinterpret an occurrence's immutable round as a successor. Migration clears availability anchor and requires fresh acknowledged recovery/grace per Plan07. Test duplicate migration updates, existing destination, queued source reminder, consumed reservation and current-card recovery.</action>
<acceptance_criteria>Migrated chats preserve quiet periods and idempotency while stale queued work is harmless.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/reminder-migration.test.ts tests/integration/chat-migration.test.ts</automated></verify>
<done>Migrated chats preserve quiet periods and idempotency while stale queued work is harmless.</done>
</task>

<task type="auto" tdd="true">
<name>05-09-02: Finish startup, bounded recovery and teardown behavior</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/app/main.ts (if present); @src/infrastructure/jobs/reminder-queue.ts (if present); @src/domain/reminders/reminder-service.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/app/main.ts, src/infrastructure/jobs/reminder-queue.ts, src/domain/reminders/reminder-service.ts, tests/integration/reminder-runtime.test.ts</files>
<behavior>Startup fails visibly on missing provisioning, teardown closes resources in order, and in-flight unknown attempts cannot resend. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Complete main composition: validated config and redacted logger, Prisma and queue schema readiness, shared coordinator, conservative abandoned-reservation recovery, bounded reconciliation, worker registration and exactly one Telegram runner. Add explicit resource ownership so every startup failure closes already-created clients; no queue.createQueue or construction/migration calls in runtime. On SIGINT/SIGTERM stop admission of new update/work claims, stop runner, bound drain of active dispatch and leave any unconfirmed reservation consumed, stop queue, then disconnect Prisma. Avoid process exit before promise settlement. Test controlled failures at queue start/recovery/runner start, repeated signal, paused HTTP, worker rejection and normal close order; logs contain IDs/reason codes but no tokens, credentials or participant payloads. Keep one-process deployment assumption explicit.</action>
<acceptance_criteria>Startup fails visibly on missing provisioning, teardown closes resources in order, and in-flight unknown attempts cannot resend.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/reminder-runtime.test.ts</automated></verify>
<done>Startup fails visibly on missing provisioning, teardown closes resources in order, and in-flight unknown attempts cannot resend.</done>
</task>

</tasks>

<threat_model>
ASVS L1; unresolved high or critical findings block completion. No credentials or participant payloads in diagnostics.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram/queue to application | External actions and stale wakeups are untrusted; durable identity and current state authorize effects. |
| Application to PostgreSQL/Telegram | Database commits precede external effects; unknown delivery is consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-09-01 | Tampering | src/domain/chat/migration-service.ts | high | mitigate | Apply the task's explicit identity, migration or state guards and run its negative cases. |
| T-05-09-02 | Information disclosure | Logging and presentation | high | mitigate | Use redacted SafeLogger and authoritative snapshot rendering; record no credentials or raw participant payloads. |
| T-05-09-03 | Denial of service | Repeated updates and wakeups | medium | mitigate | Bound reconciliation, preserve occurrence uniqueness and use focused duplicate/race verification. |
</threat_model>

<verification>
Run each task's automated command on Node >=24.19 &lt;25 or the existing Docker toolchain. New test paths are creation deliverables, not existing passing evidence. Real-DB suites use reviewed migrations plus queue provisioning first. Record failures and fix within the owning scope; do not substitute typechecking for migration/behavior verification.
</verification>

<success_criteria>
All listed truths and task criteria hold; automated evidence and any explicit checkpoint disposition are recorded. These plans do not claim guaranteed exactly-once Telegram delivery.
</success_criteria>

<output>Create `.planning/phases/05-proactive-reliable-reminders/05-09-SUMMARY.md` with changed files, test evidence, decisions and residual risks.</output>
