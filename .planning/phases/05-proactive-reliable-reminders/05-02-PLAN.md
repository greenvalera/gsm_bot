---
phase: 05-proactive-reliable-reminders
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: ["package.json","package-lock.json","prisma/provision-reminders.mjs","prisma/queue/pg-boss-12.27.0.sql","src/infrastructure/jobs/reminder-queue.ts","prisma/schema.prisma","prisma/migrations/20260913000000_reminder_ledger/migration.sql","prisma/migrate-deploy.mjs","tests/helpers/postgres.ts","tests/integration/reminder-queue.test.ts"]
autonomous: true
requirements: ["RELI-02","RELI-03"]
estimate:
  tokens: 23000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Reviewed application migrations and exact-pin queue provisioning run in a disposable PostgreSQL database before reminder verification."
    - "Runtime queue startup performs no schema creation and rejects missing or mismatched provisioning."
  artifacts:
    - path: "package.json"
      provides: "Provision the reviewed reminder and queue persistence"
    - path: "prisma/provision-reminders.mjs"
      provides: "Provision the reviewed reminder and queue persistence"
    - path: "prisma/queue/pg-boss-12.27.0.sql"
      provides: "Provision the reviewed reminder and queue persistence"
    - path: "src/infrastructure/jobs/reminder-queue.ts"
      provides: "Provision the reviewed reminder and queue persistence"
  key_links:
    - from: "prisma/migrate-deploy.mjs"
      to: "prisma/provision-reminders.mjs"
      via: "Migration stage provisions the exact queue construction SQL before runtime start"
---

<objective>
Provision the reviewed reminder and queue persistence.
Purpose: Deliver the Phase 5 goal without weakening D-01–D-18 or the completed lifecycle.
Output: package.json, package-lock.json, prisma/provision-reminders.mjs, prisma/queue/pg-boss-12.27.0.sql, src/infrastructure/jobs/reminder-queue.ts, prisma/schema.prisma, prisma/migrations/20260913000000_reminder_ledger/migration.sql, prisma/migrate-deploy.mjs, tests/helpers/postgres.ts, tests/integration/reminder-queue.test.ts
</objective>

<execution_context>
@C:/dev/gsm_bot/.codex/gsd-core/workflows/execute-plan.md
@C:/dev/gsm_bot/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-proactive-reliable-reminders/05-CONTEXT.md
@.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md
@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md
@.planning/phases/05-proactive-reliable-reminders/05-COVERAGE.md
@.planning/phases/05-proactive-reliable-reminders/05-01-SUMMARY.md
</context>

## Scope rationale

The ten-file advisory threshold is intentional: two sequential five-file tasks deliver one reviewed persistence prerequisite. Exact-pin queue provisioning and the application ledger migration share the deploy boundary and disposable PostgreSQL proof, so keeping them together prevents runtime startup against partially provisioned persistence. Each task stays within the five-file limit. If implementation expands beyond this contract, split the additional work into a dependent plan before execution rather than enlarging these tasks.

## Artifacts this phase produces

New symbols/contracts: ChatReminderState, ReminderOccurrence, ReminderKind, ReminderDisposition; PlanningRound publication/spacing fields; createReminderQueue({databaseUrl, logger}).start(handler)/wake(chatId?)/stop(); reviewed provisioning SQL.

- `package.json`
- `package-lock.json`
- `prisma/provision-reminders.mjs`
- `prisma/queue/pg-boss-12.27.0.sql`
- `src/infrastructure/jobs/reminder-queue.ts`
- `prisma/schema.prisma`
- `prisma/migrations/20260913000000_reminder_ledger/migration.sql`
- `prisma/migrate-deploy.mjs`
- `tests/helpers/postgres.ts`
- `tests/integration/reminder-queue.test.ts`

<tasks>
<task type="auto" tdd="true">
<name>05-02-01: Install the reviewed pin and prepare migration-owned queue provisioning</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @package.json (if present); @package-lock.json (if present); @prisma/provision-reminders.mjs (if present); @prisma/queue/pg-boss-12.27.0.sql (if present); @src/infrastructure/jobs/reminder-queue.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>package.json, package-lock.json, prisma/provision-reminders.mjs, prisma/queue/pg-boss-12.27.0.sql, src/infrastructure/jobs/reminder-queue.ts</files>
<behavior>The reviewed pin, construction SQL and adapter exist; construction/provisioning is migration-owned; no bot process runs DDL. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>After both 05-01 approvals, add only pg-boss 12.27.0 and update its lock entries. Generate the queue construction SQL from that exact pin's exported construction-plan API, review and commit it. provision-reminders.mjs uses pg and reviewed SQL under a deploy advisory lock, validates installed queue version, and idempotently provisions the named reminder-reconcile queue outside runtime. Export createReminderQueue({databaseUrl, logger}) returning start(handler), wake(chatId?), stop(); queue data is bounded canonical chat identity only, never participants or permission assertions. Configure runtime migrate:false, schema pgboss, no createQueue in runtime and no automatic resend retry around Telegram. Wake a bounded reconciler at least each minute, with durable jobs; queue retries may rerun reconciliation because the application ledger remains authoritative. Wire provisioning into the next task's migration stage. This is an explicit mandatory dependency prerequisite to the behavioral tracer, not a product slice.</action>
<acceptance_criteria>The reviewed pin, construction SQL and adapter exist; construction/provisioning is migration-owned; no bot process runs DDL.</acceptance_criteria>
<verify><automated>node --check prisma/provision-reminders.mjs</automated></verify>
<done>The reviewed pin, construction SQL and adapter exist; construction/provisioning is migration-owned; no bot process runs DDL.</done>
</task>

<task type="auto" tdd="true">
<name>05-02-02: Deploy and prove the ledger migration before the tracer</name>
<reversibility rating="one-way">Ledger identities and terminal attempt semantics become persistent history; later changes require migration.</reversibility>
<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @prisma/schema.prisma (if present); @prisma/migrations/20260913000000_reminder_ledger/migration.sql (if present); @prisma/migrate-deploy.mjs (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>prisma/schema.prisma, prisma/migrations/20260913000000_reminder_ledger/migration.sql, prisma/migrate-deploy.mjs, tests/helpers/postgres.ts, tests/integration/reminder-queue.test.ts</files>
<behavior>Fresh and upgraded disposable databases pass deploy twice; deliberate schema mismatch fails; queue start cannot perform DDL. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Implement the approved proposal with ReminderKind PLANNING_START/FOLLOW_UP and dispositions PENDING, RESERVED, SENT, UNKNOWN, REJECTED, SKIPPED, COALESCED, OBSOLETE. Add chat/round relations and exact composite uniqueness plus due/disposition index; retain per-round attempt spacing independently of schedule generation. Extend exact application catalog/preflight and migration history checks for every new column/table/enum/index. Backfill existing configurations once with the migration activation instant and generation 1; initial publication timestamps stay absent, with no invented historical backlog. Update the existing migration-stage runner to execute provision-reminders.mjs after application migrations and validate queue readiness separately. Extend disposable PostgreSQL helper to invoke that same reviewed provisioning after Prisma migrate deploy/status, preserving before/none modes for upgrade tests. Test fresh, Phase4 upgrade, second deploy, divergent catalog, missing queue schema and runtime migrate:false failure. Apply committed migrations before generating Prisma client and before behavior tests. Never use schema push or destructive data-loss flags.</action>
<acceptance_criteria>Fresh and upgraded disposable databases pass deploy twice; deliberate schema mismatch fails; queue start cannot perform DDL.</acceptance_criteria>
<verify><automated>npm run db:generate &amp;&amp; npm run test:integration -- tests/integration/reminder-queue.test.ts</automated></verify>
<done>Fresh and upgraded disposable databases pass deploy twice; deliberate schema mismatch fails; queue start cannot perform DDL.</done>
</task>

</tasks>

<threat_model>
ASVS L1; unresolved high or critical findings block completion. No credentials or participant payloads in diagnostics.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram/queue to application | External actions and stale wakeups are untrusted; durable identity and current state authorize effects. |
| Application to PostgreSQL/Telegram | Database commits precede external effects; unknown delivery is consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-02-01 | Tampering | package.json | high | mitigate | Apply the task's explicit identity, migration or state guards and run its negative cases. |
| T-05-02-02 | Information disclosure | Logging and presentation | high | mitigate | Use redacted SafeLogger and authoritative snapshot rendering; record no credentials or raw participant payloads. |
| T-05-02-03 | Denial of service | Repeated updates and wakeups | medium | mitigate | Bound reconciliation, preserve occurrence uniqueness and use focused duplicate/race verification. |
| T-05-SC | Tampering | npm installation | high | mitigate | Plan 01 blocking-human exact-pin legitimacy review precedes Plan 02 installation; preserve registry/repository evidence. |
</threat_model>

<verification>
Run each task's automated command on Node >=24.19 &lt;25 or the existing Docker toolchain. New test paths are creation deliverables, not existing passing evidence. Real-DB suites use reviewed migrations plus queue provisioning first. Record failures and fix within the owning scope; do not substitute typechecking for migration/behavior verification.
</verification>

<success_criteria>
All listed truths and task criteria hold; automated evidence and any explicit checkpoint disposition are recorded. These plans do not claim guaranteed exactly-once Telegram delivery.
</success_criteria>

<output>Create `.planning/phases/05-proactive-reliable-reminders/05-02-SUMMARY.md` with changed files, test evidence, decisions and residual risks.</output>
