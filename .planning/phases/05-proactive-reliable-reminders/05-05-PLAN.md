---
phase: 05-proactive-reliable-reminders
plan: "05"
type: execute
wave: 5
depends_on: ["05-04"]
files_modified: ["src/telegram/reminder-renderers.ts","src/domain/reminders/reminder-service.ts","tests/unit/reminder-renderers.test.ts","tests/integration/reminder-actions.test.ts","src/telegram/handlers.ts","src/telegram/callbacks.ts","src/telegram/planning-handlers.ts","src/shared/callback-schema.ts","tests/integration/reminder-start.test.ts"]
autonomous: true
requirements: ["REM-01","REM-02","RELI-02"]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-04: Planning reminders name the target week and offer Start planning without mentions; each click revalidates the current actor's planning permissions and enters the existing flow."
    - "A repeated Start callback or Telegram update creates no duplicate plan, transition or reminder capability."
    - "Expired, wrong-chat and stale-week actions acknowledge once and never redirect into an unrelated future-week plan."
  artifacts:
    - path: "src/telegram/reminder-renderers.ts"
      provides: "Make the reminder Start button securely enter existing planning"
    - path: "src/domain/reminders/reminder-service.ts"
      provides: "Make the reminder Start button securely enter existing planning"
    - path: "tests/unit/reminder-renderers.test.ts"
      provides: "Make the reminder Start button securely enter existing planning"
    - path: "tests/integration/reminder-actions.test.ts"
      provides: "Make the reminder Start button securely enter existing planning"
  key_links:
    - from: "src/telegram/callbacks.ts"
      to: "src/telegram/handlers.ts"
      via: "reminder-start dispatch invokes the shared current-policy planning authorization helper"
---

<objective>
Make the reminder Start button securely enter existing planning.
Purpose: Deliver the Phase 5 goal without weakening D-01–D-18 or the completed lifecycle.
Output: src/telegram/reminder-renderers.ts, src/domain/reminders/reminder-service.ts, tests/unit/reminder-renderers.test.ts, tests/integration/reminder-actions.test.ts, src/telegram/handlers.ts, src/telegram/callbacks.ts, src/telegram/planning-handlers.ts, tests/integration/reminder-start.test.ts
</objective>

<execution_context>
@C:/dev/gsm_bot/.codex/gsd-core/workflows/execute-plan.md
@C:/dev/gsm_bot/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-proactive-reliable-reminders/05-CONTEXT.md
@.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md
@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md
@.planning/phases/05-proactive-reliable-reminders/05-COVERAGE.md
@.planning/phases/05-proactive-reliable-reminders/05-04-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols/contracts: renderPlanningReminder; reminder-start opaque target parser; shared authorizePlanningStart helper and route dispatch.

- `src/telegram/reminder-renderers.ts`
- `src/domain/reminders/reminder-service.ts`
- `tests/unit/reminder-renderers.test.ts`
- `tests/integration/reminder-actions.test.ts`
- `src/telegram/handlers.ts`
- `src/telegram/callbacks.ts`
- `src/telegram/planning-handlers.ts`
- `tests/integration/reminder-start.test.ts`

<tasks>
<task type="auto" tdd="true">
<name>05-05-01: Render planning reminder and persist opaque public action</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/telegram/reminder-renderers.ts (if present); @src/domain/reminders/reminder-service.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/telegram/reminder-renderers.ts, src/domain/reminders/reminder-service.ts, tests/unit/reminder-renderers.test.ts, tests/integration/reminder-actions.test.ts</files>
<behavior>Reminder sends the target week and a bounded opaque Start capability, with no participant tags. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Create pure renderPlanningReminder(targetWeek, callbackData) with formatted week and one Start planning button, no mentions per D-04. Use existing CallbackActionKind.PLANNING with a dedicated reminder-start target discriminant parsed in callback-schema.ts; no callback enum migration is needed. Store the actual bot Telegram ID supplied by composition as actorUserId (publisher identity only), because the existing column is non-null; this route alone resolves actor binding publicly and still freshly authorizes the clicker. The bot ID is never treated as a planning author. Persist opaque chat/week/expiry capability once per occurrence before send, public actor binding resolved by route; never invent an author. Reuse the capability on known rejection retry. Keep data within 64 UTF-8 bytes, expire by target-week end, and do not embed user identity or permissions. Wire rendering into the existing tracer send.</action>
<acceptance_criteria>Reminder sends the target week and a bounded opaque Start capability, with no participant tags.</acceptance_criteria>
<verify><automated>npm test -- tests/unit/reminder-renderers.test.ts &amp;&amp; npm run test:integration -- tests/integration/reminder-actions.test.ts</automated></verify>
<done>Reminder sends the target week and a bounded opaque Start capability, with no participant tags.</done>
</task>

<task type="auto" tdd="true">
<name>05-05-02: Route reminder clicks through the current-policy planning gate</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/telegram/handlers.ts (if present); @src/telegram/callbacks.ts (if present); @src/telegram/planning-handlers.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/telegram/handlers.ts, src/telegram/callbacks.ts, src/telegram/planning-handlers.ts, src/shared/callback-schema.ts, tests/integration/reminder-start.test.ts</files>
<behavior>A permitted click enters the existing current-week flow once; all denial and stale cases acknowledge once without unauthorized writes. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Factor the existing handlers.ts /plan current-role, current configuration policy, previous-participant standing and canStartPlanning gate into a callable helper in the same module or existing authorization module without weakening any caller. Register reminder-start in the exhaustive callback route and dispatch through that helper before handlePlanCommand/startOrResume; membership alone is insufficient. Validate durable chat, expiry and target week before starting; if current manual target is no longer the reminder week, give stale/status advice rather than create a different week. Preserve exactly-one callback acknowledgement using existing boundary ownership/finally fallback, role lookup before durable access and fail-closed unknown membership. Test administrator-only, previous-participant and anyone policy, changed policy after send, nonmember, duplicate update/callback, wrong-chat, stale and current active draft cases.</action>
<acceptance_criteria>A permitted click enters the existing current-week flow once; all denial and stale cases acknowledge once without unauthorized writes.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/reminder-start.test.ts</automated></verify>
<done>A permitted click enters the existing current-week flow once; all denial and stale cases acknowledge once without unauthorized writes.</done>
</task>

</tasks>

<threat_model>
ASVS L1; unresolved high or critical findings block completion. No credentials or participant payloads in diagnostics.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram/queue to application | External actions and stale wakeups are untrusted; durable identity and current state authorize effects. |
| Application to PostgreSQL/Telegram | Database commits precede external effects; unknown delivery is consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-05-01 | Tampering | src/telegram/reminder-renderers.ts | high | mitigate | Apply the task's explicit identity, migration or state guards and run its negative cases. |
| T-05-05-02 | Information disclosure | Logging and presentation | high | mitigate | Use redacted SafeLogger and authoritative snapshot rendering; record no credentials or raw participant payloads. |
| T-05-05-03 | Denial of service | Repeated updates and wakeups | medium | mitigate | Bound reconciliation, preserve occurrence uniqueness and use focused duplicate/race verification. |
</threat_model>

<verification>
Run each task's automated command on Node >=24.19 &lt;25 or the existing Docker toolchain. New test paths are creation deliverables, not existing passing evidence. Real-DB suites use reviewed migrations plus queue provisioning first. Record failures and fix within the owning scope; do not substitute typechecking for migration/behavior verification.
</verification>

<success_criteria>
All listed truths and task criteria hold; automated evidence and any explicit checkpoint disposition are recorded. These plans do not claim guaranteed exactly-once Telegram delivery.
</success_criteria>

<output>Create `.planning/phases/05-proactive-reliable-reminders/05-05-SUMMARY.md` with changed files, test evidence, decisions and residual risks.</output>
