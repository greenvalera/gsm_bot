---
phase: 05-proactive-reliable-reminders
plan: "04"
type: execute
wave: 4
depends_on: ["05-03"]
files_modified: ["src/domain/reminders/reminder-policy.ts","src/domain/reminders/reminder-occurrences.ts","tests/unit/reminder-policy.test.ts","tests/unit/reminder-occurrences.test.ts","src/domain/reminders/reminder-service.ts","tests/integration/reminder-weekly.test.ts"]
autonomous: true
requirements: ["REM-01","REM-02","REM-03","RELI-03"]
estimate:
  tokens: 21000
  raw_tokens: 21000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: Planning reminders occur at local 10:00 Monday onward only for the current week, and an active draft suppresses them independently of week-claim status."
    - "D-02: An agreed current week does not produce next-week reminders until next Monday; manual lookahead remains available."
    - "D-05: Midweek initial setup enables only the next strictly future 10:00, including setup exactly at 10:00."
    - "Sorted duplicate configured minutes yield one occurrence; DST gaps are skipped and an overlap chooses the earlier instant once, as an implementation assumption."
  artifacts:
    - path: "src/domain/reminders/reminder-policy.ts"
      provides: "Deliver calendar-correct weekly reminders"
    - path: "src/domain/reminders/reminder-occurrences.ts"
      provides: "Deliver calendar-correct weekly reminders"
    - path: "tests/unit/reminder-policy.test.ts"
      provides: "Deliver calendar-correct weekly reminders"
    - path: "tests/unit/reminder-occurrences.test.ts"
      provides: "Deliver calendar-correct weekly reminders"
  key_links:
    - from: "src/domain/reminders/reminder-service.ts"
      to: "src/domain/reminders/reminder-occurrences.ts"
      via: "reconcile calls the civil enumerator and current-state predicate before upserting occurrence identities"
---

<objective>
Deliver calendar-correct weekly reminders.
Purpose: Deliver the Phase 5 goal without weakening D-01–D-18 or the completed lifecycle.
Output: src/domain/reminders/reminder-policy.ts, src/domain/reminders/reminder-occurrences.ts, tests/unit/reminder-policy.test.ts, tests/unit/reminder-occurrences.test.ts, src/domain/reminders/reminder-service.ts, tests/integration/reminder-weekly.test.ts
</objective>

<execution_context>
@C:/dev/gsm_bot/.codex/gsd-core/workflows/execute-plan.md
@C:/dev/gsm_bot/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-proactive-reliable-reminders/05-CONTEXT.md
@.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md
@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md
@.planning/phases/05-proactive-reliable-reminders/05-COVERAGE.md
@.planning/phases/05-proactive-reliable-reminders/05-03-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols/contracts: ReminderStream, OccurrenceCandidate, evaluateReminderEligibility and enumerateReminderOccurrences; canonical chat/week/generation occurrence keys.

- `src/domain/reminders/reminder-policy.ts`
- `src/domain/reminders/reminder-occurrences.ts`
- `tests/unit/reminder-policy.test.ts`
- `tests/unit/reminder-occurrences.test.ts`
- `src/domain/reminders/reminder-service.ts`
- `tests/integration/reminder-weekly.test.ts`

<tasks>
<task type="auto" tdd="true">
<name>05-04-01: Enumerate civil occurrences and weekly eligibility</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/domain/reminders/reminder-policy.ts (if present); @src/domain/reminders/reminder-occurrences.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/domain/reminders/reminder-policy.ts, src/domain/reminders/reminder-occurrences.ts, tests/unit/reminder-policy.test.ts, tests/unit/reminder-occurrences.test.ts</files>
<behavior>Deterministic candidates preserve wall-clock recurrence, strict activation and current-week eligibility. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Define ReminderStream, OccurrenceCandidate and evaluateReminderEligibility inputs as readonly values with injected now. Reuse CivilDate, MinuteOfDay, resolveWallClock, civil Monday arithmetic and availabilityOutcome; do not reuse manual targetWeekStart lookahead as proactive policy. Per D-01/D-02/D-05 only current-week 10:00 candidates strictly after activation are generated, with active DRAFT or CONFIRMED/BOOKED week independently suppressing planning. Enumerate follow-up configured minutes sorted/deduplicated, fixed rehearsal start cutoff, generation boundary, first-publication grace and actual attempt spacing inputs for later dispatch. Adopt research assumptions: skip nonexistent local time and choose earlier overlap once; compare instants, not 24-hour arithmetic. Cover Monday/Sunday/year boundary, empty times, duplicate minutes, timezone change, exactly setup time, and unavailable-before-pending derivation.</action>
<acceptance_criteria>Deterministic candidates preserve wall-clock recurrence, strict activation and current-week eligibility.</acceptance_criteria>
<verify><automated>npm test -- tests/unit/reminder-policy.test.ts tests/unit/reminder-occurrences.test.ts</automated></verify>
<done>Deterministic candidates preserve wall-clock recurrence, strict activation and current-week eligibility.</done>
</task>

<task type="auto" tdd="true">
<name>05-04-02: Reconcile recurring weekly work from durable boundaries</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/domain/reminders/reminder-service.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/domain/reminders/reminder-service.ts, tests/integration/reminder-weekly.test.ts</files>
<behavior>A ready chat receives its eligible daily reminder, stops for its active process and resumes only at a legitimate future week. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Connect enumerator and eligibility to service.reconcile invoked by boot and durable periodic wakeups. For each ready canonical chat generate a bounded current/future horizon (through next local day plus two-hour recovery window), upsert exact occurrence identities and dispatch due candidates; future wakeups may be rebuilt, identities may not. Existing migrated configurations use persisted activation, not process startup. Validate D-01/D-02 with active draft versus claimed week and next Monday; validate D-05 after setup. Limit scans and process batches so downtime cannot generate all historic dates. An empty/ineligible stream is silent. Current-state validation runs again at reservation. Preserve manual planning tests unchanged.</action>
<acceptance_criteria>A ready chat receives its eligible daily reminder, stops for its active process and resumes only at a legitimate future week.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/reminder-weekly.test.ts</automated></verify>
<done>A ready chat receives its eligible daily reminder, stops for its active process and resumes only at a legitimate future week.</done>
</task>

</tasks>

<threat_model>
ASVS L1; unresolved high or critical findings block completion. No credentials or participant payloads in diagnostics.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram/queue to application | External actions and stale wakeups are untrusted; durable identity and current state authorize effects. |
| Application to PostgreSQL/Telegram | Database commits precede external effects; unknown delivery is consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-04-01 | Tampering | src/domain/reminders/reminder-policy.ts | high | mitigate | Apply the task's explicit identity, migration or state guards and run its negative cases. |
| T-05-04-02 | Information disclosure | Logging and presentation | high | mitigate | Use redacted SafeLogger and authoritative snapshot rendering; record no credentials or raw participant payloads. |
| T-05-04-03 | Denial of service | Repeated updates and wakeups | medium | mitigate | Bound reconciliation, preserve occurrence uniqueness and use focused duplicate/race verification. |
</threat_model>

<verification>
Run each task's automated command on Node >=24.19 &lt;25 or the existing Docker toolchain. New test paths are creation deliverables, not existing passing evidence. Real-DB suites use reviewed migrations plus queue provisioning first. Record failures and fix within the owning scope; do not substitute typechecking for migration/behavior verification.
</verification>

<success_criteria>
All listed truths and task criteria hold; automated evidence and any explicit checkpoint disposition are recorded. These plans do not claim guaranteed exactly-once Telegram delivery.
</success_criteria>

<output>Create `.planning/phases/05-proactive-reliable-reminders/05-04-SUMMARY.md` with changed files, test evidence, decisions and residual risks.</output>
