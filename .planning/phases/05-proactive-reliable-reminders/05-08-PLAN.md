---
phase: 05-proactive-reliable-reminders
plan: "08"
type: execute
wave: 8
depends_on: ["05-07"]
files_modified: ["src/domain/reminders/reminder-service.ts","src/domain/reminders/reminder-occurrences.ts","tests/integration/reminder-recovery.test.ts","tests/unit/reminder-occurrences.test.ts","tests/integration/reminder-idempotency.test.ts","tests/integration/reminder-delivery.test.ts"]
autonomous: true
requirements: ["RELI-02","RELI-03","REM-03","REM-05"]
estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-11: Recovery sends at most one latest currently eligible missed occurrence per stream when lateness is at most two hours inclusive; older and coalesced work stays silent."
    - "D-12: Unknown delivery is terminal for its occurrence, while proven non-delivery may retry only within original eligibility and deadline."
    - "D-13: Recovery at 15:50 sends a relevant 14:00 reminder immediately and skips 16:00 because it is less than 30 minutes later."
    - "RELI-02 concurrency edge: two distinct due rows competing for one round cannot both reserve the same 30-minute window."
    - "RELI-02 idempotency edge: replaying updates/callbacks and reloading the database leaves plan, response, transition and occurrence counts unchanged."
  artifacts:
    - path: "src/domain/reminders/reminder-service.ts"
      provides: "Recover one relevant missed occurrence without uncertain resend"
    - path: "src/domain/reminders/reminder-occurrences.ts"
      provides: "Recover one relevant missed occurrence without uncertain resend"
    - path: "tests/integration/reminder-recovery.test.ts"
      provides: "Recover one relevant missed occurrence without uncertain resend"
    - path: "tests/unit/reminder-occurrences.test.ts"
      provides: "Recover one relevant missed occurrence without uncertain resend"
  key_links:
    - from: "src/domain/reminders/reminder-service.ts"
      to: "src/domain/reminders/reminder-occurrences.ts"
      via: "recovery coalesces due candidates and atomically reserves same-round spacing"
---

<objective>
Recover one relevant missed occurrence without uncertain resend.
Purpose: Deliver the Phase 5 goal without weakening D-01–D-18 or the completed lifecycle.
Output: src/domain/reminders/reminder-service.ts, src/domain/reminders/reminder-occurrences.ts, tests/integration/reminder-recovery.test.ts, tests/unit/reminder-occurrences.test.ts, tests/integration/reminder-idempotency.test.ts, tests/integration/reminder-delivery.test.ts
</objective>

<execution_context>
@C:/dev/gsm_bot/.codex/gsd-core/workflows/execute-plan.md
@C:/dev/gsm_bot/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-proactive-reliable-reminders/05-CONTEXT.md
@.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md
@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md
@.planning/phases/05-proactive-reliable-reminders/05-COVERAGE.md
@.planning/phases/05-proactive-reliable-reminders/05-07-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols/contracts: recoverAbandonedReservations, coalesceDueOccurrences, classifyReminderDelivery; terminal UNKNOWN and bounded known-rejection retry.

- `src/domain/reminders/reminder-service.ts`
- `src/domain/reminders/reminder-occurrences.ts`
- `tests/integration/reminder-recovery.test.ts`
- `tests/unit/reminder-occurrences.test.ts`
- `tests/integration/reminder-idempotency.test.ts`
- `tests/integration/reminder-delivery.test.ts`

<tasks>
<task type="auto" tdd="true">
<name>05-08-01: Coalesce bounded recovery and preserve send spacing</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/domain/reminders/reminder-service.ts (if present); @src/domain/reminders/reminder-occurrences.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/domain/reminders/reminder-service.ts, src/domain/reminders/reminder-occurrences.ts, tests/integration/reminder-recovery.test.ts, tests/unit/reminder-occurrences.test.ts</files>
<behavior>Restart sends one currently useful catch-up and never replays consumed or obsolete occurrences. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Per D-11/D-13 reconcile only valid generation occurrences due no more than two hours ago, inclusive, choose latest currently eligible per stream and atomically mark eligible siblings COALESCED. Expired, originally grace-blocked, lifecycle-obsolete and old-generation occurrences cannot be promoted; persist skipped/coalesced disposition so repeated restart cannot revive them. Catch-up reserves actual now immediately regardless of next planned time. The next scheduled due less than 30 minutes from reservation is consumed as SKIPPED; exactly 30 remains valid. Restore RESERVED from a previous process as UNKNOWN conservatively before dispatch. Test service/queue recreation against the same database, 2h equality/+1ms, accumulated 10:00/11:00/12:00 window, 15:50→16:00 example, future-only settings, blocked→unblocked and obsolete successor; no group outage notice per D-14.</action>
<acceptance_criteria>Restart sends one currently useful catch-up and never replays consumed or obsolete occurrences.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/reminder-recovery.test.ts &amp;&amp; npm test -- tests/unit/reminder-occurrences.test.ts</automated></verify>
<done>Restart sends one currently useful catch-up and never replays consumed or obsolete occurrences.</done>
</task>

<task type="auto" tdd="true">
<name>05-08-02: Classify delivery failures and prove competing-claim idempotency</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/domain/reminders/reminder-service.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/domain/reminders/reminder-service.ts, tests/integration/reminder-idempotency.test.ts, tests/integration/reminder-delivery.test.ts</files>
<behavior>Only known non-delivery can retry, bounded by original occurrence; all crash/race schedules respect durable idempotency and spacing. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Treat Telegram accepted with returned ID as SENT; explicit permanent rejection as final REJECTED; explicit flood rejection as proven no-delivery with retryAt; timeout/reset/ambiguous server result and crash after reservation as UNKNOWN. A known-rejection retry reclaims the same occurrence/attempt ownership after retryAfter, rechecks lifecycle/generation/publication and original due+2h deadline. Restore previous spacing only after proven non-delivery under the same lock and compare-and-set current attempt ownership; never undo a newer attempt or release unknown spacing. Test accepted-send/outcome-write failure, crash before reservation, after reservation and after HTTP, permanent/flood error, expired retry, two workers same row, two different due rows same round and repeated Telegram planning/vote/lifecycle callbacks. Reuse prior phase real callback fixtures; assert no duplicate plans, response rows, revisions/transitions or occurrence identities.</action>
<acceptance_criteria>Only known non-delivery can retry, bounded by original occurrence; all crash/race schedules respect durable idempotency and spacing.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/reminder-idempotency.test.ts tests/integration/reminder-delivery.test.ts</automated></verify>
<done>Only known non-delivery can retry, bounded by original occurrence; all crash/race schedules respect durable idempotency and spacing.</done>
</task>

</tasks>

<threat_model>
ASVS L1; unresolved high or critical findings block completion. No credentials or participant payloads in diagnostics.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram/queue to application | External actions and stale wakeups are untrusted; durable identity and current state authorize effects. |
| Application to PostgreSQL/Telegram | Database commits precede external effects; unknown delivery is consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-08-01 | Tampering | src/domain/reminders/reminder-service.ts | high | mitigate | Apply the task's explicit identity, migration or state guards and run its negative cases. |
| T-05-08-02 | Information disclosure | Logging and presentation | high | mitigate | Use redacted SafeLogger and authoritative snapshot rendering; record no credentials or raw participant payloads. |
| T-05-08-03 | Denial of service | Repeated updates and wakeups | medium | mitigate | Bound reconciliation, preserve occurrence uniqueness and use focused duplicate/race verification. |
</threat_model>

<verification>
Run each task's automated command on Node >=24.19 &lt;25 or the existing Docker toolchain. New test paths are creation deliverables, not existing passing evidence. Real-DB suites use reviewed migrations plus queue provisioning first. Record failures and fix within the owning scope; do not substitute typechecking for migration/behavior verification.
</verification>

<success_criteria>
All listed truths and task criteria hold; automated evidence and any explicit checkpoint disposition are recorded. These plans do not claim guaranteed exactly-once Telegram delivery.
</success_criteria>

<output>Create `.planning/phases/05-proactive-reliable-reminders/05-08-SUMMARY.md` with changed files, test evidence, decisions and residual risks.</output>
