---
phase: 01-chat-readiness
plan: 06
type: execute
wave: 6
depends_on:
  - 01-05
files_modified:
  - src/app/create-bot.ts
  - src/app/main.ts
  - src/shared/callback-schema.ts
  - src/shared/logger.ts
  - src/telegram/handlers.ts
  - src/telegram/callbacks.ts
  - tests/integration/chat-readiness.e2e.test.ts
  - .github/workflows/ci.yml
  - .planning/phases/01-chat-readiness/01-VALIDATION.md
autonomous: false
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-05
  - ROST-01
  - ROST-02
  - ROST-03
  - AUTH-01
  - AUTH-02
user_setup:
  - service: telegram
    why: "Verify actual Telegram group location, callback, edit, and demotion behavior"
    env_vars:
      - name: BOT_TOKEN
        source: "Telegram @BotFather token for a dedicated test bot"
    dashboard_config:
      - task: "Create/use a private test group, add at least two human test accounts, and make the bot plus one test account administrators"
        location: "Telegram group settings -> Administrators"
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every Phase 1 protected command, wizard input, dashboard action, roster action, and callback reaches the same current Telegram administrator authorization boundary before protected state access or mutation."
    - "Every callback is acknowledged immediately; successful mutations replace the originating message with authoritative state and a duplicate tap receives `Already applied.`."
    - "Every callback token is short, opaque, and versioned; names, permissions, schedule values, coordinates, Telegram IDs, and authorization claims remain server-side."
    - "Every callback is acknowledged immediately; successful mutations replace the originating message with authoritative state, and text-input prompts render synchronously without a separate loading surface."
    - "Validation, location, permission, expiry, save, duplicate, and stale-action failures use the corresponding Copywriting Contract row and preserve the last authoritative state."
    - "A timezone candidate shows the inferred IANA zone; settings shows all committed values in fixed section order; roster shows sorted readable identities with one `Remove member` action each."
    - "Message values wrap in the Telegram client; action labels stay bounded and opaque callback tokens contain no user-facing values. Full member identity remains in adjacent message text and the confirmation heading."
  artifacts:
    - path: "src/telegram/handlers.ts"
      provides: "Single registration point for every Phase 1 command and update handler"
      exports:
        - registerChatReadinessHandlers
    - path: "src/telegram/callbacks.ts"
      provides: "Versioned callback dispatch, acknowledgement ordering, stale/duplicate/error projection"
      exports:
        - registerChatReadinessCallbacks
    - path: "src/shared/logger.ts"
      provides: "Structured redacted Phase 1 logging"
      exports:
        - createLogger
    - path: "tests/integration/chat-readiness.e2e.test.ts"
      provides: "Complete setup/settings/roster/restart/demotion integration flow"
    - path: ".github/workflows/ci.yml"
      provides: "Formatting/static checks, unit/integration tests, fresh migrations, and image build on pull requests"
  key_links:
    - from: "src/app/create-bot.ts"
      to: "src/telegram/handlers.ts"
      via: "composition root registers each command/update exactly once"
      pattern: "registerChatReadinessHandlers"
    - from: "src/telegram/callbacks.ts"
      to: "src/shared/callback-schema.ts"
      via: "one parse/ack/load/authorize/dispatch boundary for all opaque actions"
      pattern: "safeParse"
    - from: "tests/integration/chat-readiness.e2e.test.ts"
      to: "prisma/migrations"
      via: "fresh database runs the full Telegram-native readiness flow and restart reads"
      pattern: "migrate deploy"
    - from: ".github/workflows/ci.yml"
      to: "Dockerfile"
      via: "CI blocks on checks, PostgreSQL migrations/integration suite, and production image build"
      pattern: "docker build"
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Close Phase 1 by unifying command/callback routing, proving the full migrated flow in CI, and verifying Telegram-native behavior in a live private group.

Purpose: Ensure the independently delivered setup, settings, authorization, and roster slices compose without a bypass, stale projection, migration drift, or uncovered UI state.
Output: Central adapters, redacted logging, full-stack integration test, CI workflow, completed validation map, and live Telegram verification evidence.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-PATTERNS.md
@.planning/phases/01-chat-readiness/01-VALIDATION.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/COVERAGE.md
@.planning/phases/01-chat-readiness/01-03-SUMMARY.md
@.planning/phases/01-chat-readiness/01-04-SUMMARY.md
@.planning/phases/01-chat-readiness/01-05-SUMMARY.md
@src/domain/auth/authorization-service.ts
@src/telegram/setup-handlers.ts
@src/telegram/settings-handlers.ts
@src/telegram/roster-handlers.ts
</context>

<interfaces>
- `registerChatReadinessHandlers(bot, services)` composes setup, settings, roster, location, and text routing exactly once.
- `registerChatReadinessCallbacks(bot, services)` acknowledges first, parses a version-prefixed opaque token, loads `CallbackAction`, performs current-role authorization, and dispatches by server-side kind.
- `createLogger` redacts `BOT_TOKEN`, database credentials, raw Telegram updates, location coordinates, draft payloads, and full roster identities; logs update/chat/action/round identifiers only where needed.
- The full integration test recreates a process with the same migrated database to prove configuration, drafts, and roster survive restart.
</interfaces>

## Artifacts this phase produces

- Paths: `src/telegram/handlers.ts`, `src/telegram/callbacks.ts`, `src/shared/logger.ts`, `tests/integration/chat-readiness.e2e.test.ts`, `.github/workflows/ci.yml`
- Modified contracts: `createBot` delegates all command/update registration; `main` starts exactly one runner and uses the redacted logger; `01-VALIDATION.md` maps concrete plan/task IDs and measured commands
- Symbols: `registerChatReadinessHandlers`, `registerChatReadinessCallbacks`, `dispatchCallbackAction`, `mapTelegramError`, `createLogger`, `redactTelegramContext`
- CI checks: `npm ci`, `npm run lint`, `npm run typecheck`, `npm test`, PostgreSQL 18 `prisma migrate deploy`, `npm run test:integration`, `docker build`
- No new external package, Telegram command, database field, or callback authority source is introduced in this closure plan

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Administrators prepare a persistent, access-controlled chat | 01-02–01-06 | COVERED | Tracer plus setup/settings/roster expansions and final verification. |
| REQ | CONF-01 | Initialize with confirmed IANA timezone | 01-02, 01-03, 01-04 | COVERED | Location candidate, explicit confirmation, atomic persistence. |
| REQ | CONF-02 | Configure default weekday/start | 01-03, 01-04 | COVERED | Strict setup and individual edit flow. |
| REQ | CONF-03 | Configure duration and daily boundaries | 01-03, 01-04 | COVERED | Complete invariant checks on setup/edit/save. |
| REQ | CONF-05 | Configure reminder times with 10:00/16:00 defaults | 01-03, 01-04 | COVERED | Default and edited pair paths. |
| REQ | ROST-01 | Add replied identifiable Telegram users | 01-05 | COVERED | Reply-anchor and persistent upsert/reactivation. |
| REQ | ROST-02 | Remove roster users | 01-05 | COVERED | Actor-bound second confirmation and idempotent transaction. |
| REQ | ROST-03 | View current roster | 01-05 | COVERED | Safe label, ordering, pagination, empty state. |
| REQ | AUTH-01 | Select planning-start policy | 01-03, 01-04 | COVERED | Setup default, settings edit, and policy seam. |
| REQ | AUTH-02 | Revalidate current permission before protected actions | 01-02–01-06 | COVERED | One action-bound authorization service and full routing enumeration. |
| RESEARCH | R-01 | Node/TypeScript/grammY runner scaffold | 01-02 | COVERED | Walking skeleton and Docker runtime. |
| RESEARCH | R-02 | PostgreSQL/Prisma durable active state and drafts | 01-02–01-05 | COVERED | Committed migrations plus repository transactions. |
| RESEARCH | R-03 | Live `getChatMember` authorization and draft deletion | 01-02–01-06 | COVERED | High-severity authorization mitigation. |
| RESEARCH | R-04 | Atomic setup promotion and revision-safe edits | 01-03, 01-04 | COVERED | Expected revision and one transaction. |
| RESEARCH | R-05 | Reply-anchored roster identity and soft removal | 01-05 | COVERED | Durable identity/history and active membership. |
| RESEARCH | R-06 | Vetted offline timezone resolver behind adapter | 01-01, 01-03 | COVERED | Human legitimacy checkpoint then explicit candidate confirmation. |
| RESEARCH | R-07 | Opaque callback tokens and immediate acknowledgement | 01-02–01-06 | COVERED | Server-side authority and central dispatch. |
| RESEARCH | R-08 | Zod input/config parsing and Pino redaction | 01-02, 01-06 | COVERED | Boot/update boundaries and safe logs. |
| RESEARCH | R-09 | Testcontainers/Vitest/migration verification | 01-02–01-06 | COVERED | Wave 0, focused suites, fresh schema CI gate. |
| RESEARCH | R-10 | Docker/Compose single long-poll process | 01-02 | COVERED | Portable development deployment. |
| RESEARCH | R-11 | Package legitimacy gate for every SUS package | 01-01 | COVERED | Evidence dossier and blocking human decision. |
| CONTEXT | D-01 | Guided `/setup` wizard | 01-03 | COVERED | Eight exact steps. |
| CONTEXT | D-02 | Resume for 30 inactivity minutes, then discard | 01-02, 01-03 | COVERED | Durable actor draft and injected clock. |
| CONTEXT | D-03 | Complete summary and atomic final activation | 01-03 | COVERED | Review plus transaction. |
| CONTEXT | D-04 | Reply-anchored `/roster_add` identity | 01-05 | COVERED | No typed identity. |
| CONTEXT | D-05 | Alphabetical safe roster projection | 01-05 | COVERED | Three identity forms and stable paging. |
| CONTEXT | D-06 | Inline Remove plus explicit named confirmation | 01-05 | COVERED | Bound destructive action. |
| CONTEXT | D-07 | `/settings` committed dashboard/edit controls | 01-04 | COVERED | Fixed sections and complete values. |
| CONTEXT | D-08 | 24-hour times in configured timezone | 01-03, 01-04 | COVERED | Strict local minute representation. |
| CONTEXT | D-09 | Location inference plus explicit IANA confirmation | 01-01, 01-03, 01-04 | COVERED | Audited adapter and group flow. |
| CONTEXT | D-10 | Old/new review before individual save | 01-04 | COVERED | Revision-safe edit draft. |
| CONTEXT | D-11 | Administrators-only default | 01-03 | COVERED | Closed enum default. |
| CONTEXT | D-12 | Administrators always allowed; policies broaden | 01-04 | COVERED | `canStartPlanning` truth table. |
| CONTEXT | D-13 | Private callback denial, concise command denial | 01-02–01-06 | COVERED | Central channel-specific error mapping. |
| CONTEXT | D-14 | Demotion immediately deletes actor draft | 01-02–01-06 | COVERED | Every protected boundary plus full test. |

## Spec-less Probe Equality Audit

| Probe section | Surfaced | Authored | Silent drops |
|---|---:|---:|---:|
| Edge rows | 12 | 12 explicit `must_haves.truths` across Plans 01-02–01-05 | 0 |
| Kept bespoke prohibitions | 6 | 6 descriptor-less, flagged-unverified `must_haves.prohibitions` across Plans 01-02–01-05 | 0 |
| UI consideration rows | 12 resolved rows | 7 explicit strings plus 5 flat `{statement, verification: backstop}` mappings across Plans 01-03–01-06 | 0 |

## Two-Stage Prohibition Recall

| Requirement group | Stage 1 adversarial candidates considered | Stage 2 precision result |
|---|---|---|
| CONF-01 | wrong/ambiguous zone, unconfirmed inference, misleading draft state, dependency provenance, raw-coordinate handling | KEEP unconfirmed-zone and draft-as-active transparency constraints; DROP routine resolver/error handling; refer dependency/coordinate security canon to the package gate and threat model. |
| CONF-02, CONF-03, CONF-05 | malformed times, invalid boundaries, partial save, misleading success, coercive defaults | KEEP the shared draft-as-active transparency constraint; DROP parsing/atomicity routine-engineering rows already owned by edge predicates and tests. |
| ROST-01, ROST-02, ROST-03 | ambiguous identity, accidental removal, group-membership implication, ID exposure, ordering/paging failures | KEEP group-removal implication and full-ID privacy constraints; DROP ordinary identity validation, idempotency, ordering, and paging rows owned by edges/tests. |
| AUTH-01, AUTH-02 | policy wording that excludes admins, public denial details, stale role, forged action, missing role | KEEP admin-policy transparency and denial-detail privacy constraints; refer generic access-control/tampering canon to the threat model and `$gsd-secure-phase`. |

Canon referrals dropped rather than reminted: generic injection, path traversal, dependency tampering, secret leakage, and GDPR-style retention are handled by the phase threat models, package legitimacy gate, redaction tests, and later `$gsd-secure-phase`. The six authored prohibitions are project-specific privacy/transparency constraints and contain no fabricated `check_*` descriptor.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove every Phase 1 route crosses one callback/authorization/durable-state boundary</name>
  <read_first>
    <file>src/app/create-bot.ts</file>
    <file>src/shared/callback-schema.ts</file>
    <file>src/telegram/setup-handlers.ts</file>
    <file>src/telegram/settings-handlers.ts</file>
    <file>src/telegram/roster-handlers.ts</file>
    <file>.planning/phases/01-chat-readiness/COVERAGE.md</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
  </read_first>
  <files>src/app/create-bot.ts, src/shared/callback-schema.ts, src/telegram/handlers.ts, src/telegram/callbacks.ts, tests/integration/chat-readiness.e2e.test.ts</files>
  <behavior>
    - Test 1: every `/setup`, `/settings`, `/roster`, `/roster_add`, location, text-step, and callback route is registered once and reaches current-role authorization where protected.
    - Test 2: every versioned callback is acknowledged before parse/load/role/database latency and dispatches solely from its server-side action kind/bindings.
    - Test 3: malformed, missing, stale, expired, duplicate, cross-chat, cross-actor, demoted, and persistence-failure callbacks preserve authoritative state and return exact copy.
    - Test 4: a full setup → restart → settings edit → roster add/list/remove flow persists and reads through a freshly migrated PostgreSQL database.
  </behavior>
  <action>Centralize Phase 1 handler registration and callback dispatch while keeping feature services/renderers separate. Parse only a version-prefixed opaque token within Telegram's byte limit, acknowledge callbacks immediately, load authoritative `CallbackAction`, refresh current membership, and dispatch by the stored kind. Enumerate every protected route in a table-driven integration test so a newly registered command/callback cannot bypass authorization. Exercise the complete workflow against PostgreSQL migrations, reconstruct the bot/Prisma client mid-test, and assert exact committed state, actor-bound draft behavior, duplicate handling, and UI error copy after restart. Keep the COVERAGE.md INTEGRATE rows exercised or explicitly mapped to a focused test; do not silently add a Telegram API surface absent from the matrix.</action>
  <acceptance_criteria>
    <criterion>One central registration test lists every Phase 1 command/update/callback and proves protected routes invoke the current-role gateway before state access.</criterion>
    <criterion>All callbacks satisfy acknowledgement-before-latency and server-side-authority rules, including malformed/stale/duplicate/cross-boundary paths.</criterion>
    <criterion>The full migrated workflow survives a process/client recreation with identical configuration and roster state.</criterion>
    <criterion>Every `INTEGRATE` row in COVERAGE.md has a handler/test reference and no undocumented external Telegram surface is introduced.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- chat-readiness.e2e &amp;&amp; npm run test:unit -- authorization</automated>
  </verify>
  <done>The composed bot has no protected-route authorization bypass and the complete readiness workflow survives restart on the committed schema.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce redacted operations, fresh migrations, tests, and image build in CI</name>
  <read_first>
    <file>src/app/main.ts</file>
    <file>src/app/config.ts</file>
    <file>Dockerfile</file>
    <file>compose.yaml</file>
    <file>.planning/phases/01-chat-readiness/01-VALIDATION.md</file>
    <file>.planning/phases/01-chat-readiness/01-RESEARCH.md</file>
  </read_first>
  <files>src/app/main.ts, src/shared/logger.ts, .github/workflows/ci.yml, .planning/phases/01-chat-readiness/01-VALIDATION.md</files>
  <action>Create structured Pino logging with explicit redaction for bot/database secrets, raw updates, coordinates, callback payloads, draft payloads, and full roster identity; retain update/chat/action IDs needed for diagnosis. Add a pull-request CI workflow on Node 24 and PostgreSQL 18 that uses `npm ci`, generates Prisma Client, applies committed migrations to a fresh service, confirms migration status, runs lint/typecheck/unit/integration suites without watch mode, and builds the Docker image. Update `01-VALIDATION.md` with concrete plan/task IDs, measured quick/full commands, Wave 0 completion, file existence, and `nyquist_compliant: true` only after every mapped command is green. Do not introduce a new dependency or replace committed migrations with schema push.</action>
  <acceptance_criteria>
    <criterion>A logging test injects representative token, database URL, coordinates, callback token, draft values, full Telegram user data, and verifies none appears in emitted JSON.</criterion>
    <criterion>CI applies all committed migrations to a fresh PostgreSQL 18 service before integration tests and blocks on migration status, every test suite, and image build.</criterion>
    <criterion>Every Phase 1 requirement row in `01-VALIDATION.md` has concrete task/plan/wave, command, existing test file, and green status supported by the executed command.</criterion>
    <criterion>The final full suite and image build run from clean lockfile installation and use no watch flag or destructive schema push.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm ci &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run test:integration &amp;&amp; docker build -t gsmbot:phase-01 .</automated>
  </verify>
  <done>Local and CI gates prove redaction, strict types, every requirement test, fresh migration deployment, and the production image.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Telegram-native setup, roster, settings, and immediate demotion behavior</name>
  <precondition>`BOT_TOKEN` is set for a dedicated bot that is an administrator in a private group containing an administrator test account and another human test account.</precondition>
  <read_first>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
    <file>.planning/phases/01-chat-readiness/01-VALIDATION.md</file>
    <file>.planning/phases/01-chat-readiness/COVERAGE.md</file>
  </read_first>
  <files>.planning/phases/01-chat-readiness/01-VALIDATION.md</files>
  <action>Run the already-built Compose bot against the private test group and execute the exact verification script below. Record observed results in the Plan 01-06 summary/validation evidence; do not change behavior or approve around a mismatch. This checkpoint validates Telegram client affordances and live `getChatMember` semantics that mocked updates cannot prove.</action>
  <what-built>Complete Telegram-native Chat Readiness flow with durable setup/settings/roster state, exact UI contract, current-role authorization, and CI verification.</what-built>
  <how-to-verify>
    1. As the current administrator, send `/setup`. Confirm the bold `Set up rehearsal planning` prompt has only `Start setup`; start it and verify `Setup in progress`, `Step 1 of 8`, and the location instruction.
    2. Attach an ordinary location in the group. Confirm the candidate card, use the inferred IANA zone, complete all eight values, inspect the fixed review order, and save. Restart the bot and verify `/settings` shows the committed configuration only.
    3. Edit one valid setting and confirm Current/New plus `Save change`/`Keep current value`; try an invalid conflicting schedule and confirm no committed value changes.
    4. Reply to the other human account's message with `/roster_add`, verify safe identity display, open removal, verify the named second confirmation, keep it once, then remove it and confirm a repeated tap is non-mutating.
    5. Start a new setup/settings edit as the administrator, demote that account in Telegram, then attempt the next protected command and callback. Confirm immediate exact group/private denial and that re-promoting the account does not resume the discarded draft.
    6. Inspect long/empty roster and error states used in the test group; confirm Telegram-native wrapping, no full numeric ID exposure, no reply keyboard/WebView, and authoritative message replacement after callbacks.
  </how-to-verify>
  <acceptance_criteria>
    <criterion>The real group location update reaches the correct actor-bound step and produces a confirmable IANA candidate.</criterion>
    <criterion>Configuration and roster state survive bot restart and exactly match the committed dashboard/projection.</criterion>
    <criterion>Demotion takes effect at the next command and callback, discards the actor draft, and cannot be bypassed by the prior successful action.</criterion>
    <criterion>Message hierarchy, exact copy, inline buttons, immediate callback completion, wrapping, pagination, and safe identity behavior match the approved UI contract.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run test:integration &amp;&amp; docker compose config -q</automated>
    <human-check>Complete all six private-group steps and compare every visible surface and demotion result to `01-UI-SPEC.md`.</human-check>
  </verify>
  <resume-signal>Type "approved" when all six live checks pass, or report the exact step, observed message/state, and expected contract.</resume-signal>
  <done>The real Telegram client and membership API confirm the complete Phase 1 outcome with no UI, persistence, or authorization discrepancy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| central Telegram router → feature services | Registration/dispatch mistakes could bypass authorization or acknowledge too late. |
| runtime events → structured logs | Secrets, coordinates, drafts, or roster identity could leak through raw context serialization. |
| committed repository → CI PostgreSQL/image | Migration drift or untested build output could pass locally and fail in deployment. |
| automated mocks → live Telegram group | Client rendering and real membership semantics require a final human-observed boundary check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-23 | Elevation of privilege | command/callback registration | high | mitigate | Table-driven route inventory proves every protected route invokes the shared current-role boundary. |
| T-01-24 | Tampering | callback dispatcher | high | mitigate | Ack first, validate versioned opaque token, load server-side bindings, reauthorize, and dispatch by stored kind. |
| T-01-25 | Information disclosure | Pino/runtime error logs | high | mitigate | Explicit redaction plus a fixture test containing representative secrets, coordinates, drafts, and raw Telegram identity. |
| T-01-26 | Tampering | migration/build pipeline | high | mitigate | CI uses clean lockfile install, fresh PostgreSQL 18 `migrate deploy/status`, full tests, and image build. |
| T-01-27 | Repudiation | live Telegram discrepancy | low | accept | Final checkpoint records observed step/copy/state; unresolved discrepancy blocks approval rather than being silently waived. |
| T-01-SC | Tampering | npm dependency graph | high | mitigate | CI uses `npm ci` from the human-approved committed lockfile and installs no new package. |
</threat_model>

<verification>
Run the entire clean-install/static/unit/integration/migration/image pipeline, then complete the six-step live Telegram group check. Validate API coverage syntax and confirm the source, edge, prohibition, and UI equality audits contain zero missing items.
</verification>

<success_criteria>
- All nine Phase 1 requirements and D-01 through D-14 are implemented through reachable Telegram routes.
- Every protected route revalidates current Telegram administrator permission; demotion discards the actor-bound draft immediately.
- All 12 edge rows, 12 resolved UI rows, and 6 kept descriptor-less prohibitions have zero silent drops.
- Fresh migrations, restart persistence, full tests, redaction, Docker image, and live Telegram checks pass.
- The source audit has no MISSING row and no deferred/later-phase capability has entered Phase 1.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-06-SUMMARY.md` when done.
</output>
