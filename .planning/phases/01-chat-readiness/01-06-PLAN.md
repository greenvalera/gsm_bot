---
phase: 01-chat-readiness
plan: 06
type: execute
wave: 7
depends_on:
  - 01-05
files_modified:
  - src/domain/chat/types.ts
  - src/domain/chat/schedule-validator.ts
  - src/domain/chat/setup-service.ts
  - src/telegram/keyboards.ts
  - src/telegram/renderers.ts
  - src/telegram/setup-handlers.ts
  - tests/unit/schedule-settings.test.ts
autonomous: true
requirements:
  - CONF-02
  - CONF-03
  - CONF-05
  - AUTH-01
  - AUTH-02
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-08, every setup time is strict zero-padded 24-hour `HH:MM` interpreted in the confirmed IANA zone."
    - "Per D-11, fresh setup defaults planning access to administrators while offering exactly the three supported choices."
    - "Per D-13 and D-14, every text or callback continuation refreshes current administrator status and discards the actor draft immediately on demotion."
    - "The administrator completes the remaining seven values in the approved eight-step sequence and reaches review without changing active configuration."
    - "[Edge CONF-02/unclassified] Only `Mon` through `Sun` and strict `HH:MM` start input are accepted; invalid input preserves the draft and re-prompts that field."
    - "[Edge CONF-03/unclassified] Duration is a positive whole minute count, daily start is earlier than daily end, and default start plus duration does not exceed daily end at field completion and review."
    - "[Edge CONF-05/unclassified] Reminder times default to `10:00` and `16:00`, and an edited pair must contain two valid local `HH:MM` values before review."
    - "An unfilled setup step renders its current prompt and required input format; completed values remain actor-bound and are never shown as active configuration."
  artifacts:
    - path: "src/domain/chat/types.ts"
      provides: "Closed setup, weekday, minute-of-day, and planning-policy types"
    - path: "src/domain/chat/schedule-validator.ts"
      provides: "Strict time parsing and complete schedule invariant validation"
      exports:
        - parseLocalTime
        - formatLocalTime
        - validateSchedule
    - path: "src/domain/chat/setup-service.ts"
      provides: "Durable eight-step draft transitions through review readiness"
      exports:
        - SetupService
    - path: "src/telegram/renderers.ts"
      provides: "Approved setup step and review-ready Telegram projections"
    - path: "tests/unit/schedule-settings.test.ts"
      provides: "Boundary coverage for times, duration, daily bounds, reminders, and access policy"
  key_links:
    - from: "src/domain/chat/setup-service.ts"
      to: "src/domain/chat/schedule-validator.ts"
      via: "field completion and review readiness both validate the full schedule"
      pattern: "validateSchedule"
    - from: "src/telegram/setup-handlers.ts"
      to: "src/domain/auth/authorization-service.ts"
      via: "every text and choice continuation retains the Plan 01-05 current-role boundary"
      pattern: "requireCurrentAdministrator"
    - from: "src/telegram/setup-handlers.ts"
      to: "src/telegram/renderers.ts"
      via: "each successful draft transition renders exactly the next approved step"
      pattern: "renderSetupStep"
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Complete the setup draft's schedule, reminder, and planning-access collection through the review boundary.

Purpose: Make every Phase 1 setting valid and reviewable while preserving D-03's inactive-draft invariant.
Output: Domain types, strict schedule validation, steps 2–8, Telegram keyboards/renderers, and boundary-focused unit tests.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-PATTERNS.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-05-SUMMARY.md
@src/domain/auth/authorization-service.ts
@src/domain/chat/setup-service.ts
@src/telegram/setup-handlers.ts
</context>

<interfaces>
- `parseLocalTime(value: string): number` yields minutes since local midnight only for strict `HH:MM`; `formatLocalTime(minutes)` is its renderer inverse.
- `validateSchedule({ defaultStartMinute, durationMinutes, dailyStartMinute, dailyEndMinute })` requires positive whole duration, `dailyStart < dailyEnd`, and `defaultStart + duration <= dailyEnd`.
- `SetupService` keeps all values in the actor-bound draft and exposes a review-ready result only after all eight values and complete invariants pass.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collect schedule, reminder, and planning-access values with complete invariants</name>
  <read_first>
    <file>src/domain/chat/setup-service.ts</file>
    <file>src/shared/callback-schema.ts</file>
    <file>src/telegram/setup-handlers.ts</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
    <file>.planning/phases/01-chat-readiness/01-RESEARCH.md</file>
  </read_first>
  <files>src/domain/chat/types.ts, src/domain/chat/schedule-validator.ts, src/domain/chat/setup-service.ts, src/telegram/keyboards.ts, src/telegram/renderers.ts, src/telegram/setup-handlers.ts, tests/unit/schedule-settings.test.ts</files>
  <behavior>
    - Test 1: weekday choices render in rows of four and three and advance the owner to strict default-start input.
    - Test 2: strict `HH:MM`, positive whole duration, `dailyStart < dailyEnd`, and `defaultStart + duration <= dailyEnd` reject boundary/conflict inputs while preserving other valid draft fields.
    - Test 3: reminder defaults store 10:00/16:00; editing collects exactly two valid times in order and rejects malformed input.
    - Test 4: planning policy offers exactly administrators, previous participants, and anyone; a fresh setup defaults to administrators.
    - Test 5: each successful step renders the next `Step X of 8` prompt and none writes active configuration.
  </behavior>
  <action>Define minute-of-day and weekday domain types and strict parse/format functions, then implement steps 2–8 in the exact UI sequence. Validate the complete draft schedule whenever daily bounds, duration, or default start becomes complete, and again before review; on conflict, show the exact conflict copy and re-prompt only the field that must change. Per D-08, store/display strict 24-hour local wall-clock values interpreted through the confirmed IANA zone. Model reminder times as the default `[600, 960]` or exactly two administrator-entered values. Per D-11, persist the three planning policies with `ADMINISTRATORS` as the new-chat default. Render one prompt per step, action-first buttons no longer than 24 visible characters where possible, one heading at most, one blank line before actions, and code formatting only for commands, zones, IDs, and `HH:MM` literals. Retain the D-13/D-14 current-role check from Plan 01-05 on every continuation.</action>
  <acceptance_criteria>
    <criterion>All eight UI steps, button labels, weekday layout, defaults, format hints, and validation/error copy match the approved UI contract.</criterion>
    <criterion>All schedule invariants are unit-tested at exact equality boundaries and invalid input changes neither active configuration nor unrelated valid draft values.</criterion>
    <criterion>Reminder values are precisely 10:00/16:00 by default or two confirmed valid edits.</criterion>
    <criterion>The policy value stored in the draft is one of the three supported enum members and defaults to administrators only.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run test:unit -- schedule-settings &amp;&amp; npm run test:unit -- setup</automated>
  </verify>
  <done>The draft owner can complete every remaining value and reach review with a fully valid, still-inactive configuration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram text/choice callback → setup service | Times, duration, weekday, reminder pair, and policy are untrusted. |
| setup draft → review projection | Only complete validated values may be represented as review-ready. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10 | Tampering | schedule/reminder input | medium | mitigate | Strict parsers plus complete cross-field validation run at field completion and review readiness. |
| T-01-29 | Elevation of privilege | setup continuation | high | mitigate | Every text/choice handler reuses `AuthorizationService` before actor-bound draft access per D-13/D-14. |
| T-01-30 | Spoofing | planning-access policy | medium | mitigate | Closed enum parsing rejects absent/unsupported values before draft persistence. |
| T-01-SC | Tampering | installed packages | high | mitigate | Continue using only the Plan 01-15-approved lockfile created by Plan 01-02; this plan adds no dependency. |
</threat_model>

<verification>
Run formatting plus focused setup/schedule suites and confirm all edge boundaries, exact UI sequence, default reminder pair, and inactive-draft behavior.
</verification>

<success_criteria>
- CONF-02, CONF-03, CONF-05, and setup-time AUTH-01 values are completely collected through Telegram.
- D-08 and D-11 govern parsing, display, and policy defaults.
- All three configuration edge rows are concrete automated predicates.
- The partial-step UI truth is covered without presenting drafts as active state.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-06-SUMMARY.md` when done.
</output>
