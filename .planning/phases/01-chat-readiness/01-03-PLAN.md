---
phase: 01-chat-readiness
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - src/app/config.ts
  - src/infrastructure/db/prisma.ts
  - tests/helpers/postgres.ts
  - Dockerfile
  - .dockerignore
  - compose.yaml
  - src/app/main.ts
autonomous: true
requirements:
  - CONF-01
  - AUTH-02
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Strict TypeScript, deterministic formatting, unit tests, and real-PostgreSQL integration tests execute through non-watch package scripts."
    - "Invalid boot configuration fails before Telegram or PostgreSQL startup without exposing secrets."
    - "The bot and PostgreSQL start through one Docker Compose command using the same non-root image intended for later deployment."
    - "Exactly one chat-key-sequentialized long-poll process starts after the committed migration succeeds and shuts down cleanly."
  artifacts:
    - path: "tsconfig.json"
      provides: "Strict TypeScript compiler contract"
      contains: "noUncheckedIndexedAccess"
    - path: "vitest.config.ts"
      provides: "Separated deterministic unit and PostgreSQL integration suites"
    - path: "src/app/config.ts"
      provides: "Zod-validated runtime configuration"
      exports:
        - loadConfig
    - path: "tests/helpers/postgres.ts"
      provides: "PostgreSQL 18 Testcontainers and committed-migration fixture"
    - path: "compose.yaml"
      provides: "Healthy PostgreSQL, one-shot migration, and single bot development deployment"
    - path: "Dockerfile"
      provides: "Pinned multi-stage non-root production image"
  key_links:
    - from: "tests/helpers/postgres.ts"
      to: "prisma/migrations/20260819000000_chat_readiness_core/migration.sql"
      via: "fresh PostgreSQL 18 containers always apply committed migrations before integration assertions"
      pattern: "migrate deploy"
    - from: "compose.yaml"
      to: "src/app/main.ts"
      via: "the bot waits for PostgreSQL health and one-shot migration success before starting one runner"
      pattern: "migrate"
    - from: "src/app/main.ts"
      to: "src/app/create-bot.ts"
      via: "composition root starts the already-proven bot with chat-key sequentialization and graceful shutdown"
      pattern: "createBot"
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Package the proven `/setup` tracer with strict deterministic tooling and a reproducible local deployment.

Purpose: Give every expansion slice the same type, format, test, migration, configuration, and runtime contracts without enlarging the migration/tracer plan.
Output: Strict TypeScript/Vitest/database seams, Zod configuration, multi-stage image, single-worker Compose topology, and documented verification commands.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-VALIDATION.md
@.planning/phases/01-chat-readiness/01-SKELETON.md
@.planning/phases/01-chat-readiness/01-02-SUMMARY.md
@package.json
@prisma/schema.prisma
@src/app/create-bot.ts
@tests/integration/walking-skeleton.test.ts
</context>

<interfaces>
- `loadConfig(env)` returns validated `AppConfig` for `BOT_TOKEN`, `DATABASE_URL`, log level, and explicit test/smoke mode without logging source values.
- `createPrismaClient(databaseUrl)` uses Prisma 7 with `@prisma/adapter-pg` and returns the one application database client.
- `startPostgresTestContainer()` plus `applyCommittedMigrations(databaseUrl)` creates a fresh PostgreSQL 18 database and applies only committed Prisma migrations.
- `main.ts` composes config, Prisma, Telegram membership gateway, clock, `createBot`, runner sequentialization, error boundary, and signal shutdown exactly once.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Establish strict configuration and deterministic Wave 0 verification seams</name>
  <read_first>
    <file>package.json</file>
    <file>prisma/schema.prisma</file>
    <file>src/app/create-bot.ts</file>
    <file>tests/integration/walking-skeleton.test.ts</file>
    <file>.planning/phases/01-chat-readiness/01-VALIDATION.md</file>
  </read_first>
  <files>tsconfig.json, vitest.config.ts, src/app/config.ts, src/infrastructure/db/prisma.ts, tests/helpers/postgres.ts</files>
  <action>Enable strict TypeScript with `strict`, `noUncheckedIndexedAccess`, and `exactOptionalPropertyTypes`. Implement `loadConfig` with Zod validation for `BOT_TOKEN`, `DATABASE_URL`, log level, and explicit test/smoke mode without logging secret values. Move PostgreSQL client construction behind `createPrismaClient` using Prisma 7 with `@prisma/adapter-pg`, and centralize PostgreSQL 18 Testcontainers startup plus committed-migration application in reusable test helpers. Configure Vitest so `test:unit` excludes container tests and `test:integration` runs them serially with bounded timeouts and no watch mode. Preserve the injected membership gateway and clock seams for later demotion and expiry tests. Run the pinned `format:check` script from Plan 01-02 alongside lint/type/test checks; do not substitute an unaudited formatter or a write-mode command in verification.</action>
  <acceptance_criteria>
    <criterion>Invalid or missing boot configuration exits before the bot or database starts and never prints `BOT_TOKEN` or the database password.</criterion>
    <criterion>The strict TypeScript compiler accepts the tracer without unchecked optional access or implicit `any`.</criterion>
    <criterion>Formatting, unit, and integration scripts are deterministic non-watch checks; the integration helper always applies committed migrations to a fresh PostgreSQL 18 database.</criterion>
    <criterion>`npm run test:unit` completes in under 60 seconds and its measured duration is recorded in the Plan 01-03 summary.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run test:unit &amp;&amp; npm run test:integration -- walking-skeleton</automated>
  </verify>
  <done>The repository has strict, format-checked, deterministic unit and migrated-PostgreSQL integration feedback paths for every later plan.</done>
</task>

<task type="auto">
  <name>Task 2: Package the proven tracer as the reproducible development deployment</name>
  <reversibility rating="reversible">The Docker image and Compose topology are provider-portable and can be replaced without changing domain or database contracts.</reversibility>
  <read_first>
    <file>.planning/phases/01-chat-readiness/01-SKELETON.md</file>
    <file>package.json</file>
    <file>src/app/config.ts</file>
    <file>src/app/create-bot.ts</file>
    <file>src/infrastructure/db/prisma.ts</file>
  </read_first>
  <files>Dockerfile, .dockerignore, compose.yaml, src/app/main.ts, package.json</files>
  <action>Create a multi-stage `node:24.19-bookworm-slim` image that installs from the committed lockfile, generates Prisma Client, builds the TypeScript service, and runs as a non-root user. Compose PostgreSQL 18.4 with a named development volume and health check, plus exactly one long-polling `bot` service that waits for database health and runs `prisma migrate deploy` as an explicit one-shot dependency before startup. `main.ts` constructs configuration, Prisma, membership gateway, clock, bot, `bot.catch`, and chat-key runner sequentialization, then stops gracefully on SIGTERM/SIGINT. Keep secrets environment-injected and document the executable local path in Compose labels/comments and package scripts without embedding credentials.</action>
  <acceptance_criteria>
    <criterion>The image builds from the lockfile, runs as a non-root user, and contains compiled production output rather than the development dependency tree.</criterion>
    <criterion>Compose declares PostgreSQL health, a one-shot committed-migration service, and exactly one bot long-poll process; the bot cannot start before migration success.</criterion>
    <criterion>`docker compose up --build bot` is the documented full-stack development command when `BOT_TOKEN` is supplied.</criterion>
    <criterion>Process signals close the runner and Prisma pool without starting a second poller.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; docker compose config -q &amp;&amp; docker build -t gsmbot:phase-01 . &amp;&amp; docker compose up -d postgres &amp;&amp; docker compose exec -T postgres pg_isready -U gsmbot &amp;&amp; docker compose run --rm migrate</automated>
  </verify>
  <done>The tracer has strict tooling, a reproducible non-root image, and a local full-stack deployment path backed by the same committed migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| environment → process configuration | Bot token and database credentials are untrusted secrets and invalid values must fail boot. |
| committed source/lockfile → runtime image | Only audited dependencies and generated artifacts may cross into executable production code. |
| Compose services → bot process | Database readiness and committed migration success gate the single long-poll worker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-06 | Information disclosure | configuration/runtime logs | high | mitigate | Zod boot validation and secret-safe error handling prevent bot/database credentials from entering logs. |
| T-01-07 | Denial of service | duplicate bot processes | medium | mitigate | Compose declares one bot service and `main.ts` starts one chat-key-sequentialized runner with graceful shutdown. |
| T-01-28 | Tampering | migration/runtime startup | high | mitigate | Bot startup depends on healthy PostgreSQL and successful committed `migrate deploy`; schema push is absent. |
| T-01-SC | Tampering | npm dependency install | high | mitigate | `npm ci` consumes only the Plan 01-01 human-approved lockfile, including pinned Prettier. |
</threat_model>

<verification>
Run the deterministic format/static/unit/integration commands, validate Compose, apply committed migrations to clean PostgreSQL 18, and build the non-root image.
</verification>

<success_criteria>
- Strict TypeScript, pinned formatting checks, fast unit tests, and real-PostgreSQL integration tests run from documented commands.
- Invalid configuration fails closed without exposing secrets.
- The same committed migration gates local tests and Compose startup.
- One non-root long-poll bot process starts and shuts down cleanly.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-03-SUMMARY.md` when done.
</output>
