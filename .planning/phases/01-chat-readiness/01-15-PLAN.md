---
phase: 01-chat-readiness
plan: 15
type: execute
wave: 2
depends_on: []
files_modified:
  - .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md
autonomous: false
requirements:
  - CONF-01
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The rejected `tz-lookup@6.1.25` row, dedicated evidence, human decision, and halted 01-01 history remain intact."
    - "`geo-tz@8.1.8` has an independent, fully populated provenance, boundary-data, API-entry-point, and Docker-runtime-data review before it can enter the lockfile."
    - "No `package.json`, `package-lock.json`, or `node_modules` exists until `geo-tz` and every other still-pending Phase 1 direct package receive explicit human approval."
    - "Approval produces a resumable 01-15 summary for Plan 01-02; any rejection produces a halted 01-15 summary and returns the package set to planning without installation."
  artifacts:
    - path: ".planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md"
      provides: "Historical tz-lookup rejection plus independently reviewable geo-tz and remaining direct-root decisions"
      contains: "geo-tz"
  key_links:
    - from: ".planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md"
      to: ".planning/phases/01-chat-readiness/01-15-SUMMARY.md"
      via: "the checkpoint outcome records either the fully approved install set or a new explicit halt"
      pattern: "APPROVED|REJECTED"
    - from: ".planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md"
      to: "package.json"
      via: "Plan 01-02 may install only the exact non-rejected rows approved by this recovery checkpoint"
      pattern: "geo-tz.*8\\.1\\.8"
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Recover from the deliberate 01-01 dependency halt by independently auditing and approving `geo-tz@8.1.8` and every other still-pending direct package without installing code.

Purpose: Preserve the rejected resolver evidence while creating a separately auditable prerequisite that can safely unblock Plan 01-02 and D-09's location-derived IANA confirmation flow.
Output: An appended `geo-tz@8.1.8` evidence row and dedicated provenance/data/runtime section, explicit decisions for every required direct root, and `01-15-SUMMARY.md` recording approval or a renewed halt.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md
@.planning/phases/01-chat-readiness/01-01-SUMMARY.md
</context>

<interfaces>
- The only permitted future direct install roots are `grammy@1.45.1`, `@grammyjs/runner@2.0.3`, `zod@4.4.3`, `pino@10.3.1`, `prisma@7.9.1`, `@prisma/client@7.9.1`, `@prisma/adapter-pg@7.9.1`, `pg@8.23.0`, `geo-tz@8.1.8`, `typescript@7.0.2`, `vitest@4.1.11`, `testcontainers@12.1.0`, `prettier@3.9.6`, `@types/node@24.13.3`, and `@types/pg@8.23.1`, subject to every row being APPROVED here.
- `tz-lookup@6.1.25` remains REJECTED historical evidence and is never part of the permitted install set.
- `geo-tz@8.1.8` is consumed later only through package-neutral `TimezoneResolver`; the verified current/future entry point is `geo-tz/dist/find-now`, and its runtime requires the package `data/` directory.
</interfaces>

## Artifacts this phase produces

- Path: `.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md`
- New evidence row: `geo-tz@8.1.8` with exact registry identity, maintainer/repository association, cadence, MIT license, Node engine, lifecycle hooks, direct dependencies, unpacked size, signed release, and decision
- Dedicated section: canonical source provenance, 2026c `timezone-boundary-builder` lineage, `dist/find-now` current/future semantics, multi-candidate behavior, and `node_modules/geo-tz/data` runtime/Docker consequences
- Preserved historical record: the complete `tz-lookup@6.1.25` REJECTED row, dedicated resolver review, 01-01 halt, and no-install handoff

<tasks>

<task type="auto">
  <name>Task 1: Append the independent geo-tz dossier while preserving the rejected resolver history</name>
  <reversibility rating="reversible">This task changes planning evidence only; it installs no code and can be corrected without changing runtime or persistent data.</reversibility>
  <read_first>
    <file>.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md</file>
    <file>.planning/phases/01-chat-readiness/01-01-SUMMARY.md</file>
    <file>.planning/phases/01-chat-readiness/01-RESEARCH.md</file>
    <file>.planning/phases/01-chat-readiness/01-CONTEXT.md</file>
  </read_first>
  <files>.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md</files>
  <action>Append one fully populated `geo-tz@8.1.8` package row without editing or deleting the `tz-lookup@6.1.25` REJECTED row, dedicated evidence, human decision, or 01-01 halt history. Record publisher/maintainer `evansiroky`, canonical `evansiroky/node-geo-tz`, the 2026-07-12 release and 8.1.5–8.1.8 cadence, MIT license, Node `>=16`, absence of `preinstall`/`install`/`postinstall`, direct dependencies `@turf/boolean-point-in-polygon`, `@turf/helpers`, `geobuf`, and `pbf`, 73,446,857-byte unpacked size, and a pending independent human decision. Add a dedicated provenance/data/runtime section covering the signed v8.1.8 release, the 2026c `timezone-boundary-builder` update and current/future product semantics, the verified `geo-tz/dist/find-now` import, documented one-or-many IANA results, and the requirement to retain `node_modules/geo-tz/data` in the runtime image. State that D-09 still requires one explicit actor/chat-bound action per candidate and that only the chosen IANA string will enter the existing setup/settings draft fields; do not propose a Prisma migration. Do not create or modify dependency manifests, lockfiles, installed trees, or production source.</action>
  <acceptance_criteria>
    <criterion>The audit contains both the unchanged `tz-lookup@6.1.25` REJECTED record and a separate fully populated `geo-tz@8.1.8` PENDING HUMAN REVIEW row.</criterion>
    <criterion>The dedicated geo-tz section records exact package provenance, release/data provenance, `geo-tz/dist/find-now`, multi-candidate behavior, four direct dependencies, lifecycle-hook status, unpacked size, and the Docker runtime data path.</criterion>
    <criterion>The dossier explicitly retains the existing one-string Prisma draft/configuration model and requires explicit D-09 candidate selection rather than first-result selection.</criterion>
    <criterion>No `package.json`, `package-lock.json`, or `node_modules` path is created.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md &amp;&amp; rg -q '^\| `geo-tz` \| `8\.1\.8` \|' .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md &amp;&amp; rg -q 'geo-tz/dist/find-now' .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md &amp;&amp; rg -q 'node_modules/geo-tz/data' .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md &amp;&amp; rg -q '2026c' .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md &amp;&amp; rg -q '^\| `tz-lookup` \| `6\.1\.25` .*\*\*REJECTED\*\*' .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md &amp;&amp; test ! -e package.json &amp;&amp; test ! -e package-lock.json &amp;&amp; test ! -d node_modules</automated>
  </verify>
  <done>The audit independently describes geo-tz's package, boundary data, API, ambiguity, and runtime-data risks while the rejected tz-lookup history and no-install state remain intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve geo-tz and every other still-pending direct root or halt again</name>
  <read_first>
    <file>.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md</file>
    <file>.planning/phases/01-chat-readiness/01-RESEARCH.md</file>
    <file>.planning/phases/01-chat-readiness/01-01-SUMMARY.md</file>
  </read_first>
  <files>.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md</files>
  <action>Review `geo-tz@8.1.8` independently and review every other direct-root row that still says PENDING HUMAN REVIEW. Replace each pending decision with `APPROVED` or `REJECTED` plus dated, evidence-based notes. For geo-tz, explicitly decide on maintainer/source association, signed release, 2026c boundary-data lineage, MIT license, lifecycle hooks, four direct dependencies, unpacked size, multi-candidate API, and runtime `data/` directory. A package-set approval is valid only when every required non-historical direct root is APPROVED; the historical tz-lookup rejection remains unchanged. If any required package is rejected, stop, keep all executable dependency artifacts absent, record the rejected package/evidence in `01-15-SUMMARY.md`, and return to planning. Do not substitute or install a package at this checkpoint.</action>
  <what-built>An expanded no-install dependency dossier that preserves the rejected tz-lookup record and independently documents geo-tz's package, boundary data, API, and Docker-runtime behavior.</what-built>
  <how-to-verify>
    1. Compare every pending row with its official npm package page and canonical source repository.
    2. For `geo-tz@8.1.8`, verify the `evansiroky` npm/source association, signed v8.1.8 release, 2026c builder release, MIT license, Node engine, lifecycle-hook absence, four direct dependencies, unpacked size, `dist/find-now` entry point, and runtime `data/` directory.
    3. Confirm the `tz-lookup@6.1.25` row and dedicated evidence still say REJECTED for the original 2026-08-20 reasons.
    4. Mark every remaining required direct root APPROVED or REJECTED with dated notes. Resume only if every required non-historical row is APPROVED.
  </how-to-verify>
  <acceptance_criteria>
    <criterion>Every required non-historical direct package row is explicitly APPROVED with dated reviewer notes before Plan 01-02 may run.</criterion>
    <criterion>`geo-tz@8.1.8` has its own decision covering package provenance, boundary-data provenance/freshness, API ambiguity, and Docker runtime-data behavior.</criterion>
    <criterion>`tz-lookup@6.1.25` remains REJECTED historical evidence and cannot be inferred or replaced by the new decision.</criterion>
    <criterion>Any rejection halts 01-15 with a summary and leaves package manifests, lockfile, installed tree, and production source absent.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>awk -F'|' '/^\| `/{ package=$2; decision=$11; if (package ~ /tz-lookup/) next; if (decision !~ /APPROVED/) bad=1 } END { exit bad }' .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md &amp;&amp; rg -q '^\| `geo-tz` \| `8\.1\.8` .*\*\*APPROVED\*\*' .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md &amp;&amp; rg -q '^\| `tz-lookup` \| `6\.1\.25` .*\*\*REJECTED\*\*' .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md &amp;&amp; test ! -e package.json &amp;&amp; test ! -e package-lock.json &amp;&amp; test ! -d node_modules</automated>
    <human-check>Confirm every required direct root is approved from official evidence, with geo-tz reviewed independently and the tz-lookup rejection preserved.</human-check>
  </verify>
  <resume-signal>Type "approved" only when every required non-historical direct root is marked APPROVED, or report the rejected package and evidence so 01-15 halts.</resume-signal>
  <done>The package set is either independently approved and resumable through `01-15-SUMMARY.md`, or halted again with explicit rejection evidence and no installation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm/source/release metadata → recovery dossier | Package identity, maintainer, release, data-lineage, and lifecycle claims must be checked before code executes. |
| boundary-data package → future Docker runtime | The selected resolver depends on a large on-disk data product that image construction must preserve and smoke-test. |
| human decisions → Plan 01-02 install set | Only explicit approval of every required non-historical direct root may unblock manifest and lockfile creation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | npm package installation | high | mitigate | Task 1 records exact no-install evidence; blocking Task 2 approves every direct root before Plan 01-02 may create executable artifacts. |
| T-01-36 | Spoofing | geo-tz publisher/source/release identity | high | mitigate | Compare the exact npm identity with the canonical repository, signed tag, license, lifecycle fields, and 2026c source-data release. |
| T-01-37 | Tampering | timezone boundary/runtime data | high | mitigate | Record the builder provenance, current/future entry point, multi-result semantics, unpacked size, and required Docker data path for later build/smoke enforcement. |
| T-01-38 | Repudiation | historical resolver decision | medium | mitigate | Preserve the immutable tz-lookup rejection record and create a separate 01-15 decision/summary rather than rewriting 01-01 history. |
</threat_model>

<verification>
Confirm the appended audit has complete geo-tz evidence, every required non-historical row is approved, tz-lookup remains rejected, and no executable dependency artifact exists before the checkpoint succeeds.
</verification>

<success_criteria>
- The 01-01 halt and rejected tz-lookup evidence remain intact.
- `geo-tz@8.1.8` receives an independent package, data, API, and Docker-runtime review.
- Every still-pending direct package receives an explicit decision before Plan 01-02.
- Approval creates a resumable 01-15 summary; rejection creates a halted 01-15 summary with no install or substitution.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-15-SUMMARY.md` on approval, or create the same summary with `status: halted` and the rejection evidence when the checkpoint rejects any required package.
</output>
