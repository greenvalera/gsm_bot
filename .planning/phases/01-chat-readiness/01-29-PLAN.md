---
phase: 01-chat-readiness
plan: 29
type: execute
wave: 2
depends_on:
  - 01-28
files_modified:
  - .planning/phases/01-chat-readiness/deferred-items.md
  - .planning/WINDOWS.md
autonomous: true
requirements:
  - AUTH-01
gap_closure: true
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Both formerly-failing assertions in `tests/integration/chat-configuration.test.ts` are demonstrated green by a captured, dated run of that named test file, not by assumption."
    - "The change that closed each assertion is named by commit, established from git history rather than inferred from the file's current state."
    - "For the invalid-policy assertion the record states plainly which contract won: the test was rewritten to pin a fail-soft result, and `SettingsService.selectPlanningAccessPolicy` was not changed to throw."
    - "Unsupported planning-access values fail soft as `undefined`, leave the actor-bound draft and committed configuration unchanged, and cannot be saved as a policy."
    - "Broken window 16 is closed with a reason that cites that evidence, and `deferred-items.md` no longer presents these two failures as open while still retaining their original text."
    - "The timezone surface shows one valid candidate or every valid ambiguous candidate with one bound action per zone and no automatic first-result choice; settings shows all committed values in fixed section order; roster shows sorted readable identities with one `Remove member` action each."
    - statement: "Settings projection handlers have held-out rendering tests for delayed reads and read failures, never presenting partial database results as authoritative configuration."
      verification: backstop
  artifacts:
    - path: ".planning/phases/01-chat-readiness/deferred-items.md"
      provides: "A reconciled record that retains the original finding text and adds dated evidence, commit provenance, and the explicit contract verdict"
    - path: ".planning/WINDOWS.md"
      provides: "Broken window 16 closed with a substantive, evidence-citing reason"
  key_links:
    - from: ".planning/WINDOWS.md"
      to: ".planning/phases/01-chat-readiness/deferred-items.md"
      via: "window 16's reason cites the reconciled record, which is where the commit provenance and contract verdict live"
      pattern: "deferred-items.md"
    - from: ".planning/phases/01-chat-readiness/deferred-items.md"
      to: "tests/integration/chat-configuration.test.ts"
      via: "the record names the two assertions by test name and cites the dated run that proved them green"
      pattern: "chat-configuration.test.ts"
    - from: "tests/integration/chat-configuration.test.ts"
      to: "src/domain/chat/settings-service.ts"
      via: "the invalid-policy assertion pins the fail-soft return of selectValue and its durable non-mutation, not an exception"
      pattern: "selectPlanningAccessPolicy"
  prohibitions:
    - requirement_id: AUTH-01
      category: transparency
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT close a broken window on a test that was weakened, and MUST NOT record a test-side contract change as if it were a production defect fix."
    - requirement_id: AUTH-01
      category: values
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT delete the original deferred-items finding text when reconciling it; the superseded record must remain auditable alongside its resolution."
    - requirement_id: AUTH-01
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT report the integration suite as green without actually running it; an unavailable container runtime is a halt, not a pass."
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Dispose of broken window 16 honestly: prove the two planning-access assertions green, establish from git history what actually closed each one, state which contract won for the invalid-policy case, then close the window and reconcile the deferred-items record.

Purpose: Window 16 is the last open non-F-12 item gating this phase, and closing it on assumption — or on a test that was weakened rather than a defect that was fixed — would leave a false green in the ledger the ship gate reads.
Output: A dated evidence capture, named commit provenance, an explicit contract verdict, window 16 closed with a substantive reason, and a reconciled `deferred-items.md`.
</objective>

<execution_context>
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/workflows/execute-plan.md
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/deferred-items.md
@.planning/phases/01-chat-readiness/01-25-PLAN.md
@.planning/phases/01-chat-readiness/01-25-SUMMARY.md
@tests/integration/chat-configuration.test.ts
@src/domain/chat/settings-service.ts
</context>

<interfaces>
- `SettingsService.selectPlanningAccessPolicy(chatId, actorId, draftId, policy, now)` in `src/domain/chat/settings-service.ts:274` delegates to `selectValue(...)` at `:235`, which returns `SettingsReview | undefined` and resolves `undefined` for stale, mis-bound, expired, unavailable, schedule-invalid, and unsupported values.
- The only `Unsupported planning access policy.` throw in the codebase lives in `src/domain/chat/setup-service.ts:273`, on the SETUP path — it is not, and never was, the settings-edit contract.
- `settings-handlers.ts` maps an `undefined` `selectValue` result onto the existing stale private alert, so fail-soft is already a routed, user-visible outcome rather than a swallowed error.
- `tests/integration/chat-configuration.test.ts` declares a `callbackTokenFor(keyboard, label)` helper that resolves a card action by exact visible label and throws unless exactly one match exists.
- `settingsDashboardKeyboard` in `src/telegram/keyboards.ts:91` renders `Edit time zone` first and `Edit planning access` in a later row; row order is presentation, not field identity.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Prove the two assertions green, establish provenance, and name the winning contract</name>
  <files>.planning/phases/01-chat-readiness/deferred-items.md</files>
  <read_first>
    - .planning/WINDOWS.md — read window 16 in full, and read windows 2 and 3, whose descriptions name the same two assertions at the same file
    - .planning/phases/01-chat-readiness/deferred-items.md — the record to reconcile; its current text presents both failures as open
    - tests/integration/chat-configuration.test.ts — read the `callbackTokenFor` helper near line 28, the dashboard test around line 176 that now resolves `Edit planning access` by label, and the invalid-policy block around line 328 that pins the fail-soft result with its explanatory comment
    - src/domain/chat/settings-service.ts — read `selectValue` at line 235 and `selectPlanningAccessPolicy` at line 274 to confirm no throw path exists on this surface
    - src/domain/chat/setup-service.ts — read line 273 to confirm the only `Unsupported planning access policy.` throw belongs to the setup path
    - .planning/phases/01-chat-readiness/01-25-SUMMARY.md — read what plan 01-25 recorded about the two changes it made
  </read_first>
  <precondition>A Docker-compatible container runtime is reachable, because `tests/integration/chat-configuration.test.ts` provisions PostgreSQL through Testcontainers; broken window 1 was exactly this failure, and without a runtime the evidence cannot be captured and window 16 must not be closed.</precondition>
  <action>Run `npx vitest run --project integration tests/integration/chat-configuration.test.ts` and capture the dated, sanitized result — file count, test count, and the two named test titles `renders committed settings in fixed order and changes planning access only after review` and `rejects stale, expired, duplicate, and invalid planning-access saves without revision changes`. Then establish provenance from git rather than from the file's present contents: use `git log --oneline -- tests/integration/chat-configuration.test.ts` and `git show` to identify that commit `40b77dc` (`test(01-25): address the settings dashboard action by label`) replaced the positional `inline_keyboard[0][0]` read with a `callbackTokenFor(..., "Edit planning access")` label lookup and thereby closed the first assertion, and that commit `cfa3ddc` (`test(01-25): pin unsupported policy selection as fail-soft`) closed the second. Confirm by direct inspection that `cfa3ddc` changed only the test file and that no production file changed, and state the consequence plainly in the record: the second assertion passes because the TEST was rewritten to pin the fail-soft contract, not because `SettingsService.selectPlanningAccessPolicy` was changed to throw. Confirm and record that this fail-soft contract is the intended one, citing three independent facts — `selectValue` returns `SettingsReview | undefined` by design for every rejection reason, `settings-handlers.ts` already routes `undefined` to the stale private alert so the rejection is user-visible rather than silent, and the rewritten test additionally asserts durable non-mutation of both the committed row and the actor-bound draft, so the value cannot pass by destroying the workflow. Note that plan 01-28 later added an unrelated time-zone-prompt assertion to this same file, so the file's current diff is not the provenance source. Rewrite `.planning/phases/01-chat-readiness/deferred-items.md` as a reconciled record: keep the existing `## Pre-existing integration failures` section text intact under a clearly marked superseded heading, and add a `## Resolution` section carrying the dated run evidence, the two commit SHAs with their exact subjects, the contract verdict, and a note that window 16 duplicates windows 2 and 3, which were already marked fixed on 2026-08-26 minutes after those two commits landed. Write in English per the project documentation-language constraint. Per D-12 the planning-access policy broadens access beyond administrators rather than replacing administrator access, and nothing in this task changes that.</action>
  <reversibility rating="costly">Ratifying the fail-soft invalid-policy contract keeps a shipped behaviour that `settings-handlers.ts` already maps onto the stale private alert. Reversing it later to a throwing contract would change `SettingsService`'s error surface, every caller that treats `undefined` as a rejection, and the landed 01-25 assertions — recoverable, but not a one-line revert. Flagged, not gated: this task ratifies an existing decision rather than making a new one.</reversibility>
  <verify>
    <automated>npx vitest run --project integration tests/integration/chat-configuration.test.ts &amp;&amp; grep -q '## Resolution' .planning/phases/01-chat-readiness/deferred-items.md &amp;&amp; grep -q '40b77dc' .planning/phases/01-chat-readiness/deferred-items.md &amp;&amp; grep -q 'cfa3ddc' .planning/phases/01-chat-readiness/deferred-items.md &amp;&amp; grep -q 'fail-soft' .planning/phases/01-chat-readiness/deferred-items.md &amp;&amp; grep -q 'chat-configuration.test.ts:186' .planning/phases/01-chat-readiness/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project integration tests/integration/chat-configuration.test.ts` exits 0 and the captured evidence names the observed test count.
    - `deferred-items.md` names both commits by SHA and by their exact commit subjects.
    - `deferred-items.md` states in one unambiguous sentence that the invalid-policy assertion passes because the test was rewritten, not because the service now throws.
    - `deferred-items.md` records the fail-soft contract as intended and cites the three supporting facts (undefined-by-design return, stale-alert routing, durable non-mutation assertions).
    - The original finding text is still present in the file under a superseded heading; `git diff` shows it retained, not deleted.
    - `deferred-items.md` records that window 16 duplicates windows 2 and 3.
  </acceptance_criteria>
  <done>The disposition rests on a captured run, named commits, and an explicit contract verdict rather than on the observation that the file is green today.</done>
</task>

<task type="auto">
  <name>Task 2: Close broken window 16 against the reconciled record</name>
  <files>.planning/WINDOWS.md</files>
  <read_first>
    - .planning/WINDOWS.md — read window 16, then windows 2 and 3 for the duplicate finding, and window 10 for how a substantive reason is carried in both the markdown table and the trailing JSON block
    - .planning/phases/01-chat-readiness/deferred-items.md — the Task 1 output, which is the sole evidence this closure may cite
    - .planning/phases/01-chat-readiness/01-27-SUMMARY.md — read the `Next Phase Readiness` note recording that window 16 requires disposition before Phase 1 can be declared complete
  </read_first>
  <action>Close broken window 16 by running `node /home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/bin/gsd-tools.cjs windows fixed 16`, then set the `reason` cell for row 16 — in the markdown table and in the matching object of the trailing JSON block, byte-identically — to a sentence that states all four facts: both assertions in `tests/integration/chat-configuration.test.ts` now pass in a dated captured run; commit `40b77dc` closed the dashboard-label assertion and commit `cfa3ddc` closed the invalid-policy assertion; the second passes because the test was rewritten to pin the intended fail-soft contract rather than because the service was changed to throw; and window 16 duplicates windows 2 and 3, which were already fixed. Point the reason at `.planning/phases/01-chat-readiness/deferred-items.md` for the full record. Do not alter window 17, any other fixed window, or the waived window 10, and do not edit any `resolved_at` value other than the one the tool writes for row 16. After the edit, run `gsd-tools windows status` and confirm the counts reconcile: with window 17 already closed by plan 01-28, `open_count` must reach 0, `fixed_count` 16, and `waived_count` 1. Per AUTH-01 this closes the ledger entry that stood on the planning-access settings surface.</action>
  <verify>
    <automated>node /home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/bin/gsd-tools.cjs windows status &amp;&amp; node -e "const t=require('fs').readFileSync('.planning/WINDOWS.md','utf8');const j=JSON.parse(t.slice(t.indexOf('[',t.indexOf('json')),t.lastIndexOf(']')+1));const w=j.find(x=>x.id===16);if(w.status!=='fixed')process.exit(1);if(!w.reason||w.reason.length<60)process.exit(1);if(!/40b77dc/.test(w.reason)||!/cfa3ddc/.test(w.reason))process.exit(1);if(j.some(x=>x.status==='open'))process.exit(1);if(j.find(x=>x.id===10).status!=='waived')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `gsd-tools windows status` reports `open_count: 0`, `fixed_count: 16`, `waived_count: 1`, `total_count: 17`.
    - Window 16 has status `fixed`, a non-empty `resolved_at`, and a reason naming both commits and the test-versus-service distinction.
    - The reason string is identical in the markdown table and in the JSON block.
    - Window 10 remains `waived` and every previously fixed window keeps its original `resolved_at`.
    - The reason references `deferred-items.md` as the full record.
  </acceptance_criteria>
  <done>The ledger carries no open window, and window 16's closure is auditable back to a captured run and two named commits.</done>
</task>

</tasks>

## Artifacts this phase produces

**New document elements**
- `deferred-items.md` `## Resolution` section — dated run evidence, commit provenance (`40b77dc`, `cfa3ddc`), the fail-soft contract verdict, and the window 2/3 duplication note
- `deferred-items.md` superseded heading retaining the original `## Pre-existing integration failures` text

**Modified in place, no new symbol**
- `.planning/WINDOWS.md` — window 16 `status`, `reason`, `resolved_at`, and the frontmatter counts

**No new source symbol, file, function, constant, or test is produced by this plan.** No production or test source file is modified; this plan is a disposition, and any change to `src/` or `tests/` would exceed its scope.

<assumption_delta_decision>
**Signal:** `"3. A chat administrator can choose whether administrators, previous-poll participants, or anyone may start planning."` — a derived value became chosen / a constant became a parameter.

**Noun now primary:** the per-chat planning-access policy (`ChatConfiguration.planningAccessPolicy`), rather than a fixed administrators-only constant.

**Decision:** `no-change`.

**Rationale:** the promotion already shipped in Phase 1 under AUTH-01 — the policy is a persisted, administrator-selected column with a setup step, a `/settings` edit surface, and its own tests. This plan disposes of a ledger entry on that surface and ratifies an existing invalid-input contract; it introduces no new parameter and changes no default. Recorded explicitly rather than omitted.
</assumption_delta_decision>

<schema_relevance>
**Conclusion: no Prisma schema file is touched.** This plan's `files_modified` contains only `deferred-items.md` and `.planning/WINDOWS.md`. No `prisma/schema.prisma` or `prisma/schema/*.prisma` entry exists, so no `npx prisma db push` task is injected. Recorded as a decision.
</schema_relevance>

<flagged_assumptions>
The complete 12-item spec-less edge-probe register for this phase is authored in `01-28-PLAN.md` under `<flagged_assumptions>`; it is not restated here to avoid two drifting copies. The rows relevant to this plan:

| # | Requirement | Category | Probe | Disposition |
|---|---|---|---|---|
| 9 | AUTH-01 | unclassified | review manually | **Unresolved, covered.** Planning-access choice ships with `tests/unit/planning-access.test.ts` and `tests/integration/chat-configuration.test.ts` (12/12 green). This plan ratifies the invalid-policy contract on exactly that surface and lifts it into `must_haves`. An `unclassified` row is never auto-resolved, so it stays `unresolved` regardless. |
| 10 | AUTH-02 | adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | **Unresolved, covered.** Referenced because the invalid-policy path is an authorization-adjacent rejection: an unsupported value is rejected without mutating either the committed row or the actor-bound draft, asserted by the rewritten test. |

No item is dropped here; the other ten are dispositioned in `01-28-PLAN.md`.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted callback payload to settings mutation | An unsupported planning-access value arrives as callback data and must be rejected without mutating committed configuration or the actor-bound draft. |
| Test evidence to the broken-window ledger | The ledger is the register `/gsd-ship` gates on; a closure claim must be traceable to an observed run and named commits. |
| Historical record to reconciled record | `deferred-items.md` is audit material; reconciliation must add, not overwrite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-29-01 | Repudiation | Broken-window ledger entry 16 | high | mitigate | Close only against a captured dated run of the named test file plus commit provenance established from `git log`/`git show`, and record the test-versus-service distinction in the reason itself so a weakened test cannot masquerade as a fixed defect. |
| T-01-29-02 | Elevation of Privilege | Unsupported planning-access policy value | high | mitigate | Re-prove the fail-soft contract is non-mutating: the invalid value resolves `undefined`, the committed policy and revision are unchanged, the actor-bound draft keeps its field, expiry, and replacement payload, and the value cannot be saved. |
| T-01-29-03 | Tampering | `deferred-items.md` audit trail | medium | mitigate | Retain the original finding text under a superseded heading and append the resolution; a `git diff` check proves the original was kept rather than deleted. |
| T-01-29-04 | Information Disclosure | Captured test evidence | low | mitigate | Record only file, test, and title counts plus commit SHAs; record no database contents, chat identifier, or credential. |
| T-01-29-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation or dependency change occurs in this plan, so no package-legitimacy audit or install checkpoint is required. |
</threat_model>

<verification>
`tests/integration/chat-configuration.test.ts` is proven green by a captured dated run; `deferred-items.md` names the two closing commits, states which contract won for the invalid-policy assertion, retains the original finding text, and records the window 2/3 duplication; window 16 is closed with a reason carrying that evidence; the ledger reports no open window while window 10 stays waived.
</verification>

<success_criteria>
- The named integration test file runs and exits 0, with the result captured and dated.
- Provenance is established from git history, naming `40b77dc` and `cfa3ddc`, not inferred from the current file.
- The record states unambiguously that the invalid-policy assertion passes because the test was rewritten, and ratifies the fail-soft contract with three cited facts.
- Window 16 is `fixed` with a substantive reason identical in the markdown table and the JSON block.
- `open_count` is 0, `fixed_count` 16, `waived_count` 1, and no other window row changed.
- No file under `src/` or `tests/` is modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-29-SUMMARY.md` when done.
</output>
