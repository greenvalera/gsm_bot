---
phase: 01-chat-readiness
plan: 07
type: execute
wave: 8
depends_on:
  - 01-06
files_modified:
  - src/domain/chat/setup-service.ts
  - src/telegram/setup-handlers.ts
  - src/telegram/renderers.ts
  - tests/fakes/chat-readiness.ts
  - tests/integration/chat-configuration.test.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-05
  - AUTH-01
  - AUTH-02
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-03, all eight validated values remain draft-only until `Save configuration` promotes the complete configuration and deletes the draft in one transaction."
    - "Review shows timezone, default day/start, duration, daily start/end, reminder times, and planning access in the approved fixed order."
    - "Save rechecks current administrator status, draft ownership/expiry, full validation, and expected revision before any active write."
    - "Validation, revision conflict, persistence failure, expiry, demotion, stale action, or duplicate action preserves the previous authoritative configuration."
    - "A successful save survives a new Prisma client/process read and duplicate confirmation returns `Already applied.` without another mutation."
  artifacts:
    - path: "src/domain/chat/setup-service.ts"
      provides: "Transactional complete-draft promotion with expected revision"
      exports:
        - SetupService
    - path: "src/telegram/renderers.ts"
      provides: "Complete review and committed-success projections"
    - path: "tests/fakes/chat-readiness.ts"
      provides: "Deterministic membership, clock, resolver, and persistence-failure fakes"
    - path: "tests/integration/chat-configuration.test.ts"
      provides: "Atomic promotion, expiry, revision, demotion, failure, duplicate, and restart coverage"
  key_links:
    - from: "src/domain/chat/setup-service.ts"
      to: "prisma/schema.prisma"
      via: "one transaction validates expected revision, writes full ChatConfiguration, consumes action, and deletes SetupDraft"
      pattern: "\$transaction"
    - from: "src/telegram/setup-handlers.ts"
      to: "src/domain/auth/authorization-service.ts"
      via: "save acknowledgement is followed by fresh role lookup before transaction entry"
      pattern: "requireCurrentAdministrator"
    - from: "tests/integration/chat-configuration.test.ts"
      to: "src/domain/chat/setup-service.ts"
      via: "a new Prisma client reads the committed configuration after save"
      pattern: "save"
  prohibitions:
    - requirement_id: CONF-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT present an incomplete setup draft as active chat configuration or claim it was saved."
    - requirement_id: AUTH-02
      category: privacy
      status: unverified
      flagged: true
      statement: "MUST NOT reveal draft values, prior administrator history, or internal authorization details in a permission-denial message."
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Activate the complete reviewed setup atomically and prove all failure/restart invariants.

Purpose: Close the first-time readiness outcome without partial writes, stale overwrites, or authorization bypass.
Output: Review/save transaction, deterministic fakes, and migrated PostgreSQL integration coverage.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-PATTERNS.md
@.planning/phases/01-chat-readiness/01-VALIDATION.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-06-SUMMARY.md
@prisma/schema.prisma
@src/domain/auth/authorization-service.ts
@src/domain/chat/schedule-validator.ts
</context>

<interfaces>
- `SetupService.saveConfiguration(chatId, actorId, callbackToken, now)` validates the active actor draft and expected revision inside one short Prisma transaction.
- `renderSetupReview` receives a complete validated draft and renders all eight values in fixed UI order; it never accepts partial data.
- Callback acknowledgement precedes fresh role lookup; `CallbackAction` consumption and configuration promotion occur atomically.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Review and atomically activate the complete chat configuration</name>
  <read_first>
    <file>prisma/schema.prisma</file>
    <file>src/domain/auth/authorization-service.ts</file>
    <file>src/domain/chat/schedule-validator.ts</file>
    <file>src/domain/chat/setup-service.ts</file>
    <file>src/telegram/renderers.ts</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
  </read_first>
  <files>src/domain/chat/setup-service.ts, src/telegram/setup-handlers.ts, src/telegram/renderers.ts, tests/fakes/chat-readiness.ts, tests/integration/chat-configuration.test.ts</files>
  <behavior>
    - Test 1: review displays all eight labelled values and only `Save configuration` can promote them.
    - Test 2: save performs a fresh administrator lookup, final complete validation, expected-revision check, configuration write, callback consumption, and draft deletion as one transaction.
    - Test 3: validation, revision conflict, persistence failure, expiry, or demotion leaves active configuration unchanged and emits the exact safe error/stale/denial surface.
    - Test 4: a successful duplicate tap is a non-mutating no-op with `Already applied.` and the authoritative committed settings remain visible.
    - Test 5: a new Prisma client reads every committed field after save, while cancelled setup deletes only the owner's draft.
  </behavior>
  <action>Per D-03, render the complete `Review configuration` surface in fixed field order and implement `Save configuration` as the sole atomic promotion action. Before the transaction, acknowledge the callback and obtain current administrator status; inside one short Prisma transaction require the active actor-bound draft, re-run complete validation, compare expected configuration revision, create/update the full `ChatConfiguration`, consume the callback action, and delete the draft. On transaction failure keep the previous active record and return the generic save copy; on stale/expired/duplicate action return the specified alert without another write. Cancel deletes only the owner's draft. Ensure the integration test applies committed migrations, reads committed values through a new Prisma query/client after the transaction, and proves restart persistence.</action>
  <acceptance_criteria>
    <criterion>The review contains timezone, default day/start, duration, daily start/end, reminders, and planning access in the specified order.</criterion>
    <criterion>There is no intermediate active-setting update; the transaction yields either the entire confirmed configuration plus no draft, or the previous active state plus the draft/error state required by the failure.</criterion>
    <criterion>Duplicate, stale, expired, revision-conflicting, failed, and demoted-actor saves are non-mutating and return exact contract copy.</criterion>
    <criterion>A fresh Prisma client after save reads the complete configuration, proving restart durability.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run test:unit -- setup &amp;&amp; npm run test:unit -- schedule-settings &amp;&amp; npm run test:integration -- chat-configuration</automated>
  </verify>
  <done>A current administrator can review and atomically activate all required chat settings; every failure preserves the prior authoritative state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| setup review callback → current membership gateway | Callback actor and prior role are untrusted until a fresh Telegram lookup succeeds. |
| complete SetupDraft → ChatConfiguration | Only a valid actor-bound, unexpired, revision-current draft may cross into active state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-31 | Elevation of privilege | setup save | high | mitigate | Acknowledge, reauthorize current actor, then enter the transaction; denial deletes actor draft per D-14. |
| T-01-32 | Tampering | active configuration promotion | high | mitigate | Full validation, expected revision, full-record write, action consumption, and draft deletion share one transaction. |
| T-01-12 | Repudiation | duplicate/stale save | low | accept | Single-use action plus revision checks yield deterministic no-op copy and preserve authoritative state. |
| T-01-SC | Tampering | installed packages | high | mitigate | Continue using only the Plan 01-15-approved lockfile created by Plan 01-02; this plan adds no dependency. |
</threat_model>

<verification>
Run formatting, focused setup/schedule tests, and the migrated chat-configuration integration suite; inspect both atomic outcomes and restart reads.
</verification>

<success_criteria>
- CONF-01, CONF-02, CONF-03, CONF-05, AUTH-01, and AUTH-02 reach durable active state only through D-03 confirmation.
- D-13/D-14 remain effective at final save.
- Duplicate, stale, conflicting, failed, expired, and demoted paths preserve authoritative state.
- Restart persistence proves the full first-time configuration outcome.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-07-SUMMARY.md` when done.
</output>
