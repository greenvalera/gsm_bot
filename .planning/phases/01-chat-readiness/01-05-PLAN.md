---
phase: 01-chat-readiness
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - src/app/create-bot.ts
  - src/domain/auth/authorization-service.ts
  - src/domain/chat/setup-service.ts
  - src/infrastructure/time/timezone-resolver.ts
  - src/shared/callback-schema.ts
  - src/telegram/setup-handlers.ts
  - tests/unit/setup.test.ts
autonomous: true
requirements:
  - CONF-01
  - AUTH-02
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-01 and D-02, `/setup` begins or resumes only the current administrator's actor-bound draft and expires it after 30 minutes of inactivity."
    - "Per D-09, a group location resolves to a candidate IANA timezone that remains inactive until the same administrator explicitly confirms it."
    - "Per D-13 and D-14, every protected command, location, text, or callback refreshes the current Telegram role and denial immediately deletes the actor's setup/settings draft with channel-appropriate copy."
    - statement: "A location lookup renders an in-flight state and then atomically replaces it with either the candidate zone or the documented resolution failure."
      verification: backstop
    - "[Edge CONF-01/unclassified] Neither raw coordinates nor an inferred candidate can become the active timezone until `Use <IANA zone>` is confirmed and final setup save commits."
  artifacts:
    - path: "src/domain/auth/authorization-service.ts"
      provides: "Current-role revalidation and denial-triggered draft deletion"
      exports:
        - AuthorizationService
        - PermissionDeniedError
    - path: "src/domain/chat/setup-service.ts"
      provides: "Actor-bound resumable setup draft transitions"
      exports:
        - SetupService
    - path: "src/infrastructure/time/timezone-resolver.ts"
      provides: "Fallible coordinate-to-IANA adapter"
      exports:
        - TimezoneResolver
        - TzLookupTimezoneResolver
    - path: "src/telegram/setup-handlers.ts"
      provides: "Authorized setup command, location, text, and callback adapters"
      exports:
        - registerSetupHandlers
  key_links:
    - from: "src/telegram/setup-handlers.ts"
      to: "src/domain/auth/authorization-service.ts"
      via: "every setup entry point calls requireCurrentAdministrator before protected draft access"
      pattern: "requireCurrentAdministrator"
    - from: "src/telegram/setup-handlers.ts"
      to: "src/infrastructure/time/timezone-resolver.ts"
      via: "only the active draft owner's group location reaches the resolver and stores a candidate"
      pattern: "resolve"
    - from: "src/domain/chat/setup-service.ts"
      to: "prisma/schema.prisma"
      via: "SetupDraft ownership and inactivity expiry persist across restarts"
      pattern: "setupDraft"
  prohibitions:
    - requirement_id: CONF-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT silently store a timezone inferred from coordinates without showing the IANA candidate and receiving explicit administrator confirmation."
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Expand the `/setup` tracer through the authorized, resumable, location-confirmed timezone slice.

Purpose: Establish the shared current-role and draft-expiry contracts before the wizard gains additional values.
Output: Authorization service, timezone adapter, opaque callback schema, setup handlers, and deterministic setup tests.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-PATTERNS.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-02-SUMMARY.md
@.planning/phases/01-chat-readiness/01-04-SUMMARY.md
@prisma/schema.prisma
@src/app/create-bot.ts
</context>

<interfaces>
- `AuthorizationService.requireCurrentAdministrator(chatId, actorId)` returns only after a fresh Telegram role lookup; on denial it deletes that actor's setup/settings drafts and throws `PermissionDeniedError`.
- `SetupService` methods accept `chatId`, `actorId`, validated input, and `now`; they never accept administrator authority as a caller-supplied boolean.
- `TimezoneResolver.resolve(latitude, longitude): Promise<string>` is fallible; confirmation validates the returned string through `Intl.DateTimeFormat` before storing it in the draft.
- Callback text contains only a short versioned opaque token; `CallbackAction` supplies chat, actor, kind, target, expiry, and consumption state.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deliver location-confirmed timezone setup with resumable authorization</name>
  <reversibility rating="costly">The action-bound authorization and draft-service interfaces become cross-phase contracts, but can still be migrated before production data exists.</reversibility>
  <read_first>
    <file>src/app/create-bot.ts</file>
    <file>prisma/schema.prisma</file>
    <file>.planning/phases/01-chat-readiness/01-CONTEXT.md</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
    <file>.planning/phases/01-chat-readiness/01-PATTERNS.md</file>
  </read_first>
  <files>src/app/create-bot.ts, src/domain/auth/authorization-service.ts, src/domain/chat/setup-service.ts, src/infrastructure/time/timezone-resolver.ts, src/shared/callback-schema.ts, src/telegram/setup-handlers.ts, tests/unit/setup.test.ts</files>
  <behavior>
    - Test 1: `/setup` or `Start setup` by a current administrator starts/resumes only their unexpired draft and renders `Setup in progress` plus `Step 1 of 8`.
    - Test 2: a location from the draft owner in the same group yields `Time zone found` and a valid IANA candidate but does not update `ChatConfiguration`.
    - Test 3: resolver failure uses the exact UI copy and preserves the timezone step; a different actor's location cannot advance the draft.
    - Test 4: after 30 minutes of inactivity the next action deletes the expired draft and returns the exact expiry copy without mutation.
    - Test 5: demotion before any command, location, text, or callback action deletes the actor's draft and returns channel-appropriate denial copy per D-13/D-14.
  </behavior>
  <action>Per D-01, extend `/setup` as the guided wizard. Extract the tracer's live-role logic into `AuthorizationService` and invoke it at every setup action boundary, including read/resume paths. Per D-02, implement actor/chat-bound lazy 30-minute inactivity expiry with an injected clock. Parse only short versioned callback tokens and load chat/actor/action bindings from `CallbackAction`. Per D-09, wrap the Plan 01-01-approved `tz-lookup` behind `TzLookupTimezoneResolver`, accept ordinary group locations only while the same actor owns an active timezone step, validate the result as an IANA identifier, persist it as `candidateTimezone`, and require `Use <timezone>` before copying it into the draft timezone. Acknowledge callbacks immediately, display the specified in-flight transition, and atomically replace it with either the candidate or exact failure surface. Per D-13 and D-14, use channel-appropriate denial copy and immediately delete the demoted actor's draft. Exclude manual typed-zone input, private-chat location buttons, cached role authorization, and raw coordinates from active configuration.</action>
  <acceptance_criteria>
    <criterion>Every setup command, location, text step, and callback performs a fresh role lookup before draft access or mutation.</criterion>
    <criterion>Draft ownership, chat binding, inactivity expiry, resolver failure, and demotion are covered by deterministic fake clock/gateway/resolver tests.</criterion>
    <criterion>Only an explicitly confirmed valid IANA candidate enters the draft timezone field; active configuration remains absent or unchanged.</criterion>
    <criterion>All prompt, failure, expiry, stale-action, and denial copy matches the UI contract and callback tokens contain no coordinates, zone, names, permissions, or schedule values.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run test:unit -- setup</automated>
  </verify>
  <done>A current administrator can securely begin/resume setup and confirm a location-derived IANA timezone without activating partial configuration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram command/location/text/callback → setup adapter | Actor, chat, input, coordinates, and callback token are untrusted. |
| setup adapter → current membership gateway | Current Telegram role is refreshed before each protected state access. |
| coordinates → timezone resolver | Third-party boundary data produces a candidate that requires human confirmation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-08 | Elevation of privilege | resumable setup draft | high | mitigate | `AuthorizationService` refreshes role at every boundary and deletes the actor draft on denial per D-14. |
| T-01-09 | Spoofing | draft owner/location sender | high | mitigate | Bind draft and CallbackAction to chat and actor; reject a different sender before resolver or state access. |
| T-01-11 | Tampering | timezone inference | medium | mitigate | Validate IANA output, display it as a candidate, and require explicit D-09 confirmation before draft transition. |
| T-01-SC | Tampering | approved `tz-lookup` package | high | mitigate | The adapter is usable only after the independent Plan 01-01 human approval and remains isolated behind `TimezoneResolver`. |
</threat_model>

<verification>
Run formatting and focused setup tests; confirm exact copy, no active configuration write, fresh-role checks at every entry point, and deterministic expiry/resolver failure.
</verification>

<success_criteria>
- D-01, D-02, D-09, D-13, and D-14 govern the reachable timezone setup slice.
- CONF-01 remains candidate-only until explicit confirmation and later final activation.
- AUTH-02 is enforced at command, location, text, and callback boundaries.
- The location UI backstop and CONF-01 edge/prohibition are covered by automated predicates.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-05-SUMMARY.md` when done.
</output>
