---
phase: 01-chat-readiness
plan: 11
type: execute
wave: 11
depends_on:
  - 01-10
files_modified:
  - src/app/create-bot.ts
  - src/app/main.ts
  - src/shared/callback-schema.ts
  - src/shared/logger.ts
  - src/telegram/handlers.ts
  - src/telegram/callbacks.ts
  - tests/unit/logger.test.ts
  - tests/integration/chat-readiness.e2e.test.ts
  - .github/workflows/ci.yml
  - .planning/phases/01-chat-readiness/01-VALIDATION.md
autonomous: false
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-05
  - ROST-01
  - ROST-02
  - ROST-03
  - AUTH-01
  - AUTH-02
user_setup:
  - service: telegram
    why: "Verify actual Telegram group location, callback, edit, and demotion behavior"
    env_vars:
      - name: BOT_TOKEN
        source: "Telegram @BotFather token for a dedicated test bot"
    dashboard_config:
      - task: "Create/use a private test group, add at least two human test accounts, and make the bot plus one test account administrators"
        location: "Telegram group settings -> Administrators"
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every Phase 1 protected command, wizard input, dashboard action, roster action, and callback reaches the same current Telegram administrator boundary before protected state access or mutation."
    - "Every callback is acknowledged immediately; successful mutations replace the originating message with authoritative state and duplicate taps receive `Already applied.`."
    - "Every callback token is short, opaque, and versioned; names, permissions, schedule values, coordinates, Telegram IDs, and authorization claims remain server-side."
    - "Validation, location, permission, expiry, save, duplicate, stale-action, delay, and read failures use approved copy and preserve authoritative state."
    - "A timezone candidate shows the inferred IANA zone; settings shows committed values in fixed order; roster shows sorted safe identities with adjacent removal actions."
    - "Message values wrap in Telegram; action labels remain bounded and full member identity appears only in adjacent message/confirmation text."
    - "A clean CI install deterministically checks formatting, lint, types, unit/integration tests, committed PostgreSQL migrations, and the production image."
  artifacts:
    - path: "src/telegram/handlers.ts"
      provides: "Single registration point for every Phase 1 command and update handler"
      exports:
        - registerChatReadinessHandlers
    - path: "src/telegram/callbacks.ts"
      provides: "Versioned callback dispatch, acknowledgement ordering, and stale/duplicate/error projection"
      exports:
        - registerChatReadinessCallbacks
    - path: "src/shared/logger.ts"
      provides: "Structured redacted Phase 1 logging"
      exports:
        - createLogger
    - path: "tests/unit/logger.test.ts"
      provides: "Secret, coordinate, callback, draft, and identity redaction coverage"
    - path: "tests/integration/chat-readiness.e2e.test.ts"
      provides: "Complete setup/settings/roster/restart/demotion integration flow"
    - path: ".github/workflows/ci.yml"
      provides: "Formatting/static checks, unit/integration tests, fresh migrations, and image build on pull requests"
  key_links:
    - from: "src/app/create-bot.ts"
      to: "src/telegram/handlers.ts"
      via: "composition root registers every command/update exactly once"
      pattern: "registerChatReadinessHandlers"
    - from: "src/telegram/callbacks.ts"
      to: "src/shared/callback-schema.ts"
      via: "one parse/ack/load/authorize/dispatch boundary for all opaque actions"
      pattern: "safeParse"
    - from: "tests/integration/chat-readiness.e2e.test.ts"
      to: "prisma/migrations"
      via: "fresh database runs the full Telegram-native readiness flow and restart reads"
      pattern: "migrate deploy"
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "clean install invokes pinned `format:check` before lint/type/test/migration/image gates"
      pattern: "format:check"
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Close Phase 1 by unifying command/callback routing, proving the full migrated flow in CI, and verifying Telegram-native behavior in a live private group.

Purpose: Ensure setup, settings, authorization, and roster slices compose without bypass, stale projection, migration drift, formatting drift, or uncovered UI state.
Output: Central adapters, redacted logging, full-stack integration test, deterministic CI workflow, completed validation map, and live Telegram evidence.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-PATTERNS.md
@.planning/phases/01-chat-readiness/01-VALIDATION.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/COVERAGE.md
@.planning/phases/01-chat-readiness/01-06-SUMMARY.md
@.planning/phases/01-chat-readiness/01-08-SUMMARY.md
@.planning/phases/01-chat-readiness/01-10-SUMMARY.md
@src/domain/auth/authorization-service.ts
@src/telegram/setup-handlers.ts
@src/telegram/settings-handlers.ts
@src/telegram/roster-handlers.ts
</context>

<interfaces>
- `registerChatReadinessHandlers(bot, services)` composes setup, settings, roster, location, and text routing exactly once.
- `registerChatReadinessCallbacks(bot, services)` acknowledges first, parses a version-prefixed opaque token, loads `CallbackAction`, performs current-role authorization, and dispatches by server-side kind.
- `createLogger` redacts `BOT_TOKEN`, database credentials, raw updates, coordinates, callback payloads, draft payloads, and full roster identities; logs only diagnostic identifiers/action kinds.
- The full integration test recreates the process/client with the same migrated database to prove configuration and roster survive restart.
</interfaces>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Administrators prepare a persistent, access-controlled chat | 01-02–01-11 | COVERED | Tracer plus setup/settings/roster expansions and closure. |
| REQ | CONF-01 | Initialize with confirmed IANA timezone | 01-02, 01-04, 01-06, 01-08 | COVERED | Candidate, explicit confirmation, activation, and edit. |
| REQ | CONF-02 | Configure default weekday/start | 01-05, 01-06, 01-08 | COVERED | Strict setup, activation, and individual edit. |
| REQ | CONF-03 | Configure duration and daily boundaries | 01-05, 01-06, 01-08 | COVERED | Complete invariant checks on collect/save/edit. |
| REQ | CONF-05 | Configure reminder times with 10:00/16:00 defaults | 01-05, 01-06, 01-08 | COVERED | Default, activation, and edited pair. |
| REQ | ROST-01 | Add replied identifiable Telegram users | 01-09 | COVERED | Reply anchor and persistent upsert/reactivation. |
| REQ | ROST-02 | Remove roster users | 01-09, 01-10 | COVERED | Initiator-bound confirmation and hardened projection. |
| REQ | ROST-03 | View current roster | 01-09, 01-10 | COVERED | Safe label, ordering, pagination, empty/delay/error states. |
| REQ | AUTH-01 | Select planning-start policy | 01-05, 01-07, 01-08 | COVERED | Setup default, policy seam, and complete edits. |
| REQ | AUTH-02 | Revalidate current permission before protected actions | 01-02, 01-04–01-11 | COVERED | Shared action-bound authorization and route inventory. |
| RESEARCH | R-01 | Node/TypeScript/grammY runner scaffold | 01-02, 01-03 | COVERED | Tracer then strict runtime/deployment. |
| RESEARCH | R-02 | PostgreSQL/Prisma durable active state and drafts | 01-02, 01-06–01-10 | COVERED | Committed migrations and repository transactions. |
| RESEARCH | R-03 | Live `getChatMember` authorization and draft deletion | 01-02, 01-04–01-11 | COVERED | Shared high-severity authorization mitigation. |
| RESEARCH | R-04 | Atomic setup promotion and revision-safe edits | 01-06–01-08 | COVERED | Expected revision and transactions. |
| RESEARCH | R-05 | Reply-anchored roster identity and soft removal | 01-09, 01-10 | COVERED | Durable identity/history and safe projections. |
| RESEARCH | R-06 | Vetted offline timezone resolver behind adapter | 01-01, 01-04, 01-08 | COVERED | Human legitimacy path then explicit candidate confirmation. |
| RESEARCH | R-07 | Opaque callback tokens and immediate acknowledgement | 01-02, 01-04–01-11 | COVERED | Server-side authority and central dispatch. |
| RESEARCH | R-08 | Zod input/config parsing and Pino redaction | 01-03, 01-11 | COVERED | Boot boundary and safe logs. |
| RESEARCH | R-09 | Testcontainers/Vitest/migration verification | 01-02–01-11 | COVERED | Tracer migration gate, focused suites, and fresh-schema CI. |
| RESEARCH | R-10 | Docker/Compose single long-poll process | 01-03 | COVERED | Portable development deployment. |
| RESEARCH | R-11 | Package legitimacy gate for every SUS package | 01-01 | COVERED | Evidence dossier and blocking human decision. |
| RESEARCH | R-12 | Pinned deterministic formatting check | 01-01–01-03, 01-11 | COVERED | Audit Prettier, pin/install it, define scripts, and gate CI. |
| CONTEXT | D-01 | Guided `/setup` wizard | 01-04–01-06 | COVERED | Authorized timezone, remaining steps, activation. |
| CONTEXT | D-02 | Resume for 30 inactivity minutes, then discard | 01-02, 01-04, 01-06 | COVERED | Durable actor draft, clock, final expiry check. |
| CONTEXT | D-03 | Complete summary and atomic final activation | 01-06 | COVERED | Review plus one transaction. |
| CONTEXT | D-04 | Reply-anchored `/roster_add` identity | 01-09 | COVERED | No typed identity. |
| CONTEXT | D-05 | Alphabetical safe roster projection | 01-09, 01-10 | COVERED | Identity forms, ordering, and paging. |
| CONTEXT | D-06 | Inline Remove plus explicit named confirmation | 01-09, 01-10 | COVERED | Bound destructive action and projection. |
| CONTEXT | D-07 | `/settings` committed dashboard/edit controls | 01-07, 01-08 | COVERED | Fixed sections and every value. |
| CONTEXT | D-08 | 24-hour times in configured timezone | 01-05, 01-08 | COVERED | Strict local-minute representation. |
| CONTEXT | D-09 | Location inference plus explicit IANA confirmation | 01-01, 01-04, 01-08 | COVERED | Audited adapter and group flow. |
| CONTEXT | D-10 | Old/new review before individual save | 01-07, 01-08 | COVERED | Revision-safe edit draft. |
| CONTEXT | D-11 | Administrators-only default | 01-05 | COVERED | Closed enum default. |
| CONTEXT | D-12 | Administrators always allowed; policies broaden | 01-07, 01-08 | COVERED | `canStartPlanning` truth table. |
| CONTEXT | D-13 | Private callback denial, concise command denial | 01-02, 01-04–01-11 | COVERED | Shared channel-specific error mapping. |
| CONTEXT | D-14 | Demotion immediately deletes actor draft | 01-02, 01-04–01-11 | COVERED | Protected boundaries plus full integration test. |

## Spec-less Probe Equality Audit

| Probe section | Surfaced | Authored | Silent drops |
|---|---:|---:|---:|
| Edge rows | 12 | 12 explicit `must_haves.truths` across Plans 01-02, 01-04–01-10 | 0 |
| Kept bespoke prohibitions | 6 | 6 descriptor-less flagged `must_haves.prohibitions` across Plans 01-02, 01-04, 01-06, 01-07, 01-09 | 0 |
| UI consideration rows | 12 resolved rows | 7 explicit strings plus 5 flat `{statement, verification: backstop}` mappings across Plans 01-04–01-11 | 0 |

## Two-Stage Prohibition Recall

| Requirement group | Stage 1 adversarial candidates considered | Stage 2 precision result |
|---|---|---|
| CONF-01 | wrong/ambiguous zone, unconfirmed inference, misleading draft state, dependency provenance, raw-coordinate handling | KEEP unconfirmed-zone and draft-as-active transparency constraints; DROP routine resolver/error handling; refer dependency/coordinate security canon to package gate/threat model. |
| CONF-02, CONF-03, CONF-05 | malformed times, invalid boundaries, partial save, misleading success, coercive defaults | KEEP shared draft-as-active transparency constraint; DROP parsing/atomicity rows already owned by edge predicates/tests. |
| ROST-01, ROST-02, ROST-03 | ambiguous identity, accidental removal, group-membership implication, ID exposure, ordering/paging failures | KEEP group-removal implication and full-ID privacy constraints; DROP routine validation/idempotency/ordering/paging rows owned by edges/tests. |
| AUTH-01, AUTH-02 | policy wording excluding admins, public denial details, stale role, forged action, missing role | KEEP admin-policy transparency and denial-detail privacy constraints; refer generic access-control/tampering canon to threat model/security audit. |

Canon referrals dropped rather than reminted: generic injection, path traversal, dependency tampering, secret leakage, and retention are handled by threat models, package legitimacy, redaction tests, and later security verification. The six authored prohibitions remain project-specific privacy/transparency constraints without fabricated descriptors.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove every Phase 1 route crosses one callback/authorization/durable-state boundary</name>
  <read_first>
    <file>src/app/create-bot.ts</file>
    <file>src/shared/callback-schema.ts</file>
    <file>src/telegram/setup-handlers.ts</file>
    <file>src/telegram/settings-handlers.ts</file>
    <file>src/telegram/roster-handlers.ts</file>
    <file>.planning/phases/01-chat-readiness/COVERAGE.md</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
  </read_first>
  <files>src/app/create-bot.ts, src/shared/callback-schema.ts, src/telegram/handlers.ts, src/telegram/callbacks.ts, tests/integration/chat-readiness.e2e.test.ts</files>
  <behavior>
    - Test 1: every `/setup`, `/settings`, `/roster`, `/roster_add`, location, text-step, and callback route registers once and reaches current-role authorization where protected.
    - Test 2: every versioned callback is acknowledged before parse/load/role/database latency and dispatches solely from server-side kind/bindings.
    - Test 3: malformed, missing, stale, expired, duplicate, cross-chat, cross-actor, demoted, and persistence-failure callbacks preserve authoritative state and return exact copy.
    - Test 4: a full setup → restart → settings edit → roster add/list/remove flow persists through freshly applied PostgreSQL migrations.
  </behavior>
  <action>Centralize Phase 1 handler registration and callback dispatch while keeping feature services/renderers separate. Parse only a version-prefixed opaque token within Telegram's byte limit, acknowledge callbacks immediately, load authoritative `CallbackAction`, refresh current membership, and dispatch by stored kind. Enumerate every protected route in a table-driven integration test so no registered command/callback can bypass authorization. Exercise the complete workflow against committed PostgreSQL migrations, reconstruct the bot/Prisma client mid-test, and assert committed state, actor-bound drafts, duplicate behavior, and UI error copy after restart. Keep every COVERAGE.md INTEGRATE row exercised or explicitly mapped to a focused test; introduce no unlisted Telegram surface.</action>
  <acceptance_criteria>
    <criterion>One central registration test lists every Phase 1 command/update/callback and proves protected routes invoke current-role gateway before state access.</criterion>
    <criterion>All callbacks satisfy acknowledgement-before-latency and server-side-authority rules, including malformed/stale/duplicate/cross-boundary paths.</criterion>
    <criterion>The full migrated workflow survives process/client recreation with identical configuration and roster state.</criterion>
    <criterion>Every `INTEGRATE` row in COVERAGE.md has a handler/test reference and no undocumented external Telegram surface appears.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run test:integration -- chat-readiness.e2e &amp;&amp; npm run test:unit -- authorization</automated>
  </verify>
  <done>The composed bot has no protected-route authorization bypass and the complete readiness workflow survives restart on committed schema.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce formatting, redacted operations, fresh migrations, tests, and image build in CI</name>
  <read_first>
    <file>package.json</file>
    <file>src/app/main.ts</file>
    <file>src/app/config.ts</file>
    <file>Dockerfile</file>
    <file>compose.yaml</file>
    <file>.planning/phases/01-chat-readiness/01-VALIDATION.md</file>
    <file>.planning/phases/01-chat-readiness/01-RESEARCH.md</file>
  </read_first>
  <files>src/app/main.ts, src/shared/logger.ts, tests/unit/logger.test.ts, .github/workflows/ci.yml, .planning/phases/01-chat-readiness/01-VALIDATION.md</files>
  <action>Create structured Pino logging with explicit redaction for bot/database secrets, raw updates, coordinates, callback payloads, draft payloads, and full roster identity; retain only IDs needed for diagnosis. Add a pull-request CI workflow on Node 24 and PostgreSQL 18 that uses `npm ci`, runs the pinned `npm run format:check` before static checks, generates Prisma Client, applies committed migrations to a fresh service, confirms migration status, runs lint/typecheck/unit/integration suites without watch mode, and builds the Docker image. Update `01-VALIDATION.md` with concrete plan/task IDs, measured quick/full commands including format check, Wave 0 completion, file existence, and `nyquist_compliant: true` only after every command is green. Introduce no new dependency and never replace committed migrations with schema push.</action>
  <acceptance_criteria>
    <criterion>A logging test injects representative token, database URL, coordinates, callback token, draft values, and full Telegram user data and verifies none appears in JSON.</criterion>
    <criterion>CI runs `npm run format:check` deterministically from the audited pinned Prettier lockfile before lint/type/test gates.</criterion>
    <criterion>CI applies all committed migrations to fresh PostgreSQL 18 before integration tests and blocks on status, every suite, and image build.</criterion>
    <criterion>Every Phase 1 validation row has concrete task/plan/wave, command, existing test file, and green status supported by executed command.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm ci &amp;&amp; npm run format:check &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run test:integration &amp;&amp; docker build -t gsmbot:phase-01 .</automated>
  </verify>
  <done>Local and CI gates prove deterministic formatting, redaction, strict types, every requirement test, fresh migrations, and production image.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Telegram-native setup, roster, settings, and immediate demotion behavior</name>
  <precondition>`BOT_TOKEN` is set for a dedicated bot that is an administrator in a private group containing an administrator test account and another human test account.</precondition>
  <read_first>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
    <file>.planning/phases/01-chat-readiness/01-VALIDATION.md</file>
    <file>.planning/phases/01-chat-readiness/COVERAGE.md</file>
  </read_first>
  <files>.planning/phases/01-chat-readiness/01-VALIDATION.md</files>
  <action>Run the built Compose bot against the private test group and execute the verification script below. Record observed results in the Plan 01-11 summary/validation evidence; do not change behavior or approve around a mismatch. This checkpoint validates Telegram client affordances and live `getChatMember` semantics that mocked updates cannot prove.</action>
  <what-built>Complete Telegram-native Chat Readiness flow with durable setup/settings/roster state, exact UI contract, current-role authorization, and CI verification.</what-built>
  <how-to-verify>
    1. As the current administrator, send `/setup`. Confirm the bold `Set up rehearsal planning` prompt has only `Start setup`; start it and verify `Setup in progress`, `Step 1 of 8`, and the location instruction.
    2. Attach an ordinary group location. Confirm the candidate card, use the inferred IANA zone, complete all eight values, inspect the fixed review order, and save. Restart the bot and verify `/settings` shows committed configuration only.
    3. Edit one valid setting and confirm Current/New plus `Save change`/`Keep current value`; try a conflicting schedule and confirm no committed value changes.
    4. Reply to the other human account's message with `/roster_add`, verify safe identity display, open removal, verify the named second confirmation, keep once, then remove and confirm a repeated tap is non-mutating.
    5. Start a new setup/settings edit as administrator, demote that account in Telegram, then attempt the next protected command and callback. Confirm immediate group/private denial and that re-promotion does not resume the discarded draft.
    6. Inspect long/empty roster and error states used in the group; confirm Telegram-native wrapping, no full numeric ID exposure, no reply keyboard/WebView, and authoritative message replacement after callbacks.
  </how-to-verify>
  <acceptance_criteria>
    <criterion>The real group location update reaches the correct actor-bound step and produces a confirmable IANA candidate.</criterion>
    <criterion>Configuration and roster state survive bot restart and exactly match committed dashboard/projection.</criterion>
    <criterion>Demotion takes effect at next command/callback, discards actor draft, and cannot be bypassed by prior success.</criterion>
    <criterion>Message hierarchy, exact copy, inline buttons, immediate callback completion, wrapping, pagination, and safe identity match UI contract.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run test:integration &amp;&amp; docker compose config -q</automated>
    <human-check>Complete all six private-group steps and compare every visible surface/demotion result to `01-UI-SPEC.md`.</human-check>
  </verify>
  <resume-signal>Type "approved" when all six live checks pass, or report the exact step, observed message/state, and expected contract.</resume-signal>
  <done>The real Telegram client and membership API confirm the complete Phase 1 outcome with no UI, persistence, authorization, or formatting-gate discrepancy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| central Telegram router → feature services | Registration/dispatch mistakes could bypass authorization or acknowledge too late. |
| runtime events → structured logs | Secrets, coordinates, drafts, or roster identity could leak through raw context serialization. |
| committed repository → CI PostgreSQL/image | Formatting/migration drift or untested output could pass locally and fail deployment. |
| automated mocks → live Telegram group | Client rendering and real membership semantics require human-observed boundary verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-23 | Elevation of privilege | command/callback registration | high | mitigate | Table-driven route inventory proves every protected route invokes shared current-role boundary. |
| T-01-24 | Tampering | callback dispatcher | high | mitigate | Ack first, validate opaque token, load server-side bindings, reauthorize, and dispatch by stored kind. |
| T-01-25 | Information disclosure | Pino/runtime logs | high | mitigate | Explicit redaction plus fixture test containing representative secrets, coordinates, drafts, and identity. |
| T-01-26 | Tampering | format/migration/build pipeline | high | mitigate | CI uses clean lockfile install, pinned format check, fresh PostgreSQL migrate deploy/status, full tests, and image build. |
| T-01-27 | Repudiation | live Telegram discrepancy | low | accept | Final checkpoint records observed step/copy/state; discrepancy blocks approval rather than being waived. |
| T-01-SC | Tampering | npm dependency graph | high | mitigate | CI uses `npm ci` from the human-approved lockfile and installs no package. |
</threat_model>

<verification>
Run the clean-install format/static/unit/integration/migration/image pipeline, complete the six-step live Telegram check, validate API coverage syntax, and confirm source/edge/prohibition/UI audits contain zero missing items.
</verification>

<success_criteria>
- All nine Phase 1 requirements and D-01 through D-14 are implemented through reachable Telegram routes.
- Every protected route revalidates current permission and demotion discards actor-bound drafts immediately.
- All 12 edge rows, 12 resolved UI rows, and 6 kept prohibitions have zero silent drops.
- Deterministic formatting, fresh migrations, restart persistence, full tests, redaction, image, and live Telegram checks pass.
- The source audit has no MISSING row and no deferred/later-phase capability enters Phase 1.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-11-SUMMARY.md` when done.
</output>
