---
phase: 01-chat-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - prisma.config.ts
  - prisma/schema.prisma
  - prisma/migrations/20260819000000_chat_readiness_core/migration.sql
  - src/app/config.ts
  - src/app/create-bot.ts
  - src/app/main.ts
  - src/infrastructure/db/prisma.ts
  - tests/helpers/postgres.ts
  - tests/integration/walking-skeleton.test.ts
  - Dockerfile
  - .dockerignore
  - compose.yaml
autonomous: true
requirements:
  - CONF-01
  - AUTH-02
user_setup:
  - service: telegram
    why: "Run the long-polling bot and perform the end-of-phase live group verification"
    env_vars:
      - name: BOT_TOKEN
        source: "Telegram @BotFather token for the test bot"
    dashboard_config:
      - task: "Add the bot to a private test group as an administrator so getChatMember is reliable for other users"
        location: "Telegram group settings -> Administrators"
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A current chat administrator can send `/setup`, cause one actor-bound setup draft to be written and read from PostgreSQL, and receive the authoritative readiness prompt through grammY."
    - "A non-administrator cannot create or resume the setup draft, and denial uses the required command or callback wording."
    - "The committed Prisma migration applies cleanly to an empty PostgreSQL 18 integration database before the tracer verification runs."
    - "The bot and PostgreSQL can be started through one documented Docker Compose command using the same image intended for later deployment."
    - "[Edge AUTH-02/adjacency] A previously successful administrator check grants no grace period: the current Telegram role is fetched again at the next protected action."
    - "[Edge AUTH-02/empty] Missing chat, actor, membership result, or an unsupported Telegram member status is fail-closed; when an actor-bound draft can be identified it is deleted before denial is returned."
    - "[Edge AUTH-02/ordering] Callback acknowledgement occurs first, then current-role lookup settles, and only then may protected draft state be read or mutated."
  artifacts:
    - path: "package.json"
      provides: "Pinned runtime dependencies and lint/typecheck/unit/integration/build/start scripts"
      contains: "test:integration"
    - path: "prisma/schema.prisma"
      provides: "ChatConfiguration, SetupDraft, and CallbackAction persistence contracts"
      contains: "model SetupDraft"
    - path: "prisma/migrations/20260819000000_chat_readiness_core/migration.sql"
      provides: "Reviewed initial PostgreSQL schema migration"
      contains: "SetupDraft"
    - path: "src/app/create-bot.ts"
      provides: "createBot composition seam and real `/setup` tracer route"
      exports:
        - createBot
    - path: "tests/integration/walking-skeleton.test.ts"
      provides: "End-to-end grammY → authorization → PostgreSQL → reply tracer verification"
    - path: "compose.yaml"
      provides: "Healthy PostgreSQL and single bot development deployment"
  key_links:
    - from: "src/app/create-bot.ts"
      to: "Telegram getChatMember"
      via: "the `/setup` action-bound current administrator check"
      pattern: "getChatMember"
    - from: "src/app/create-bot.ts"
      to: "prisma/schema.prisma"
      via: "SetupDraft upsert/read keyed by chatId and actorUserId"
      pattern: "setupDraft\.(upsert|findUnique)"
    - from: "tests/integration/walking-skeleton.test.ts"
      to: "prisma/migrations/20260819000000_chat_readiness_core/migration.sql"
      via: "fresh PostgreSQL container plus prisma migrate deploy before the grammY update"
      pattern: "migrate deploy"
    - from: "compose.yaml"
      to: "src/app/main.ts"
      via: "one long-poll bot process starts only after PostgreSQL health and migration checks"
      pattern: "bot"
  prohibitions:
    - requirement_id: CONF-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT present an incomplete setup draft as active chat configuration or claim it was saved."
    - requirement_id: AUTH-02
      category: privacy
      status: unverified
      flagged: true
      statement: "MUST NOT reveal draft values, prior administrator history, or internal authorization details in a permission-denial message."
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Establish the whole-application walking skeleton with the first production `/setup` path through Telegram, current-role authorization, PostgreSQL, and a Telegram response.

Purpose: Prove the selected runtime, framework, authorization boundary, migration workflow, durable state, and development deployment before feature expansion.
Output: Runnable Node/TypeScript bot scaffold, committed initial Prisma migration, green tracer integration test, and Docker Compose development deployment.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-PATTERNS.md
@.planning/phases/01-chat-readiness/01-VALIDATION.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-SKELETON.md
@.planning/phases/01-chat-readiness/COVERAGE.md
@.planning/phases/01-chat-readiness/01-01-SUMMARY.md
</context>

<interfaces>
- `createBot(deps: BotDependencies): Bot` registers handlers without starting long polling.
- `BotDependencies` supplies Prisma, current-time clock, and a `TelegramMembershipGateway.getCurrentRole(chatId: bigint, actorId: bigint)` seam.
- `SetupDraft` is uniquely addressed by `(chatId, actorUserId)`, stores `step`, `expiresAt`, validated partial fields, and the configuration revision it began from.
- `CallbackAction` stores a short opaque token plus `kind`, `chatId`, `actorUserId`, `targetId`, `expiresAt`, and `consumedAt`; callback text never supplies authority.
</interfaces>

## Artifacts this phase produces

- Paths: `package.json`, `package-lock.json`, `tsconfig.json`, `vitest.config.ts`, `prisma.config.ts`, `prisma/schema.prisma`, `prisma/migrations/20260819000000_chat_readiness_core/migration.sql`, `src/app/config.ts`, `src/app/create-bot.ts`, `src/app/main.ts`, `src/infrastructure/db/prisma.ts`, `tests/helpers/postgres.ts`, `tests/integration/walking-skeleton.test.ts`, `Dockerfile`, `.dockerignore`, `compose.yaml`
- Command: `/setup`
- npm commands: `build`, `start`, `dev`, `lint`, `typecheck`, `test`, `test:unit`, `test:integration`, `db:generate`, `db:migrate:dev`, `db:migrate:deploy`, `db:migrate:status`
- Symbols: `AppConfig`, `loadConfig`, `BotDependencies`, `TelegramMembershipGateway`, `CurrentTelegramRole`, `createBot`, `startBot`, `createPrismaClient`, `startPostgresTestContainer`, `applyCommittedMigrations`
- Prisma enums: `PlanningAccessPolicy`, `SetupStep`, `CallbackActionKind`
- Prisma models and fields: `ChatConfiguration(chatId, timezone, defaultWeekday, defaultStartMinute, durationMinutes, dailyStartMinute, dailyEndMinute, reminderMinutes, planningAccessPolicy, revision, createdAt, updatedAt)`; `SetupDraft(id, chatId, actorUserId, step, candidateTimezone, timezone, defaultWeekday, defaultStartMinute, durationMinutes, dailyStartMinute, dailyEndMinute, reminderMinutes, planningAccessPolicy, expectedRevision, expiresAt, createdAt, updatedAt)`; `CallbackAction(token, kind, chatId, actorUserId, targetId, expiresAt, consumedAt, createdAt)`

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: [BLOCKING] End-to-end `/setup` tracer with committed Prisma schema application</name>
  <precondition>Every package required by this task is marked APPROVED in `.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md`.</precondition>
  <reversibility rating="costly">The initial database and bot composition contracts are consumed by every later phase; changing them later requires coordinated migrations and call-site updates, but no production data exists yet.</reversibility>
  <read_first>
    <file>.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md</file>
    <file>.planning/phases/01-chat-readiness/01-SKELETON.md</file>
    <file>.planning/phases/01-chat-readiness/01-RESEARCH.md</file>
    <file>.planning/phases/01-chat-readiness/01-PATTERNS.md</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
    <file>.planning/research/STACK.md</file>
  </read_first>
  <files>package.json, package-lock.json, prisma.config.ts, prisma/schema.prisma, prisma/migrations/20260819000000_chat_readiness_core/migration.sql, src/app/create-bot.ts, tests/integration/walking-skeleton.test.ts</files>
  <behavior>
    - Test 1: a synthetic group `/setup` update from a current administrator creates one `(chatId, actorUserId)` draft and sends the `Set up rehearsal planning` prompt with one `Start setup` button.
    - Test 2: sending `/setup` again within 30 minutes reads/resumes the same durable draft instead of creating a duplicate.
    - Test 3: a current non-administrator receives the exact command denial copy and no draft row is created.
    - Test 4: an approved callback is acknowledged before its durable action; a denied callback returns the exact private alert and performs no protected mutation.
  </behavior>
  <action>Install only the exact package set approved by Plan 01-01 and commit the generated lockfile. Define the initial Prisma 7 configuration, PostgreSQL schema, and reviewed migration for `ChatConfiguration`, `SetupDraft`, and opaque `CallbackAction`, using database/TypeScript bigint at the repository boundary and timestamptz timestamps. In the integration test, start a clean PostgreSQL 18 container, run `prisma migrate deploy`, assert `prisma migrate status` reports no pending migrations, then exercise a grammY `/setup` update through `createBot`. Per D-02, the handler upserts/reads an actor-bound draft with a 30-minute inactivity expiry. It must call `getChatMember` for the current actor at this action boundary, map only Telegram creator/administrator statuses to allowed, and send the exact readiness surface. Acknowledge callbacks before awaiting role lookup, use private/group denial copy per D-13, then prohibit every draft read or mutation until that lookup succeeds; denial deletes the actor draft per D-14. This migration application is blocking: do not run the tracer assertions, generate client output against a different schema, or continue to Task 2 if the clean database does not match the committed migration.</action>
  <acceptance_criteria>
    <criterion>The committed lockfile contains only package versions approved in `DEPENDENCY-AUDIT.md` and `npm ls --depth=0` has no invalid or extraneous direct dependency.</criterion>
    <criterion>`prisma migrate deploy` and `prisma migrate status` succeed against an empty PostgreSQL 18 container before any tracer behavior assertion runs.</criterion>
    <criterion>The administrator happy path performs a real database draft write followed by a read and emits the UI contract's readiness prompt via grammY.</criterion>
    <criterion>The denial and missing-context cases are fail-closed, use the exact UI copy, and cannot create/read a protected draft before current-role resolution.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- walking-skeleton</automated>
  </verify>
  <done>The production-quality `/setup` happy path traverses grammY, live-role authorization, a migrated PostgreSQL database, and a Telegram response; denial is non-mutating.</done>
</task>

<task type="auto">
  <name>Task 2: Establish strict configuration and deterministic Wave 0 verification seams</name>
  <read_first>
    <file>package.json</file>
    <file>prisma/schema.prisma</file>
    <file>src/app/create-bot.ts</file>
    <file>tests/integration/walking-skeleton.test.ts</file>
    <file>.planning/phases/01-chat-readiness/01-VALIDATION.md</file>
  </read_first>
  <files>tsconfig.json, vitest.config.ts, src/app/config.ts, src/infrastructure/db/prisma.ts, tests/helpers/postgres.ts</files>
  <action>Enable strict TypeScript with `strict`, `noUncheckedIndexedAccess`, and `exactOptionalPropertyTypes`. Implement `loadConfig` with Zod validation for `BOT_TOKEN`, `DATABASE_URL`, log level, and explicit test/smoke mode without logging secret values. Move PostgreSQL client construction behind `createPrismaClient` using Prisma 7 with `@prisma/adapter-pg`, and centralize the PostgreSQL 18 Testcontainers startup plus committed-migration application in reusable test helpers. Configure Vitest so `test:unit` excludes container tests and `test:integration` runs them serially with bounded timeouts and no watch mode. Keep the injected membership gateway and clock seams deterministic for later demotion and expiry tests.</action>
  <acceptance_criteria>
    <criterion>Invalid or missing boot configuration exits before the bot or database starts and never prints `BOT_TOKEN` or the database password.</criterion>
    <criterion>The strict TypeScript compiler accepts the tracer without unchecked optional access or implicit `any`.</criterion>
    <criterion>Unit and integration scripts are non-watch commands; the integration helper always applies committed migrations to a fresh PostgreSQL 18 database.</criterion>
    <criterion>`npm run test:unit` completes in under 60 seconds and its measured duration is recorded in the Plan 01-02 summary.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run test:unit &amp;&amp; npm run test:integration -- walking-skeleton</automated>
  </verify>
  <done>The greenfield repository has deterministic strict-type, unit, and real-PostgreSQL integration feedback paths used by every expansion plan.</done>
</task>

<task type="auto">
  <name>Task 3: Package the proven tracer as the reproducible development deployment</name>
  <reversibility rating="reversible">The Docker image and Compose topology are provider-portable and can be replaced without changing domain or database contracts.</reversibility>
  <read_first>
    <file>.planning/phases/01-chat-readiness/01-SKELETON.md</file>
    <file>package.json</file>
    <file>src/app/config.ts</file>
    <file>src/app/create-bot.ts</file>
    <file>src/infrastructure/db/prisma.ts</file>
  </read_first>
  <files>Dockerfile, .dockerignore, compose.yaml, src/app/main.ts, package.json</files>
  <action>Create a multi-stage `node:24.19-bookworm-slim` image that installs from the committed lockfile, generates Prisma Client, builds the TypeScript service, and runs as a non-root user. Compose PostgreSQL 18.4 with a named development volume and health check, plus exactly one long-polling `bot` service that waits for database health and runs `prisma migrate deploy` as an explicit one-shot dependency before startup. `main.ts` must construct configuration, Prisma, membership gateway, clock, bot, `bot.catch`, and chat-key runner sequentialization, then stop gracefully on SIGTERM/SIGINT. Keep secrets environment-injected and document the executable local path in Compose labels/comments and package scripts without embedding real credentials.</action>
  <acceptance_criteria>
    <criterion>The image builds from the lockfile, runs as a non-root user, and contains compiled production output rather than the development dependency tree.</criterion>
    <criterion>Compose declares PostgreSQL health, a one-shot committed-migration service, and exactly one bot long-poll process; the bot cannot start before migration success.</criterion>
    <criterion>`docker compose up --build bot` is the documented full-stack development command when `BOT_TOKEN` is supplied.</criterion>
    <criterion>Process signals close the runner and Prisma pool without starting a second poller.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>docker compose config -q &amp;&amp; docker build -t gsmbot:phase-01 . &amp;&amp; docker compose up -d postgres &amp;&amp; docker compose exec -T postgres pg_isready -U gsmbot &amp;&amp; docker compose run --rm migrate</automated>
  </verify>
  <done>The tracer has a reproducible Docker image and local full-stack deployment path backed by the same committed migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update → grammY handler | Chat, actor, command, callback, and location data are untrusted and unordered. |
| grammY handler → Telegram membership API | Current administrator status is external authorization evidence and must be refreshed per action. |
| application service → PostgreSQL | Only validated, authorized state transitions may cross into durable storage. |
| environment → process configuration | Bot token and database credentials are secrets and invalid values must fail boot. |
| npm/build context → runtime image | Approved dependencies and generated artifacts cross into executable production code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-03 | Spoofing | `/setup` actor authorization | high | mitigate | `createBot` calls live `getChatMember` for `chat.id` and `from.id` at every protected action and accepts only creator/administrator. |
| T-01-04 | Elevation of privilege | resumed setup draft | high | mitigate | Actor/chat uniqueness plus current-role revalidation precede every protected read or write; denial removes an identifiable actor draft. |
| T-01-05 | Tampering | callback/update payload | medium | mitigate | Validate update shape and opaque callback token before loading server-side action; authority never comes from callback text. |
| T-01-06 | Information disclosure | configuration/logging | high | mitigate | Zod boot validation and Pino redaction keep bot/database secrets and raw Telegram updates out of logs. |
| T-01-07 | Denial of service | duplicate/in-flight updates | low | accept | Chat-key sequentialization bounds local overlap; database uniqueness provides correctness for the small trusted group workload. |
| T-01-SC | Tampering | npm dependency install | high | mitigate | Install only the exact human-approved Plan 01-01 set and commit the resulting lockfile. |
</threat_model>

<verification>
Run `npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm test &amp;&amp; npm run test:integration`, then build the image, validate Compose, apply migrations to a clean PostgreSQL 18 service, and confirm the walking-skeleton test performs the real draft write/read and grammY reply.
</verification>

<success_criteria>
- The first implementation slice is a green end-to-end `/setup` tracer, not a disconnected scaffold.
- A clean integration database exactly matches the committed Prisma migration before tracer verification.
- Current-role denial is fail-closed and non-mutating.
- Strict TypeScript, fast unit tests, real-PostgreSQL integration tests, Docker image, and Compose development deployment all execute from documented commands.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-02-SUMMARY.md` when done.
</output>
