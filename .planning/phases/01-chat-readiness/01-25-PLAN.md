---
phase: 01-chat-readiness
plan: 25
type: execute
wave: 23
depends_on:
  - 01-22
files_modified:
  - tests/integration/chat-configuration.test.ts
autonomous: true
requirements:
  - AUTH-01
gap_closure: true
estimate:
  tokens: 11000
  raw_tokens: 11000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The composed bot opens planning-access editing by the `Edit planning access` label rather than a positional keyboard index, reviews a non-default policy, saves it, and persists a one-step revision increase."
    - "Unsupported planning-access values fail soft as `undefined`, leave the actor-bound draft and committed configuration unchanged, and cannot be saved as a policy."
    - "Current Telegram administrators remain allowed independently of the broadened policy; this plan closes policy choice, not Phase 2 policy enforcement."
    - "The timezone surface shows one valid candidate or every valid ambiguous candidate with one bound action per zone and no automatic first-result choice; settings shows all committed values in fixed section order; roster shows sorted readable identities with one `Remove member` action each."
  artifacts:
    - path: "tests/integration/chat-configuration.test.ts"
      provides: "Green composed-bot non-default policy flow and explicit invalid-policy fail-soft contract"
  key_links:
    - from: "tests/integration/chat-configuration.test.ts"
      to: "src/telegram/keyboards.ts"
      via: "button lookup scans serialized keyboard labels for `Edit planning access` and `Anyone in chat`"
      pattern: "Edit planning access"
    - from: "tests/integration/chat-configuration.test.ts"
      to: "src/domain/chat/settings-service.ts"
      via: "real PostgreSQL assertions prove review-before-save, undefined invalid selection, and revision-safe persistence"
      pattern: "selectPlanningAccessPolicy"
  prohibitions:
    - requirement_id: AUTH-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT let keyboard row order choose which setting is edited, report an invalid policy as saved, or imply that a broadened policy excludes current Telegram administrators."
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Close broken windows 2 and 3 with a green, non-default planning-access path and one explicit invalid-input contract.

Purpose: Restore the composed AUTH-01 regression gate without changing correct production behavior or hiding failures.
Output: A label-addressed end-to-end settings test and a non-mutating fail-soft invalid-policy assertion against real PostgreSQL.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-VERIFICATION.md
@.planning/WINDOWS.md
@.planning/quick/260826-e62-fix-cr-01-authorization-service-swallows/260826-e62-SUMMARY.md
@src/domain/chat/settings-service.ts
@src/telegram/settings-handlers.ts
@src/telegram/keyboards.ts
@tests/integration/chat-configuration.test.ts
</context>

<interfaces>
- `settingsDashboardKeyboard` intentionally renders `Edit time zone` first and `Edit planning access` in its own later row; keyboard order is presentation, not field identity.
- `SettingsService.selectPlanningAccessPolicy(...)` delegates to `selectValue(...)` and currently returns `undefined` for stale, mismatched-field, expired, unavailable, schedule-invalid, or unsupported replacement values.
- The production callback dispatcher already maps an undefined `selectValue` result to the existing stale private alert; it does not require an exception to stay fail-closed and non-mutating.
- D-10 requires Current/New review before save; D-12 keeps current administrators allowed while the stored policy only broadens access.
</interfaces>

## Contract Decision for Window 3

Retain the existing fail-soft `undefined` contract. An unsupported value is untrusted callback data and must remain non-mutating; throwing would escape the same safe result contract used for stale and mis-bound selections without adding user value. The regression will assert the returned `undefined` and both durable non-mutations explicitly. This is a test-contract correction, not a production behavior change.

## Gap Disposition

- Broken window 2: covered by Task 1; the stale positional expectation is replaced with semantic label lookup and the composed path writes `ANYONE_IN_CHAT`.
- Broken window 3: covered by Task 2; fail-soft is selected and pinned with durable non-mutation evidence.
- Phase 2 enforcement of the stored planning policy remains deferred exactly as `01-VERIFICATION.md` states and is not added here.

## Artifacts this phase produces

- No new source symbol or file.
- One local label-based keyboard helper may be added inside `tests/integration/chat-configuration.test.ts`.
- No production code, API surface, dependency, Prisma schema, or migration change.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the composed bot save a non-default policy through the labelled dashboard action</name>
  <precondition>A Docker-compatible container runtime is available for the PostgreSQL integration test.</precondition>
  <read_first>
    <file>tests/integration/chat-configuration.test.ts</file>
    <file>src/telegram/keyboards.ts</file>
    <file>src/telegram/settings-handlers.ts</file>
    <file>src/domain/chat/settings-service.ts</file>
    <file>.planning/WINDOWS.md</file>
  </read_first>
  <files>tests/integration/chat-configuration.test.ts</files>
  <behavior>
    - "The dashboard action is found by exact button label, so adding or reordering unrelated settings rows cannot redirect the test."
    - "The composed bot selects `Anyone in chat`, renders Current `Admins only` / New `Anyone in chat`, saves, and re-renders the committed dashboard."
    - "The real ChatConfiguration row stores ANYONE_IN_CHAT and increments revision exactly once."
  </behavior>
  <action>In the existing composed-bot integration test, add or reuse a helper that walks every serialized inline-keyboard row and returns the callback token for an exact visible label. Replace the row-zero lookup with `Edit planning access`; keep the policy selection on the non-default `Anyone in chat` button. Preserve and tighten the existing Current/New, final dashboard, persisted enum, and revision assertions. Do not reorder the production dashboard to satisfy a test, do not switch the e2e back to the default ADMINS_ONLY value, and do not skip or filter either broken window.</action>
  <acceptance_criteria>
    <criterion>The test fails if `Edit planning access` is absent, duplicated ambiguously, or routes to another field.</criterion>
    <criterion>The review card proves the requested transition is non-default before Save.</criterion>
    <criterion>The committed row proves a real write and exact single revision increment after Save.</criterion>
    <criterion>No production file changes in this task.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project integration --no-file-parallelism tests/integration/chat-configuration.test.ts</automated>
  </verify>
  <done>The composed AUTH-01 path is green and can no longer pass through a default-policy no-op or a keyboard-position accident.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin unsupported policy selection as fail-soft and durably non-mutating</name>
  <precondition>A Docker-compatible container runtime is available for the PostgreSQL integration test.</precondition>
  <read_first>
    <file>tests/integration/chat-configuration.test.ts</file>
    <file>src/domain/chat/settings-service.ts</file>
    <file>src/telegram/settings-handlers.ts</file>
    <file>.planning/phases/01-chat-readiness/01-VERIFICATION.md</file>
  </read_first>
  <files>tests/integration/chat-configuration.test.ts</files>
  <behavior>
    - "Calling selectPlanningAccessPolicy with an unsupported value resolves undefined."
    - "The committed policy, committed revision, draft field, draft expiry, and draft replacement payload remain unchanged after the invalid call."
    - "A later valid PREVIOUS_PARTICIPANTS selection and save still succeeds once, proving the invalid attempt did not poison the draft."
  </behavior>
  <action>Replace the inherited throw expectation with the explicit fail-soft contract chosen above. Snapshot the committed row and actor-bound edit draft before the invalid call, assert `undefined`, reload both records, and compare every mutation-sensitive field. Continue the same test through the existing valid `PREVIOUS_PARTICIPANTS` selection/save and duplicate-save assertions so the invalid input cannot pass merely by destroying the workflow. Add a concise test comment that production callback dispatch maps undefined to a safe stale result. Do not change `SettingsService` to throw, coerce the invalid value, consume an action, or rewrite the configured policy.</action>
  <acceptance_criteria>
    <criterion>Unsupported selection returns undefined and changes zero durable fields.</criterion>
    <criterion>The same draft remains usable for a valid non-default selection afterward.</criterion>
    <criterion>The final valid save changes the policy and revision once; duplicate save stays non-mutating.</criterion>
    <criterion>The full integration file has no failing test and no skipped/todo replacement for either window.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project integration --no-file-parallelism tests/integration/chat-configuration.test.ts &amp;&amp; npm run build &amp;&amp; npm run format:check</automated>
  </verify>
  <done>Window 3 has one documented fail-soft contract and the integration gate proves both invalid non-mutation and valid recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram inline keyboard → callback action | Button position is untrusted presentation order; server-side action identity owns the selected field/value. |
| Callback selection → SettingsService | Unsupported values must not become a persisted planning policy. |
| Settings draft → ChatConfiguration transaction | Only reviewed, valid, revision-matched values may become committed policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-25-01 | Tampering | planning-policy selection | high | mitigate | Assert unsupported values return undefined and leave both draft and committed row byte-for-byte unchanged. |
| T-01-25-02 | Elevation of privilege | broadened planning policy | high | mitigate | Preserve D-12 semantics and verify only a reviewed enum value is written; Phase 2 remains responsible for enforcement. |
| T-01-25-03 | Repudiation | positional integration test | medium | mitigate | Address the action by exact visible label and prove the requested non-default transition in review and PostgreSQL. |
| T-01-25-04 | Denial of service | invalid selection recovery | low | accept | The invalid attempt returns a safe non-mutating result; the same draft remains usable for a valid choice, which bounds disruption to one rejected action. |
</threat_model>

<verification>
- `npm run build`
- `npx vitest run --project integration --no-file-parallelism tests/integration/chat-configuration.test.ts`
- `npm run format:check`
- Confirm neither test is skipped, filtered, or converted to `continue-on-error`.
</verification>

<success_criteria>
- Broken windows 2 and 3 are resolved by one green integration file.
- A composed non-default policy change proves the AUTH-01 write path.
- Unsupported input is explicitly fail-soft and durably non-mutating.
- Production source remains unchanged because the diagnosed defects are stale/undecided test contracts, not product behavior defects.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-25-SUMMARY.md` when done.
</output>
