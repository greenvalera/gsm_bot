---
phase: 01-chat-readiness
plan: 28
type: execute
wave: 1
depends_on:
  - 01-27
files_modified:
  - .planning/phases/01-chat-readiness/01-UI-SPEC.md
  - src/telegram/renderers.ts
  - tests/unit/timezone-prompt-copy.test.ts
  - tests/integration/chat-readiness.e2e.test.ts
  - .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md
  - .planning/WINDOWS.md
autonomous: true
requirements:
  - CONF-01
gap_closure: true
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The setup Step 1 prompt and the `/settings` time-zone edit prompt both state the required reply gesture, using one sentence that is byte-identical across the two surfaces."
    - "That sentence has exactly one source of truth: a `Time-zone location prompt` row in the `01-UI-SPEC.md` Copywriting Contract, quoted verbatim by a single exported renderer constant that both branches interpolate."
    - "No renderer still emits the superseded time-zone instruction recorded by broken window 17."
    - "The live-verification runbook Step 1 operator expectation quotes the shipped sentence, so the next operator is told to reply with a location rather than to attach one."
    - "An unfilled step always renders its current prompt and required input format; completed draft values remain actor-bound, `Step X of 8` identifies progress, and no partial draft is presented as active configuration."
    - "Validation, location, permission, expiry, save, duplicate, and stale-action failures use the corresponding Copywriting Contract row and preserve the last authoritative state."
    - statement: "A location lookup renders an in-flight state and then atomically replaces it with either the candidate zone or the documented resolution failure."
      verification: backstop
  artifacts:
    - path: ".planning/phases/01-chat-readiness/01-UI-SPEC.md"
      provides: "The single normative `Time-zone location prompt` Copywriting Contract row plus an authority note that forbids restating it elsewhere"
    - path: "src/telegram/renderers.ts"
      provides: "The exported `TIMEZONE_LOCATION_HINT` constant consumed by both time-zone prompt branches"
    - path: "tests/unit/timezone-prompt-copy.test.ts"
      provides: "Focused literal assertions at both renderer sites plus a cross-renderer drift guard"
    - path: "tests/integration/chat-readiness.e2e.test.ts"
      provides: "Composed-bot coverage that the reply gesture reaches the real setup and settings surfaces"
    - path: ".planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md"
      provides: "An operator instruction that matches shipped copy at Step 1 and 2a"
    - path: ".planning/WINDOWS.md"
      provides: "Broken window 17 closed against the landed commit, with live confirmation explicitly still pending"
  key_links:
    - from: ".planning/phases/01-chat-readiness/01-UI-SPEC.md"
      to: "src/telegram/renderers.ts"
      via: "the exported constant quotes the Copywriting Contract row byte-for-byte"
      pattern: "TIMEZONE_LOCATION_HINT"
    - from: "src/telegram/renderers.ts"
      to: "src/telegram/renderers.ts"
      via: "renderSetupStep step-1 branch and renderSettingsEditPrompt TIMEZONE case interpolate the same single constant, so they cannot drift"
      pattern: "SettingsField.TIMEZONE"
    - from: "src/telegram/renderers.ts"
      to: "tests/unit/timezone-prompt-copy.test.ts"
      via: "the test imports the constant and additionally asserts the raw literal at both sites, so a silent constant edit still fails"
      pattern: "renderSettingsEditPrompt"
    - from: "src/telegram/renderers.ts"
      to: ".planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md"
      via: "the Step 1 operator expectation quotes the shipped sentence"
      pattern: "Step 1 of 8"
  prohibitions:
    - requirement_id: CONF-01
      category: privacy
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT solve time-zone discoverability by adding a private-chat location-request button, a reply keyboard, a WebView, or any surface outside the group message flow."
    - requirement_id: CONF-01
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT disable or work around Telegram privacy mode, and MUST NOT widen the update filter so the bot observes ordinary group conversation in order to receive a non-reply location."
    - requirement_id: CONF-01
      category: privacy
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT place a raw coordinate, a resolved IANA zone paired with a location, a Telegram identity, or a group name into prompt copy, test fixtures, or repository evidence."
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Close F-12 / broken window 17 by making the time-zone prompt tell the administrator the gesture Telegram privacy mode actually requires, from one contract row through both renderers, their tests, and the live runbook.

Purpose: With privacy mode enabled a group location reaches this bot only as a reply to the bot's own message, so a prompt that omits the reply gesture is not a wording preference — it makes the only advancing action undiscoverable and makes the bot look frozen.
Output: One normative Copywriting Contract row, one exported renderer constant used by both time-zone prompts, focused unit and composed-bot coverage, an aligned operator instruction, and window 17 closed.
</objective>

<execution_context>
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/workflows/execute-plan.md
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md
@.planning/phases/01-chat-readiness/01-27-SUMMARY.md
@src/telegram/renderers.ts
@tests/unit/schedule-settings.test.ts
@tests/integration/chat-readiness.e2e.test.ts
</context>

<interfaces>
- `renderSetupStep(draft: SetupRenderDraft): SetupProjection` in `src/telegram/renderers.ts` selects the wizard step by first-null draft field; the `draft.timezone === null` branch is Step 1 and owns no buttons.
- `renderSettingsEditPrompt(field: SettingsField, current: SettingsDashboardConfiguration)` in the same module switches on `SettingsField`; the `SettingsField.TIMEZONE` case is the `/settings` time-zone edit prompt and is reached through `settings-handlers.ts:261`.
- `TIME_HINT` at `src/telegram/renderers.ts:43` is the established module-constant pattern for contract-fixed copy shared by several branches. The new constant follows exactly that shape, but is exported so tests can bind to it.
- `tests/integration/chat-readiness.e2e.test.ts` already exposes `tokenLabelled(card, label)` and `lastOf(method)` helpers, and already reads the Step 1 card as `reentry` near line 986 and the settings dashboard as `dashboard` near line 878.
- `tests/unit/schedule-settings.test.ts` already imports `renderSetupStep` and declares a local `stepText` helper; it is the precedent for renderer-level unit assertions but is NOT the file this plan extends.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the contract row through the setup Step 1 prompt end to end</name>
  <files>.planning/phases/01-chat-readiness/01-UI-SPEC.md, src/telegram/renderers.ts, tests/unit/timezone-prompt-copy.test.ts</files>
  <read_first>
    - .planning/phases/01-chat-readiness/01-UI-SPEC.md — read `## Copywriting Contract` (rows begin at line 131) and the `Empty roster` surface row at line 97, which is the established authority convention: copy lives ONLY in the Copywriting Contract and other sections reference it without restating it
    - src/telegram/renderers.ts — read the `TIME_HINT` constant at line 43 for the module-constant pattern, and `renderSetupStep` starting at line 242 for the first-null branch selection
    - .planning/WINDOWS.md — read window 17 for the exact defect and its recorded required scope
    - .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md — read the section `## ⚠️ Two traps to know before starting` at line 54, which already documents the privacy-mode reply requirement in operator prose; the shipped prompt must now say the same thing
    - tests/unit/schedule-settings.test.ts — read lines 30 to 60 for the existing renderer-unit-test shape and the `SetupRenderDraft` fixture style
  </read_first>
  <behavior>
    - Test 1: `renderSetupStep` with `timezone: null` returns text whose first line is `Setup in progress`, whose second line is `Step 1 of 8`, and which ends with the exact sentence `Reply to this message with a location to choose this chat's time zone.`
    - Test 2: that Step 1 text equals `Setup in progress\nStep 1 of 8\n\n` followed by the exported `TIMEZONE_LOCATION_HINT` constant, so the branch cannot quietly stop using the shared constant.
    - Test 3 (edge case): the Step 1 projection still carries no `buttons`, because Step 1 collects a location and owns no inline keyboard.
  </behavior>
  <action>Add one row to the `## Copywriting Contract` table in `.planning/phases/01-chat-readiness/01-UI-SPEC.md`, immediately before the existing `Location resolution failure` row, with element name `Time-zone location prompt` and copy `Reply to this message with a location to choose this chat's time zone.` Then, following the authority convention already used by the `Empty roster` surface row, extend item 1 of `### Setup wizard sequence` with a sentence stating that this prompt's copy lives ONLY in the Copywriting Contract row named `Time-zone location prompt`, that both the setup step-1 renderer and the `/settings` time-zone edit prompt quote that row verbatim, and that no other section of the spec restates it — do not restate the sentence itself anywhere outside the contract table, because the duplicated-sentence pattern is what manufactured the false F-8 finding recorded as N-4. In `src/telegram/renderers.ts`, add an exported module constant `TIMEZONE_LOCATION_HINT` next to the existing `TIME_HINT` at line 43, holding exactly `Reply to this message with a location to choose this chat's time zone.` and carrying a short JSDoc line naming the Copywriting Contract row as its source of truth and the Telegram privacy-mode reason the reply gesture is named. Rewrite only the `draft.timezone === null` branch of `renderSetupStep` so its text is `Setup in progress\nStep 1 of 8\n\n` followed by an interpolation of `TIMEZONE_LOCATION_HINT`; change nothing else in that function, keep the branch button-free, and leave `TIME_HINT` and every other step untouched. Create `tests/unit/timezone-prompt-copy.test.ts` importing `renderSetupStep` and `TIMEZONE_LOCATION_HINT` from `../../src/telegram/renderers.js`, declaring a `SetupRenderDraft` fixture with `timezone: null` and every later field null or empty, and asserting the three behaviors above — assert the raw literal in at least one case so that editing the constant alone still fails the test. Per D-09 the administrator chooses the time zone by sharing a location, and this task makes the gesture that decision depends on discoverable under privacy mode; per D-01 the sentence stays inside the guided group wizard.</action>
  <reversibility rating="reversible">User-visible copy governed by a single contract row and a single exported constant; reverting is a one-line change with no schema, data, or routing impact.</reversibility>
  <verify>
    <automated>npx vitest run --project unit tests/unit/timezone-prompt-copy.test.ts &amp;&amp; npm run build &amp;&amp; awk '/^## Copywriting Contract/,/^## UI Considerations/' .planning/phases/01-chat-readiness/01-UI-SPEC.md | grep -q 'Time-zone location prompt' &amp;&amp; grep -c "Reply to this message with a location to choose this chat's time zone." src/telegram/renderers.ts | grep -qx '1'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project unit tests/unit/timezone-prompt-copy.test.ts` exits 0 with three passing assertions.
    - `npm run build` (`tsc --noEmit`) exits 0.
    - The `## Copywriting Contract` region of `01-UI-SPEC.md` contains exactly one row whose element cell is `Time-zone location prompt`.
    - `src/telegram/renderers.ts` contains the literal sentence exactly once, as the value of the exported `TIMEZONE_LOCATION_HINT` constant.
    - `renderSetupStep` with `timezone: null` returns a projection with no `buttons` key.
    - `### Setup wizard sequence` item 1 names the Copywriting Contract row as sole authority and does not restate the sentence.
  </acceptance_criteria>
  <done>The contract row, the exported constant, the setup Step 1 branch, and a green focused test form one proven path from spec to rendered prompt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the same proven path to the settings time-zone edit prompt</name>
  <files>src/telegram/renderers.ts, tests/unit/timezone-prompt-copy.test.ts, tests/integration/chat-readiness.e2e.test.ts</files>
  <read_first>
    - src/telegram/renderers.ts — read `renderSettingsEditPrompt` starting at line 172 and specifically the `SettingsField.TIMEZONE` case, plus the sibling cases that show the `<b>Field</b>` heading convention every settings edit prompt follows
    - tests/unit/timezone-prompt-copy.test.ts — the file created in Task 1, which this task extends rather than replaces
    - tests/integration/chat-readiness.e2e.test.ts — read lines 877 to 900 for the settings dashboard card, the `tokenLabelled` helper usage and the `editMessageText` assertion shape, and lines 980 to 998 for the Step 1 `reentry` card assertions
    - src/telegram/keyboards.ts — read `settingsDashboardKeyboard` starting at line 91 to confirm the dashboard action label is exactly `Edit time zone`
    - src/telegram/settings-handlers.ts — read the `renderSettingsEditPrompt` call site at line 261 to confirm the prompt is delivered through the settings surface and not the setup wizard
  </read_first>
  <precondition>A Docker-compatible container runtime is reachable, because `tests/integration/chat-readiness.e2e.test.ts` provisions PostgreSQL through Testcontainers; without it the integration gate cannot run and this task must halt rather than report a green it did not observe.</precondition>
  <behavior>
    - Test 1: `renderSettingsEditPrompt(SettingsField.TIMEZONE, config)` returns text whose first line is `<b>Time zone</b>` and which ends with the exact sentence `Reply to this message with a location to choose this chat's time zone.`
    - Test 2 (drift guard): the trailing sentence of the setup Step 1 text and the trailing sentence of the settings TIMEZONE prompt text are byte-identical to each other and to `TIMEZONE_LOCATION_HINT`.
    - Test 3: driving the composed bot, the `/setup` Step 1 card and the card produced by tapping the dashboard action labelled `Edit time zone` both contain the sentence.
  </behavior>
  <action>In `src/telegram/renderers.ts`, rewrite only the `SettingsField.TIMEZONE` case of `renderSettingsEditPrompt` so its text is `<b>Time zone</b>\n\n` followed by an interpolation of `TIMEZONE_LOCATION_HINT`; keep the bold field heading, change no other case, and do not alter the function signature. Extend `tests/unit/timezone-prompt-copy.test.ts` with the settings assertion and with an explicit cross-renderer drift guard that compares the two rendered texts against each other and against the constant, so a future edit to one branch alone fails. Then add composed-bot coverage in `tests/integration/chat-readiness.e2e.test.ts` inside the existing `completes setup, survives a restart, edits settings, and manages the roster` test: beside the current Step 1 `reentry` assertions near line 991, assert the Step 1 card contains the sentence; and beside the settings dashboard block near line 878, send a callback for `tokenLabelled(dashboard, "Edit time zone")` and assert the resulting `editMessageText` payload contains both `<b>Time zone</b>` and the sentence. Use the existing helpers and update-id numbering style of that file; do not restructure the test or introduce a new harness. Verify no renderer branch still carries the superseded instruction recorded by broken window 17, and do not reintroduce it in a comment. Per D-09 and D-10 the settings edit surface must state the same gesture as setup, because the same privacy-mode constraint governs both.</action>
  <verify>
    <automated>npx vitest run --project unit tests/unit/timezone-prompt-copy.test.ts &amp;&amp; npm run test:unit &amp;&amp; npx vitest run --project integration tests/integration/chat-readiness.e2e.test.ts &amp;&amp; npm run build &amp;&amp; [ "$(grep -v '^\s*\*\|^\s*//' src/telegram/renderers.ts | grep -c 'in this group to choose' || true)" = "0" ] &amp;&amp; [ "$(grep -c 'TIMEZONE_LOCATION_HINT' src/telegram/renderers.ts)" -ge 3 ]</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` exits 0 with no test removed or skipped relative to the pre-task baseline of 13 files and 83 tests.
    - `npx vitest run --project integration tests/integration/chat-readiness.e2e.test.ts` exits 0.
    - `npm run build` exits 0.
    - Counting non-comment lines of `src/telegram/renderers.ts` that match `in this group to choose` yields exactly 0, proving the superseded instruction is gone from every branch of that file.
    - `src/telegram/renderers.ts` references `TIMEZONE_LOCATION_HINT` at least three times: the declaration plus one use in each of the two time-zone branches.
    - The unit drift guard fails if either renderer branch is edited without the other.
  </acceptance_criteria>
  <done>Both time-zone prompts quote one sentence, a drift guard makes divergence a test failure, and the composed bot proves the sentence reaches both real surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Align the live runbook instruction and close broken window 17</name>
  <files>.planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md, .planning/WINDOWS.md</files>
  <read_first>
    - .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md — read `## Step 1 — Entering setup` at line 64 and `### 2a. Location → candidates` at line 82; the historical Run 1, Run 2, and Run 3 result lines below each are evidence and must not be rewritten
    - .planning/WINDOWS.md — read window 17 and, for the closure convention, windows 4 to 12, which were all marked fixed when their fix landed and confirmed live only in the following run
    - .planning/phases/01-chat-readiness/01-27-SUMMARY.md — read the recorded required follow-up for F-12
    - src/telegram/renderers.ts — take the shipped sentence from `TIMEZONE_LOCATION_HINT` as the single source for the runbook quotation
  </read_first>
  <action>In `.planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md`, update only the forward-looking operator instructions and leave every dated historical result line for Runs 1, 2, and 3 exactly as recorded. In `## Step 1 — Entering setup`, replace the final bullet of the `**Expected:**` list so that instead of describing an unnamed location instruction it quotes the shipped sentence `Reply to this message with a location to choose this chat's time zone.` verbatim after `Setup in progress` and `Step 1 of 8`. In `### 2a. Location → candidates`, keep the existing `**Action:**` requirement that the location is attached as a reply and add a one-line note that this gesture is now stated by the Step 1 prompt itself, cross-referencing `## ⚠️ Two traps to know before starting`. Then close broken window 17 by running `node /home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/bin/gsd-tools.cjs windows fixed 17`, and afterwards set the `reason` cell of row 17 — in both the markdown table and the matching object in the trailing JSON block, byte-identically — to a sentence naming the landed commit for Tasks 1 and 2, naming the four surfaces the window required (contract row, both renderers, focused tests, runbook instruction), and stating explicitly that live confirmation is still pending the bounded run. Do not touch window 16, any fixed window, or the waived window 10. Per D-09 the runbook is the operator-facing statement of the same decision the prompt now carries.</action>
  <verify>
    <automated>node /home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/bin/gsd-tools.cjs windows status &amp;&amp; awk '/^## Step 1 — Entering setup/,/^## Step 2/' .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md | grep -q "Reply to this message with a location to choose this chat's time zone." &amp;&amp; grep -q '"id": 17' .planning/WINDOWS.md &amp;&amp; node -e "const t=require('fs').readFileSync('.planning/WINDOWS.md','utf8');const j=JSON.parse(t.slice(t.indexOf('[',t.indexOf('json')),t.lastIndexOf(']')+1));const w=j.find(x=>x.id===17);if(w.status!=='fixed'||!w.reason||w.reason.length<40)process.exit(1);const s=j.find(x=>x.id===16);if(s.status!=='open')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - The `## Step 1 — Entering setup` region of the runbook contains the shipped sentence verbatim.
    - The dated Run 1, Run 2, and Run 3 result lines under Steps 1 and 2a are unchanged, verified by `git diff` showing no edit on those lines.
    - `gsd-tools windows status` reports window 17 as `fixed` and window 16 still `open`, with `fixed_count` increased by exactly one.
    - Window 17's `reason` is non-empty and identical in the markdown table and the JSON block, names the landed commit, and states that live confirmation is pending.
    - No other window row changes status, reason, or `resolved_at`.
  </acceptance_criteria>
  <done>The next live operator is told the same gesture the bot now prints, and the ledger records window 17 closed on landed code with live confirmation still owed.</done>
</task>

</tasks>

## Artifacts this phase produces

**New files**
- `tests/unit/timezone-prompt-copy.test.ts`

**New exported symbols**
- `TIMEZONE_LOCATION_HINT` — exported string constant in `src/telegram/renderers.ts`, value `Reply to this message with a location to choose this chat's time zone.`

**New test names**
- `time-zone location prompt copy > states the reply gesture in the setup step 1 prompt`
- `time-zone location prompt copy > states the same reply gesture in the settings time-zone edit prompt`
- `time-zone location prompt copy > keeps both time-zone prompts byte-identical to the copywriting contract sentence`
- `time-zone location prompt copy > leaves the setup step 1 projection without inline buttons`

**New document elements**
- `01-UI-SPEC.md` `## Copywriting Contract` row `Time-zone location prompt`
- `01-UI-SPEC.md` `### Setup wizard sequence` item 1 authority sentence

**Modified in place, no new symbol**
- `src/telegram/renderers.ts` — `renderSetupStep` step-1 branch, `renderSettingsEditPrompt` `SettingsField.TIMEZONE` case
- `tests/integration/chat-readiness.e2e.test.ts` — assertions added inside the existing `completes setup, survives a restart, edits settings, and manages the roster` test
- `01-LIVE-VERIFICATION-RUNBOOK.md` — Step 1 expectation, 2a note
- `.planning/WINDOWS.md` — window 17 status and reason

<assumption_delta_decision>
**Signal:** `"3. A chat administrator can choose whether administrators, previous-poll participants, or anyone may start planning."` — a derived value became chosen / a constant became a parameter.

**Noun now primary:** the per-chat planning-access policy (`ChatConfiguration.planningAccessPolicy`), rather than a fixed administrators-only constant.

**Decision:** `no-change`.

**Rationale:** the promotion already happened and already shipped in Phase 1 under AUTH-01 — the policy is a persisted, administrator-selected column with its own setup step 8, its own `/settings` edit surface, and its own tests. This gap set changes time-zone prompt copy, a broken-window disposition, and a bounded live run; none of them touches the policy noun, so promoting it again would be a no-op rewrite of shipped behaviour.
</assumption_delta_decision>

<schema_relevance>
**Conclusion: no Prisma schema file is touched by this gap set.** `files_modified` across plans 01-28, 01-29, and 01-30 contains no `prisma/schema.prisma` and no `prisma/schema/*.prisma` entry, so no `npx prisma db push` task is injected. This is recorded as a decision rather than left as an omission: the F-12 fix is renderer copy, its tests, planning documents, and a live observation — none of which changes a model, a column, an index, or a migration.
</schema_relevance>

<flagged_assumptions>
Spec-less probe fallback fired for this phase (`EDGE_ABSENT=1`, `PROHIB_ABSENT=1`). The deterministic edge probe returned 12 applicable items, all `unresolved`. This register is the phase-set authority; plans 01-29 and 01-30 reference it and restate only their own gap-relevant rows. No item is dropped, and no `unclassified` row is auto-resolved.

| # | Requirement | Category | Probe | Disposition |
|---|---|---|---|---|
| 1 | CONF-01 | unclassified | review manually | **Unresolved, largely covered.** Time-zone initialization is shipped and green: `tests/unit/setup.test.ts`, `tests/integration/chat-configuration.test.ts`, `tests/integration/walking-skeleton.test.ts`, and live Run 3 rows 3-02, 3-05, 3-06. The one genuinely open part is the Step 1 *instruction copy*, which is F-12 and is closed by this plan; it is carried in this plan's `must_haves`. |
| 2 | CONF-02 | unclassified | review manually | **Unresolved, covered.** Default weekday and start time ship with `tests/unit/schedule-settings.test.ts` and live Run 3 rows 3-03, 3-08. Not touched by this gap set. |
| 3 | CONF-03 | unclassified | review manually | **Unresolved, covered.** Duration and daily boundaries ship with `src/domain/chat/schedule-validator.ts` (including the floor rule added for F-6), `tests/unit/schedule-settings.test.ts`, and live Run 3 rows 3-09, 3-10. Not touched by this gap set. |
| 4 | CONF-05 | unclassified | review manually | **Unresolved, covered.** Reminder defaults and edits ship with `tests/unit/schedule-settings.test.ts`, `tests/unit/settings.test.ts`, and live Run 3 row 3-05. Not touched by this gap set. |
| 5 | ROST-01 | unclassified | review manually | **Unresolved, covered.** Reply-anchored add ships with `tests/unit/roster-add.test.ts`, `tests/integration/roster-repository.test.ts`, and live Run 3 rows 3-11, 3-13. Not touched by this gap set. |
| 6 | ROST-02 | idempotency | What happens if this runs twice on the same input? | **Unresolved, covered.** A repeat `/roster_add` reactivates the same soft-deleted membership row rather than inserting a duplicate — proven at the data level in live Run 3 row 3-12 and recorded in UAT test 10, which observed the row keeping its Run 1 id across an add/remove/re-add cycle. |
| 7 | ROST-02 | concurrency | If interrupted or run in parallel, what is guaranteed? | **Unresolved and genuinely open.** No test drives two simultaneous roster mutations for one chat. The stack decision (`@grammyjs/runner` sequentialized by `chat.id`) and the compound unique constraint make a collision unlikely, but neither is asserted. Out of scope for this gap set — it is not Gap A, B, or C — and is surfaced here for phase verification to dispose. |
| 8 | ROST-03 | unclassified | review manually | **Unresolved, covered with a named residual.** Projection and removal ship with `tests/unit/roster-rendering.test.ts`, `tests/unit/roster-remove.test.ts`, and live Run 3 rows 3-13, 3-14, 3-15, 3-20. Residual: UAT test 16 (live pagination wiring) remains owner-deferred and stays deferred here. |
| 9 | AUTH-01 | unclassified | review manually | **Unresolved, covered.** Planning-access choice ships with `tests/unit/planning-access.test.ts` and `tests/integration/chat-configuration.test.ts` (12/12 green today). Plan 01-29 ratifies the invalid-policy contract on this exact surface. |
| 10 | AUTH-02 | adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | **Unresolved, covered on both readings.** Schedule-boundary adjacency is asserted by `tests/unit/schedule-settings.test.ts` (`rejects exact schedule boundary conflicts`). Authorization adjacency — a role changing at the instant of an action — is covered by the recheck-per-action design, observed live in Run 3 row 3-17. |
| 11 | AUTH-02 | empty | What is the result for empty, single-element, or null input? | **Unresolved, covered.** `AuthorizationService.requireCurrentAdministrator` now throws `PermissionDeniedError` on an unobserved role without deleting drafts (CR-01, closed; `tests/unit/authorization.test.ts`), and the zero-member roster surface is proven live in Run 3 row 3-20. |
| 12 | AUTH-02 | ordering | When elements compare equal, is output order specified and stable? | **Unresolved, covered.** Draft deletion strictly precedes denial inside `authorization-service.ts`; the single honoured callback answer is the one carrying the outcome (plan 01-16, live Run 3 row 3-17); roster ordering is deterministic per `tests/unit/roster-rendering.test.ts`. |

**No-silent-drop check:** 12 probe items surfaced, 12 accounted for here (11 covered-and-cited, 1 genuinely open and named). Only the gap-relevant predicate for this plan — CONF-01, item 1 — is lifted into `must_haves`.
</flagged_assumptions>

<source_audit>
| Source | Item | Status | Covering plan |
|---|---|---|---|
| GOAL | Administrators can prepare a persistent, access-controlled chat for rehearsal coordination | COVERED | Shipped across plans 01-01…01-27; this gap set closes the one remaining blocking UX defect |
| REQ | CONF-01 | COVERED | 01-28 (prompt copy), 01-30 (live confirmation) |
| REQ | CONF-02, CONF-03, CONF-05, ROST-01, ROST-02, ROST-03, AUTH-02 | COVERED | Shipped and green in plans 01-05…01-27; untouched by this gap set by design |
| REQ | AUTH-01 | COVERED | 01-29 (window 16 disposition on the planning-access surface) |
| RESEARCH | Telegram privacy mode stays enabled; a group location reaches the bot only as a reply to its own message | COVERED | 01-28 — the constraint is now stated in shipped copy, not only in operator prose |
| RESEARCH | Group-only interaction; no reply keyboard, no WebView, no private-chat location request | COVERED | 01-28 prohibitions and acceptance criteria |
| CONTEXT | D-09 — time zone chosen by sharing a location, with explicit confirmation | COVERED | 01-28, 01-30 |
| CONTEXT | D-01 — guided `/setup` wizard | COVERED | 01-28 (Step 1 prompt stays inside the wizard) |
| CONTEXT | D-10 — settings changes reviewed before activation | COVERED | 01-28 (settings time-zone edit prompt) |
| CONTEXT | D-02…D-08, D-11…D-14 | COVERED | Shipped in plans 01-05…01-27; not re-planned |

No source item is MISSING. Deferred Ideas: CONTEXT.md records none. Owner-deferred items retained as deferred, not as gaps: UAT test 16 (live roster pagination), UAT test 2 multi-candidate branch, UAT test 8 residual clause, N-6.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram group members to bot prompts | The time-zone prompt solicits location data from an untrusted group surface; what it asks for and how it asks are both security-relevant. |
| Planning contract to shipped copy | `01-UI-SPEC.md` is the authority for user-facing text; drift between contract and renderer is how F-1, F-8, and F-12 each arose. |
| Working tree to repository evidence | Tests, fixtures, and planning documents must carry no location, identity, or credential data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-28-01 | Information Disclosure | Time-zone prompt copy | medium | mitigate | Keep the prompt a group-native reply instruction; add no private-chat location request, reply keyboard, or WebView, and echo no coordinate or resolved zone in copy, fixtures, or evidence. |
| T-01-28-02 | Tampering | `01-UI-SPEC.md` Copywriting Contract versus renderers | medium | mitigate | Single-source the sentence in one contract row and one exported constant; a region-scoped grep proves the row exists, a file grep proves the superseded sentence is gone, and a unit drift guard fails if the two branches diverge. |
| T-01-28-03 | Spoofing | Telegram privacy-mode posture | high | mitigate | The change is copy only. No handler, update filter, or privacy setting is modified; the full unit and e2e integration suites are re-run to prove the `message:location` reply-only routing is untouched. |
| T-01-28-04 | Repudiation | Broken-window ledger | low | accept | Window 17 closes on landed code following the windows 4-12 precedent, and its reason states explicitly that live confirmation is still pending, so the ledger cannot be read as a live pass. |
| T-01-28-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation or dependency change occurs in this plan, so no package-legitimacy audit or install checkpoint is required. |
</threat_model>

<verification>
The Copywriting Contract carries one normative row for the time-zone location prompt; both renderers interpolate one exported constant that quotes it; unit, drift-guard, and composed-bot tests are green; `npm run build` is clean; the superseded sentence is absent from `src/telegram/renderers.ts`; the runbook Step 1 expectation quotes the shipped sentence; window 17 is closed with a reason that keeps live confirmation owed.
</verification>

<success_criteria>
- `npm run test:unit`, `npx vitest run --project integration tests/integration/chat-readiness.e2e.test.ts`, and `npm run build` all exit 0.
- Exactly one occurrence of the new sentence exists in `src/telegram/renderers.ts`, and both time-zone branches reach it through `TIMEZONE_LOCATION_HINT`.
- The superseded instruction is absent from `src/telegram/renderers.ts`.
- `01-UI-SPEC.md` states the sentence once, in the Copywriting Contract, with an authority note preventing restatement.
- Historical runbook run results are unchanged; only forward-looking operator instructions moved.
- `gsd-tools windows status` shows window 17 fixed with a substantive reason and window 16 still open.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-28-SUMMARY.md` when done.
</output>
