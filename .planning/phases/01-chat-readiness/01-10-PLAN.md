---
phase: 01-chat-readiness
plan: 10
type: execute
wave: 10
depends_on:
  - 01-09
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260819020000_roster/migration.sql
  - src/app/create-bot.ts
  - src/domain/roster/roster-service.ts
  - src/telegram/roster-handlers.ts
  - tests/unit/roster-add.test.ts
  - tests/integration/roster-repository.test.ts
autonomous: true
requirements:
  - ROST-01
  - ROST-03
  - AUTH-02
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-04, `/roster_add` accepts only the identifiable non-bot `from` user on the replied group message; typed names/usernames never become membership keys."
    - "Per D-05, `/roster` reads active durable memberships and shows safe labels without exposing full numeric Telegram IDs."
    - "[Edge ROST-01/unclassified] Missing reply, anonymous/channel origin, absent `from`, or bot target is rejected before identity or membership write."
  artifacts:
    - path: "prisma/migrations/20260819020000_roster/migration.sql"
      provides: "Known Telegram identity and active/soft-deleted chat membership schema"
      contains: "ChatMembership"
    - path: "src/domain/roster/roster-service.ts"
      provides: "Reply-anchored membership upsert/list operations"
      exports:
        - RosterService
    - path: "src/telegram/roster-handlers.ts"
      provides: "Authorized `/roster_add` and `/roster` adapters"
      exports:
        - registerRosterHandlers
    - path: "tests/integration/roster-repository.test.ts"
      provides: "Migration, uniqueness, reactivation, and active-membership listing coverage"
  key_links:
    - from: "src/telegram/roster-handlers.ts"
      to: "Telegram reply_to_message.from"
      via: "extract the exact non-bot target for `/roster_add` before calling RosterService"
      pattern: "reply_to_message"
    - from: "src/domain/roster/roster-service.ts"
      to: "prisma/schema.prisma"
      via: "TelegramUser upsert and unique `(chatId, telegramUserId)` membership create/reactivate/list"
      pattern: "chatMembership"
    - from: "src/telegram/roster-handlers.ts"
      to: "src/domain/auth/authorization-service.ts"
      via: "fresh role check before list or add"
      pattern: "requireCurrentAdministrator"
  prohibitions:
    - requirement_id: ROST-03
      category: privacy
      status: unverified
      flagged: true
      statement: "MUST NOT expose a member's complete numeric Telegram user ID in roster, confirmation, error, or callback text when no readable identity exists."
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Deliver migrated roster persistence with the reply-anchored add and safe view flow per D-04 and D-05.

Purpose: Establish the durable participant source before the destructive removal and projection-hardening slices.
Output: Roster migration, service, Telegram add/view adapters, and migration/idempotency tests.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-PATTERNS.md
@.planning/phases/01-chat-readiness/01-VALIDATION.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-09-SUMMARY.md
@prisma/schema.prisma
@src/domain/auth/authorization-service.ts
</context>

<interfaces>
- `RosterService.addFromRepliedUser(chatId, actorId, TelegramUserIdentity)` upserts last-known readable identity and creates/reactivates one unique membership.
- `RosterService.listActive(chatId)` returns durable identity/membership records; rendering derives a safe label and never authorizes from stored identity.
- Membership identity uses Telegram bigint keys and soft-active timestamps so later removal/reactivation preserves history.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add and view persistent roster members through the committed roster migration</name>
  <reversibility rating="costly">Telegram identity and membership keys become participant references in later phases; changing them later requires migration of durable roster data.</reversibility>
  <read_first>
    <file>prisma/schema.prisma</file>
    <file>src/app/create-bot.ts</file>
    <file>src/domain/auth/authorization-service.ts</file>
    <file>.planning/phases/01-chat-readiness/01-CONTEXT.md</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
    <file>.planning/phases/01-chat-readiness/01-PATTERNS.md</file>
  </read_first>
  <files>prisma/schema.prisma, prisma/migrations/20260819020000_roster/migration.sql, src/app/create-bot.ts, src/domain/roster/roster-service.ts, src/telegram/roster-handlers.ts, tests/unit/roster-add.test.ts, tests/integration/roster-repository.test.ts</files>
  <behavior>
    - Test 1: a current administrator replying to a non-bot user's message with `/roster_add` creates one known identity and active membership, then emits exact added copy.
    - Test 2: adding the same active user is idempotent; adding a previously inactive membership reactivates that same membership.
    - Test 3: no reply, anonymous/channel origin, missing `from`, or bot target is rejected without a write.
    - Test 4: `/roster` reads active memberships only and renders the documented empty state or safe sorted entries.
    - Test 5: demotion before add or list deletes the actor's draft and denies before roster read/write.
  </behavior>
  <action>Add `TelegramUser` and `ChatMembership` with bigint Telegram identity, unique chat membership, last-known readable identity, and soft-active timestamps; commit the roster migration. Apply it to a clean PostgreSQL 18 integration database, assert `prisma migrate status` and database constraints match the committed schema, then run roster behavior tests. Per D-04, implement `/roster_add` by reading only `ctx.msg.reply_to_message.from`, rejecting bot/anonymous/unreadable targets, revalidating the current administrator, and upserting/reactivating transactionally. Per D-05, implement `/roster` from active durable records with alphabetical safe identity projection, exact empty copy, and final-four fallback. Halt before removal work if migration application or clean-schema checks fail.</action>
  <acceptance_criteria>
    <criterion>The clean integration database has no pending migrations and enforces one membership per `(chatId, telegramUserId)`.</criterion>
    <criterion>Only a replied, identifiable, non-bot Telegram user can create/reactivate membership; typed name/username is never accepted as authority.</criterion>
    <criterion>Repeated add is idempotent and inactive membership reactivation preserves identity/history rather than inserting a duplicate.</criterion>
    <criterion>Empty and populated roster projections use exact UI copy, active records only, safe labels, and no full numeric ID exposure.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run test:unit -- roster-add &amp;&amp; npm run test:integration -- roster-repository</automated>
  </verify>
  <done>A current administrator can add identifiable replied users and view a durable safe roster after a clean committed migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| replied Telegram message → durable identity | Nested `from` may be absent, bot-owned, anonymous, or unrelated to typed command text. |
| roster command → current administrator check | Listing and adding require fresh external authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-18 | Spoofing | `/roster_add` target identity | high | mitigate | Accept only non-bot `reply_to_message.from`; never build membership from typed text/username. |
| T-01-20 | Elevation of privilege | roster add/list action | high | mitigate | Revalidate current administrator before list/add; denial discards actor drafts. |
| T-01-21 | Information disclosure | fallback identity | medium | mitigate | Expose only final four ID digits in UI and keep full identity server-side. |
| T-01-SC | Tampering | installed packages | high | mitigate | Use the approved lockfile and add no dependency in this plan. |
</threat_model>

<verification>
Apply and inspect the committed roster migration, then run formatting, add unit tests, and repository integration tests.
</verification>

<success_criteria>
- ROST-01, base ROST-03, and add/list AUTH-02 behavior are reachable through Telegram commands.
- D-04, D-05, D-13, and D-14 are directly implemented and tested.
- Roster identity privacy and add-edge predicates remain automated.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-10-SUMMARY.md` when done.
</output>
