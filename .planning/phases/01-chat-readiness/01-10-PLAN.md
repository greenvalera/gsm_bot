---
phase: 01-chat-readiness
plan: 10
type: execute
wave: 10
depends_on:
  - 01-09
files_modified:
  - src/telegram/roster-handlers.ts
  - src/telegram/roster-renderers.ts
  - src/telegram/keyboards.ts
  - tests/unit/roster-rendering.test.ts
  - tests/unit/roster-remove.test.ts
autonomous: true
requirements:
  - ROST-02
  - ROST-03
  - AUTH-02
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-05, active members sort alphabetically by safe display label and render name+username, name only, or masked final-four ID fallback."
    - "Zero members uses the documented empty state without removal controls; 1/20/21/many members use deterministic pages of 20 with correct footer and controls."
    - statement: "Roster retrieval delay, failure, and retry behavior is exercised for populated and empty results without losing ordering, paging, or action identity."
      verification: backstop
    - statement: "A held-out Unicode case verifies long names/usernames wrap safely while callback routing retains the intended membership identity."
      verification: backstop
    - "[Edge ROST-03/unclassified] Empty, one-member, duplicate-label, unreadable-identity, Unicode, and more-than-20-member rosters have deterministic non-leaking projections."
  artifacts:
    - path: "src/telegram/roster-renderers.ts"
      provides: "Safe identity labels, deterministic sort/pagination, and removal confirmation"
      exports:
        - renderRoster
        - renderRemovalConfirmation
    - path: "src/telegram/keyboards.ts"
      provides: "Page-bound navigation and per-member opaque removal actions"
    - path: "tests/unit/roster-rendering.test.ts"
      provides: "Identity, Unicode, 0/1/20/21, equal-label, delay, failure, retry, and stale-page coverage"
  key_links:
    - from: "src/telegram/roster-renderers.ts"
      to: "src/domain/roster/roster-service.ts"
      via: "active membership records become sorted safe labels with internal-only bigint tie-breaker"
      pattern: "safeMemberLabel"
    - from: "src/telegram/keyboards.ts"
      to: "CallbackAction"
      via: "every page/removal button receives an opaque action bound to the exact rendered membership set"
      pattern: "CallbackAction"
    - from: "src/telegram/roster-handlers.ts"
      to: "src/domain/auth/authorization-service.ts"
      via: "page, retry, and remove actions revalidate the current administrator before roster access"
      pattern: "requireCurrentAdministrator"
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Harden roster projection, pagination, delay, failure, and Unicode behavior without changing roster persistence.

Purpose: Complete ROST-03 and the UI backstops while preserving exact membership/action identity across every projection state.
Output: Deterministic safe roster renderer/keyboards and held-out projection tests.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-PATTERNS.md
@.planning/phases/01-chat-readiness/01-VALIDATION.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-09-SUMMARY.md
@src/domain/roster/roster-service.ts
@src/telegram/roster-handlers.ts
</context>

<interfaces>
- Roster ordering uses normalized safe display label and Telegram user ID as an unseen tie-breaker; pagination is 20 entries with stable boundaries.
- `safeMemberLabel` never returns a complete numeric Telegram ID; unreadable identity uses only the final four decimal digits.
- Page/removal actions are opaque server-side `CallbackAction` rows bound to the exact chat, actor, membership/page, expiry, and rendered snapshot.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make roster identity, pagination, delay, and failure projections deterministic</name>
  <read_first>
    <file>src/domain/roster/roster-service.ts</file>
    <file>src/telegram/roster-handlers.ts</file>
    <file>src/telegram/roster-renderers.ts</file>
    <file>src/telegram/keyboards.ts</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
  </read_first>
  <files>src/telegram/roster-handlers.ts, src/telegram/roster-renderers.ts, src/telegram/keyboards.ts, tests/unit/roster-rendering.test.ts, tests/unit/roster-remove.test.ts</files>
  <behavior>
    - Test 1: name+username, name-only, and unreadable identity render exactly as contracted, exposing only four trailing ID digits in the fallback.
    - Test 2: Unicode and equal display labels sort deterministically with an internal ID tie-breaker and wrap without corrupting callback target identity.
    - Test 3: 0, 1, 20, 21, and more members render correct controls/pages/footer; page actions retain per-member removal bindings.
    - Test 4: delayed populated/empty reads show an in-flight state, and read failure plus retry never displays partial ordering or mismatched actions.
  </behavior>
  <action>Centralize safe identity labels, Unicode-aware deterministic sorting, and fixed pages of 20. Use full readable identity only in message text; if neither readable name nor username exists, show exactly the bullet-masked final-four fallback. Resolve duplicate labels with internal Telegram bigint solely as an unseen stable tie-breaker. Create opaque server-side page and removal actions for the exact rendered membership set. Add held-out tests for long Unicode names/usernames, 0/1/20/21 boundaries, equal labels, delayed reads, database failure, retry, and stale pages. Keep button labels action-first and bounded, with exact footer and neutral Previous/Next controls. Retain fresh authorization and initiating-admin removal binding from Plan 01-09 on every new page/retry path.</action>
  <acceptance_criteria>
    <criterion>All identity forms, Unicode wrapping, tie ordering, and ID masking match the UI contract without leaking a full numeric ID.</criterion>
    <criterion>Pagination is stable at 20 entries, footer ranges are correct, and every visible removal button maps to its adjacent membership.</criterion>
    <criterion>Empty, one, populated, delayed, failure, retry, and stale-page paths have focused tests and never mix labels with another membership's action.</criterion>
    <criterion>The empty roster shows no removal controls and ends with the exact `/roster_add` reply instruction.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run test:unit -- roster-rendering &amp;&amp; npm run test:unit -- roster-remove &amp;&amp; npm run test:integration -- roster-repository</automated>
  </verify>
  <done>Roster projections are safe and deterministic across empty, Unicode, equal-label, paginated, delayed, failed, and retried states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| durable identity → public Telegram projection | Names, usernames, and IDs require privacy-preserving display and stable action mapping. |
| page/retry callback → roster snapshot | Token may be stale or cross-boundary; server-side bindings must select the exact current projection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-21 | Information disclosure | fallback identity/projection | medium | mitigate | Render only final-four ID fallback and keep full IDs in server-side keys/log redaction scope. |
| T-01-34 | Tampering | page/removal action mapping | high | mitigate | Bind opaque actions to actor/chat/membership/page/expiry and test label-to-action adjacency at boundaries. |
| T-01-22 | Denial of service | long roster projection | low | accept | Fixed deterministic pages of 20 bound message size/callback count for the small band-chat domain. |
| T-01-SC | Tampering | installed packages | high | mitigate | Use the approved lockfile and add no dependency in this plan. |
</threat_model>

<verification>
Run formatting, renderer/removal suites, and roster repository integration tests; cover identity forms, Unicode, pagination boundaries, delay/failure/retry, and stale actions.
</verification>

<success_criteria>
- ROST-03 is complete for safe identity, ordering, empty state, pagination, delay/failure, and Unicode behavior.
- D-05/D-06 action identity remains intact through every page and retry.
- The ROST-03 edge row and both roster UI backstops are automated.
- Full numeric IDs never cross into public text or callback data.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-10-SUMMARY.md` when done.
</output>
