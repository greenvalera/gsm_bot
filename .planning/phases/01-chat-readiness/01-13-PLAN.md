---
phase: 01-chat-readiness
plan: 13
type: execute
wave: 14
depends_on:
  - 01-12
files_modified:
  - src/app/create-bot.ts
  - src/shared/callback-schema.ts
  - src/telegram/handlers.ts
  - src/telegram/callbacks.ts
  - tests/integration/chat-readiness.e2e.test.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-05
  - ROST-01
  - ROST-02
  - ROST-03
  - AUTH-01
  - AUTH-02
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Every Phase 1 protected command, wizard input, dashboard action, roster action, and callback reaches the same current Telegram administrator boundary before protected state access or mutation."
    - "Every callback is acknowledged immediately; successful mutations replace the originating message with authoritative state and duplicate taps receive `Already applied.`."
    - "Every callback token is short, opaque, and versioned; names, permissions, schedule values, coordinates, Telegram IDs, and authorization claims remain server-side."
    - "Validation, location, permission, expiry, save, duplicate, stale-action, delay, and read failures use approved copy and preserve authoritative state."
    - "The complete setup → restart → settings edit → roster add/list/remove workflow passes against freshly applied committed migrations."
  artifacts:
    - path: "src/telegram/handlers.ts"
      provides: "Single registration point for every Phase 1 command and update handler"
      exports:
        - registerChatReadinessHandlers
    - path: "src/telegram/callbacks.ts"
      provides: "Versioned callback dispatch, acknowledgement ordering, and stale/duplicate/error projection"
      exports:
        - registerChatReadinessCallbacks
    - path: "tests/integration/chat-readiness.e2e.test.ts"
      provides: "Complete setup/settings/roster/restart/demotion integration flow"
  key_links:
    - from: "src/app/create-bot.ts"
      to: "src/telegram/handlers.ts"
      via: "composition root registers every command/update exactly once"
      pattern: "registerChatReadinessHandlers"
    - from: "src/telegram/callbacks.ts"
      to: "src/shared/callback-schema.ts"
      via: "one parse/ack/load/authorize/dispatch boundary for all opaque actions"
      pattern: "safeParse"
    - from: "tests/integration/chat-readiness.e2e.test.ts"
      to: "prisma/migrations"
      via: "fresh database runs the full Telegram-native readiness flow and restart reads"
      pattern: "migrate deploy"
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Unify every Phase 1 command/callback route and prove the complete migrated readiness workflow end to end.

Purpose: Ensure setup, settings, authorization, and roster slices compose without a bypass, stale projection, or migration drift before operational closure.
Output: Central route/callback adapters and a full setup/settings/roster/restart/demotion integration suite.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-PATTERNS.md
@.planning/phases/01-chat-readiness/01-VALIDATION.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/COVERAGE.md
@.planning/phases/01-chat-readiness/01-07-SUMMARY.md
@.planning/phases/01-chat-readiness/01-09-SUMMARY.md
@.planning/phases/01-chat-readiness/01-12-SUMMARY.md
@src/domain/auth/authorization-service.ts
@src/telegram/setup-handlers.ts
@src/telegram/settings-handlers.ts
@src/telegram/roster-handlers.ts
</context>

<interfaces>
- `registerChatReadinessHandlers(bot, services)` composes setup, settings, roster, location, and text routing exactly once.
- `registerChatReadinessCallbacks(bot, services)` acknowledges first, parses a version-prefixed opaque token, loads `CallbackAction`, performs current-role authorization, and dispatches by server-side kind.
- The full integration test recreates the process/client with the same migrated database to prove configuration and roster survive restart.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove every Phase 1 route crosses one callback/authorization/durable-state boundary</name>
  <read_first>
    <file>src/app/create-bot.ts</file>
    <file>src/shared/callback-schema.ts</file>
    <file>src/telegram/setup-handlers.ts</file>
    <file>src/telegram/settings-handlers.ts</file>
    <file>src/telegram/roster-handlers.ts</file>
    <file>.planning/phases/01-chat-readiness/COVERAGE.md</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
  </read_first>
  <files>src/app/create-bot.ts, src/shared/callback-schema.ts, src/telegram/handlers.ts, src/telegram/callbacks.ts, tests/integration/chat-readiness.e2e.test.ts</files>
  <behavior>
    - Test 1: every `/setup`, `/settings`, `/roster`, `/roster_add`, location, text-step, and callback route registers once and reaches current-role authorization where protected.
    - Test 2: every versioned callback is acknowledged before parse/load/role/database latency and dispatches solely from server-side kind/bindings.
    - Test 3: malformed, missing, stale, expired, duplicate, cross-chat, cross-actor, demoted, and persistence-failure callbacks preserve authoritative state and return exact copy.
    - Test 4: a full setup → restart → settings edit → roster add/list/remove flow persists through freshly applied PostgreSQL migrations.
  </behavior>
  <action>Centralize Phase 1 handler registration and callback dispatch while keeping feature services/renderers separate. Parse only a version-prefixed opaque token within Telegram's byte limit, acknowledge callbacks immediately, load authoritative `CallbackAction`, refresh current membership, and dispatch by stored kind. Enumerate every protected route in a table-driven integration test so no registered command/callback can bypass authorization. Exercise the complete workflow against committed PostgreSQL migrations, reconstruct the bot/Prisma client mid-test, and assert committed state, actor-bound drafts, duplicate behavior, and UI error copy after restart. Keep every COVERAGE.md INTEGRATE row exercised or explicitly mapped to a focused test; introduce no unlisted Telegram surface.</action>
  <acceptance_criteria>
    <criterion>One central registration test lists every Phase 1 command/update/callback and proves protected routes invoke current-role gateway before state access.</criterion>
    <criterion>All callbacks satisfy acknowledgement-before-latency and server-side-authority rules, including malformed/stale/duplicate/cross-boundary paths.</criterion>
    <criterion>The full migrated workflow survives process/client recreation with identical configuration and roster state.</criterion>
    <criterion>Every `INTEGRATE` row in COVERAGE.md has a handler/test reference and no undocumented external Telegram surface appears.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run test:integration -- chat-readiness.e2e &amp;&amp; npm run test:unit -- authorization</automated>
  </verify>
  <done>The composed bot has no protected-route authorization bypass and the complete readiness workflow survives restart on committed schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| central Telegram router → feature services | Registration/dispatch mistakes could bypass authorization or acknowledge too late. |
| Telegram callback token → server-side action | Untrusted tokens must never carry identity, permission, or mutation authority. |
| migrated PostgreSQL state → recreated bot process | Restart must preserve committed state without reviving expired or consumed actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-23 | Elevation of privilege | command/callback registration | high | mitigate | Table-driven route inventory proves every protected route invokes shared current-role boundary. |
| T-01-24 | Tampering | callback dispatcher | high | mitigate | Ack first, validate opaque token, load server-side bindings, reauthorize, and dispatch by stored kind. |
| T-01-SC | Tampering | npm dependency graph | high | mitigate | The E2E suite uses the human-approved lockfile and installs no package. |
</threat_model>

<verification>
Run formatting, the route inventory/authorization suite, and the full migrated restart flow; validate every API coverage integration row.
</verification>

<success_criteria>
- All nine Phase 1 requirements and D-01 through D-14 are reachable through the composed Telegram routes.
- Every protected route revalidates current permission and demotion discards actor-bound drafts immediately.
- The full workflow survives process/client recreation on committed migrations.
- API coverage has no unlisted Telegram integration or unmapped INTEGRATE row.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-13-SUMMARY.md` when done.
</output>
