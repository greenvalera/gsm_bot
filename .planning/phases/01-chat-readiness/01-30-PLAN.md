---
phase: 01-chat-readiness
plan: 30
type: execute
wave: 3
depends_on:
  - 01-28
  - 01-29
files_modified:
  - .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md
  - .planning/phases/01-chat-readiness/01-UAT.md
  - .planning/WINDOWS.md
autonomous: false
requirements:
  - CONF-01
gap_closure: true
user_setup:
  - service: telegram
    why: "A human administrator must exercise the two fixed time-zone prompts in a real Telegram group; privacy-mode reply delivery cannot be reproduced by an automated transport double."
    env_vars:
      - name: BOT_TOKEN
        source: "Existing BotFather-issued token, supplied locally and never committed"
      - name: POSTGRES_PASSWORD
        source: "Local verification secret, supplied locally and never committed"
    dashboard_config:
      - task: "The existing bot remains an administrator in the same disposable private group used by Run 3; privacy mode stays enabled."
        location: "Telegram client group settings"
estimate:
  tokens: 17000
  raw_tokens: 17000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A real Telegram administrator sees the new reply-gesture sentence at setup Step 1 and at the `/settings` time-zone edit prompt in a live group."
    - "A location sent as a reply to the bot's Step 1 prompt resolves time-zone candidates, proving the new instruction is truthful and not merely better-worded."
    - "The runbook records a clearly-labelled scoped run that claims only the rows it exercised and is never presented as a full re-run of the Run 3 matrix."
    - "`01-UAT.md` test 1 carries its observed live outcome and its F-12 note reflects the bounded run rather than the superseded Run 3 rejection."
    - "No artifact flips `01-14-SUMMARY.md` out of `halted` or declares Phase 1 complete; that decision stays with verify-phase and depends on this run's verdict."
    - "An unfilled step always renders its current prompt and required input format; completed draft values remain actor-bound, `Step X of 8` identifies progress, and no partial draft is presented as active configuration."
    - "Validation, location, permission, expiry, save, duplicate, and stale-action failures use the corresponding Copywriting Contract row and preserve the last authoritative state."
    - statement: "A location lookup renders an in-flight state and then atomically replaces it with either the candidate zone or the documented resolution failure."
      verification: backstop
  artifacts:
    - path: ".planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md"
      provides: "A clearly-labelled scoped Run 4 section with its own bounded row set, sanitized evidence, and an explicit human verdict"
    - path: ".planning/phases/01-chat-readiness/01-UAT.md"
      provides: "Test 1 rescored to its observed live outcome with an updated F-12 note and a summary that distinguishes the scoped run"
    - path: ".planning/WINDOWS.md"
      provides: "A new open window for any newly observed live failure, without erasing prior evidence"
  key_links:
    - from: ".planning/phases/01-chat-readiness/01-28-SUMMARY.md"
      to: "compose.yaml"
      via: "the exact commit that landed the F-12 fix is the candidate launched for Run 4"
      pattern: "TIMEZONE_LOCATION_HINT"
    - from: "compose.yaml"
      to: "Telegram private group"
      via: "the long-polling bot process against the preserved named PostgreSQL volume"
      pattern: "gsmbot-postgres-data"
    - from: ".planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md"
      to: ".planning/phases/01-chat-readiness/01-UAT.md"
      via: "test 1's status cites its matching scoped Run 4 observation and nothing else"
      pattern: "Run 4"
  prohibitions:
    - requirement_id: CONF-01
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT edit the candidate under live test during the run; a failure opens a new window and requires a fresh candidate rather than an in-run fix."
    - requirement_id: CONF-01
      category: privacy
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT retain a Telegram identity, group name, username, chat or user identifier, raw coordinate, resolved candidate zone, credential, or unrelated screenshot content in repository evidence."
    - requirement_id: CONF-01
      category: transparency
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT infer approval from silence, from a passing sub-row, or from a green automated gate, and MUST NOT mark passed any row the bounded run did not exercise."
    - requirement_id: CONF-01
      category: safety
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT destroy the preserved named PostgreSQL volume; `docker compose down -v` is forbidden for the entire run."
    - requirement_id: CONF-01
      category: transparency
      status: unverified
      flagged: true
      verification: judgment
      statement: "MUST NOT present the scoped run as a full re-verification of Phase 1, and MUST NOT flip `01-14-SUMMARY.md` out of `halted` or declare the phase complete from inside this plan."
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Run a deliberately bounded live check of the two fixed time-zone prompts in a real Telegram group, and reconcile only the artifacts that check actually touches.

Purpose: F-12 was a live finding, so only a live observation can retire it — but the user scoped this to the fixed issues alone, so re-walking the whole Run 3 matrix would spend an operator's time proving what Run 3 already proved.
Output: A labelled scoped Run 4 record, an explicit human verdict, a rescored UAT test 1, and an unchanged phase-completion decision that remains verify-phase's to make.
</objective>

<user_story>
**As a** Telegram chat administrator, **I want to** be told the exact gesture that hands the bot my location, **so that** setting the chat's time zone does not look like the bot has frozen.
</user_story>

<execution_context>
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/workflows/execute-plan.md
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-UAT.md
@.planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md
@.planning/phases/01-chat-readiness/01-27-PLAN.md
@.planning/phases/01-chat-readiness/01-27-SUMMARY.md
@.planning/phases/01-chat-readiness/01-28-SUMMARY.md
@.planning/phases/01-chat-readiness/COVERAGE.md
@compose.yaml
</context>

<interfaces>
- `compose.yaml` defines `postgres` (healthchecked), a one-shot `migrate`, and a single `bot` service bound to the named volume `gsmbot-postgres-data`; `compose.yaml` declares no healthcheck for `bot`, so readiness is proven by running state plus the structured startup record.
- `src/app/main.ts` emits the exact structured record `Telegram long-poll runner started` when the runner reaches long polling; that string is the authoritative readiness signal and exposes no token.
- `src/telegram/renderers.ts` exports `TIMEZONE_LOCATION_HINT`, whose value is the sentence both live surfaces must display verbatim.
- The runbook's `## ⚠️ Two traps to know before starting` already documents both invariants this run depends on: the location must be sent as a reply, and only `docker compose restart bot` may be used.
- Run 3's row set (`3-01` … `3-25`), high-attention rows H-1/H-2/H-3, and its acceptance roll-up are complete historical evidence and are read-only for this plan.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Pin and launch the F-12-fixed candidate against the preserved volume</name>
  <files>.planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/01-chat-readiness/01-28-SUMMARY.md — take the commit SHA that landed the F-12 fix and its recorded gate results; halt if plan 01-28 was not fully successful
    - .planning/phases/01-chat-readiness/01-29-SUMMARY.md — confirm window 16 is disposed and the ledger reports no open window before a live run is worth an operator's time
    - .planning/phases/01-chat-readiness/01-27-PLAN.md — read Task 1 for the established launch, boundary-capture, and restart-identity pattern this repo uses for a live candidate
    - compose.yaml — use the existing `postgres`, `migrate`, and `bot` services and the named volume; never recreate the volume
    - src/app/main.ts — recognize the authoritative long-poll startup record without exposing the bot token
    - .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md — read `## Run 3 — reproducibility metadata` at line 498 for the metadata table shape this run's own section must mirror
  </read_first>
  <precondition>`BOT_TOKEN` and `POSTGRES_PASSWORD` are available in the local environment, the bot is still an administrator in the same disposable private group with privacy mode enabled, the named volume `gsmbot-postgres-data` from Run 3 still exists, and the current checkout is the exact commit that landed plans 01-28 and 01-29.</precondition>
  <action>Append a new top-level section titled `## Run 4 — scoped re-check of the F-12 fix (NOT a full re-run)` to `01-LIVE-VERIFICATION-RUNBOOK.md`, placed after the Run 3 verdict and leaving every Run 1, Run 2, and Run 3 line untouched. Open it with an explicit scope paragraph naming the only three things this run adjudicates — the setup Step 1 prompt copy, a replied location resolving candidates, and the `/settings` time-zone edit prompt copy — and stating that every other Run 3 row keeps its Run 3 verdict and is not re-claimed here. Fill a reproducibility metadata table mirroring the Run 3 one: candidate commit SHA, working-tree cleanliness, image build time, named volume and its preserved identity, migrations applied, `LOG_LEVEL`, and sanitized role availability. Record the automated preflight results for `npm run format:check`, `npm run build`, `npm run test:unit`, `npx vitest run --project integration`, and `gsd-tools windows status`. Confirm `.env` is git-ignored before using local secrets. Launch `postgres`, the one-shot `migrate`, and the single `bot` service from `compose.yaml` without deleting `gsmbot-postgres-data`; prove the migration exited 0, PostgreSQL is healthy, the bot is running, and a structured `Telegram long-poll runner started` record appeared after a captured UTC boundary. Record sanitized timestamps and environment descriptors only; never record the token, database password, chat identifier, user identifier, username, or private group name. Per D-01 and D-09 this pins the durable candidate carrying the corrected time-zone prompt before any human interaction.</action>
  <verify>
    <automated>git check-ignore -q .env &amp;&amp; docker compose config -q &amp;&amp; npm run build &amp;&amp; npm run test:unit &amp;&amp; node /home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/bin/gsd-tools.cjs windows status &amp;&amp; launch_boundary="$(date -u +%Y-%m-%dT%H:%M:%S.%NZ)" &amp;&amp; docker compose up --build --force-recreate -d bot &amp;&amp; docker inspect "$(docker compose ps -q migrate)" --format '{{.State.ExitCode}}' | grep -qx '0' &amp;&amp; docker inspect "$(docker compose ps -q postgres)" --format '{{.State.Health.Status}}' | grep -qx 'healthy' &amp;&amp; docker compose ps --status running --services bot | grep -qx 'bot' &amp;&amp; docker volume inspect gsmbot-postgres-data --format '{{.Name}}' | grep -qx 'gsmbot-postgres-data' &amp;&amp; docker compose logs --no-color --since "$launch_boundary" bot | grep -q 'Telegram long-poll runner started' &amp;&amp; grep -q '## Run 4 — scoped re-check of the F-12 fix (NOT a full re-run)' .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - The live candidate SHA exactly matches the commit that landed plans 01-28 and 01-29, and the working tree is clean at that SHA.
    - Compose migration exits 0, PostgreSQL reports `healthy`, and the single `bot` service is running.
    - A structured `Telegram long-poll runner started` record appears only after the captured launch boundary.
    - The named volume `gsmbot-postgres-data` still exists and was neither removed nor recreated.
    - The runbook contains a section headed exactly `## Run 4 — scoped re-check of the F-12 fix (NOT a full re-run)` whose first paragraph names the three adjudicated items and disclaims the rest.
    - Run 1, Run 2, and Run 3 content is byte-identical in `git diff`.
    - No credential, chat identifier, user identifier, username, or private group name appears in the added section.
  </acceptance_criteria>
  <done>The fixed candidate runs reproducibly against Run 3's preserved durable state, and the runbook carries a correctly-scoped Run 4 shell ready for human observation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Observe the two fixed prompts and the replied-location path, then adjudicate</name>
  <files>.planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md — the Run 4 section created in Task 1, plus `## Step 1 — Entering setup` and `### 2a. Location → candidates` for the aligned operator instructions
    - src/telegram/renderers.ts — take the exact sentence from `TIMEZONE_LOCATION_HINT` as the string to compare against the screen
    - .planning/phases/01-chat-readiness/01-UI-SPEC.md — read the `Time-zone location prompt` Copywriting Contract row, the `Timezone candidate selection` surface row at line 91, and the platform rule at line 27 that forbids a reply keyboard, a WebView, and a private-chat location-request button
    - .planning/phases/01-chat-readiness/01-27-PLAN.md — read Task 2 for the established checkpoint shape, sanitization rule, and literal-verdict requirement
  </read_first>
  <what-built>The exact commit that landed the F-12 fix is running against the preserved PostgreSQL volume, and both time-zone prompts now quote one Copywriting Contract sentence that names the reply gesture Telegram privacy mode requires.</what-built>
  <action>Pause automation and have the user act as the real Telegram administrator in the same disposable private group, with privacy mode left enabled. Exercise exactly four bounded observations and nothing else. R4-01: reach setup Step 1 — on an unconfigured or draft-free chat send `/setup` and press `Start setup`, or on the configured chat send `/setup` — and compare the third block of the card against the shipped sentence `Reply to this message with a location to choose this chat's time zone.` character for character, confirming the card still begins `Setup in progress` and shows `Step 1 of 8` and carries no inline buttons. R4-02: from that same prompt, send a Telegram location as a reply to the bot's Step 1 message and confirm the bot answers with the `Time zone found` candidate card carrying one confirmation action per candidate and a `Send another location` action — this is the observation that makes the new instruction truthful rather than merely better worded, and a silent bot here is a FAIL. R4-03: send `/settings`, tap `Edit time zone`, and compare the resulting card against `<b>Time zone</b>` followed by the identical sentence. R4-04: confirm across all three surfaces that no reply keyboard, no WebView, and no private-chat location request appeared. Record each observation in the Run 4 row set with sanitized evidence; screenshots may be inspected but must not be copied into the repository, and no map, coordinate, resolved zone, identity, username, group name, or unrelated chat content may be recorded. Do not fix anything during the run: if a surface fails, record it as observed, open a broken window, and leave the verdict negative — the candidate stays immutable. Ask the administrator for a literal verdict; do not infer approval from silence, from a passing sub-row, or from the green automated preflight.</action>
  <how-to-verify>In the live group, walk R4-01 through R4-04 in order, comparing each rendered card against the quoted sentence in the runbook before moving on, then state the verdict for the scoped run only.</how-to-verify>
  <reversibility rating="reversible">The run adjudicates already-landed copy. A negative verdict opens a window and requires a fresh candidate; no live action here mutates the fix or the durable volume beyond ordinary chat configuration state.</reversibility>
  <verify>
    <automated>grep -q 'R4-01' .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md &amp;&amp; grep -q 'R4-02' .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md &amp;&amp; grep -q 'R4-03' .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md &amp;&amp; grep -q 'R4-04' .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md &amp;&amp; awk '/^## Run 4 —/,0' .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md | grep -qE 'APPROVED|NOT APPROVED'</automated>
    <human_check>All four Run 4 rows carry an observed result, the two prompt comparisons were made against the quoted sentence rather than from memory, the replied location actually produced a candidate card, and the administrator stated exactly APPROVED or NOT APPROVED for the scoped run.</human_check>
  </verify>
  <acceptance_criteria>
    - Rows R4-01 through R4-04 each carry an observed pass or fail result with sanitized evidence.
    - R4-01 and R4-03 each record a character-for-character comparison against the shipped sentence, not a paraphrase.
    - R4-02 records that a location sent as a reply produced the `Time zone found` candidate card; bot silence is recorded as FAIL.
    - R4-04 records the absence of a reply keyboard, a WebView, and any private-chat location request.
    - The verdict is a literal human statement of APPROVED or NOT APPROVED scoped to Run 4.
    - No Run 3 row is marked, re-marked, or referenced as re-verified by this run.
    - No screenshot, coordinate, resolved zone, identity, username, group name, or credential is committed.
  </acceptance_criteria>
  <resume-signal>Reply `APPROVED` only if all four Run 4 rows passed, or reply `NOT APPROVED` with the failing row identifiers and what was observed.</resume-signal>
  <done>A human has supplied an explicit, bounded verdict backed by direct Telegram observation of the two fixed prompts and the replied-location path.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the runbook, UAT test 1, and the ledger to the scoped verdict</name>
  <files>.planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md, .planning/phases/01-chat-readiness/01-UAT.md, .planning/WINDOWS.md</files>
  <read_first>
    - .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md — use the Run 4 rows and verdict from Task 2 as the sole live source of truth
    - .planning/phases/01-chat-readiness/01-UAT.md — read the `## Current Test` block, test 1 at line 34, the `## Summary` at line 195, and the `## Deferred Follow-Ups` list; preserve every resolved, invalid, and deferred disposition
    - .planning/WINDOWS.md — read the current ledger state; register any new live failure as a new open window rather than reopening or erasing a closed one
    - .planning/phases/01-chat-readiness/01-14-SUMMARY.md — read its `status` and D5 fields ONLY to confirm they are left unchanged by this plan
    - .planning/phases/01-chat-readiness/COVERAGE.md — retain the existing Phase 1 API coverage matrix unchanged; it is complete and valid
  </read_first>
  <action>Branch solely on the Task 2 verdict and change nothing the bounded run did not observe. If APPROVED: finalize the Run 4 section as approved for its stated scope only; rescore `01-UAT.md` test 1 from `fail` to `pass` with `source: live-run 4 (runbook R4-01/R4-02)`, and rewrite its note so it records that the reply-gesture copy was observed live at both time-zone prompts and that a replied location resolved candidates, superseding the Run 3 rejection; clear the `## Current Test` block's `awaiting` line; and update `## Summary` so `issues` drops to 0 while the summary paragraph states plainly that Run 4 was scoped to the F-12 fix and that every other row still stands on its Run 3 evidence. If NOT APPROVED: leave test 1 at `fail`, record the new observation in its note, and run `node /home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/bin/gsd-tools.cjs windows append --kind deviation --phase 01 --file {affected-path} --description {sanitized-observation}` once per new actionable issue. In both branches: retain UAT test 16 as owner-deferred, retain test 8's unverified `only the offending field is re-asked` clause exactly as recorded, retain the test 2 multi-candidate deferral, retain every resolved and invalid gap disposition, retain Runs 1 through 3 verbatim, retain every fixed and waived window, and leave `COVERAGE.md` untouched. Do not modify `01-14-SUMMARY.md`, do not change its `status: halted`, do not mark D5 satisfied, and do not write any sentence declaring Phase 1 complete or approved — the phase-completion decision belongs to verify-phase and depends on this verdict; state that boundary explicitly in the Run 4 section so a later reader cannot mistake a scoped approval for a phase approval. Run the summary coverage classifier and correct only structural evidence-link errors.</action>
  <verify>
    <automated>node /home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/bin/gsd-tools.cjs uat classify-coverage --summary .planning/phases/01-chat-readiness/01-14-SUMMARY.md &amp;&amp; node /home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/bin/gsd-tools.cjs windows status &amp;&amp; grep -q 'status: halted' .planning/phases/01-chat-readiness/01-14-SUMMARY.md &amp;&amp; grep -q 'Run 4' .planning/phases/01-chat-readiness/01-UAT.md &amp;&amp; grep -q 'result: skipped' .planning/phases/01-chat-readiness/01-UAT.md &amp;&amp; awk '/^## Run 3 — verdict/,/^## Run 4 —/' .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md | grep -q 'NOT APPROVED' &amp;&amp; git diff --quiet -- .planning/phases/01-chat-readiness/COVERAGE.md .planning/phases/01-chat-readiness/01-14-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `01-UAT.md` test 1 status and note match the Task 2 verdict and cite a Run 4 row identifier.
    - The UAT summary paragraph states that Run 4 was scoped to the F-12 fix and does not claim a full re-verification.
    - UAT test 16 still reads `result: skipped` and test 8's residual clause text is unchanged.
    - `01-14-SUMMARY.md` still carries `status: halted` with D5 unsatisfied, and `git diff` shows it and `COVERAGE.md` unmodified.
    - The Run 3 verdict section still reads NOT APPROVED and Runs 1 through 3 are byte-identical in `git diff`.
    - On a NOT APPROVED verdict, one new open window exists per new actionable issue and no previously closed window changed status.
    - `uat classify-coverage` reports no structural errors and `windows status` runs clean.
  </acceptance_criteria>
  <done>Every artifact the bounded run touches agrees with the human verdict, claims exactly what was exercised, and leaves the phase-completion decision open.</done>
</task>

</tasks>

## Artifacts this phase produces

**New document elements**
- `01-LIVE-VERIFICATION-RUNBOOK.md` section `## Run 4 — scoped re-check of the F-12 fix (NOT a full re-run)`, containing a scope disclaimer paragraph, a reproducibility metadata table, an automated preflight table, the bounded row set `R4-01`, `R4-02`, `R4-03`, `R4-04`, and a Run 4 verdict table
- `01-UAT.md` test 1 rescored entry with a `Run 4` source citation and a rewritten note

**Modified in place, no new symbol**
- `.planning/phases/01-chat-readiness/01-UAT.md` — `## Current Test` block and `## Summary`
- `.planning/WINDOWS.md` — new open window entries only on a negative verdict

**No new source symbol, file, function, constant, or test is produced by this plan.** No file under `src/` or `tests/` is modified: the candidate under live test is immutable for the duration of the run.

<assumption_delta_decision>
**Signal:** `"3. A chat administrator can choose whether administrators, previous-poll participants, or anyone may start planning."` — a derived value became chosen / a constant became a parameter.

**Noun now primary:** the per-chat planning-access policy (`ChatConfiguration.planningAccessPolicy`), rather than a fixed administrators-only constant.

**Decision:** `no-change`.

**Rationale:** the promotion already shipped in Phase 1 under AUTH-01 and was observed live in Run 3. This plan is a bounded observation of time-zone prompt copy plus artifact reconciliation; it exercises no planning-access surface and changes no default. Recorded explicitly rather than omitted, as the signal fired on already-shipped behaviour.
</assumption_delta_decision>

<schema_relevance>
**Conclusion: no Prisma schema file is touched.** This plan's `files_modified` contains only planning documents. No `prisma/schema.prisma` or `prisma/schema/*.prisma` entry exists, so no `npx prisma db push` task is injected. The run does apply existing migrations at container start against the preserved volume, which is a deploy of already-committed migrations rather than a schema edit. Recorded as a decision.
</schema_relevance>

<flagged_assumptions>
The complete 12-item spec-less edge-probe register for this phase is authored in `01-28-PLAN.md` under `<flagged_assumptions>`; it is not restated here to avoid two drifting copies. The row relevant to this plan:

| # | Requirement | Category | Probe | Disposition |
|---|---|---|---|---|
| 1 | CONF-01 | unclassified | review manually | **Unresolved, and this run closes its one open part.** Time-zone initialization is otherwise shipped and green (`tests/unit/setup.test.ts`, `tests/integration/chat-configuration.test.ts`, `tests/integration/walking-skeleton.test.ts`, live Run 3 rows 3-02, 3-05, 3-06). The open part was the Step 1 instruction copy — F-12 — which plan 01-28 fixed and this plan observes live. An `unclassified` row is never auto-resolved, so it stays `unresolved` regardless of the verdict, and is lifted into this plan's `must_haves`. |

Also carried forward unresolved and out of this run's scope, restated so it is not lost at the point a phase verdict is discussed: **item 7, ROST-02 concurrency** — no test drives two simultaneous roster mutations for one chat. The other ten items are dispositioned in `01-28-PLAN.md`.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local secrets to Compose | Bot and database credentials enter the process environment and must never enter repository evidence. |
| Telegram user to bot | A real location message, a real chat, and real identities cross into the system; none of them may be retained. |
| Bot process to PostgreSQL | The run executes against a preserved named volume carrying inherited Run 3 state that must survive intact. |
| Human observation to planning status | Only explicit, bounded evidence may move an artifact, and only within the run's stated scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-30-01 | Information Disclosure | Live evidence capture | high | mitigate | Exclude the token, database password, chat and user identifiers, usernames, display names, group name, raw coordinates, and the resolved candidate zone; inspect screenshots without copying them into the repository, and record field names and row identifiers instead of values. |
| T-01-30-02 | Tampering | Preserved named PostgreSQL volume | high | mitigate | Use only `docker compose up`/`restart bot`; `docker compose down -v` is forbidden for the whole run, and the named volume identity is asserted before and after launch. |
| T-01-30-03 | Repudiation | Human approval | high | mitigate | Require the literal APPROVED or NOT APPROVED resume signal with per-row evidence; never infer consent from silence, a passing sub-row, or the green automated preflight. |
| T-01-30-04 | Spoofing | Telegram privacy-mode posture during the run | high | mitigate | Privacy mode stays enabled for the entire run; the replied-location observation R4-02 is only meaningful under it, and no group-privacy setting may be changed to make a surface pass. |
| T-01-30-05 | Elevation of Privilege | Scope creep of the verdict | medium | mitigate | The Run 4 section names its three adjudicated items and disclaims the rest; no Run 3 row may be re-marked, and `01-14-SUMMARY.md` and `COVERAGE.md` are asserted unmodified by `git diff`. |
| T-01-30-06 | Denial of Service | An in-run fix to the candidate | medium | mitigate | The candidate is immutable for the run's duration; a failure opens a window and requires a fresh candidate, asserted by no `src/` or `tests/` file appearing in this plan's diff. |
| T-01-30-SC | Tampering | npm/pip/cargo installs | low | accept | No package installation or dependency change occurs in this plan; the image is built from the already-audited lockfile. |
</threat_model>

<verification>
The F-12-fixed commit runs in Compose against the preserved named volume; a human observes exactly four bounded rows in a real privacy-mode Telegram group, including a replied location that actually resolves candidates; the runbook carries a labelled scoped Run 4 with an explicit verdict that claims nothing it did not exercise; UAT test 1 and the ledger match that verdict; and the phase-completion decision is left untouched for verify-phase.
</verification>

<success_criteria>
- The launched candidate is the commit that landed plans 01-28 and 01-29, running against `gsmbot-postgres-data` with migrations applied and the bot long-polling.
- Rows R4-01 through R4-04 all carry direct human evidence, and R4-02 proves a replied location resolves candidates.
- The verdict is recorded literally and is scoped to Run 4 alone.
- UAT test 1 matches the verdict and cites a Run 4 row; tests 2, 8, and 16 keep their existing dispositions.
- `01-14-SUMMARY.md` remains `halted` with D5 unsatisfied, and `COVERAGE.md` is unmodified.
- Runs 1 through 3 are byte-identical, and no secret or private Telegram identifier is committed.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-30-SUMMARY.md` when done.
</output>
