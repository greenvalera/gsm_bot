---
phase: 01-chat-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md
autonomous: false
requirements:
  - CONF-01
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "No npm package is installed until its registry identity, publisher/maintainer provenance, source repository, release cadence, license, install scripts, and direct dependency tree have been reviewed."
    - "The coordinate-to-IANA resolver receives an explicit human legitimacy decision before it can enter the lockfile."
  artifacts:
    - path: ".planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md"
      provides: "Human-reviewable evidence for every proposed Phase 1 package"
      contains: "tz-lookup"
  key_links:
    - from: ".planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md"
      to: "package.json"
      via: "approved package names and exact versions become the only permitted install set in Plan 01-02"
      pattern: "APPROVED|REJECTED"
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Create the mandatory dependency-legitimacy evidence and stop for a human decision before the greenfield package installation.

Purpose: Close the npm supply-chain boundary identified by research, with extra scrutiny for the unverified coordinate-to-IANA resolver.
Output: `.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md` containing one evidence row and disposition for every proposed direct dependency.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/research/STACK.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-VALIDATION.md
</context>

## Artifacts this phase produces

- Path: `.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md`
- Audit fields: `Package`, `Exact version`, `Registry`, `Publisher/maintainers`, `Source repository`, `Release cadence`, `License`, `Install scripts`, `Direct dependencies`, `Decision`, `Reviewer notes`
- Approved package set: `grammy@1.45.1`, `@grammyjs/runner@2.0.3`, `zod@4.4.3`, `pino@10.3.1`, `prisma@7.9.1`, `@prisma/client@7.9.1`, `@prisma/adapter-pg@7.9.1`, `pg@8.23.0`, `tz-lookup@6.1.25`, `typescript@7.0.2`, `vitest@4.1.11`, `testcontainers@12.1.0`, and the matching published `@types/node`/`@types/pg` versions selected by the executor only after their rows pass the same review

<tasks>

<task type="auto">
  <name>Task 1: Build the direct-dependency provenance dossier without installing packages</name>
  <read_first>
    <file>.planning/phases/01-chat-readiness/01-RESEARCH.md</file>
    <file>.planning/research/STACK.md</file>
    <file>.planning/phases/01-chat-readiness/01-VALIDATION.md</file>
  </read_first>
  <files>.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md</files>
  <action>Query the official npm registry metadata and the repository URLs declared by each proposed direct dependency. Record exact version, package publisher/maintainers, canonical repository, latest release date plus recent cadence, SPDX license, lifecycle/install scripts, and the direct dependency list. Cover every package named in the artifact list, not only `tz-lookup`. Give `tz-lookup` a dedicated section comparing its npm publisher to the GitHub repository owner, the freshness of timezone polygon data/releases, license compatibility, dependency depth, and unresolved ownership signals. Do not run `npm install`, `npm add`, `npm ci`, or otherwise create a lockfile in this task.</action>
  <acceptance_criteria>
    <criterion>The audit contains exactly one fully populated evidence row per proposed direct package and no row has an empty publisher, repository, release-cadence, license, install-script, dependency-tree, or decision field.</criterion>
    <criterion>The resolver section explicitly records `tz-lookup@6.1.25`, publisher, repository, release cadence, license, dependency tree, and boundary-data freshness evidence.</criterion>
    <criterion>No `package.json`, `package-lock.json`, or `node_modules` path is created by this task.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md &amp;&amp; for package in grammy runner zod pino prisma adapter-pg pg tz-lookup typescript vitest testcontainers; do rg -q "$package" .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md || exit 1; done &amp;&amp; test ! -e package-lock.json &amp;&amp; test ! -d node_modules</automated>
  </verify>
  <done>A complete evidence dossier exists and the repository still contains no installed package tree or generated lockfile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve or reject the Phase 1 package set, with a separate resolver decision</name>
  <read_first>
    <file>.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md</file>
    <file>.planning/phases/01-chat-readiness/01-RESEARCH.md</file>
  </read_first>
  <files>.planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md</files>
  <action>Review each package on its official npm package page and canonical source repository. Record `APPROVED` or `REJECTED` plus reviewer notes in the dossier. Treat the coordinate resolver as an independent decision: approval of the established framework/tooling packages does not implicitly approve `tz-lookup`. If any direct package is rejected, stop and return the rejected name and evidence to planning; do not substitute or install another package during execution.</action>
  <what-built>A direct-dependency audit assembled from npm and source-repository evidence without installing code.</what-built>
  <how-to-verify>
    1. Open every official `npmjs.com/package/...` page named in `DEPENDENCY-AUDIT.md` and compare the publisher/maintainer identity and repository link to the recorded evidence.
    2. Review the exact release cadence, license, lifecycle scripts, and direct dependency tree.
    3. For `tz-lookup`, inspect the publisher, `github.com/darkskyapp/tz-lookup`, release freshness, license, dependency tree, and timezone-boundary data provenance; decide whether that maintenance posture is acceptable for confirmed IANA-zone inference.
    4. Mark every row `APPROVED` or `REJECTED` with a short evidence-based note. Approval is valid only when every package needed by Plan 01-02 is approved.
  </how-to-verify>
  <acceptance_criteria>
    <criterion>Every package row has an explicit reviewer decision and notes tied to publisher, repository, cadence, license, scripts, and dependency evidence.</criterion>
    <criterion>`tz-lookup` has its own explicit approval or rejection and is never inferred from the disposition of other packages.</criterion>
    <criterion>A rejected package blocks Plan 01-02 and returns control to replanning.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md &amp;&amp; ! rg -n '\|[[:space:]]*(PENDING|TBD|UNKNOWN)[[:space:]]*\|' .planning/phases/01-chat-readiness/DEPENDENCY-AUDIT.md</automated>
    <human-check>Confirm each row against its official registry and source repository, especially the `tz-lookup` publisher, repository, cadence, license, dependency tree, and boundary-data maintenance.</human-check>
  </verify>
  <resume-signal>Type "approved" only if every required package is marked APPROVED, or report the rejected package and evidence.</resume-signal>
  <done>The approved package list is explicit and human-attested; any rejection has halted installation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry/repository metadata → local dependency decision | Untrusted package identity and maintainer claims are evaluated before code can execute locally. |
| human review → executable install set | Only explicitly approved exact packages may cross into the lockfile in Plan 01-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-SC | Tampering | npm package installation | high | mitigate | Task 1 records provenance and executable-script evidence; blocking Task 2 requires human approval before any install. |
| T-01-01 | Spoofing | package/repository identity | high | mitigate | Compare npm publisher/maintainers and repository ownership against canonical project pages for every direct dependency. |
| T-01-02 | Information disclosure | audit evidence | low | accept | The dossier contains public package metadata only and no registry tokens or private credentials. |
</threat_model>

<verification>
The dependency dossier is complete, contains no pending decisions after the checkpoint, and no install artifact exists before approval.
</verification>

<success_criteria>
- Every direct dependency has an evidence-backed human disposition.
- `tz-lookup` is approved independently with publisher, repository, release cadence, license, dependency-tree, and boundary-data review recorded.
- Plan 01-02 remains blocked on any rejection.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-01-SUMMARY.md` when done.
</output>
