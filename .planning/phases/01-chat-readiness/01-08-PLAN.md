---
phase: 01-chat-readiness
plan: 08
type: execute
wave: 9
depends_on:
  - 01-07
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260819010000_settings_edits/migration.sql
  - src/app/create-bot.ts
  - src/domain/auth/planning-access-service.ts
  - src/domain/chat/settings-service.ts
  - src/telegram/settings-handlers.ts
  - tests/integration/chat-configuration.test.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-05
  - AUTH-01
  - AUTH-02
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-07, `/settings` reads one complete committed configuration and exposes `Schedule`, `Availability reminders`, and `Planning access` in fixed order."
    - "Per D-10, the planning-access edit shows Current/New and changes active configuration only after `Save change`; keep-current and failures preserve the exact committed revision."
    - "Per D-12, current administrators always pass `canStartPlanning`; previous-participant and anyone policies only broaden non-administrator access."
    - "[Edge AUTH-01/unclassified] Exactly the three supported planning-access policies persist, with administrators always allowed and unsupported/null values rejected."
    - "The committed settings migration applies cleanly before dashboard/edit behavior runs."
  artifacts:
    - path: "prisma/migrations/20260819010000_settings_edits/migration.sql"
      provides: "Durable actor-bound SettingsEditDraft schema"
      contains: "SettingsEditDraft"
    - path: "src/domain/chat/settings-service.ts"
      provides: "Committed settings projection and revision-safe single-field edits"
      exports:
        - SettingsService
    - path: "src/domain/auth/planning-access-service.ts"
      provides: "Future-facing planning-start policy evaluator"
      exports:
        - PlanningAccessService
        - canStartPlanning
    - path: "src/telegram/settings-handlers.ts"
      provides: "Authorized `/settings` dashboard and planning-access edit slice"
      exports:
        - registerSettingsHandlers
  key_links:
    - from: "src/telegram/settings-handlers.ts"
      to: "src/domain/auth/authorization-service.ts"
      via: "fresh current-administrator check precedes dashboard read, edit draft mutation, and save"
      pattern: "requireCurrentAdministrator"
    - from: "src/domain/chat/settings-service.ts"
      to: "prisma/schema.prisma"
      via: "SettingsEditDraft expectedRevision and transactional ChatConfiguration update"
      pattern: "expectedRevision"
    - from: "src/domain/auth/planning-access-service.ts"
      to: "ChatConfiguration.planningAccessPolicy"
      via: "current administrators short-circuit allowed; configured policy evaluates non-admin actors"
      pattern: "planningAccessPolicy"
  prohibitions:
    - requirement_id: AUTH-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT imply or implement a broadened planning-access policy as excluding current Telegram administrators."
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Introduce the committed settings dashboard and one complete planning-access edit through the settings migration.

Purpose: Prove the dashboard, edit-draft, revision, and future planning-policy contracts before expanding edits and failure projections.
Output: Settings migration, service/handler slice, planning-access evaluator, and migrated integration coverage.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-PATTERNS.md
@.planning/phases/01-chat-readiness/01-VALIDATION.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-07-SUMMARY.md
@prisma/schema.prisma
@src/domain/auth/authorization-service.ts
@src/domain/chat/schedule-validator.ts
</context>

<interfaces>
- `SettingsService.getCommitted(chatId)` returns a complete `ChatConfiguration` projection or a typed not-configured/read-failure result; it never returns a partial draft.
- `SettingsEditDraft` is unique per `(chatId, actorUserId)`, names exactly one editable field/value-pair, stores validated replacement payload, expected configuration revision, and 30-minute expiry.
- `SettingsService.saveChange` consumes an actor-bound opaque action, validates the whole resulting configuration, compares revision, and commits exactly one new revision in a transaction after the handler reauthorizes.
- `canStartPlanning({ currentRole, policy, wasPreviousParticipant })` always allows current administrators and otherwise evaluates the selected broadening policy.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: View settings and change planning access through a committed edit migration</name>
  <reversibility rating="costly">Settings draft and planning-policy contracts are consumed by future planning flows; changing their persistence shape later requires a coordinated migration.</reversibility>
  <read_first>
    <file>prisma/schema.prisma</file>
    <file>src/domain/auth/authorization-service.ts</file>
    <file>src/domain/chat/setup-service.ts</file>
    <file>src/telegram/renderers.ts</file>
    <file>.planning/phases/01-chat-readiness/01-CONTEXT.md</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
  </read_first>
  <files>prisma/schema.prisma, prisma/migrations/20260819010000_settings_edits/migration.sql, src/app/create-bot.ts, src/domain/auth/planning-access-service.ts, src/domain/chat/settings-service.ts, src/telegram/settings-handlers.ts, tests/integration/chat-configuration.test.ts</files>
  <behavior>
    - Test 1: `/settings` by a current administrator reads one complete committed configuration and renders the three sections in fixed order.
    - Test 2: selecting planning access creates an actor-bound edit, shows Current/New, and saves only after confirmation with a fresh revision.
    - Test 3: `canStartPlanning` always allows current admins; previous-participant and anyone policies broaden access exactly as named.
    - Test 4: stale revision, expired action, duplicate confirmation, invalid enum, or demotion leaves the prior policy unchanged.
  </behavior>
  <action>Add the `SettingsEditDraft` model/enum and committed migration, then apply it with `prisma migrate deploy` to a fresh PostgreSQL 18 integration database and assert migration status is clean before behavior verification. Per D-07, implement the `/settings` committed projection and the first complete edit slice for planning access. Per D-10, each callback is acknowledged, current administrator status is fetched, the opaque server-side action is actor/chat/expiry-bound, Current/New is rendered, and save performs full validation plus an expected-revision transaction. Per D-12, implement `canStartPlanning` so Telegram creator/administrator always succeeds, `PREVIOUS_PARTICIPANTS` additionally accepts the future participant-history input, and `ANYONE` accepts any current chat member; null/unsupported values fail closed. Halt before behavior expansion if the live integration schema differs from the committed migration.</action>
  <acceptance_criteria>
    <criterion>The fresh integration database has no pending migration and exposes the exact `SettingsEditDraft` constraints expected by Prisma Client.</criterion>
    <criterion>The dashboard shows committed values only, in fixed order, with one edit control per approved value/value-pair.</criterion>
    <criterion>Planning access changes only after Current/New review and fresh-role/revision checks; failed or duplicate saves do not increment revision.</criterion>
    <criterion>The policy evaluator covers current admin, prior participant, ordinary member, missing membership, and every supported/invalid policy combination.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; npm run test:unit -- planning-access &amp;&amp; npm run test:integration -- chat-configuration</automated>
  </verify>
  <done>A current administrator can view committed settings and safely change planning access; future phases receive a policy evaluator that never excludes admins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| settings command/callback → authorization | Each continuation may be sent by a stale/different actor and requires a fresh role result. |
| SettingsEditDraft → ChatConfiguration | Draft value, revision, actor, and target field cross into active configuration only in one transaction. |
| policy value → Phase 2 authorization | Invalid or misinterpreted values could broaden or narrow planning-start access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-13 | Elevation of privilege | protected settings continuation | high | mitigate | Fresh `AuthorizationService` check precedes dashboard read, edit, and save; denial deletes actor draft. |
| T-01-14 | Tampering | stale settings write | high | mitigate | Actor-bound action, expected revision, full validation, and one transaction prevent lost updates. |
| T-01-15 | Spoofing | planning-access policy | medium | mitigate | Persist a closed enum and evaluate current Telegram role independently; administrators always pass per D-12. |
| T-01-SC | Tampering | installed packages | high | mitigate | Continue using only the Plan 01-15-approved lockfile created by Plan 01-02; this plan adds no dependency. |
</threat_model>

<verification>
Apply and inspect the committed settings migration, then run formatting, planning-access tests, and the chat-configuration integration slice.
</verification>

<success_criteria>
- D-07, D-10, D-12, D-13, and D-14 govern the dashboard and planning-policy edit.
- `/settings` renders complete committed state only.
- AUTH-01 persists through a deterministic future-facing `canStartPlanning` seam.
- The AUTH-01 edge/prohibition and revision-safe failure paths are automated.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-08-SUMMARY.md` when done.
</output>
