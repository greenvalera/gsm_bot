---
phase: 08-localized-reminders-and-bilingual-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/i18n/index.ts
  - src/shared/i18n/en.ts
  - src/shared/i18n/uk.ts
  - src/telegram/reminder-renderers.ts
  - src/app/main.ts
  - tests/integration/localized-reminders.test.ts
  - src/shared/i18n/planning-format.ts
  - tests/unit/planning-format.test.ts
  - tests/unit/reminder-renderers.test.ts
autonomous: true
requirements: ["LREM-01","LREM-02"]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01, D-02, D-04: Ukrainian planning reminders are one sentence with the unchanged start capability below and no mentions."
    - "D-03: civil week ranges use same-month/cross-month Ukrainian forms without years; English remains supported."
    - "EDGE LREM-01/adjacency: work scheduled before en→uk or uk→en uses the locale committed before its render boundary."
    - "EDGE LREM-01/empty: absent language preference renders English without making absent Ukrainian keys acceptable."
    - "EDGE LREM-01/ordering: repeated or competing dispatches of the same occurrence produce at most one attempted send, and text/control locale agree."
  artifacts:
    - path: src/app/main.ts
      provides: "New createPlanningReminderTransport; production and integration tests share its real render/send seam"
    - path: src/shared/i18n/planning-format.ts
      provides: "New formatReminderWeekRange(locale, targetWeek)"
    - path: src/shared/i18n/index.ts
      provides: "New reminder.planning.* and reminder.followup.* MessageParameters keys, implemented in en.ts and uk.ts"
    - path: tests/integration/localized-reminders.test.ts
      provides: "New persisted reminder delivery integration suite"
  key_links:
    - from: src/app/main.ts
      to: src/telegram/presentation-locale.ts
      via: "resolvePresentationLocale at transport rendering"
    - from: src/telegram/reminder-renderers.ts
      to: src/shared/i18n/index.ts
      via: "renderMessage with one explicit locale"
---

<objective>
Deliver a production planning reminder in the current group language.
Purpose: Complete Phase 8 current-language delivery and evidence without changing scheduling or domain authority.
Output: Scoped production/test artifacts and revision-specific evidence.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-CONTEXT.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-PATTERNS.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-SOURCE-AUDIT.md
</context>

<assumption_delta_decision>
Primary noun: existing Telegram group identity and its persisted selected locale. Decision: no-change. The detector's fallback signal concerns strict translation verification, not a new tenant, platform or identity model.
</assumption_delta_decision>

Sizing exception: the tracer's six files are the smallest complete existing typed-catalog → renderer → production transport → persisted test path. Splitting the three-file catalog contract out would create a horizontal prerequisite. Task 2 likewise keeps the three-file typed contract atomic. Neither changes another subsystem or introduces a schema.

<tasks>
<!-- Execution clarifications: main.ts already guards main() with import.meta.url === pathToFileURL(process.argv[1]).href. Preserve this guard; importing createPlanningReminderTransport must not load environment configuration, connect services or start polling. Assert the injected send method is called only when the returned transport is invoked. The follow-up heading parameter contract must additionally carry startTime:string and durationMinutes:number so the English catalog can preserve its current date/start/duration sentence; Ukrainian uses date/range/timezone. Task 2 modifies six files listed in its files field; the planning-format tests cover reminder week ranges, while reminder-renderers tests remain owned by plan 02. -->
<task type="tracer" tdd="true">
<name>Task 1: Current-language planning reminder from durable occurrence to captured Telegram send</name>
<files>src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, src/telegram/reminder-renderers.ts, src/app/main.ts, tests/integration/localized-reminders.test.ts</files>
<read_first>src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, src/telegram/reminder-renderers.ts, src/app/main.ts, tests/integration/localized-reminders.test.ts, src/telegram/presentation-locale.ts, src/domain/reminders/reminder-service.ts, tests/helpers/reminders.ts, tests/integration/localization-tracer.test.ts</read_first>
<behavior>D-01/D-02/D-04: a due saved occurrence sends Час запланувати репетицію на 21–27 вересня. and Почати планування using unchanged callback data after en→uk; absent preference sends original English body/button; one send and one durable occurrence remain.</behavior>
<action>Start with a failing persisted integration case: schedule an English planning occurrence, commit Ukrainian preference, reconcile, capture the actual Telegram send payload. Add typed reminder.planning.body { weekRange: string } and reminder.planning.start undefined entries in MessageParameters and both satisfies MessageCatalog objects. Ukrainian body is Час запланувати репетицію на {weekRange}. and button Почати планування per D-01, D-02, D-04. Add a trailing Locale argument defaulting to en to renderPlanningReminder; preserve English body and Start planning label. For this tracer format the real same-month range from the existing civil-date helpers and formatPlanningDate month representation, retaining full dates internally. In main.ts extract the existing planning transport into exported createPlanningReminderTransport using a narrow sendMessage-capable argument, prisma and logger; resolvePresentationLocale immediately before renderPlanningReminder and send text plus keyboard with that one locale. Main must instantiate this factory; the integration test must invoke the same factory through ReminderService, not duplicate transport assembly. Keep returned messageId, callback_data and claim flow unchanged. Assert zero mentions, one sentence and start button below. Add absent-preference English case. No queue payload or schema changes.</action>
<reversibility rating="reversible">Source/test changes use existing durable identities; revert the scoped commit without data conversion.</reversibility>
<acceptance_criteria>D-01/D-02/D-04: a due saved occurrence sends Час запланувати репетицію на 21–27 вересня. and Почати планування using unchanged callback data after en→uk; absent preference sends original English body/button; one send and one durable occurrence remain.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/localized-reminders.test.ts</automated></verify>
<done>D-01/D-02/D-04: a due saved occurrence sends Час запланувати репетицію на 21–27 вересня. and Почати планування using unchanged callback data after en→uk; absent preference sends original English body/button; one send and one durable occurrence remain.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Full calendar ranges and typed follow-up phrase contracts</name>
<files>src/shared/i18n/planning-format.ts, tests/unit/planning-format.test.ts, src/telegram/reminder-renderers.ts, src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts</files>
<read_first>src/shared/i18n/planning-format.ts, tests/unit/planning-format.test.ts, src/telegram/reminder-renderers.ts, src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, tests/unit/reminder-renderers.test.ts, src/infrastructure/time/civil.ts</read_first>
<behavior>D-03: same-month, cross-month and cross-year Ukrainian ranges omit years and retain correct target dates; en retains existing ISO output; all new keys share typed parameters and pass typecheck.</behavior>
<action>Per D-03 add formatReminderWeekRange(locale, targetWeek) using civil addDays, preserving English ISO start/end and rendering Ukrainian same-month 21–27 вересня, cross-month 28 вересня – 4 жовтня and cross-year ranges without displayed years. Replace the tracer's same-month composition with this complete helper. Preserve authoritative dates and previous planning capitalization. Add typed follow-up keys now needed by the next vertical slice: reminder.followup.heading { date: string; range: string; timezone: string }, reminder.followup.pending { mentions: string }, reminder.followup.link undefined, reminder.followup.basicInstruction undefined, reminder.followup.basicRecovery undefined. D-05..D-12 Ukrainian values are precisely those in 08-CONTEXT; English keys preserve current reminder text/navigation behavior. Key parameters represent already-escaped labels/values, with mentions supplied only by the renderer's trusted link builder; catalogs must not create arbitrary destinations. Unit cases exercise month/year boundary and both locales. This task expands the working planning path to every target week.</action>
<reversibility rating="reversible">Source/test changes use existing durable identities; revert the scoped commit without data conversion.</reversibility>
<acceptance_criteria>D-03: same-month, cross-month and cross-year Ukrainian ranges omit years and retain correct target dates; en retains existing ISO output; all new keys share typed parameters and pass typecheck.</acceptance_criteria>
<verify><automated>npm run test:unit -- tests/unit/planning-format.test.ts; npm run typecheck</automated></verify>
<done>D-03: same-month, cross-month and cross-year Ukrainian ranges omit years and retain correct target dates; en retains existing ISO output; all new keys share typed parameters and pass typecheck.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted chat/round and user labels to Telegram output | Locale controls presentation; names, IDs and claims retain their existing authority. |
| Source/catalog/build to verification evidence | Checks must measure actual outbound paths and the shipped image. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01-01 | Tampering | Reminder rendering | high | mitigate | Retain HTML escaping, validated navigation paths, pending-snapshot filtering and unchanged callback destinations; assert hostile Unicode labels and all mentions. |
| T-08-01-02 | Elevation of privilege | Locale and durable delivery | high | mitigate | Use existing chat identity and current authorization; locale changes never reopen claims or change permissions; preserve duplicate and unknown-outcome tests. |
| T-08-01-03 | Repudiation | Verification evidence | medium | mitigate | Record exact revision, commands and observed outcomes; distinguish native acceptance and scoped waivers from automated evidence. |
| T-08-01-SC | Tampering | Dependencies | low | accept | No dependency additions or version changes; use the existing approved lockfile and existing CI installation. |

ASVS L1; high severity and above blocks completion until mitigated. No new schema or package installation is introduced by this plan.
</threat_model>

## Artifacts this phase produces

- `src/app/main.ts`: New createPlanningReminderTransport; production and integration tests share its real render/send seam.
- `src/shared/i18n/planning-format.ts`: New formatReminderWeekRange(locale, targetWeek).
- `src/shared/i18n/index.ts`: New reminder.planning.* and reminder.followup.* MessageParameters keys, implemented in en.ts and uk.ts.
- `tests/integration/localized-reminders.test.ts`: New persisted reminder delivery integration suite.

<verification>
Run every task's automated command. Preserve unrelated workspace changes. Tests using PostgreSQL require the repository's isolated integration database and existing migration setup; never use the live Telegram fixture database. Record failures accurately and rerun affected checks after corrections. Research was explicitly skipped; research-derived Nyquist VALIDATION.md is not applicable. These concrete tests remain mandatory.
</verification>

<success_criteria>
All listed truths and task criteria have executable evidence. No unresolved assumption is counted as a behavioral pass. Source audit coverage remains complete.
</success_criteria>

<output>
Create .planning/phases/08-localized-reminders-and-bilingual-verification/08-01-SUMMARY.md with changes, commands, tested revision and residual acceptance status.
</output>
