---
phase: 08-localized-reminders-and-bilingual-verification
plan: "04"
task: 1
total_tasks: 3
status: awaiting_tracer_verification
last_updated: 2026-09-19
---

# Phase 8 continuation: migrated-chat recovery tracer

Plans 08-01, 08-02 and 08-03 are complete. Plan 08-04 Task 1 is implemented and committed; Tasks 2 and 3 have not started. Plan 08-05 remains pending. The prior user approval applied to the 08-01 tracer only. The interactive execute-plan tracer gate now requires approval of this slice before expansion.

## Completed work

- `d11d2d8` — test(08-04): cover destination-locale migrated callback refusal (RED: Ukrainian expected, hardcoded English received).
- `28ad736` — feat(08-04): localize migrated-chat recovery in destination language (GREEN).
- Added parameterless `migration.upgraded` to the typed English/Ukrainian catalogs. The migration boundary resolves presentation locale from the authoritative destination ID and retains the early return for every old-chat update.
- Composed-bot regression performs real persisted migration, confirms preference transfer, switches destination uk/en/uk, then deletes the preference to exercise English default. Each callback acknowledges once, sends/edits nothing, retains `/plan_status`, and preserves post-write snapshots of preferences, migration ledger, rounds/participants, actions, configuration, reminders/occurrences, memberships, drafts and cooldowns. Old commands remain silent.

## Verification

- `npm run test:integration -- tests/integration/chat-migration.test.ts`: 7/7 passed on disposable PostgreSQL.
- `npm run typecheck`: passed.
- Prettier check for all five task files and scoped `git diff --check`: passed.
- Extra `npm run test:unit -- tests/unit/i18n.test.ts`: 15/16 passed. The pre-existing catch-all catalog sample omits `weekRange`, so `reminder.planning.body` renders `undefined`. Task 3 explicitly replaces this fixture with exhaustive typed samples. This is not a migration-key regression; do not claim the full unit suite passes.
- No live Telegram calls, deployment, installations, or domain/schema changes were made.

## Reviewable output

- Ukrainian: `Цю групу оновлено. Відкрий /plan_status у супергрупі.`
- English (unchanged): `This group was upgraded. Open /plan_status in the supergroup.`

## Next action

After explicit tracer approval, execute 08-04 Task 2 (AST-backed outbound inventory), then Task 3 (typed strict catalog samples), and create the complete 08-04 SUMMARY only after those tasks pass. Preserve all unrelated dirty work. The orchestrator owns STATE checkpoint updates. This checkpoint is automated evidence plus copy review; native Telegram acceptance is still separate.
