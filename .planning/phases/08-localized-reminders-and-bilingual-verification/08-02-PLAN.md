---
phase: 08-localized-reminders-and-bilingual-verification
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/telegram/reminder-renderers.ts
  - src/domain/reminders/reminder-service.ts
  - tests/integration/localized-reminders.test.ts
  - tests/unit/reminder-renderers.test.ts
autonomous: true
requirements: ["LREM-02","L10N-03"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05, D-06, D-08: reminders use the selected two-line Ukrainian copy with full date, authoritative 24-hour range and saved IANA timezone."
    - "D-07: pending-only mentions remain comma-separated in the established stable display order."
    - "D-09, D-10, D-11, D-12: supergroups retain direct card links; basic groups retain reply navigation and the separate /plan_status hint."
    - "EDGE LREM-02/empty: zero pending participants sends nothing; one pending participant produces exactly one real mention."
    - "EDGE LREM-02/encoding: hostile and long Unicode labels are escaped and shortened at code-point boundaries; ≤4096 encoded units includes translated copy and every pending mention or the occurrence is unsendable."
    - "EDGE LREM-02/ordering: equal-name participants retain the existing roster ordering in both locales."
  artifacts:
    - path: src/telegram/reminder-renderers.ts
      provides: "Locale-aware renderFollowupReminder and authoritative endMinute projection input"
    - path: tests/integration/localized-reminders.test.ts
      provides: "Follow-up delivery, timezone and navigation integration cases"
    - path: tests/unit/reminder-renderers.test.ts
      provides: "Bilingual mention, empty, ordering, encoding and output-limit cases"
  key_links:
    - from: src/domain/reminders/reminder-service.ts
      to: src/telegram/reminder-renderers.ts
      via: "current locale and authoritative round projection before reservation"
    - from: src/telegram/reminder-renderers.ts
      to: src/telegram/roster-renderers.ts
      via: "unchanged stable sorting and safe mention construction"
---

<objective>
Deliver complete Ukrainian pending-participant reminders with preserved navigation and safety.
Purpose: Complete Phase 8 current-language delivery and evidence without changing scheduling or domain authority.
Output: Scoped production/test artifacts and revision-specific evidence.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-CONTEXT.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-PATTERNS.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-SOURCE-AUDIT.md
</context>



<tasks>
<!-- Execution clarification: inspect src/telegram/planning-handlers.ts planningTimeRange and src/infrastructure/time/zoned-clock.ts. planningTimeRange is private, so do not import it. In the reminder service use round.endsAt when present; otherwise resolveWallClock in round.timezone plus duration, then civilNow(round.timezone,end).minuteOfDay, exactly matching that established algorithm. The renderer gains endMinute; English heading also receives startTime and durationMinutes from the typed catalog contract. -->
<task type="auto" tdd="true">
<name>Task 1: Deliver localized pending reminders through the existing claim path</name>
<files>src/telegram/reminder-renderers.ts, src/domain/reminders/reminder-service.ts, tests/integration/localized-reminders.test.ts</files>
<read_first>src/telegram/reminder-renderers.ts, src/domain/reminders/reminder-service.ts, tests/integration/localized-reminders.test.ts, src/shared/i18n/index.ts, src/shared/i18n/planning-format.ts, src/telegram/planning-renderers.ts, src/telegram/presentation-locale.ts, src/domain/planning/planning-service.ts</read_first>
<behavior>D-05..D-08: Ukrainian output has the exact two-line structure, all and only pending user-ID links comma-separated in existing order, saved round timezone and correct range after a later chat timezone change; English compatibility assertions pass.</behavior>
<action>Add trailing explicit Locale defaulting to en to renderFollowupReminder. Resolve the current persisted locale in ReminderService at its follow-up rendering boundary and use it for the complete payload. Read via the existing LanguageService/resolvePresentationLocale contract outside nested database transactions, preserving the same-chat coordinator ordering; do not introduce a query that waits on a lock already held by this transaction. Resolve after awaited external chat metadata and immediately before the projection/claim transaction. Keep current-state eligibility checked under the existing transaction. Per D-05..D-08 render Ukrainian date/time on the first line: Репетиція — понеділок, 21 вересня, 19:00–21:00 (Europe/Kyiv). Then Нагадаймо про репетицію: {mentions} — дай знати, чи зможеш прийти. Obtain the range through the same authoritative saved-round instant/timezone projection used by planning cards, including midnight/DST end times, rather than naive addition or the later chat timezone. Lowercase only the Ukrainian weekday used inside this sentence; leave accepted card headings unchanged. Pass the needed authoritative end minute into the renderer from the existing range projection. Preserve English phrase semantics/wording and its current formatting unless the shared projection requires a documented narrow correction. Filter and order from authoritative round participants. Capture actual follow-up send payloads for both locales and committed switches.</action>
<reversibility rating="reversible">Source/test changes use existing durable identities; revert the scoped commit without data conversion.</reversibility>
<acceptance_criteria>D-05..D-08: Ukrainian output has the exact two-line structure, all and only pending user-ID links comma-separated in existing order, saved round timezone and correct range after a later chat timezone change; English compatibility assertions pass.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/localized-reminders.test.ts</automated></verify>
<done>D-05..D-08: Ukrainian output has the exact two-line structure, all and only pending user-ID links comma-separated in existing order, saved round timezone and correct range after a later chat timezone change; English compatibility assertions pass.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Preserve navigation, Unicode budgets and empty outcomes</name>
<files>src/telegram/reminder-renderers.ts, tests/unit/reminder-renderers.test.ts</files>
<read_first>src/telegram/reminder-renderers.ts, tests/unit/reminder-renderers.test.ts, src/telegram/roster-renderers.ts, src/shared/i18n/uk.ts, src/shared/i18n/en.ts</read_first>
<behavior>D-07/D-09..D-12: both navigation types retain destinations; basic recovery is a separate line, no such hint is appended to supergroups; every pending mention survives or outcome is unsendable; output ≤4096 and valid Unicode/escaped HTML.</behavior>
<action>Per D-09 retain validated public/private supergroup card URLs and use Відповісти щодо репетиції as their label. Per D-10/D-11/D-12 basic groups retain reply_parameters.message_id and allow_sending_without_reply:false; append Щоб відповісти щодо репетиції, відкрий картку, на яку відповідає це повідомлення. then its own line Не знаходиш картку? Скористайся /plan_status. Supergroups get only their existing direct navigation. Keep answer controls on the card. Reuse the existing pending filter, stable roster comparator, HTML escaping, code-point truncation loop and conservative 4096 encoded-unit cap; count all localized suffixes/navigation before choosing a label limit. Reuse localized fallback member labels without sorting by them. Test zero pending→empty/no send, one pending→exactly one link, equal visible names→existing ID tie-break order, hostile HTML and astral Unicode, 60 mentions, invalid anchors/group paths→unsendable, impossible all-mentions capacity→unsendable rather than dropped people. Preserve service unsendable disposition and unreserved spacing.</action>
<reversibility rating="reversible">Source/test changes use existing durable identities; revert the scoped commit without data conversion.</reversibility>
<acceptance_criteria>D-07/D-09..D-12: both navigation types retain destinations; basic recovery is a separate line, no such hint is appended to supergroups; every pending mention survives or outcome is unsendable; output ≤4096 and valid Unicode/escaped HTML.</acceptance_criteria>
<verify><automated>npm run test:unit -- tests/unit/reminder-renderers.test.ts</automated></verify>
<done>D-07/D-09..D-12: both navigation types retain destinations; basic recovery is a separate line, no such hint is appended to supergroups; every pending mention survives or outcome is unsendable; output ≤4096 and valid Unicode/escaped HTML.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted chat/round and user labels to Telegram output | Locale controls presentation; names, IDs and claims retain their existing authority. |
| Source/catalog/build to verification evidence | Checks must measure actual outbound paths and the shipped image. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-02-01 | Tampering | Reminder rendering | high | mitigate | Retain HTML escaping, validated navigation paths, pending-snapshot filtering and unchanged callback destinations; assert hostile Unicode labels and all mentions. |
| T-08-02-02 | Elevation of privilege | Locale and durable delivery | high | mitigate | Use existing chat identity and current authorization; locale changes never reopen claims or change permissions; preserve duplicate and unknown-outcome tests. |
| T-08-02-03 | Repudiation | Verification evidence | medium | mitigate | Record exact revision, commands and observed outcomes; distinguish native acceptance and scoped waivers from automated evidence. |
| T-08-02-SC | Tampering | Dependencies | low | accept | No dependency additions or version changes; use the existing approved lockfile and existing CI installation. |

ASVS L1; high severity and above blocks completion until mitigated. No new schema or package installation is introduced by this plan.
</threat_model>

## Artifacts this phase produces

- `src/telegram/reminder-renderers.ts`: Locale-aware renderFollowupReminder and authoritative endMinute projection input.
- `tests/integration/localized-reminders.test.ts`: Follow-up delivery, timezone and navigation integration cases.
- `tests/unit/reminder-renderers.test.ts`: Bilingual mention, empty, ordering, encoding and output-limit cases.

<verification>
Run every task's automated command. Preserve unrelated workspace changes. Tests using PostgreSQL require the repository's isolated integration database and existing migration setup; never use the live Telegram fixture database. Record failures accurately and rerun affected checks after corrections. Research was explicitly skipped; research-derived Nyquist VALIDATION.md is not applicable. These concrete tests remain mandatory.
</verification>

<success_criteria>
All listed truths and task criteria have executable evidence. No unresolved assumption is counted as a behavioral pass. Source audit coverage remains complete.
</success_criteria>

<output>
Create .planning/phases/08-localized-reminders-and-bilingual-verification/08-02-SUMMARY.md with changes, commands, tested revision and residual acceptance status.
</output>
