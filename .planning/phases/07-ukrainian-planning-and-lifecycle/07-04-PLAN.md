---
phase: 07-ukrainian-planning-and-lifecycle
plan: "04"
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - src/shared/i18n/index.ts
  - src/shared/i18n/en.ts
  - src/shared/i18n/uk.ts
  - src/telegram/planning-renderers.ts
  - tests/unit/localized-lifecycle-cards.test.ts
  - src/telegram/keyboards.ts
  - src/telegram/planning-handlers.ts
  - tests/integration/localized-lifecycle.e2e.test.ts
  - tests/unit/i18n.test.ts
  - tests/unit/planning-keyboards.test.ts
autonomous: true
requirements: [TEXT-03, LFMT-01, LANG-06]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Unanimity, blocked slots and manual external-booking confirmation use the exact selected Ukrainian phrases (D-05 through D-08)."
    - "Change/cancel commands and inline confirmations preserve current authority, successor links, notification rules and current control-bearing message."
    - "Retired, superseded, retracted, cancelled and booked messages render truthful Ukrainian recovery state without creating announcement entitlement."
  artifacts:
    - path: tests/unit/localized-lifecycle-cards.test.ts
      provides: "Executable bilingual behavior regressions for this plan"
    - path: tests/integration/localized-lifecycle.e2e.test.ts
      provides: "Executable bilingual behavior regressions for this plan"
  key_links:
    - from: src/telegram/planning-handlers.ts
      to: src/telegram/planning-renderers.ts
      via: "announcement directives and recovery select localized lifecycle renderers"
    - from: src/telegram/keyboards.ts
      to: src/telegram/planning-handlers.ts
      via: "book-keep retains existing dismiss/keep transition"
  prohibitions:
    - statement: "Readiness and manual booking copy must not imply the bot booked a studio or conceal whether an external booking was confirmed."
      status: unresolved
      reason: "Flagged-unverified spec-less fallback; end-of-phase transparency judgment is pending."
  assumptions:
    - "FLAGGED UNRESOLVED: TEXT-03 / unclassified raw probe has no classified predicate. Concrete lifecycle tests cover named contracts; the raw row remains unresolved for verifier review."
---

<objective>
Booking, replanning, cancellation and recovery in Ukrainian.
Purpose: Deliver the Phase 7 full interactive Ukrainian rehearsal workflow without changing its durable planning, authorization or scheduling semantics.
Output: tests/unit/localized-lifecycle-cards.test.ts, tests/integration/localized-lifecycle.e2e.test.ts and the production presentation wiring described below.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-CONTEXT.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-PATTERNS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-VALIDATION.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-SOURCE-AUDIT.md
</context>

## Artifacts this phase produces

- Typed planning.lifecycle.* keys and locale-aware lifecycle row factories; existing lifecycle renderer names are retained.
- tests/unit/localized-lifecycle-cards.test.ts
- tests/integration/localized-lifecycle.e2e.test.ts

## Execution constraints

Research was explicitly skipped by the user. These instructions derive from repository patterns and locked decisions; no RESEARCH artifact or external research is assumed. Existing dependencies and schema are sufficient. Preserve unrelated working-tree edits. Scope is interactive presentation; actual reminder delivery and full outbound/runtime inventory stay in Phase 8. English documentation and existing English behavior remain supported. One polling process and current chat serialization remain deployment assumptions.

Calibration factor 1, sample count 0, confidence low. 3 tasks; no task modifies more than five files. Shared catalogs/renderers/handlers require this sequential wave ordering. New tests are written first in their owning task; no separate unimplemented scaffold is accepted as completion.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Translate lifecycle messages while retaining their state distinctions</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <read_first>src/telegram/planning-renderers.ts, .planning/milestones/v1.0-phases/04-replanning-and-rehearsal-lifecycle/04-CONTEXT.md</read_first>
  <files>src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, src/telegram/planning-renderers.ts, tests/unit/localized-lifecycle-cards.test.ts</files>
  <behavior>Unanimity says Усі можуть! Час бронювати репетицію.; blocked says Цей час підходить не всім. Обери іншу дату й час. Booking confirmation retains actual date/time and asks the approved question.</behavior>
  <action>Create typed planning.lifecycle.* phrase keys, including all D-06/D-07 booking and change/cancel control labels consumed by Task 2, and localize renderReadyAnnouncement, renderBlockedAnnouncement, renderRetractedAnnouncement, renderBookingConfirmation, renderCancellationConfirmation, renderChangeConfirmation, renderCancellationNotice, renderSupersededAttemptLine and renderRetiredPlanningMessage, including the existing cancellationSlot helper and every helper fragment. Apply D-05 and D-08 exactly; retain unavailable-participant identification with escaped labels. Apply D-07 question Студію вже заброньовано на цей час? and keep the actual round date/range. Use D-09/D-11 date and D-12 summary formatting from Plan 02. Translate terminal/recovery facts without suggesting an untracked retired copy represents current state or that readiness means externally booked. Keep English outputs and domain-selected lifecycle distinctions.</action>
  <acceptance_criteria>Unanimity says Усі можуть! Час бронювати репетицію.; blocked says Цей час підходить не всім. Обери іншу дату й час. Booking confirmation retains actual date/time and asks the approved question. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/unit/localized-lifecycle-cards.test.ts</automated>
  </verify>
  <done>Every lifecycle renderer produces complete bilingual output, selected phrases are exact and recovery text reflects the true terminal/current status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire manual booking and lifecycle controls into normal dispatch</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <read_first>src/telegram/planning-handlers.ts, src/telegram/keyboards.ts, tests/unit/planning-keyboards.test.ts, tests/integration/planning-booking.test.ts</read_first>
  <files>src/telegram/keyboards.ts, src/telegram/planning-handlers.ts, tests/unit/planning-keyboards.test.ts</files>
  <behavior>Студію заброньовано opens confirmation; Так, заброньовано applies an eligible booking; Назад performs existing book-keep. Lifecycle commands and buttons use one locale per payload.</behavior>
  <action>First extend planning-keyboards.test.ts with failing en/uk lifecycle assertions for exact D-06 request and D-07 confirmation labels, Назад bound to the unchanged book-keep token, unchanged change/cancel action bindings, missing-token omission and BOOKED answer-control removal. Consume lifecycle catalog keys created in Task 1. Add catalog-backed lifecycle row factories and wire them through withLifecycleControls, booking request/keep/apply and change/cancel request/keep/apply call sites. Per D-06 the booking request label is Студію заброньовано; per D-07 confirmation labels are Так, заброньовано / Назад with the latter still targeting book-keep. Localize change/cancel labels using existing exact transition semantics and agreed informal voice. Thread the same locale into dispatchAnnouncement's edit/post/retract branch and any terminal-card edit; preserve the early return for directive none, durable claims, compensation and current controlBearingMessageId. Keep announcement/card identities, action eligibility and token binding. Make focused assertions pass here; composed lifecycle journeys and any catalog parity sample additions remain Task 3.</action>
  <acceptance_criteria>Студію заброньовано opens confirmation; Так, заброньовано applies an eligible booking; Назад performs existing book-keep. Lifecycle commands and buttons use one locale per payload. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/unit/planning-keyboards.test.ts tests/unit/localized-lifecycle-cards.test.ts</automated>
  </verify>
  <done>All existing lifecycle entry points produce Ukrainian controls and messages while retaining their original transitions and notification rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Exercise bilingual lifecycle transitions and recovery</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <precondition>The existing tests/helpers/postgres.ts fixture can start migrated PostgreSQL using a reachable Docker engine; assert docker info succeeds before the integration run.</precondition>
  <read_first>tests/integration/planning-booking.test.ts, tests/integration/planning-replan-telegram.test.ts, tests/integration/planning-cancel-telegram.test.ts, tests/integration/planning-change-telegram.test.ts, tests/integration/planning-recovery.test.ts</read_first>
  <files>tests/integration/localized-lifecycle.e2e.test.ts, tests/unit/localized-lifecycle-cards.test.ts, tests/unit/i18n.test.ts, src/telegram/planning-handlers.ts, src/telegram/planning-renderers.ts</files>
  <behavior>Both languages complete blocked replan, readiness, book Back/apply, date/time change, draft/collecting/booked cancellation and missing-message recovery. Concurrent eligible actions do not create duplicate successors or announcements.</behavior>
  <action>Build a focused real-PostgreSQL/createBot lifecycle fixture from existing lifecycle and localized onboarding scenarios. Cover request and apply authorization rechecks, lost unanimity, Back preserving the round, superseding/resnapshotting, old-control recovery, cancelled/no-round distinctions, booked notification versus collecting edit, /plan_status reanchoring and missing message recovery. Assert current locale at newly emitted render operations and exact D-05 through D-08 output; compare English transitions and row/claim counts. Update catalog sample parameters if lifecycle keys introduced any. Fix only missed presentation plumbing in the declared handler/renderer files. Flag the raw TEXT-03/unclassified probe as unresolved in the plan assumption record; these concrete lifecycle checks provide scoped evidence but do not falsely classify that probe.</action>
  <acceptance_criteria>Both languages complete blocked replan, readiness, book Back/apply, date/time change, draft/collecting/booked cancellation and missing-message recovery. Concurrent eligible actions do not create duplicate successors or announcements. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- tests/integration/localized-lifecycle.e2e.test.ts</automated>
  </verify>
  <done>Bilingual real-database lifecycle scenarios pass and retain authority, transition counts, control placement and recovery destinations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update to planning authority | Callback actor/chat/token are untrusted; locale does not authorize the action. |
| Stored identities to Telegram output | Names may contain HTML metacharacters and long Unicode data; cards and private alerts have different escaping rules. |
| Committed state to external delivery | A translated payload must not create a new transition or notification entitlement. |

## STRIDE Threat Register

ASVS Level 1; unresolved high or critical findings block completion.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-04-01 | Elevation of privilege | planning callback/command paths | high | mitigate | Keep fresh role, chat/token and action-specific eligibility checks; test localized denials and unchanged transition/consumption state. Never use preference fallback or labels as authority. |
| T-07-04-02 | Information disclosure / Tampering | dynamic member labels in presentation | medium | mitigate | Use localizedMemberLabel once for HTML and localizedPlainMemberLabel plus 200 UTF-16-unit safe truncation for alerts; preserve masked fallback IDs and test hostile names. |
| T-07-04-03 | Tampering / Repudiation | no-op rendering and recovery | high | mitigate | Preserve revision/consumed-token checks, full text-and-keyboard fingerprints and durable announcement claims; distinguish uncommitted failure from committed/uncertain delivery in regression assertions. |
</threat_model>

<verification>
Run each task's named checks, then npm run build and the affected unit/integration regression set in 07-VALIDATION.md. Do not silently skip PostgreSQL coverage or replace it with catalog snapshots. No implementation tests were run during planning; record actual commands/results in the summary during execution.
</verification>

<success_criteria>
All must_haves truths and task acceptance criteria are demonstrated in both locales where applicable; current authority, domain transitions, timezone meaning and acknowledgement behavior remain intact. No high/critical threat is left unresolved. The unclassified probe and pending wording judgment remain explicitly flagged; passing concrete tests does not silently close them.
</success_criteria>

<output>
Create .planning/phases/07-ukrainian-planning-and-lifecycle/07-04-SUMMARY.md with changes, exact verification evidence, decision coverage and any flagged assumptions when done.
</output>
