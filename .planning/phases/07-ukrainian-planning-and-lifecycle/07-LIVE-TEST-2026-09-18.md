# Phase 7 — Native Telegram Web acceptance

## Session and provenance

Date: 2026-09-18, approximately 18:08–18:22 Europe/Kyiv (UTC+03:00).
Actor A: Валерій Погорєлов / @greensmilemind. Browser selected the authenticated Chrome Telegram Web K session. The account menu exposed only A and Add Account; B was not available in this session. The primary group title and Web peer `-4358185686` were verified before sending commands. No actions were performed in the secondary group.

Resumed the existing four-group UAT without resetting acceptance. GSD init and the verify-pre API coverage gate passed. Counts remain 0/4 accepted, 4 pending: native observations below are partial evidence, not the four required human judgments. No prior waiver was broadened.

Evidence is from real command entry, visible inline buttons, fresh DOM snapshots, and one visual screenshot inspection of the availability card. No hidden Telegram state, synthetic callback, or direct Telegram request was used. The visible screenshot showed readable full card text and buttons. No claim is made about phone push/sound or protocol-level acknowledgement counts.

## Runtime and baseline

The existing single Compose bot was running the Phase 6 image: `dist/shared/i18n/uk.js` lacked the Phase 7 ready phrase. Built the current bot/migrate images and recreated the existing service through Compose. Migration service exited successfully; the existing PostgreSQL volume/history remained intact. Exactly one bot service continued polling. Subsequent scoped fixes below were rebuilt into the same local test service with `--no-deps`; no production deployment was performed.

Fresh `/plan_status` at 18:11 reported no active rehearsal. Fresh `/settings` showed English; Europe/Kyiv; Wednesday 14:00; 120 minutes rendered as `2 hours`; daily 10:00–21:00; reminders 10:00/16:00; admins only. `/roster` confirmed B (Valeriii Phorielov / @greenvaleratest) and A. Neither roster, identities, roles nor scheduling values was changed.

## Observed subcases

| Subcase | Native action and result |
| --- | --- |
| Ukrainian settings and natural duration | Switched through `Мова / Language` → `Українська`; same values rendered with `Тривалість: 2 години`. |
| Day/time/review | `/plan` at 18:12; chose `Сб 19` then `⭐ 14:00`. Full `Субота, 19 вересня`, range `14:00–16:00`, two named participants and Ukrainian confirmation were shown. Review did not repeat duration beside the range. |
| Pending collection | `Підтвердити репетицію` showed `Відповіли 0 з 2.`, `Очікуємо відповідь`, A/B names and `Організатор`. |
| Unavailable and blocked | A clicked `👎 Не можу`; card became 1/2, with B pending, A unavailable, `Не може`, and `Цей час підходить не всім.`. A separate blocked message named the unavailable actor and said `Цей час підходить не всім. Обери іншу дату й час.` |
| Reversal and retraction | A clicked `👍 Можу`; same slot became 1/2 with A available and B pending. Earlier blocked announcement became `Ще збираємо відповіді` and explained it was no longer current. |
| Ukrainian → English mid-round | Changed settings language to English, then used the retained Ukrainian `Можу` control. Body/buttons became English together, date/slot/participants/A answer remained visible, and modal said `Already applied.`. |
| English → Ukrainian mid-round | Selected Ukrainian and used retained `Can attend`. Body/buttons became Ukrainian together with the same 1/2 state; modal said `Усе гаразд, цю дію вже виконано.`. Both modals were dismissed. |
| Change confirmation and Keep | Opened `Змінити дату чи час`; reviewed same-week/reset-answers warning; chose `Залишити цей час`. Slot and responses remained. Initial wording and missing organizer defects were fixed below. |
| Cancellation confirmation and Keep | Opened `Скасувати репетицію`; chose `Залишити репетицію`. Slot and responses remained. Initial wording and missing organizer defects were fixed below. |
| Corrected change/cancel wording | After rebuilding, native cards read `Змінити дату й час репетиції?` followed by `Зараз: Субота, 19 вересня о 14:00`, and `Скасувати репетицію?` followed by the standalone slot. |
| Corrected organizer preservation | On the final rebuilt image, both cancel-keep and change-keep returned to a card with `Організатор: Валерій Погорєлов`, the same slot and 1/2 response state. |

## Findings and fixes

**G-07-1 — grammatical date composition (minor).** Initial day card said `тиждень від Понеділок, 14 вересня`. Native change/cancel cards similarly said `Змінити Субота...` / `Скасувати Субота...`. GSD debug `.planning/debug/uk-week-heading.md` traced the mismatch between standalone nominative date labels and enclosing phrases. Corrected three Ukrainian catalog phrases and strengthened existing literal card assertions. 66 tests passed across localized planning cards, lifecycle cards and planning-format. Corrected change/cancel phrases were observed natively; corrected week-start heading still needs its next native day-card rendering. Human wording acceptance remains pending.

**G-07-2 — organizer omitted after Keep (minor).** Native collecting cards lost the organizer line after change-keep and cancel-keep. GSD debug `.planning/debug/keep-card-owner.md` found both projection calls omitted the optional owner. Added the shared persisted-identity resolver to both paths, preserving returned participant snapshots. Four new composed bilingual regression cases failed for the exact missing-line reason before the fix. After correction, 53/53 integration tests passed across localized-lifecycle, planning-cancel-telegram and planning-change-telegram. Both paths passed native retest. `tsc --noEmit` and runtime image build passed.

## Pending acceptance and handoff

B was asked through the current task to press `Можу` on the temporary **Saturday 19 September, 14:00–16:00** card in `GSM_bot_test_group`. No answer from B was observed by this checkpoint. Do not fabricate B's response or remove B from the roster to force unanimity.

Next: recheck current card; after B's answer, verify ready → booking request → Back → booking apply, then relevant change/replan/cancel/recovery wording. Retest the corrected day heading when a new day-selection card is legitimately opened. Single-participant and count/duration boundary wording review remain outstanding; the focused automated formatter cases are supporting evidence only. Permission/stale/safe-retry surfaces not observed here remain pending in the broader wording judgment.

The raw TEXT-03 item was inspected: its entire predicate is `unclassified — review manually`. No concrete acceptance condition exists in that item. Its disposition remains explicitly unresolved; no invented test or waiver replaces it.

**Restoration is not yet complete because this is an active test handoff:** one temporary confirmed collecting rehearsal remains, A available and B pending, and group language remains Ukrainian instead of baseline English. At completion, cancel only this run's temporary rehearsal(s), restore English, and verify `/plan_status` plus settings/roster. Scheduling values, roster, names and roles were never changed. No messages or database history were deleted. Secondary group remains untouched. Chrome tab is retained for continuation; obtain fresh handles if the browser session changes. One local bot and existing PostgreSQL remain running.
