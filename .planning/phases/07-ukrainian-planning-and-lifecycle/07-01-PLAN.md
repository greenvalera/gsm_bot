---
phase: 07-ukrainian-planning-and-lifecycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/i18n/index.ts
  - src/shared/i18n/en.ts
  - src/shared/i18n/uk.ts
  - src/telegram/planning-handlers.ts
  - tests/integration/localized-planning.e2e.test.ts
autonomous: true
requirements: [LANG-06, TEXT-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A consumed planning action is acknowledged once in the current durable group language without another domain transition (D-16)."
    - "Definitely uncommitted failures invite retry in Ukrainian; committed or uncertain outcomes retain truthful recovery guidance (D-15)."
  artifacts:
    - path: tests/integration/localized-planning.e2e.test.ts
      provides: "Executable bilingual behavior regressions for this plan"
  key_links:
    - from: src/telegram/planning-handlers.ts
      to: src/shared/i18n/index.ts
      via: "resolvePresentationLocale and renderMessage before branch-owned feedback"
---

<objective>
Durable-locale interactive callback tracer.
Purpose: Deliver the Phase 7 full interactive Ukrainian rehearsal workflow without changing its durable planning, authorization or scheduling semantics.
Output: tests/integration/localized-planning.e2e.test.ts and the production presentation wiring described below.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-CONTEXT.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-PATTERNS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-VALIDATION.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-SOURCE-AUDIT.md
</context>

## Artifacts this phase produces

- Typed planning.applied and planning.retrySafe catalog keys; localized duplicate/retry feedback on the existing planning dispatcher.
- tests/integration/localized-planning.e2e.test.ts

## Execution constraints

Research was explicitly skipped by the user. These instructions derive from repository patterns and locked decisions; no RESEARCH artifact or external research is assumed. Existing dependencies and schema are sufficient. Preserve unrelated working-tree edits. Scope is interactive presentation; actual reminder delivery and full outbound/runtime inventory stay in Phase 8. English documentation and existing English behavior remain supported. One polling process and current chat serialization remain deployment assumptions.

Calibration factor 1, sample count 0, confidence low. 2 tasks; no task modifies more than five files. Shared catalogs/renderers/handlers require this sequential wave ordering. New tests are written first in their owning task; no separate unimplemented scaffold is accepted as completion.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove localized duplicate feedback through the composed bot</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <precondition>The existing tests/helpers/postgres.ts fixture can start migrated PostgreSQL using a reachable Docker engine; assert docker info succeeds before the integration run.</precondition>
  <read_first>tests/integration/localized-onboarding.e2e.test.ts, tests/integration/planning-availability.test.ts, src/telegram/callbacks.ts, src/telegram/presentation-locale.ts</read_first>
  <files>src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, src/telegram/planning-handlers.ts, tests/integration/localized-planning.e2e.test.ts</files>
  <behavior>A real persisted consumed planning control, replayed through createBot.handleUpdate in a Ukrainian group with an English client, produces exactly one Ukrainian acknowledgement and no second mutation. English and an independent group retain existing feedback.</behavior>
  <action>Start with the failing end-to-end test in localized-planning.e2e.test.ts. Reuse the migrated PostgreSQL fixture, createBot transport interceptor and real planning service/token rows from the existing suites; seed the prior completed transition, then replay its callback through the complete route. Add the no-parameter planning.applied catalog contract, keeping English Already applied. and Ukrainian Усе гаразд, цю дію вже виконано. per D-16. At the existing duplicate-outcome branch resolvePresentationLocale from the action chat and renderMessage that key. Keep the boundary's branch-owned single-shot acknowledgement, role checks, token checks and semantic duplicate result. Assert the stored round, answers and action consumption remain unchanged. This is the production vertical tracer: Telegram callback -> authorization/token and durable preference reads -> semantic duplicate outcome -> typed presentation -> actual intercepted answerCallbackQuery. Do not continue to formatting/card expansions until this end-to-end path passes.</action>
  <acceptance_criteria>A real persisted consumed planning control, replayed through createBot.handleUpdate in a Ukrainian group with an English client, produces exactly one Ukrainian acknowledgement and no second mutation. English and an independent group retain existing feedback. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- tests/integration/localized-planning.e2e.test.ts -t "duplicate feedback tracer"</automated>
  </verify>
  <done>The named tracer test passes against migrated PostgreSQL, a Ukrainian duplicate alert is delivered exactly once, and database snapshots prove replay made no transition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand feedback to safe retry and committed delivery failures</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <precondition>The existing tests/helpers/postgres.ts fixture can start migrated PostgreSQL using a reachable Docker engine; assert docker info succeeds before the integration run.</precondition>
  <read_first>src/telegram/planning-handlers.ts, tests/integration/planning-recovery.test.ts</read_first>
  <files>src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, src/telegram/planning-handlers.ts, tests/integration/localized-planning.e2e.test.ts</files>
  <behavior>A definitely rejected operation uses the selected-language safe retry phrase. A committed action followed by a failed edit and an uncertain send do not falsely report that the action was undone or definitely failed.</behavior>
  <action>Add planning.retrySafe and explicit committed-delivery recovery phrases to the typed catalogs; the Ukrainian safe-retry phrase is Ой, щось пішло не так. Спробуй ще раз трохи пізніше. per D-15. Trace existing catch sites and result branches to their commit status before selecting copy. Wire this first into the tracer's adjacent action/save/recovery paths, preserving existing compensation, log reason and silence for uncertain announcement delivery. Distinct known-uncommitted, committed-render-failed and uncertain-send outcomes remain distinct. Keep English behavior and command recovery destinations. Extend the same composed fixture with known non-delivery and committed delivery failure injection; inspect durable state and all outbound calls rather than only checking a catalog value.</action>
  <acceptance_criteria>A definitely rejected operation uses the selected-language safe retry phrase. A committed action followed by a failed edit and an uncertain send do not falsely report that the action was undone or definitely failed. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- tests/integration/localized-planning.e2e.test.ts -t "retry classification"</automated>
  </verify>
  <done>Safe failures show D-15 exactly; committed/uncertain outcomes preserve state and notification policy and are not mislabeled as safe-to-repeat mutations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update to planning authority | Callback actor/chat/token are untrusted; locale does not authorize the action. |
| Stored identities to Telegram output | Names may contain HTML metacharacters and long Unicode data; cards and private alerts have different escaping rules. |
| Committed state to external delivery | A translated payload must not create a new transition or notification entitlement. |

## STRIDE Threat Register

ASVS Level 1; unresolved high or critical findings block completion.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01-01 | Elevation of privilege | planning callback/command paths | high | mitigate | Keep fresh role, chat/token and action-specific eligibility checks; test localized denials and unchanged transition/consumption state. Never use preference fallback or labels as authority. |
| T-07-01-02 | Information disclosure / Tampering | dynamic member labels in presentation | medium | mitigate | Use localizedMemberLabel once for HTML and localizedPlainMemberLabel plus 200 UTF-16-unit safe truncation for alerts; preserve masked fallback IDs and test hostile names. |
| T-07-01-03 | Tampering / Repudiation | no-op rendering and recovery | high | mitigate | Preserve revision/consumed-token checks, full text-and-keyboard fingerprints and durable announcement claims; distinguish uncommitted failure from committed/uncertain delivery in regression assertions. |
</threat_model>

<verification>
Run each task's named checks, then npm run build and the affected unit/integration regression set in 07-VALIDATION.md. Do not silently skip PostgreSQL coverage or replace it with catalog snapshots. No implementation tests were run during planning; record actual commands/results in the summary during execution.
</verification>

<success_criteria>
All must_haves truths and task acceptance criteria are demonstrated in both locales where applicable; current authority, domain transitions, timezone meaning and acknowledgement behavior remain intact. No high/critical threat is left unresolved. 
</success_criteria>

<output>
Create .planning/phases/07-ukrainian-planning-and-lifecycle/07-01-SUMMARY.md with changes, exact verification evidence, decision coverage and any flagged assumptions when done.
</output>

