---
phase: 07-ukrainian-planning-and-lifecycle
plan: "06"
type: execute
wave: 6
depends_on: ["07-05"]
files_modified:
  - src/telegram/planning-handlers.ts
  - tests/integration/planning-language-switch.e2e.test.ts
  - tests/unit/planning-language-render.test.ts
  - tests/integration/localized-lifecycle.e2e.test.ts
autonomous: true
requirements: [LANG-06, TEXT-02, TEXT-03, TEXT-04, LFMT-01, LFMT-02]
estimate:
  tokens: 29000
  raw_tokens: 29000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Repeating the same answer/selection after switching language updates text and keyboard together without changing round revision, answers, ownership or valid controls (LANG-06 idempotency)."
    - "A committed language switch governs the next render boundary; in-flight sends keep their captured locale, while concurrent updates preserve existing transactional and announcement-claim guarantees (LANG-06 concurrency)."
    - "Cold-cache, missing-message and committed-edit-failure recovery use current language while keeping actual anchor/announcement identity and notification entitlement."
    - "Language-only changes leave selected civil date, start/end instants, roster snapshot, author, schedules and due times unchanged in both switching directions."
  artifacts:
    - path: tests/integration/planning-language-switch.e2e.test.ts
      provides: "Executable bilingual behavior regressions for this plan"
    - path: tests/unit/planning-language-render.test.ts
      provides: "Executable bilingual behavior regressions for this plan"
  key_links:
    - from: src/telegram/planning-handlers.ts
      to: src/telegram/planning-renderers.ts
      via: "one locale snapshot builds text plus keyboard before full-payload fingerprint"
    - from: tests/integration/planning-language-switch.e2e.test.ts
      to: src/app/create-bot.ts
      via: "real settings language callback followed by still-valid planning control"
---

<objective>
Safe mid-round switching and ordinary recovery updates.
Purpose: Deliver the Phase 7 full interactive Ukrainian rehearsal workflow without changing its durable planning, authorization or scheduling semantics.
Output: tests/integration/planning-language-switch.e2e.test.ts, tests/unit/planning-language-render.test.ts and the production presentation wiring described below.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-CONTEXT.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-PATTERNS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-VALIDATION.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-SOURCE-AUDIT.md
</context>

## Artifacts this phase produces

- Ordinary same-state rendering and full-payload comparison hardening in existing handler functions; no new domain or wire interface.
- tests/integration/planning-language-switch.e2e.test.ts
- tests/unit/planning-language-render.test.ts

## Execution constraints

Research was explicitly skipped by the user. These instructions derive from repository patterns and locked decisions; no RESEARCH artifact or external research is assumed. Existing dependencies and schema are sufficient. Preserve unrelated working-tree edits. Scope is interactive presentation; actual reminder delivery and full outbound/runtime inventory stay in Phase 8. English documentation and existing English behavior remain supported. One polling process and current chat serialization remain deployment assumptions.

Calibration factor 1, sample count 0, confidence low. 2 tasks; no task modifies more than five files. Shared catalogs/renderers/handlers require this sequential wave ordering. New tests are written first in their owning task; no separate unimplemented scaffold is accepted as completion.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refresh ordinary no-op card updates without a domain transition</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <precondition>The existing tests/helpers/postgres.ts fixture can start migrated PostgreSQL using a reachable Docker engine; assert docker info succeeds before the integration run.</precondition>
  <read_first>src/telegram/planning-handlers.ts, src/telegram/settings-handlers.ts, tests/integration/localized-planning.e2e.test.ts, tests/integration/localized-lifecycle.e2e.test.ts</read_first>
  <files>src/telegram/planning-handlers.ts, tests/integration/planning-language-switch.e2e.test.ts, tests/unit/planning-language-render.test.ts</files>
  <behavior>Switch en -> uk -> en during DAY/TIME/REVIEW/CONFIRMED and reuse valid controls. Repeated answers/selections repaint complete payloads only when rendered bytes differ, with no extra mutations or announcement sends.</behavior>
  <action>Add a composed regression using real settings language controls, current-role checks and persisted planning rows. Capture round revision/status/author, selected civil date/start/end/timezone, participant answers/snapshot, callback target validity, schedule generation and queued due times before switching. Assert changing language itself edits only its ordinary settings response; the next authorized planning action updates the active card's text and keyboard with one resolved locale. Cover repeated same-day, same-time and same-answer results and /plan_status. If an early no-op return bypasses rendering, route that branch through the existing authorized ordinary-card refresh without forcing a domain revision or reminting action capabilities solely for language. Preserve editRoundMessage's full text-plus-keyboard fingerprint and remember only successful/not-modified payloads. Keep directive none silent for announcements; language switching never manufactures a notification claim.</action>
  <acceptance_criteria>Switch en -> uk -> en during DAY/TIME/REVIEW/CONFIRMED and reuse valid controls. Repeated answers/selections repaint complete payloads only when rendered bytes differ, with no extra mutations or announcement sends. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- tests/integration/planning-language-switch.e2e.test.ts -t "ordinary update"</automated>
  </verify>
  <done>Both language directions work on ordinary same-state actions with matching text/buttons and identical durable planning/schedule state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden switching across cold cache, concurrent actions and failed delivery</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <read_first>src/app/create-bot.ts, tests/integration/planning-recovery.test.ts, src/telegram/planning-handlers.ts, .planning/phases/07-ukrainian-planning-and-lifecycle/07-VALIDATION.md</read_first>
  <files>src/telegram/planning-handlers.ts, tests/integration/planning-language-switch.e2e.test.ts, tests/unit/planning-language-render.test.ts, tests/integration/localized-lifecycle.e2e.test.ts</files>
  <behavior>A fresh process/cache and not-modified response behave like a warm cache. Delivery failure does not cache an undelivered translation. In-flight sends are not recalled and the next render reads the committed preference.</behavior>
  <action>Use module isolation or a fresh bot/module fixture to test a cold LAST_RENDER cache without adding a production test-only reset API. Inject not-modified, known edit rejection, missing message and uncertain announcement send; verify retry/status recovery renders the current locale and preserves existing claims/compensation. Gate one send while a preference change commits to demonstrate a payload captures one coherent locale and the subsequent render reads the new one. Exercise rapid duplicate settings/answer callbacks under createBot's chat serialization, and concurrent real-service transitions where existing integration tests already model them; assert one successor and existing claim uniqueness, not exactly-once external delivery. Cover active availability plus ready/blocked announcement control placement, booked/cancelled status, token validity and authorization after role changes. Run the affected suites and build checks from 07-VALIDATION.md. Keep raw TEXT-03/unclassified explicitly unresolved in summary/verification handoff and preserve historical native waivers; collect end-of-phase wording review of the flagged transparency/no-blame prohibitions without claiming automated proof.</action>
  <acceptance_criteria>A fresh process/cache and not-modified response behave like a warm cache. Delivery failure does not cache an undelivered translation. In-flight sends are not recalled and the next render reads the committed preference. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/unit/planning-language-render.test.ts</automated>
  </verify>
  <done>Cold/warm cache, delivery failures and interleavings preserve the language boundary and original domain/notification semantics; all required affected checks have recorded results and unresolved assumptions remain visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update to planning authority | Callback actor/chat/token are untrusted; locale does not authorize the action. |
| Stored identities to Telegram output | Names may contain HTML metacharacters and long Unicode data; cards and private alerts have different escaping rules. |
| Committed state to external delivery | A translated payload must not create a new transition or notification entitlement. |

## STRIDE Threat Register

ASVS Level 1; unresolved high or critical findings block completion.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-06-01 | Elevation of privilege | planning callback/command paths | high | mitigate | Keep fresh role, chat/token and action-specific eligibility checks; test localized denials and unchanged transition/consumption state. Never use preference fallback or labels as authority. |
| T-07-06-02 | Information disclosure / Tampering | dynamic member labels in presentation | medium | mitigate | Use localizedMemberLabel once for HTML and localizedPlainMemberLabel plus 200 UTF-16-unit safe truncation for alerts; preserve masked fallback IDs and test hostile names. |
| T-07-06-03 | Tampering / Repudiation | no-op rendering and recovery | high | mitigate | Preserve revision/consumed-token checks, full text-and-keyboard fingerprints and durable announcement claims; distinguish uncommitted failure from committed/uncertain delivery in regression assertions. |
</threat_model>

<verification>
Run each task's named checks, then npm run build and the affected unit/integration regression set in 07-VALIDATION.md. Do not silently skip PostgreSQL coverage or replace it with catalog snapshots. No implementation tests were run during planning; record actual commands/results in the summary during execution.
<human-check>At end of phase, review Ukrainian full dates, natural durations, exact selected wording, no-blame participant status and readiness versus external-booking meaning. Follow telegram-web-uat for any live Telegram verification, preserve fixture restoration and historical native waivers. Report TEXT-03/unclassified and descriptor-less prohibition items as flagged until explicit judgment resolves them.</human-check>
</verification>

<success_criteria>
All must_haves truths and task acceptance criteria are demonstrated in both locales where applicable; current authority, domain transitions, timezone meaning and acknowledgement behavior remain intact. No high/critical threat is left unresolved. 
</success_criteria>

<output>
Create .planning/phases/07-ukrainian-planning-and-lifecycle/07-06-SUMMARY.md with changes, exact verification evidence, decision coverage and any flagged assumptions when done.
</output>

