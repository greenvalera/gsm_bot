---
phase: 03-availability-and-booking-decision
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/domain/planning/planning-service.ts
  - src/telegram/planning-renderers.ts
  - src/telegram/planning-handlers.ts
  - src/telegram/keyboards.ts
  - tests/unit/planning-availability-card.test.ts
  - tests/unit/planning-logging.test.ts
  - tests/integration/planning-availability.test.ts
autonomous: true
requirements:
  - AVAIL-02
  - AVAIL-03
  - AVAIL-04
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The card renders one roster-ordered line per participant with a leading pending / can-attend / cannot-attend marker and a legend above the list, advertising only the markers actually in use (D-08)."
    - "Overall completion is a single count line above the list reading how many of how many have answered; no separate outstanding-names line is rendered (AVAIL-04, D-09)."
    - "AVAIL-04 adjacency — two participants whose safe display labels compare equal under the roster collator render as two distinct lines in a stable order broken by the Telegram id; equal labels never merge into one line and never swap position between renders."
    - "AVAIL-04 empty — a round with exactly one participant renders one participant line and a count line that reads one of one once they answer; a zero-participant availability round is unreachable because Confirm already refuses an empty lineup."
    - "AVAIL-04 ordering — the participant line order is fixed by the roster collator at every render and does not reshuffle as answers arrive, whatever order the answers came in."
    - "Tapping the other control overwrites a participant's previous answer; re-tapping the answer they already gave is an idempotent no-op acknowledged with a short private alert and produces no second durable transition (D-04)."
    - "A cannot-attend answer is recorded and the round stays open for the remaining participants; once everyone has answered and at least one said no, the card states plainly that the slot does not work and offers no replan control (D-05)."
    - "A current chat member who is not in the confirm-time snapshot is refused with a private callback alert and no card edit; a participant removed from the roster mid-round can still answer and still counts toward completion (AVAIL-03, D-06, D-07)."
    - "Every exported refusal constant on the planning surface is at most 200 characters, including any interpolated member label."
  artifacts:
    - tests/unit/planning-availability-card.test.ts
  key_links:
    - "availabilityOutcome -> renderAvailabilityCard outcome sentence -> unit assertions (one derivation, no call-site repetition)"
    - "sortRosterMembers collator + bigint tie-break -> stable participant line order across renders"
    - "PLANNING_OUTCOMES / PLANNING_REASONS -> tests/unit/planning-logging.test.ts BRANCHES enumeration"
  prohibitions:
    - requirement_id: AVAIL-04
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT use chasing, shaming, or blame framing toward participants who have not answered — no separate outstanding-names line, no grouping or reordering that singles out non-responders, and no copy that pressures an individual by name. The product's whole value is agreeing a rehearsal without chasing anyone."
    - requirement_id: AVAIL-03
      category: privacy
      status: resolved
      verification: judgment
      statement: "MUST NOT disclose the lineup composition, member display names, or full numeric Telegram ids to a user outside the participant snapshot, and MUST NOT refuse that user with a message visible to the group."
---

<objective>
Complete the availability card and the answer path around the tracer slice: markers and legend, the completion count, stable roster ordering, the three derived outcome states, the snapshot-membership refusal, the change-your-mind and already-applied branches, and the bounded log vocabulary that keeps every new branch distinguishable to an operator.

Purpose: the tracer proved one happy path. This is where AVAIL-03 and AVAIL-04 actually become true, and where D-04 through D-10 stop being prose.
Output: `tests/unit/planning-availability-card.test.ts`, the completed `renderAvailabilityCard` with its legend and outcome sentence, the refusal and duplicate branches on `dispatchAvailabilityAnswer`, and an extended `BRANCHES` enumeration in the logging gate.
</objective>

<execution_context>
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/workflows/execute-plan.md
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-availability-and-booking-decision/03-CONTEXT.md
@.planning/phases/03-availability-and-booking-decision/03-RESEARCH.md
@.planning/phases/03-availability-and-booking-decision/03-PATTERNS.md
@.planning/phases/03-availability-and-booking-decision/03-VALIDATION.md
@.planning/phases/03-availability-and-booking-decision/03-01-SUMMARY.md
</context>

## Artifacts this phase produces

See the "Artifacts this phase produces" section of `03-01-PLAN.md` — it is the single canonical list of every symbol Phase 3 creates. This plan adds no new file beyond `tests/unit/planning-availability-card.test.ts` and no new exported symbol beyond `PLANNING_AVAILABILITY_LEGEND`.

## Flagged assumptions (unresolved probe edges)

AVAIL-02 and AVAIL-03 remain `unresolved` on the deterministic edge probe (`category: unclassified`). This plan proceeds on the assumptions recorded in `03-01-PLAN.md` → Flagged assumptions: the answer is a mutable value on the participant row rather than an append-only event log, and the rejected actor is a current chat member outside the snapshot (a non-member never reaches the dispatcher).

**Probe edges authored here:** items 1-3 of the ten-item ledger in `03-01-PLAN.md` → "Probe ledger" — AVAIL-04 `adjacency`, `empty` and `ordering`. Each is the correspondingly prefixed entry in this plan's `must_haves.truths` and each has its own case in `tests/unit/planning-availability-card.test.ts` (Task 1). The AVAIL-07 trio is authored the same way in `03-04-PLAN.md`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Markers, legend, completion count, and the one derived outcome</name>
  <files>src/telegram/planning-renderers.ts, src/telegram/keyboards.ts, tests/unit/planning-availability-card.test.ts</files>
  <read_first>
    - src/telegram/planning-renderers.ts (lines 90-200 for `PLANNING_DAY_LEGEND`, the fixed `LEGEND_ORDER`, `legendLine` and `legendFor`; lines 340-440 for `lineupLines`, `renderReviewStep`, `planningOwnerLine`; and the new `renderAvailabilityCard` from plan 03-01)
    - src/telegram/roster-renderers.ts (lines 39-78 — `plainMemberLabel`, `memberLabel`, `sortRosterMembers` and the collator tie-break)
    - src/telegram/keyboards.ts (lines 150-250 — the marker constants, the label width rule, `PLANNING_REVIEW_ROWS`, `planningControlRows`)
    - src/domain/planning/planning-service.ts (`availabilityStepProjection`, `availabilityOutcome`, `AvailabilityStepProjection` from plan 03-01; and lines 210-260 for the `DayMarker` one-marker-per-cell precedent)
    - tests/unit/planning-day-card.test.ts and tests/unit/planning-time-card.test.ts (the fixture-and-serialized-keyboard shape this new suite copies)
    - src/telegram/planning-handlers.ts (lines 40-160 — the exported refusal text constants and `planningNotAuthorText`)
    - .planning/phases/03-availability-and-booking-decision/03-CONTEXT.md (D-05, D-08, D-09, D-10)
  </read_first>
  <behavior>
    - A projection with three pending participants renders three lines, each with the pending marker, and a count line reading zero of three.
    - After one available and one unavailable answer, the same projection renders the same three lines in the same order with two markers changed and a count line reading two of three.
    - Two participants whose display labels compare equal under the collator render as two separate lines whose relative order is identical across two independent renders.
    - A single-participant projection renders one line, and once that participant answers the count line reads one of one.
    - The legend advertises only the markers present in the projection, in a fixed order that does not reshuffle.
    - `availabilityOutcome` returns the collecting state while any participant is pending, the all-available state when every participant is available, and the blocked state when all have answered and at least one is unavailable.
    - The blocked render states plainly that the slot does not work and contains no control row and no copy suggesting a replan action.
    - A booked projection renders with no control row at all.
    - Every exported refusal constant on the planning surface is at most 200 characters.
  </behavior>
  <action>
Complete `renderAvailabilityCard` in `src/telegram/planning-renderers.ts` and pin it with a new unit suite.

**Legend (D-08)** — add `PLANNING_AVAILABILITY_LEGEND` as a `Readonly<Record<ParticipantMarker, string>>` mapping each marker to its glyph plus a short word, and a fixed `LEGEND_ORDER`-style constant for the availability markers, copying all three pieces of the existing day-legend pattern: the record, the fixed order constant, and the "advertise only the markers actually used" filter over the set of markers present in the projection. The legend line sits above the list.

**Count line (AVAIL-04, D-09)** — one line above the participant list reading how many of how many have answered, built from `answeredCount` and `totalCount`. Render exactly one such line. Do not render a second line enumerating who is still missing: the marked list underneath already says that, and a named outstanding list is the chasing framing this product exists to remove — see this plan's `must_haves.prohibitions`.

**Participant lines (D-08, D-10)** — `sortRosterMembers(projection.participants)` mapped to a leading marker glyph plus `memberLabel(cell)`. Reuse both functions rather than sorting or formatting in the renderer: the collator plus the Telegram-id tie-break is what makes two equal labels render as two stable, distinct lines that never swap between renders, and `memberLabel` carries the masked-identity form and already escapes, so escaping again double-encodes. Names are plain labels; never emit a Telegram mention link or any anchor markup for a user, because the card is edited on every answer and a mention would re-notify the whole lineup each time.

**Outcome sentence (D-05)** — a closing sentence chosen from `availabilityOutcome`. The collecting state says answers are still coming in; the all-available state says everyone can make it; the blocked state says plainly that this slot does not work for the band. The blocked copy must not offer or imply a replan control — Phase 3 ships none, and a card that hints at a tap that does not exist is worse than one that says nothing. Keep the derivation in `availabilityOutcome`; the renderer chooses copy from its result and never re-derives the state.

**Controls** — pass the control lookup through `planningControlRows` unchanged. A projection whose `booked` flag is set is rendered by supplying no answer tokens, so the existing skip-an-unminted-control behavior drops the keyboard entirely. Do not add a disabled-button concept.

**Refusal length** — `answerCallbackQuery` text is capped at 200 characters by the Bot API, so every exported refusal constant, including one with an interpolated `plainMemberLabel`, must fit.

Create `tests/unit/planning-availability-card.test.ts` in the shape of `tests/unit/planning-day-card.test.ts`: build `AvailabilityStepProjection` fixtures by hand (this is why `owner` is optional on the type), assert on the rendered text and on the *serialized* keyboard shape, and cover every behavior listed above. Include an assertion that iterates every exported refusal constant on the planning surface and holds each to the 200-character ceiling, and an assertion that two renders of the same projection with different answer arrival orders produce byte-identical participant line ordering.
  </action>
  <verify>
    <automated>npx vitest run --project unit tests/unit/planning-availability-card.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PLANNING_AVAILABILITY_LEGEND' src/telegram/planning-renderers.ts` is at least 2 (definition plus use)
    - `grep -c 'sortRosterMembers' src/telegram/planning-renderers.ts` is at least 1 and `grep -c '\.sort(' src/telegram/planning-renderers.ts` is 0 (ordering is never re-derived in the renderer)
    - `grep -c 'tg://user' src/telegram/planning-renderers.ts` is 0
    - `grep -c 'availabilityOutcome' src/telegram/planning-renderers.ts` is at most 1 — the renderer consumes the derived value, it does not re-derive it
    - `test -f tests/unit/planning-availability-card.test.ts`
    - Behavior: a three-participant projection with zero answers renders exactly one count line and three marker lines; with two answers it renders the same three lines in the same order
    - Behavior: two equal-label participants render as two lines whose relative order is identical across two renders
    - Behavior: the blocked-outcome render contains no control row
    - Behavior: every exported planning refusal constant is at most 200 characters
    - `npx vitest run --project unit tests/unit/planning-availability-card.test.ts` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The availability card shows every participant as pending, available or unavailable with a legend and a single completion count line, in an order that never reshuffles, with one derived outcome sentence that offers no replan control, and the whole surface is pinned by a new unit suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Snapshot-membership refusal, changeable answers, and the bounded log vocabulary</name>
  <files>src/domain/planning/planning-service.ts, src/telegram/planning-handlers.ts, tests/unit/planning-logging.test.ts, tests/integration/planning-availability.test.ts</files>
  <reversibility rating="one-way">D-06 makes the confirm-time snapshot authoritative for the round's whole life; Phase 4's AVAIL-06 and Phase 5's follow-up targeting both read this rule, so changing it later changes who a booked round was ever asking. Recorded per 03-CONTEXT.md D-06, where the owner already chose it.</reversibility>
  <read_first>
    - src/domain/planning/planning-service.ts (`answerAvailability` from plan 03-01, and lines 1751-1800 for the compare-and-set idiom it copies)
    - src/telegram/planning-handlers.ts (lines 160-360 for `PLANNING_CATCH_SITES`, `PLANNING_OUTCOMES`, `PLANNING_REASONS`; 392-450 for `logPlanning` and `refuseNonAuthor`; 535-560 for `isNotModified`; the `dispatchAvailabilityAnswer` branch from plan 03-01)
    - src/shared/logger.ts (lines 21-50 — the redaction allow list; there is no participant-identity field on it)
    - tests/unit/planning-logging.test.ts (the `BRANCHES` array around line 488 and the uniqueness/exactly-one-line assertions around lines 848-879)
    - tests/integration/planning-availability.test.ts (the suite created by plan 03-01)
    - src/telegram/callbacks.ts (lines 400-440 — what the boundary checks and what it does not)
    - .planning/phases/03-availability-and-booking-decision/03-RESEARCH.md (Pitfall 4, Pitfall 8, Pitfall 9, Pitfall 10, Pitfall 11)
  </read_first>
  <behavior>
    - A participant who answered available and then taps the other control has their row overwritten to unavailable, with `answeredAt` moved forward, and the card re-rendered.
    - The same participant re-tapping the control they already used affects zero rows, receives the established already-applied private alert, and produces no second durable transition and no anchor edit.
    - A current chat member with no `PlanningParticipant` row for the round receives a private alert naming the reason, no card edit occurs, and the answer token stays unconsumed.
    - A participant whose `ChatMembership` was deactivated after the snapshot still answers successfully and still counts toward the completion denominator.
    - A cannot-attend answer leaves the round status unchanged and the remaining participants still able to answer.
    - A Telegram error carrying the flood-control status code is classified at the delivery catch site and absorbed; the handler does not retry in place and does not surface an exception.
    - Every new dispatch branch emits exactly one log line whose outcome-and-reason pair is unique across the whole enumeration.
  </behavior>
  <action>
Complete the answer path's refusal and no-op branches and make them observable.

**Handler branches** — in `dispatchAvailabilityAnswer` (`src/telegram/planning-handlers.ts`), give every member of the `AnswerResult` union its own branch, in the shape of `dispatchConfirm`: one `logPlanning` call and one `answerCallbackQuery` call per branch, from the branch that owns the outcome, never up front, with `show_alert: true`, and a trailing unguarded stale fallthrough.
  - `not-a-participant` → log the snapshot-refusal outcome and answer with `PLANNING_NOT_A_PARTICIPANT`. Perform NO `editMessageText`: nothing durable changed, and re-rendering would spend the round's card on a stranger's mistake. The alert names why the tap was refused and must not name, count, or otherwise describe the lineup — see this plan's privacy prohibition. It must not route the user to roster management; that is Phase 1's surface.
  - `duplicate` → log the already-recorded reason and answer with the established already-applied text. No edit.
  - `already-booked` → log the booked reason and answer with `PLANNING_ALREADY_BOOKED`.
  - `stale` and `failed` → the existing stale text and save-failed text, with `failed` routed through `logPlanningFailure` at `PLANNING_CATCH_SITES.answer` so the caught value is bound under the key the redactor renders structurally.

**Flood classification** — add an `isFloodControl(error)` predicate beside `isNotModified`, narrowing on a `GrammyError` whose `error_code` is 429, and use it inside `editAnchor`'s catch to select a distinct bounded reason at `PLANNING_CATCH_SITES.delivery` rather than to retry. The card is re-rendered by the next answer anyway, so a dropped edit self-heals; a retry loop inside a chat-sequentialized handler would block every other update for that chat. Do not install a retry plugin — this phase adds no npm root.

**Bounded vocabulary** — every branch added here needs its own `PLANNING_REASONS` entry so two dead buttons never look alike to an operator, and its own `PLANNING_OUTCOMES` entry where the outcome is genuinely new. Add at least: the flood reason for the delivery catch site, and confirm that `answer-recorded-for-participant`, `answer-already-recorded`, `actor-not-in-participant-snapshot`, `round-already-booked-at-tap`, `answer-target-no-longer-actionable` and `answer-transaction-failed` are all present from plan 03-01. Express diagnostics only in allow-listed fields; there is no participant-identity field on the redactor's allow list, and `actorId` already carries the tapping user, so do not add one.

**Logging gate** — extend the `BRANCHES` enumeration in `tests/unit/planning-logging.test.ts` with one entry per new branch, driving each for real. The gate requires every outcome-and-reason pair to be globally unique and every branch to emit exactly one matching line, so a new branch that is not enumerated silently stops being covered.

**Integration coverage** — extend `tests/integration/planning-availability.test.ts` with the behaviors above: the overwrite, the idempotent re-tap, the non-participant refusal with an unconsumed token and an untouched card, the roster-removed-but-still-in-snapshot participant answering successfully and counting toward completion (D-06 — this is the reconciliation of the fresh-permission rule for this surface: the permission revalidated at the boundary is snapshot membership read fresh from `PlanningParticipant`, never live roster membership), and a cannot-attend answer leaving the round open (D-05).
  </action>
  <verify>
    <automated>npx vitest run --project integration tests/integration/planning-availability.test.ts &amp;&amp; npx vitest run --project unit tests/unit/planning-logging.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'isFloodControl' src/telegram/planning-handlers.ts` is at least 2 (definition plus use)
    - `grep -c 'actor-not-in-participant-snapshot' src/telegram/planning-handlers.ts` is at least 2 (vocabulary entry plus the branch that emits it)
    - `grep -c 'answer-already-recorded' src/telegram/planning-handlers.ts` is at least 2
    - `grep -c 'PLANNING_CATCH_SITES.answer' src/telegram/planning-handlers.ts` is at least 1
    - `grep -c 'telegramUserId\|participantId' src/telegram/planning-handlers.ts` is 0 inside `logPlanning` call sites — diagnostics use only allow-listed fields
    - Behavior: a non-participant tap leaves `callback_actions.consumed_at` null for both answer rows and produces no `editMessageText` call
    - Behavior: a participant whose membership was deactivated after the snapshot still records an answer and still appears in the completion denominator
    - Behavior: re-tapping the same control affects zero participant rows and triggers no anchor edit
    - `npx vitest run --project unit tests/unit/planning-logging.test.ts` exits 0 with no "indistinguishable" failure
    - `npx vitest run --project integration tests/integration/planning-availability.test.ts` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Every included participant can answer and change their answer, a user outside the confirm-time snapshot is refused privately with no card edit and no token spent, a roster-removed snapshot member still answers and still counts, and every new branch emits exactly one distinguishable bounded log line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram `callback_query` → callback boundary | Any current chat member can present any token |
| Callback boundary → planning dispatcher | Chat membership established; snapshot membership is not |
| Refusal alert → the group chat | A refusal must stay private to the tapper |
| Card text → chat history | Member identities are rendered into a persistent group message |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Elevation of privilege | `dispatchAvailabilityAnswer` non-participant branch | high | mitigate | The `not-a-participant` result is produced by a transactional `PlanningParticipant` read and re-asserted by the update's `WHERE` clause; the branch refuses without editing the card or consuming the token (AVAIL-03, ASVS V4) |
| T-03-10 | Information disclosure | non-participant refusal alert | high | mitigate | The alert is an `answerCallbackQuery` (private to the tapper) whose text names only the reason — never a member label, a lineup size, or a numeric Telegram id (ASVS V8) |
| T-03-11 | Information disclosure | availability card participant lines | high | mitigate | `memberLabel` is the only identity renderer on the card, carrying the masked-id fallback and the single escaper; no mention link or anchor markup is emitted (threat T-01-21, ASVS V8) |
| T-03-12 | Tampering | repeated identical answer taps | medium | mitigate | The nullable-safe `OR` compare-and-set reports zero affected rows on a re-tap; the branch answers already-applied with no second transition (RELI-02) |
| T-03-13 | Denial of service | burst of answers driving Telegram flood control | medium | mitigate | `isFloodControl` classifies the 429 at the delivery catch site and absorbs it; the next answer re-renders. No in-handler retry inside the chat-sequentialized path |
| T-03-14 | Repudiation | an unenumerated new branch emitting no distinguishable line | medium | mitigate | The `BRANCHES` gate requires globally unique outcome-and-reason pairs and exactly one line per branch; every new branch is added to the enumeration in the same task (ASVS V7) |
| T-03-15 | Information disclosure | new diagnostic log fields | medium | mitigate | Only allow-listed fields are emitted; a field outside the allow list is silently redacted, so no participant-identity field is introduced (ASVS V7) |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed by this phase; no install task exists |
</threat_model>

<verification>
- `npx vitest run --project unit tests/unit/planning-availability-card.test.ts` passes.
- `npx vitest run --project unit tests/unit/planning-logging.test.ts` passes with no indistinguishable-branch failure.
- `npx vitest run --project integration tests/integration/planning-availability.test.ts` passes.
- `npm run test:unit && npm run test:integration && npm run typecheck && npm run lint` all pass.
</verification>

<success_criteria>
- AVAIL-03 is enforced by durable state, not by the callback boundary alone.
- AVAIL-04 is complete: markers, legend, count line, stable ordering, and one derived outcome.
- D-04 through D-10 are each pinned by at least one assertion.
- No new branch is invisible to an operator.
</success_criteria>

<output>
Create `.planning/phases/03-availability-and-booking-decision/03-02-SUMMARY.md` when done
</output>
