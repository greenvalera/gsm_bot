---
phase: 03-availability-and-booking-decision
plan: 08
type: execute
wave: 8
gap_closure: true
depends_on:
  - 03-06
  - 03-07
files_modified:
  - src/domain/planning/planning-service.ts
  - src/telegram/planning-handlers.ts
  - tests/unit/planning-logging.test.ts
  - tests/integration/planning-availability.test.ts
  - tests/integration/planning-recovery.test.ts
autonomous: true
requirements:
  - AVAIL-02
  - AVAIL-07
  - LIFE-01
estimate:
  tokens: 86000
  raw_tokens: 86000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every live ready-to-book announcement is retractable and closable: there is no fault on the announcement path that can leave a message in the chat asserting the rehearsal is ready to book while no code path can ever edit it (gap G-03)."
    - "When the round's pointer to a just-sent announcement cannot be recorded, the claim on `readyAnnouncedAt` is released back to the value it held before that claim, so the next answer re-announces into a message the round can address — and the release is a compare-and-set that fires only while the claim it is releasing is still the newest one."
    - "An announcement whose pointer could not be recorded is stripped of its markup before the handler returns, so an unaddressable message is at least not an actionable one."
    - "A round never has two live announcements, including through the fault window: after a failed pointer write, the message left in the chat carries no control and the round's next announcement is the only one anybody can act on (03-04 truth 10, now true in the fault case too)."
    - "The retract-or-edit decision `claimAnnouncement` makes for a refused claim is derived from a read taken inside the answer transaction, not from the round snapshot captured before the claim, so an answer landing in the send-to-record window sees the pointer that actually exists."
    - "An in-place edit that changes nothing reports the same result whether or not the process happened to have the render cached: a Telegram not-modified response and a fingerprint cache hit are both `unchanged` (review finding WR-05)."
    - "Because of that, the already-applied acknowledgement a repeat tap receives and the announcement log line an operator reads no longer depend on how many other chats have been active or on how recently the process restarted."
    - statement: "No branch on the planning surface logs a synthesised exception to describe a non-exception outcome, so `err` in a planning failure line always means something actually threw."
      verification: backstop
  artifacts:
    - src/domain/planning/planning-service.ts
    - src/telegram/planning-handlers.ts
    - tests/integration/planning-availability.test.ts
  key_links:
    - "dispatchAnnouncement post branch -> recordAnnouncement failure -> releaseAnnouncementClaim compare-and-set -> clearSupersededCard on the orphan -> the next answer re-announces"
    - "repostAnchor announcement slot -> reanchorAnnouncement failure -> the same release -> the same orphan strip"
    - "claimAnnouncement refused branch -> a read taken inside the answer transaction -> retract or edit or none"
    - "editRoundMessage not-modified catch -> unchanged -> editAnchor already-applied alert AND dispatchAnnouncement silence, identically with a cold cache"
    - "logPlanningFailure err binding -> reserved for thrown errors -> a bounded reason carries every non-exception failure"
  prohibitions:
    - requirement_id: AVAIL-07
      category: security
      status: resolved
      verification: judgment
      statement: "MUST NOT release the announcement claim on a Telegram SEND failure. The claim is durable before the send precisely so an outage cannot be replayed into a second notification (T-03-25); only a failure to record the POINTER of a message that did land may release it, and only back to the value the claim replaced."
    - requirement_id: AVAIL-07
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT clear `readyAnnouncedAt` to null unconditionally as the release. A round that had announced before must be restored to its previous timestamp, or a re-announce path would gain a fresh window it never earned."
    - requirement_id: AVAIL-07
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT post a compensating message, an apology, or a retry announcement on the failed-pointer path. D-03's rule governs the whole surface: a failed Telegram interaction never produces a second message."
    - requirement_id: AVAIL-02
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT make the render fingerprint cache authoritative for anything a user or an operator observes. `LAST_RENDER` is process memory, capped, shared across every chat and evicted first-in-first-out; it may save a request and may never decide a message, an alert, or a log line."
    - requirement_id: LIFE-01
      category: values
      status: resolved
      verification: judgment
      statement: "MUST NOT change what booking closes. The orphan strip removes an unaddressable message's markup; it does not book, retract, or re-open a round, and the D-16 closing edits still address `announcementMessageId` alone."
---

<objective>
Close the fault window in which a ready-to-book announcement becomes permanently unretractable, and stop a process-memory cache deciding what a user and an operator observe.

Purpose: `recordAnnouncement` is a post-send write with no compensation. When it fails, the message is live in the chat carrying a Mark-as-booked control while `announcementMessageId` stays null — and every correction path in the phase (`claimAnnouncement`'s retract branch, `retractStaleAnnouncement`, `closeBookedRound`, `dispatchAnnouncement`'s edit and retract branches) is guarded on that column being non-null. The orphan then goes on asserting that the rehearsal is ready to book after unanimity is lost and after the round is booked, and a post-cooldown re-announcement cannot clear it because the superseded pointer is null — producing the two-live-announcements state `03-04-PLAN.md` truth 10 forbids. No test covers the window (gap G-03). Separately, `editRoundMessage` reports a Telegram not-modified response as `edited`, so whether a repeat tapper sees the already-applied alert and whether an operator sees an unchanged-anchor line depend on a 128-entry process-global cache shared by every chat (review finding WR-05).
Output: `releaseAnnouncementClaim` on `PlanningService`, both claiming paths compensating a failed pointer write, a transaction-local read behind the retract-or-edit decision, an honest `unchanged` result, and the fault-injection cases that would have caught G-03.
</objective>

<execution_context>
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/workflows/execute-plan.md
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-availability-and-booking-decision/03-CONTEXT.md
@.planning/phases/03-availability-and-booking-decision/03-PATTERNS.md
@.planning/phases/03-availability-and-booking-decision/03-VERIFICATION.md
@.planning/phases/03-availability-and-booking-decision/03-REVIEW.md
@.planning/phases/03-availability-and-booking-decision/03-04-SUMMARY.md
@.planning/phases/03-availability-and-booking-decision/03-06-PLAN.md
</context>

## Artifacts this phase produces

Additions to the canonical list in `03-01-PLAN.md` → "Artifacts this phase produces":

**`src/domain/planning/planning-service.ts`**
- `releaseAnnouncementClaim` and its result union — the compensation for a claim whose message could not be pointed at
- `recordAnnouncement` loses its unused clock parameter
- `claimAnnouncement`'s refused branch reads the round inside the transaction rather than from the caller's snapshot

**`src/telegram/planning-handlers.ts`**
- a bounded-reason failure logger for non-exception failures (suggested name `logPlanningNotRecorded`), replacing the three synthesised `Error` constructions
- `editRoundMessage`'s not-modified branch reports `unchanged`
- `PLANNING_REASONS` additions for the released claim and for the stripped orphan

No new column, no migration, no npm root.

## Recorded decisions

**D-26 — the release restores the previous value under a compare-and-set; it does not null the column.**
D-18 says `readyAnnouncedAt` is never nulled and that the passage of the window is the release. That rule is about the ordinary lifecycle and it stands. This is a compensation for a claim that provably produced no addressable announcement, so the correct restoration is the value the claim replaced — null for a round's first announcement, the previous timestamp for a re-announce — carried on the round snapshot the answer transaction already returned. The write is guarded on the round still carrying the exact instant this dispatch claimed, so a claim won by somebody else in the meantime is never released by us. Reversibility: **reversible**.

**D-27 — the release is owned by the dispatch that made the claim, not by the service's write method.**
`recordAnnouncement` answers `stale` as well as `failed`, and only the caller knows which claim instant it is compensating. Putting the release inside the write method would need the pre-claim value threaded in anyway and would hide a durable write inside a method named for a different one. Reversibility: **reversible**.

**D-28 — a Telegram not-modified response is `unchanged`, and the fingerprint cache is demoted to a request-saver.**
The two edit call sites branch on the result in opposite directions, so the cache deciding the result made both branches non-deterministic. After this change the cache can only skip a request; it can never change an answer. `03-04-PLAN.md`'s statement that a not-modified is absorbed is unchanged — what changes is what the absorption is called. Reversibility: **reversible**.

**D-29 — `recordAnnouncement` drops its unused clock parameter rather than acquiring a write for it.**
IN-02 offered both. The parameter's stated justification was that a future timestamp column should not have to change callers' shape; `releaseAnnouncementClaim` now demonstrates the clocked write on this very path, so the justification no longer holds and the honest form is the smaller one. Reversibility: **reversible** — re-adding a parameter is a signature edit.

## Deferred review findings

None deferred by this plan. It closes `03-REVIEW.md` WR-03 (as gap G-03), WR-05, IN-02 and IN-03. WR-04 and WR-07 with IN-01 are closed by `03-09-PLAN.md`. Nothing from the review or the verification report is carried.

## Probe edges

None authored or re-authored here. `03-07-PLAN.md` re-authored the one ordering edge a gap invalidated; G-03 and WR-05 are fault-path and observability defects that change no shipped edge resolution.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The release, and a retract decision read inside the transaction</name>
  <files>src/domain/planning/planning-service.ts</files>
  <reversibility rating="reversible">One new guarded `updateMany`, one read moved inside a transaction that was already open, and one unused parameter removed. No column, no migration.</reversibility>
  <read_first>
    - src/domain/planning/planning-service.ts (lines 2224-2274 — `claimAnnouncement`: the unanimity branch that decides retract-or-none from `round.readyAnnouncedAt` and `round.announcementMessageId`, the compare-and-set, and the refused-claim branch that decides edit-or-none from the same pre-write snapshot; lines 2276-2311 — `recordAnnouncement`, its status-guarded `updateMany`, its `stale` and `failed` answers, and the doc paragraph justifying the unused clock parameter that D-29 retires; lines 2380-2445 — `answerAvailability`: where `round` is read at the top of the transaction and therefore why the snapshot handed to the surface predates the claim; lines 98-115 — `READY_ANNOUNCE_COOLDOWN_MS` and D-18's "the window IS the release", which D-26 is deliberately narrower than; lines 3192-3216 — `reanchorAnnouncement`, the other write that points a round at an announcement and the other place a claim can be stranded after `03-06-PLAN.md`)
    - .planning/phases/03-availability-and-booking-decision/03-06-PLAN.md ("Artifacts this phase produces" and Task 1 — `claimAnnouncementRepost` and the shared claim statement, so the release compensates both claiming paths and not just the answer path)
  </read_first>
  <behavior>
    - `releaseAnnouncementClaim` restores `readyAnnouncedAt` to the passed previous value when the round still carries the exact instant the caller claimed, and answers that it released.
    - It writes nothing and answers that it did not release when the round's `readyAnnouncedAt` has moved on — somebody else's claim is never released.
    - It writes nothing when the round already has a non-null `announcementMessageId` — a pointer that did land is not compensated.
    - It writes nothing for a round that is no longer confirmed.
    - It answers a failure rather than throwing, in the shape the sibling announcement writes use.
    - A refused claim on a round whose announcement pointer was written after the answer transaction's snapshot was taken still produces the edit directive, not none.
    - A lost unanimity on such a round produces the retract directive, not none.
    - `recordAnnouncement` behaves exactly as before with the clock parameter gone.
  </behavior>
  <action>
Give the announcement claim a compensation, and stop deciding corrections from a stale snapshot.

**`releaseAnnouncementClaim`.** Add a public method on `PlanningService` taking the round id, the instant the caller claimed, the value to restore, and returning a closed result union in the shape of `RecordAnnouncementResult` — released, refused, or failed with the error. It performs one `planningRound.updateMany` guarded on: the id; the confirmed status; `readyAnnouncedAt` equal to the claimed instant; and a null `announcementMessageId`. It writes `readyAnnouncedAt` back to the restore value and nothing else — no `revision` bump, for the same reason the claim does not bump it. It answers released when exactly one row was affected, refused otherwise, and failed on a throw.

Document it as a compensation and mark its boundaries plainly: it is reached only when a message that DID land could not be pointed at, never when the send itself failed — the claim is durable before the send precisely so a Telegram outage cannot be replayed into a second notification, and releasing on a send failure would hand an attacker an outage-shaped flood amplifier (T-03-25). Record D-26's reasoning: this is not D-18's "never nulled" being repealed, it is a claim that provably produced no addressable announcement being handed back, and the restore value is what the claim replaced rather than null.

**`recordAnnouncement`.** Remove the unused clock parameter and the doc paragraph that justified it, replacing that paragraph with a sentence naming `releaseAnnouncementClaim` as the clocked write this path now has. Update both call sites in the handler module (Task 2 owns the handler edits; if the compiler reaches them first, make the minimal signature-following change and leave the rest of Task 2's work alone).

**The transaction-local read.** In `claimAnnouncement`, take the round's current row inside the transaction — a `findUnique` on the id — and derive both the unanimity-lost branch and the refused-claim branch from that row's `readyAnnouncedAt` and `announcementMessageId` rather than from the `round` parameter's fields. The parameter stays as the source of the id and the caller's snapshot; only the two decisions move. Document why: an answer landing in the window between another dispatch's send and its pointer write reads a snapshot taken before that write, and deciding from it skips a retraction the chat can see is needed. Keep the method's existing structure, its `OR` claim shape and its comments otherwise intact.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run --project unit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*|/\*)' src/domain/planning/planning-service.ts | grep -c 'releaseAnnouncementClaim'` is at least 1 (the method definition)
    - `grep -vE '^\s*(//|\*|/\*)' src/domain/planning/planning-service.ts | grep -c 'async recordAnnouncement'` is exactly 1 and the declaration no longer carries a third clock parameter
    - Behavior: the release restores the previous value and answers released when the round still carries the claimed instant and has no announcement pointer
    - Behavior: the release writes nothing and answers refused when `readyAnnouncedAt` has moved, when `announcementMessageId` is non-null, or when the round is no longer confirmed
    - Behavior: the release answers a failure rather than throwing
    - Behavior: a refused claim on a round whose pointer landed after the answer transaction's snapshot still produces the edit directive; a lost unanimity on such a round produces the retract directive
    - `npm run typecheck` exits 0
    - `npx vitest run --project unit` exits 0 with all pre-existing unit cases passing
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>The service can hand back a claim whose message it could not address, and it decides corrections from a row read inside the transaction rather than from the caller's snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Both claiming paths compensate a failed pointer write, and the fault-injection cases that prove it</name>
  <files>src/telegram/planning-handlers.ts, tests/unit/planning-logging.test.ts, tests/integration/planning-availability.test.ts, tests/integration/planning-recovery.test.ts</files>
  <precondition>A Docker daemon is reachable — the integration project starts a PostgreSQL container through Testcontainers (`tests/helpers/postgres.ts`).</precondition>
  <reversibility rating="reversible">Two failure branches gain a compensating call and an existing markup-stripping call. Nothing durable is created and no message is deleted.</reversibility>
  <read_first>
    - src/telegram/planning-handlers.ts (lines 1773-1904 — `dispatchAnnouncement` end to end: the edit and retract branches guarded on a non-null pointer, the post branch's send, the `recordAnnouncement` call and the failure branch at lines 1874-1886 that logs and returns, and the superseded clear at the tail that is the call this task reuses; lines 1093-1124 — `clearSupersededCard`, the no-`reply_markup` edit, its own catch site, and why a failure here is absorbed; lines 1146-1245 — `repostAnchor` and the failed-re-anchor branch at 1218-1245 that already strips the new card's markup, which is the precedent and, after `03-06-PLAN.md`, the second place a claim can be stranded; lines 2203-2229 — `retractStaleAnnouncement`; lines 2533-2566 — `closeBookedRound`; both guarded on the pointer, which is why an orphan is permanent; lines 625-645 — `logPlanningFailure` and its `err` binding; lines 261-330 — `PLANNING_CATCH_SITES`, including the announcement and announcement-record sites; lines 459-623 — `PLANNING_REASONS`)
    - tests/integration/planning-availability.test.ts (lines 840-885 — "keeps the claim when the announcement send fails", the T-03-25 case whose behavior must NOT change and which the new cases sit beside; lines 925-1007 — the retraction and in-cooldown edit cases the orphan would otherwise break; lines 1008-1051 — the post-cooldown re-announce and its superseded clear)
    - tests/integration/planning-recovery.test.ts (lines 1600-1676 — the `vi.spyOn(PlanningService.prototype, ...)` idiom this task uses for fault injection)
    - tests/unit/planning-logging.test.ts (lines 694-730 — the `BRANCHES` table; lines 1400-1500 — the distinctness gates; lines 1545-1580 — the failure-branch gate)
  </read_first>
  <behavior>
    - When `recordAnnouncement` answers failed, the handler releases the claim back to the round's pre-claim value, strips the just-sent message's markup, logs at the announcement-record catch site with a bounded reason, and posts nothing.
    - When `recordAnnouncement` answers stale, the same compensation runs — the message is equally unaddressable.
    - After that fault, the next answer that re-achieves unanimity posts a fresh announcement and records its pointer, because the window was handed back.
    - The orphaned message carries no control: an attempt to press what was on it is refused at the callback boundary, and the round is never booked through it.
    - The round never ends up with two live announcements: after the fault and the subsequent re-announcement, exactly one message in the chat carries the booking control.
    - When `reanchorAnnouncement` fails after a `/plan_status` re-announcement won its claim, the same release and the same strip run.
    - The send-failure case is unchanged: the claim is kept, nothing is released, nothing compensating is posted.
    - No planning failure line carries a synthesised exception; the three former synthetic sites log a bounded reason instead.
  </behavior>
  <action>
Make both claiming paths clean up after themselves, and prove it with injected faults.

**`dispatchAnnouncement`.** In the branch reached when the pointer could not be recorded — for both the failed and the stale answers, which are equally unaddressable — do three things before returning, in this order:
  1. Release: call `deps.planning.releaseAnnouncementClaim` with the round id, the clock instant this dispatch claimed with, and the round snapshot's own `readyAnnouncedAt`, which is the pre-claim value because the answer transaction read the round before the claim ran. Absorb whatever it answers; a refused release means somebody else's claim is live and must not be touched.
  2. Strip: call the existing `clearSupersededCard` with the id of the message just sent and the card just rendered, so the orphan keeps its text and loses its buttons. This is the same call `repostAnchor` already makes for its own failed re-anchor, for the same reason.
  3. Log once, at the announcement-record catch site, through the bounded-reason logger Task 3 introduces — no synthesised exception. Add a `PLANNING_REASONS` member naming the released claim so an operator can see the compensation happened, distinct from the existing announcement-not-recorded reason if the two branches would otherwise be indistinguishable.

Post nothing. D-03's rule governs the whole surface.

**`repostAnchor`.** After `03-06-PLAN.md`, the announcement slot wins a claim before it posts, so a failed `reanchorAnnouncement` strands one too. In the failed-re-anchor branch, when the slot is the announcement slot, release the claim the same way before the existing markup strip. The anchor slot keeps its current behavior exactly — it makes no announcement claim and has nothing to release. Thread whatever the caller needs for the pre-claim value; the status handler has the round snapshot in hand.

**Unit gate.** Add `BRANCHES` entries for the new reasons so the distinctness gates cover them, and keep every existing entry unchanged.

**Integration — `tests/integration/planning-availability.test.ts`.** Beside the existing send-failure case, add the fault-injection cases G-03's `missing:` names, using `vi.spyOn(PlanningService.prototype, "recordAnnouncement")` to answer failed once (and stale once, in a second case):
  - the round's `readyAnnouncedAt` is back to its pre-claim value and `announcementMessageId` is still null;
  - exactly one edit was aimed at the just-sent message and it carries no `reply_markup`;
  - exactly one failure line at the announcement-record catch site, with the bounded reason and no synthesised error;
  - no second message was sent;
  - then, immediately afterwards on the same clock, a participant flips to cannot-attend and back to can-attend: assert a fresh announcement is posted and its pointer recorded, and that exactly one message in the chat now carries the booking control.
  - a case that presses the orphan's control token after the fault and asserts the round's status is unchanged.
  - assert the pre-existing send-failure case still passes with the claim kept — the two faults must stay distinguishable.

**Integration — `tests/integration/planning-recovery.test.ts`.** Add the mirrored case for the status path: spy `reanchorAnnouncement` to answer stale after a `/plan_status` re-announcement that won its claim, and assert the claim is released, the just-posted message is stripped, and the next answer-path unanimity can announce.
  </action>
  <verify>
    <automated>npx vitest run --project integration tests/integration/planning-availability.test.ts tests/integration/planning-recovery.test.ts &amp;&amp; npx vitest run --project unit tests/unit/planning-logging.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*|/\*)' src/telegram/planning-handlers.ts | grep -c 'releaseAnnouncementClaim'` is at least 2 — the answer path and the status re-post path
    - `grep -vE '^\s*(//|\*|/\*)' src/telegram/planning-handlers.ts | grep -c 'clearSupersededCard'` is at least 6 — the definition, the four pre-existing call sites (the count is 5 before this task), and the new orphan strip in the failed-record branch
    - Behavior: with `recordAnnouncement` answering failed, `readyAnnouncedAt` returns to its pre-claim value, `announcementMessageId` stays null, the sent message receives exactly one edit carrying no `reply_markup`, and no second message is sent
    - Behavior: the same with `recordAnnouncement` answering stale
    - Behavior: after that fault, the next re-achieved unanimity posts a fresh announcement and records its pointer, and exactly one message in the chat carries the booking control
    - Behavior: pressing the orphan's control token after the fault leaves the round's status unchanged
    - Behavior: with `reanchorAnnouncement` answering stale after a `/plan_status` re-announcement, the claim is released and the just-posted message is stripped
    - Behavior: the pre-existing send-failure case still keeps the claim, releases nothing, and posts nothing compensating
    - `npx vitest run --project unit tests/unit/planning-logging.test.ts` exits 0 with no indistinguishable-branch failure
    - `npx vitest run --project integration tests/integration/planning-availability.test.ts tests/integration/planning-recovery.test.ts` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>There is no fault on the announcement path that leaves a permanently unretractable "Ready to book" message in the chat, and the window in which one could exist has a test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: An honest unchanged result, and no synthesised exceptions in the logs</name>
  <files>src/telegram/planning-handlers.ts, tests/unit/planning-logging.test.ts, tests/integration/planning-availability.test.ts</files>
  <reversibility rating="reversible">One return value in a catch branch, and one logging helper replacing three synthesised error constructions. Both are local and both are covered by existing gates.</reversibility>
  <read_first>
    - src/telegram/planning-handlers.ts (lines 911-947 — `LAST_RENDER`, `LAST_RENDER_LIMIT` and `rememberRender`, including that it re-sets an existing key without moving it, so eviction is first-in-first-out and not least-recently-used; lines 949-1006 — `editRoundMessage`: the fingerprint hit that returns unchanged, the success path, and the not-modified catch that today returns edited; lines 1007-1038 — `editAnchor`, which emits the anchor-unchanged line and the already-applied alert only on unchanged; lines 1786-1826 — `dispatchAnnouncement`'s edit branch, which logs only on edited and is therefore the opposite-direction call site; lines 1226, 1385 and 1881 — the three synthesised `Error` constructions IN-03 names; lines 625-659 — `logPlanningFailure` and the redactor's `err` binding)
    - tests/unit/planning-logging.test.ts (lines 694-730 — the `BRANCHES` shape; lines 1400-1500 — the distinctness gates; lines 1545-1600 — the failure-branch gate that reads the `err` binding)
    - tests/integration/planning-availability.test.ts (lines 1081-1102 — "edits the announcement once for a repeat that leaves the outcome unchanged", the case whose meaning changes when a not-modified becomes unchanged; lines 1103-1148 — the absorbed-rejection case)
  </read_first>
  <behavior>
    - A repeat tap whose render is byte-identical receives the already-applied alert and emits the anchor-unchanged line, whether or not the process had the render cached.
    - The announcement's edit branch stays silent for a repeat that changes nothing, whether or not the process had the render cached.
    - A genuine content change still reports edited from both a warm and a cold cache.
    - A rejection that is not a not-modified still reports failed and still logs at the caller's site, with flood control keeping its own site.
    - The cache is still populated on both the success path and the not-modified path, so the request-saving benefit is retained.
    - No planning failure line carries an `err` whose value did not come from something that threw.
  </behavior>
  <action>
Take the two observable behaviors off the cache, and take the fake exceptions out of the logs.

**WR-05.** In `editRoundMessage`'s catch branch, when the error is a not-modified, remember the render and return `unchanged` rather than `edited`. Nothing else in the function moves. Rewrite the `LAST_RENDER` doc comment to say what is now true: the cache saves a request and never decides an answer, and a cache miss on an identical card takes the longer road to the same result. Add a sentence to `editRoundMessage`'s own doc comment recording that its three results are now derived from what durably happened at Telegram rather than partly from process memory, and naming both call sites so a future reader can see that they branch in opposite directions and why that made the old shape non-deterministic.

Check the two call sites for consequential changes and make none beyond what the contract change implies: `editAnchor` will now emit the anchor-unchanged line and the already-applied alert on a cold cache as well as a warm one, which is the intent; `dispatchAnnouncement`'s edit branch will now stay silent on a cold cache as well as a warm one, which is also the intent. Do not add a compensating log line to either.

**IN-03.** Add a failure logger for non-exception outcomes (suggested name `logPlanningNotRecorded`) taking the same deps, catch site, route and context as `logPlanningFailure` plus a bounded reason, emitting the same event and level with the site's outcome and that reason, and no `err` key at all. Replace all three synthesised-`Error` sites with it, keeping the thrown-error path on `logPlanningFailure` in each: the `repostAnchor` failed-re-anchor branch, the `/plan` handler's failed-anchor branch, and `dispatchAnnouncement`'s failed-record branch. Where a branch needs a reason that does not exist yet, add it to `PLANNING_REASONS` with a doc comment; reuse an existing one where it already says the right thing. Document on the new logger why it exists: `err` in a planning line must mean something threw, or `err.name` is meaningless for the lines that did not.

**Gates and tests.** Extend `tests/unit/planning-logging.test.ts` so the failure-branch gate asserts that a non-exception failure line carries a bounded reason and no `err`, and add or adjust `BRANCHES` entries so the three converted sites stay distinguishable. In `tests/integration/planning-availability.test.ts`, add a cold-cache case: clear or bypass the render cache between two identical renders and assert the second still produces the already-applied alert on the anchor path and still produces no announcement line on the announcement path — the case that fails with today's shape.
  </action>
  <verify>
    <automated>npx vitest run --project unit tests/unit/planning-logging.test.ts &amp;&amp; npx vitest run --project integration tests/integration/planning-availability.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*|/\*)' src/telegram/planning-handlers.ts | grep -c 'return "edited";'` is exactly 1 — only the successful-edit path reports an edit
    - `grep -vE '^\s*(//|\*|/\*)' src/telegram/planning-handlers.ts | grep -c 'return "unchanged";'` is at least 2 — the fingerprint hit and the not-modified absorption
    - `grep -cF "new Error(" src/telegram/planning-handlers.ts` is 0 — all three throwaway exception constructions are gone and no branch fabricates one to reach a log binding (the count is 3 before this task)
    - `grep -vE '^\s*(//|\*|/\*)' src/telegram/planning-handlers.ts | grep -c 'logPlanningNotRecorded'` is at least 4 (the definition and the three converted sites)
    - Behavior: a repeat tap with a cold render cache still receives the already-applied alert and still emits the anchor-unchanged line
    - Behavior: the announcement edit branch with a cold render cache still stays silent for a repeat that changes nothing
    - Behavior: a genuine content change still reports an edit from both a warm and a cold cache
    - Behavior: a rejection that is not a not-modified still reports failed at the caller's catch site, and flood control still uses its own site
    - Behavior: no planning failure line carries an `err` that did not come from a thrown value
    - `npx vitest run --project unit tests/unit/planning-logging.test.ts` exits 0
    - `npx vitest run --project integration tests/integration/planning-availability.test.ts` exits 0
    - `npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>What a tapper is told and what an operator reads are decided by what durably happened, not by how many other chats have been active since the process started.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram send → the round's pointer write | The message exists in the chat before the round can address it; that gap is a fault window, not an instant |
| An orphaned message → the group | A message asserting a rehearsal is ready to book, carrying a live control, that no correction path can reach |
| Process memory → user-visible behavior | A capped, shared, first-in-first-out cache is not a source of truth for anything anybody observes |
| Log `err` binding → an operator's conclusions | A synthesised stack invites a diagnosis of an exception that never occurred |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-51 | Spoofing | an unretractable ready-to-book announcement | high | mitigate | The claim is released and the orphan's markup stripped when the pointer cannot be recorded, so no message can go on asserting the rehearsal is ready to book after unanimity is lost or after the round is booked (gap G-03, 03-04 truth 10) |
| T-03-52 | Elevation of privilege | a live booking control on an unaddressable message | high | mitigate | The orphan is stripped of markup before the handler returns, so the control cannot be pressed; the callback boundary refuses a token whose message the round no longer renders |
| T-03-53 | Tampering | releasing a claim somebody else holds | high | mitigate | The release is a compare-and-set guarded on the round still carrying the exact instant this dispatch claimed and on a null pointer; a claim won in the meantime is never released (D-26) |
| T-03-54 | Denial of service | releasing on a Telegram send failure | high | mitigate | The release is reachable only from the pointer-write failure branch. A send failure keeps the claim, exactly as T-03-25 requires, so an outage cannot be replayed into a second notification |
| T-03-55 | Tampering | a correction decided from a pre-write snapshot | medium | mitigate | `claimAnnouncement` reads the round inside the answer transaction for its retract-or-edit decision, so an answer landing in the send-to-record window sees the pointer that actually exists |
| T-03-56 | Repudiation | non-determinism in the acknowledgement and the log line | medium | mitigate | A not-modified response reports unchanged, so both edit call sites behave identically with a warm and a cold cache; the render cache is demoted to a request-saver (WR-05, D-28) |
| T-03-57 | Repudiation | synthesised exceptions in failure lines | low | mitigate | Non-exception failures log a bounded reason and no `err`, so `err` in a planning line always means something threw (IN-03, ASVS V7) |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package is installed by this plan (`03-RESEARCH.md` → Package Legitimacy Audit: "Packages installed by this phase: none"); no install task exists |
</threat_model>

<verification>
- `npx vitest run --project integration tests/integration/planning-availability.test.ts tests/integration/planning-recovery.test.ts tests/integration/planning-booking.test.ts` passes, including the fault-injection cases.
- `npx vitest run --project unit tests/unit/planning-logging.test.ts` passes with the distinctness and failure-branch gates covering the new reasons.
- `npm run test:unit && npm run test:integration && npm run typecheck && npm run lint` all pass.
- Every correction path guarded on `announcementMessageId` is now reachable for every announcement that was ever sent, because a pointer that could not be written releases the claim instead of stranding it.

## Capability contributions

- **Security:** active (ASVS level 1, block on high). T-03-51 through T-03-54 are gap G-03 and are mitigated.
- **Schema gate:** NOT APPLICABLE — no schema file in `files_modified`, no migration, no column.
- **API coverage:** already satisfied by the phase's `COVERAGE.md` declaration; this plan adds no external service, SDK or provider.
- **Assumption delta:** resolved for the whole gap-closure set in `03-06-PLAN.md`. This plan introduces no new seam — it narrows an existing one.
</verification>

<success_criteria>
- Gap G-03 is closed: no fault on the announcement path can leave a live, actionable, unretractable ready-to-book message in the chat.
- The fault window has a test, so a future change that reopens it goes red.
- Review finding WR-05 is closed: the already-applied alert and the announcement log lines no longer depend on process memory.
- IN-02 and IN-03 are closed: no unused injected clock on the announcement path, and no synthesised exception in a failure line.
</success_criteria>

<output>
Create `.planning/phases/03-availability-and-booking-decision/03-08-SUMMARY.md` when done
</output>
