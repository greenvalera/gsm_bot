---
phase: 04-replanning-and-rehearsal-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - prisma/schema.prisma
  - src/domain/planning/planning-service.ts
  - src/telegram/planning-renderers.ts
  - src/telegram/planning-handlers.ts
  - tests/unit/planning-availability-card.test.ts
  - tests/unit/planning-logging.test.ts
  - tests/integration/planning-availability.test.ts
  - tests/integration/planning-replan.test.ts
autonomous: true
requirements: [AVAIL-05, AVAIL-08]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "When a slot is blocked, the band hears about it once in the chat's break-through message slot — the same slot that says everyone is available when the round is unanimous."
    - "The blocked message and the ready-to-book message never coexist: the announcement slot carries whichever fact the round currently warrants, and reopening the round retracts the blocked message the same way a flipped answer already retracts a ready-to-book one."
    - "A participant toggling their own answer cannot notify the band more than once inside the existing announcement cooldown window."
    - "Requesting planning status after a slot is blocked re-posts the blocked announcement, not the availability card, and never points the announcement column at an availability card."
    - "A tap on a superseded round's button gets a private alert saying the slot was replanned and pointing at the status command, and never the generic no-longer-available advice that would send the tapper to a command that will itself refuse."
    - "A tap on a cancelled round's button gets its own distinct private alert, separate from both the replanned alert and the generic one."
    - "A superseded round's answer token is refused by the dispatcher with the replanned alert while it is still unexpired, and only reaches the callback boundary's generic refusal after its own expiry has passed."
    - "The replanned alert is a distinct named constant that is not equal to the generic stale copy, is at most 200 UTF-16 code units, and names no command that would refuse the tapper."
  prohibitions:
    - statement: "A break-through group message must not be sent again on every answer flip — one participant toggling between the two live buttons must not be able to notify the whole band repeatedly inside a single conversation."
      status: resolved
      verification: judgment
    - statement: "A refusal must not send the person to an action that will also fail — superseded-round copy must never instruct the tapper to start planning again, because the replanned round already holds the week and that command would refuse them a second time with no explanation."
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/domain/planning/planning-service.ts"
      provides: "claimAnnouncement widened to treat blocked as claimable (D-03); replanned and already-cancelled refusal members on the answer and booking unions"
    - path: "src/telegram/planning-handlers.ts"
      provides: "One extracted announcementBody predicate consumed by both the render path and the status command; four dispatcher refusal branches; the distinct replanned and cancelled alert constants"
    - path: "src/telegram/planning-renderers.ts"
      provides: "renderBlockedAnnouncement"
    - path: "prisma/schema.prisma"
      provides: "Rewritten readyAnnouncedAt doc comment recording the column's second meaning"
  key_links:
    - "claimAnnouncement -> claimReadyAnnouncementWindow: the caller's outcome test widens, the window's CONFIRMED guard does not — a superseded or cancelled round must never win the right to notify"
    - "announcementBody -> renderStep AND the status command: one predicate, two callers, or the two silently drift and the announcement column ends up pointing at an availability card"
    - "The still-live superseded answer token -> the dispatcher: expiring it would move the refusal above the dispatcher into the boundary, where the copy is fixed"
    - "New refusal union members -> four dispatchers: a new member with no branch lands in the unguarded fallthrough and typechecks"
---

<objective>
Give the blocked round its break-through message and give a superseded or cancelled button its own
words.

Purpose: plan 04-01 made a round blocked on the first "Cannot attend" but left the fact inside the
card. D-03 puts it in the announcement slot so the band actually hears it, and D-09 replaces the
refusal that would otherwise send a superseded-button tapper to a command guaranteed to refuse them.
Together these complete AVAIL-05 and deliver AVAIL-08.
Output: One announcement slot carrying two facts, and three distinct refusals where there was one.
</objective>

<execution_context>
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/workflows/execute-plan.md
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-replanning-and-rehearsal-lifecycle/04-CONTEXT.md
@.planning/phases/04-replanning-and-rehearsal-lifecycle/04-RESEARCH.md
@.planning/phases/04-replanning-and-rehearsal-lifecycle/04-PATTERNS.md
@.planning/phases/04-replanning-and-rehearsal-lifecycle/04-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See the full phase-wide list in `04-01-PLAN.md` under the same heading. This plan creates:

- `announcementBody()` in `src/telegram/planning-handlers.ts` — the one predicate deciding which body
  the announcement slot carries, consumed by both the render path and the status command
- `renderBlockedAnnouncement()` in `src/telegram/planning-renderers.ts`
- `PLANNING_REPLANNED_TEXT` and `PLANNING_ALREADY_CANCELLED` copy constants in
  `src/telegram/planning-handlers.ts`
- `replanned` and `already-cancelled` members on the answer-result and booking-refusal unions in
  `src/domain/planning/planning-service.ts`
- New `PLANNING_OUTCOMES` / `PLANNING_REASONS` members for the two refusals
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give the announcement slot its blocked meaning behind one predicate</name>
  <reversibility rating="costly">D-03 gives `readyAnnouncedAt` and the announcement directive union a second meaning; splitting the two message kinds apart later means a new column, a new re-post slot, and a change to what the claim column asserts.</reversibility>
  <files>src/domain/planning/planning-service.ts, src/telegram/planning-handlers.ts, src/telegram/planning-renderers.ts, prisma/schema.prisma, tests/unit/planning-availability-card.test.ts, tests/integration/planning-availability.test.ts</files>
  <read_first>
    - `src/domain/planning/planning-service.ts` — `claimAnnouncement` in full, `claimReadyAnnouncementWindow` and its `CONFIRMED` guard, `READY_ANNOUNCE_COOLDOWN_MS` and the comment naming the flip-flopping tap, `recordAnnouncement`, `releaseAnnouncementClaim`, `reanchorAnnouncement`, the `AnnouncementDirective` type, `availabilityOutcome` as edited by plan 04-01, `availabilityStepProjection`
    - `src/telegram/planning-handlers.ts` — `renderStep` in full including the comment above its announcement predicate explaining why the slot claim and the message body must agree, `handlePlanStatusCommand` and its ready-to-book predicate, `dispatchAnnouncement`, `retractStaleAnnouncement`, `repostReasonFor` and its comment, `withoutEmptyKeyboard`
    - `src/telegram/planning-renderers.ts` — `renderReadyAnnouncement` in full (the structural analog: same lineup lines, same civil-pair repetition, same never-a-mention rule), `lineupLines`, `planningOwnerLine`
    - `prisma/schema.prisma` — the `readyAnnouncedAt` doc comment, which currently asserts the narrower fact D-03 falsifies
    - `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-PATTERNS.md` — the `claimAnnouncement()` and `planning-handlers.ts` sections, which quote both predicates verbatim
    - `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-RESEARCH.md` — Pattern 3 and Pitfall 2
  </read_first>
  <behavior>
Write these first and watch them fail.

`tests/unit/planning-availability-card.test.ts` (extend):
- `renderBlockedAnnouncement` for a projection with one unavailable and two pending participants
  produces text stating the slot does not work and naming the blocking member through `memberLabel`.
- The blocked announcement carries no raw `@` mention of any member.
- `announcementBody(round, projection)` returns the blocked body for a CONFIRMED round with a
  non-null announcement claim and a blocked outcome; the ready body for the same round with an
  all-available outcome; and null for a collecting outcome.
- `announcementBody` returns null for a round whose status is not CONFIRMED, whatever the outcome.

`tests/integration/planning-availability.test.ts` (extend):
- The first UNAVAILABLE answer on a CONFIRMED round yields a `post` directive and records the
  announcement claim.
- A second UNAVAILABLE answer inside the cooldown window yields `edit`, not a second `post`.
- The blocking participant flipping back to AVAILABLE while others are pending yields `retract` and
  clears the announcement message id.
- A round that never announced and moves from collecting to collecting yields `none`.
- A round whose status is SUPERSEDED cannot win the announcement window: the claim update affects no
  row and the directive is not `post`.
- After the answer that blocks the round, the status command re-posts the blocked announcement and
  leaves the anchor column pointing at the availability card, with neither column holding the other's
  message id.
  </behavior>
  <action>
Three coupled edits that must land together, per D-03:

1. **`claimAnnouncement` (domain).** It currently short-circuits whenever the outcome is not
   all-available and returns retract-or-none. Restructure it into an explicit three-way branch over
   `AvailabilityOutcome`: an all-available outcome claims the window; a blocked outcome also claims the
   window; a collecting outcome retracts when a message is on screen and otherwise does nothing.
   Add no fourth member to `AnnouncementDirective` — the directive says what to do to the message, the
   projection's outcome says which body to render, and keeping them separate is what keeps the claim
   column single-purpose.
2. **`claimReadyAnnouncementWindow` — do not touch its WHERE clause.** Its `status: CONFIRMED` guard
   and its cooldown-inside-the-WHERE compare-and-set stay exactly as shipped. A superseded or cancelled
   round must never win the right to notify the group, and the existing 30-minute window is precisely
   the protection against a participant toggling their answer repeatedly. Add no second, blocked-specific
   cooldown.
3. **One extracted predicate on the surface.** `renderStep` and `handlePlanStatusCommand` each
   independently test that the round is CONFIRMED, the announcement claim is non-null, and the outcome
   is all-available. Extract one named function — `announcementBody(round, projection)` returning the
   ready body, the blocked body, or null — and make both call sites consume it. This is the same
   one-derivation-two-callers move the codebase already makes for the announcement window, and it is
   the only defence against the drift where the status command re-posts the availability card over the
   blocked announcement, or worse points the announcement column at an availability card.
4. **`renderBlockedAnnouncement`** in `src/telegram/planning-renderers.ts`, structurally copying
   `renderReadyAnnouncement`: the same lineup lines, the same civil day-and-time repetition, the same
   rule that a card edit path never carries a real mention. Its copy states that the slot does not
   work and who cannot make it, and points at the replan control. It attributes no fault and offers no
   undo.
5. **Rewrite the `readyAnnouncedAt` doc comment** in `prisma/schema.prisma`. It currently asserts that
   a null value means the round has never been announced ready to book; under D-03 the column claims the
   right to send whichever break-through message the round currently warrants. Leaving the old prose is
   a documentation defect that will mislead Phase 5.
6. Check `repostReasonFor` needs no new arm and prove it with a test rather than by inspection: the
   cancelled position is deliberately outside the recoverable status set, and the status read only ever
   returns a round inside that set, so neither terminal position can reach the function.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run --project unit tests/unit/planning-availability-card.test.ts tests/unit/planning-logging.test.ts &amp;&amp; npx vitest run --project integration tests/integration/planning-availability.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior listed above passes under the two commands in `<verify>`.
    - `npm run build` passes and `AnnouncementDirective` still has exactly four members.
    - `src/telegram/planning-handlers.ts` contains exactly one function deciding which announcement body a round warrants, and both the render path and the status command call it — verified by a unit test that changes the predicate's input and observes both call sites change together.
    - The `WHERE` clause of `claimReadyAnnouncementWindow` is byte-identical to its pre-task form, proven by the integration case asserting a SUPERSEDED round cannot win the window.
    - `npx vitest run --project unit && npx vitest run --project integration` are both green.
  </acceptance_criteria>
  <done>The announcement slot carries either the ready-to-book fact or the blocked fact, chosen by one predicate that both the render path and the status command consume, and a flipped answer retracts it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give a superseded or cancelled button its own words, from the dispatcher</name>
  <files>src/domain/planning/planning-service.ts, src/telegram/planning-handlers.ts, tests/unit/planning-logging.test.ts, tests/integration/planning-replan.test.ts</files>
  <read_first>
    - `src/domain/planning/planning-service.ts` — `answerAvailability`'s status ladder in full, `openBookingGate`'s identical status pair, the `AnswerResult` and `BookingRefusal` unions and the comment explaining why `not-a-participant` and `already-booked` are separate members rather than flavours of stale, `availabilityExpiresAt`, `AVAILABILITY_ACTION_SLACK_MS`, `reapExpiredActions` and `PLANNING_ACTION_RETENTION_MS`, `requestBooking`'s expire-the-previous-pair statement and the comment recording the defect it closes
    - `src/telegram/callbacks.ts` — `registerCallbackBoundary`'s expiry refusal branch and the planning route's fixed stale text, and the constant it resolves to
    - `src/telegram/planning-handlers.ts` — `dispatchAvailabilityAnswer`, `dispatchBookRequest`, `dispatchBookKeep` and `dispatchBookApply` in full, each one's trailing unguarded fallthrough, the two stale copy constants at the top of the file, `PLANNING_OUTCOMES`, `PLANNING_REASONS`, `logPlanning`, `boundedLabel`, `PLANNING_BOOKING_NOT_ELIGIBLE`
    - `tests/unit/planning-logging.test.ts` — the branch gate that fails a re-post shape reusing another one's reason, and the existing per-dispatcher assertions
    - `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-RESEARCH.md` — Pattern 5, Pitfall 4, Pitfall 5
  </read_first>
  <behavior>
Write these first and watch them fail.

`tests/unit/planning-logging.test.ts` (extend):
- The replanned alert constant is not equal to the generic stale constant and not equal to the
  callback boundary's fixed stale text.
- The replanned alert does not match `/\/plan\b/` — it must not name the command that would refuse
  the tapper — and does match the status command it should point at instead.
- The cancelled alert constant is distinct from both the replanned alert and the generic stale copy.
- Both new alerts are at most 200 UTF-16 code units (`[...s].length` measured via `s.length`).
- Each of the four dispatchers emits a distinct outcome/reason pair for the replanned refusal and for
  the cancelled refusal — no pair is reused from another branch.

`tests/integration/planning-replan.test.ts` (extend):
- After a replan, tapping the superseded round's still-unexpired answer token reaches the dispatcher
  and produces the replanned refusal; the active successor round's participant rows are unchanged.
- After a replan, tapping the superseded round's book-request token produces the replanned refusal.
- Tapping a superseded round's answer token after its own `expiresAt` has passed is refused by the
  callback boundary instead, with the boundary's fixed copy — proving the dispatcher owns the refusal
  only while the token is live.
- A round at the cancelled position refuses an answer tap with the cancelled refusal, not the
  replanned one and not the generic stale one.
  </behavior>
  <action>
Per D-09 and AVAIL-08:

1. **Domain — two status ladders gain two arms each.** In `answerAvailability`, before the existing
   fallthrough that returns stale for any status other than CONFIRMED, add an arm for the superseded
   position returning a new `replanned` result member and an arm for the cancelled position returning a
   new `already-cancelled` result member. Add the identical pair to `openBookingGate`, whose refusal
   union is shared by all three booking transitions. Document each new member in the same
   justification-per-member style the existing members use: a distinct fact gets distinct words because
   it sends a person somewhere different.
2. **Do not expire the superseded round's answer or book-request rows.** The supersede transaction
   written in plan 04-01 deliberately leaves them live. `requestBooking`'s expire-the-previous-pair
   move is the right pattern for a confirmation pair, whose refusal genuinely is "no longer available",
   and the wrong one here: expiring these rows pushes the refusal above the dispatcher into the callback
   boundary, where the copy is fixed to exactly the text D-09 forbids. The rows are swept on schedule by
   the existing expired-action reaper, and the terminal keyboard-less card edit is what removes the
   buttons from screen.
3. **Surface — two new copy constants**, declared beside the existing stale and already-applied
   constants in the same style. The replanned copy states that the slot was replanned and directs the
   tapper to `/plan_status` to find the current card. It must not name the bare planning-start command:
   the replanned round already holds the week, so that command would refuse with a week-taken error and
   leave the tapper with two failures and no explanation. The cancelled copy says the rehearsal was cancelled.
   Both stay within the 200 UTF-16 code unit alert budget, and any member label they quote goes through
   `boundedLabel` rather than a character slice.
4. **Four dispatcher branches per new member.** Add an explicit branch for both new members to
   `dispatchAvailabilityAnswer`, `dispatchBookRequest`, `dispatchBookKeep` and `dispatchBookApply`, each
   owning exactly one `answerCallbackQuery` and one `logPlanning` call with its own outcome and reason.
   Then close the trap that made this necessary: convert each dispatcher's trailing unguarded
   fallthrough into an exhaustiveness assertion so a future union member fails at typecheck instead of
   silently receiving the generic copy. Add the new outcome and reason members to the two closed arrays,
   each with the doc comment stating why it is a distinct fact.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run --project unit tests/unit/planning-logging.test.ts &amp;&amp; npx vitest run --project integration tests/integration/planning-replan.test.ts tests/integration/planning-booking.test.ts tests/integration/planning-action-retention.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior listed above passes under the command in `<verify>`.
    - `npm run build` passes, and deleting any one of the eight new dispatcher branches makes it fail — the exhaustiveness assertion is real, verified by the executor removing one branch, observing the type error, and restoring it.
    - Each of the two new union members has a branch in all four dispatchers: eight new branches total.
    - `npx vitest run --project unit && npx vitest run --project integration` are both green.
  </acceptance_criteria>
  <done>A superseded button and a cancelled button each get their own private alert from the dispatcher, neither of which sends the tapper to a command that would refuse them, and a future union member can no longer land in a generic fallthrough unnoticed.</done>
</task>

</tasks>

<flagged_assumptions>
- **A6 (research) — DISCHARGED, no longer an assumption.** `editMessageText` was retrieved during plan
  revision. Its only time bound reads: *"business messages that were not sent by the bot and do not
  contain an inline keyboard can only be edited within 48 hours"* — and all three conditions fail here
  (sent by the bot, group not business, inline keyboard present). No age limit applies. See
  `04-RESEARCH.md` Open Question 3 (RESOLVED). The standing rule that a failed edit never rolls back a
  committed durable transition is retained regardless, because an edit can still fail for unrelated
  reasons.
- **Pitfall 9 (research), accepted per A7:** a cancelled round leaves the recoverable status set, so
  the status command can surface an older booked round from a previous week. This is pre-existing
  behaviour that plan 04-03's cancellation makes reachable for the first time. Accepted; the re-posted
  booked card already leads with its day heading, and this belongs in UAT rather than a status-query
  week bound, which would be a wider change than this phase scoped.
</flagged_assumptions>

<threat_model>
**Security posture:** active — `security_enforcement: true`, **ASVS level 1**, **blocking severity threshold: high**. Every `high` or `critical` threat below must carry disposition `mitigate`; a `high` left at `accept` is a rule violation.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram update -> callback boundary | Untrusted, replayable callback data; the boundary refuses an expired row before the dispatcher runs |
| Callback boundary -> planning dispatcher | Which layer owns the refusal decides which copy the tapper sees |
| Dispatcher -> announcement claim | The right to notify the whole group is a durable compare-and-set, not a rendering decision |
| Application -> Telegram Bot API | Group-visible break-through messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Denial of Service | Group-notification flooding via the blocked announcement | high | mitigate | The existing 30-minute announcement cooldown lives inside the claim's WHERE clause and already names the flip-flopping tap as its rationale; Task 1 adds no second window and changes only the caller's outcome test. Covered by the second-answer-inside-cooldown integration case. |
| T-04-11 | Elevation of Privilege | A superseded or cancelled round winning the right to notify the group | high | mitigate | `claimReadyAnnouncementWindow`'s `status: CONFIRMED` guard is untouched; the integration case proves a SUPERSEDED round's claim update affects no row. |
| T-04-12 | Spoofing | A stale token mutating the active round | high | mitigate | AVAIL-08 is structural under D-05: an old button's token names a superseded round id, and the refusal is decided by round identity inside the transaction, not by a revision comparison that can be raced. |
| T-04-13 | Information Disclosure | Blocked announcement naming the blocker in permanent group history | medium | mitigate | The blocker goes through `memberLabel`'s masked fallback and single escaper; the announcement carries no real mention, preserving the Phase 5 targeted-follow-up path. |
| T-04-14 | Denial of Service | Over-long or unbounded refusal alert leaving a tap unacknowledged | low | mitigate | Both new constants are asserted at most 200 UTF-16 code units by unit test; any quoted label goes through `boundedLabel`, cut on code-point boundaries. |
| T-04-15 | Repudiation | A new refusal reusing another branch's log vocabulary | low | mitigate | The existing branch gate in the logging unit test fails a shape that reuses another one's reason; the eight new branches each carry their own outcome/reason pair. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | **Re-rated from `high` to `low`:** severity is impact x likelihood, and this phase installs no external package at all, so the likelihood is zero rather than small. `04-RESEARCH.md` records an empty Package Legitimacy Audit with no candidate name and `COVERAGE.md` confirms no new external service. The `high` rating described the generic threat class, not this phase's instance, and under the declared block-on-high threshold an `accept` at `high` would be a rule violation in form. Contingency unchanged: a package concluded necessary during execution re-opens this row at `high`, must run the legitimacy check, and must sit behind a blocking human checkpoint that `workflow.auto_advance` may not skip. |
</threat_model>

<verification>
- `npm run lint && npm run build && npm run test:unit && npm run test:integration` all green.
- The announcement slot's two bodies and the status command's re-post agree, proven by the integration
  case that blocks a round and then requests status.
- The three refusals are provably distinct strings and each of the four dispatchers answers both new
  union members.
</verification>

<success_criteria>
- A blocked round posts one break-through message saying the slot does not work; a reopened round
  retracts it; a toggled answer cannot notify the band twice inside the cooldown.
- The status command re-posts the blocked announcement rather than the availability card, and the two
  durable message columns never hold each other's ids.
- A superseded-round tap and a cancelled-round tap each receive their own private alert, and neither
  names a command that would refuse the tapper.
- A future refusal union member cannot silently land in a generic fallthrough.
</success_criteria>

<output>
Create `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-02-SUMMARY.md` when done.
</output>
