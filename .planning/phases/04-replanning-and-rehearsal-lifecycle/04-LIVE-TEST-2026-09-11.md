# Phase 4 residual live verification — 2026-09-11

Status: partial phase acceptance; G-04-2 was reproduced, fixed by the delegated debug session, and verified live in the recovery section below. Earlier observations retain their historical context.

## Authorization and method

The user requested real Telegram Web testing and opened Chrome. In this conversation the user explicitly extended the previous scope to deleting one specially created test card and temporarily promoting/demoting B (`@greenvaleratest`) in `GSM_bot_test_group`, restoring B afterwards. Other participants, account names, unrelated messages and database contents were not changed. All interactions used the supported Chrome browser client and real UI controls. Times are Telegram UI Europe/Kyiv.

## Observations

- Initial runtime: one running bot, healthy PostgreSQL, migration container exited 0. `/plan_status` at 02:11:31 reported no active plan. `/roster` at 02:12 showed A/B. A was the signed-in owner.
- **Deleted draft recovery passed:** A created a disposable draft at 02:12:14. Through its context menu, A deleted exactly that bot card for all members. At 02:12:47 `/plan_change` posted a fresh confirmation. Keep restored all seven day buttons and both lifecycle controls. Inline Change followed by Apply produced a readable superseded predecessor and a new day selector for the same week at 02:13:09. This proves draft command recovery after deletion; it does not prove booked-card deletion or a retained inline keyboard after remote deletion.
- **Unexpected Telegram migration:** while preparing B as a temporary administrator, Telegram automatically upgraded the basic group to a supergroup. The UI exposed no separate migration confirmation. Old Web peer: `-5576109925`; new Web peer: `-4358185686` (a Web peer identifier, not asserted to be the Bot API supergroup ID). The chat list displayed “This group was upgraded to a supergroup”. History remained visible.
- The initial role form was reduced to Pin Messages before saving. On subsequent inspection Telegram displayed Change Group Info and Pin Messages as enabled. B was immediately dismissed as administrator; the final member list showed B as an ordinary member, A as owner, and the pre-existing other administrator and bot still as administrators. No removal from the group occurred.
- **G-04-2 reproduced:** in the migrated group `/plan_status` at 02:15:05 replied “This chat isn't set up for rehearsals yet. Send /setup first, then try /plan again.” `/settings` at 02:15:12 replied “This chat is not configured yet. Send /setup to start.” No new setup was started.
- Read-only SQL confirmed the original configuration remains at `chat_id=-5576109925`, timezone Europe/Kyiv, ADMINS_ONLY. All planning rows remain there: 1 DRAFT, 9 SUPERSEDED, 14 CANCELLED. The surviving test draft is `cmtw5829z001701s4gvaqos6m`, anchor message 358. Attempting the visible old cancellation button did not produce a usable confirmation. The draft remains preserved and cannot be claimed cleaned up.

## Root cause evidence

Application-source search found no `migrate_to_chat_id` or `migrate_from_chat_id` handling. `src/telegram/handlers.ts` declares command, text, location and callback routes, without a migration route. `SettingsService.getCommitted` (`src/domain/chat/settings-service.ts:187`) and `PlanningService.status` (`src/domain/planning/planning-service.ts:4149`) read configuration by the incoming exact chat ID. The schema independently scopes configuration, roster membership, rounds, prompts and callback actions by chat ID. Telegram changed the chat identity; the application did not move or resolve its existing state.

Recovery must preserve the original configuration, roster and history; simply running setup again would hide the migration defect and strand the old state. A fix needs idempotent migration handling, a deliberate policy for old message anchors/callbacks and a safe recovery path for this already-observed migration. Test duplicate/out-of-order migration updates and target-state collisions before applying recovery to the retained database.

## Remaining work and final state

- H7 is complete: the user individually accepted P01–P14 earlier in this conversation. This does not waive live residuals.
- H5 gains the deleted-draft command recovery/Keep/Apply evidence above.
- H4/H5 demotion-between-request-and-apply was interrupted before B opened a lifecycle confirmation. Restoring B is cleanup, not a passing demotion test.
- Three-person/name variants, retained stale-keyboard variants, phone notification prominence and calendar/end-time boundaries remain outstanding.
- B is an ordinary member again. The group remains a supergroup; the original group type/identity was not restored. The database was preserved without writes or reset. Source code was not changed. The bot remains running, but the migrated group is currently unconfigured from its perspective. Restore migration continuity before continuing this fixture.

## Post-fix live recovery acceptance — 02:29–02:30

The user explicitly requested the G-04-2 fix in a subagent. The delegated GSD debug session implemented migration continuity, ran regressions, rebuilt the local bot, and recovered the existing fixture through the migration service. This section supersedes the earlier blocked final state; it does not erase the original defect evidence.

- **02:29:51 `/settings` — pass:** original Europe/Kyiv, Wednesday, 14:00, duration 120, daily 10:00–21:00, reminder times 10:00/16:00 and Admins only were visible without repeating setup.
- **02:29:55 `/roster` — pass:** both original A/B members were listed.
- **02:29:59 `/plan_status` — pass:** the retained original draft was recovered as a fresh week-of-Mon-7-Sep day selector. Selecting Fri 11 opened the 10:00–19:00 time selector with the usual-time marker at 14:00. Fresh buttons operated in the migrated group.
- **Cleanup — pass:** fresh Cancel rehearsal opened “Cancel Fri 11 Sep? The rehearsal will be called off.” Applying “Yes, cancel it” produced “Cancelled — Fri 11 Sep”, both roster names, and “This rehearsal was cancelled.”
- **02:30:35 `/plan_status` — pass:** “Nobody is planning a rehearsal right now. Send /plan to start one.”
- **Read-only final SQL:** all rounds now use actual Bot API chat ID `-1004358185686`: 9 SUPERSEDED, 15 CANCELLED, no DRAFT/CONFIRMED/BOOKED. Two memberships remain. This is continuity of retained data, not a reset.
- **Runtime:** one polling bot running, PostgreSQL healthy. B remains an ordinary member; no further Telegram role changes occurred during recovery acceptance.

G-04-2 recovery is live-verified. Duplicate/out-of-order migration delivery and target-conflict handling are covered by automated tests reported by the debugger, not claimed as new native Telegram migrations. The group remains a supergroup. H4/H5 demotion-between-request-and-apply and other residual Phase 4 cases remain separate from this fix.
