---
phase: 04-replanning-and-rehearsal-lifecycle
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - src/domain/planning/planning-service.ts
  - src/domain/planning/target-week.ts
  - tests/unit/target-week.test.ts
  - tests/unit/planning-day-card.test.ts
  - tests/integration/planning-round.test.ts
  - tests/integration/planning-booking.test.ts
autonomous: true
requirements: [LIFE-02, LIFE-05, LIFE-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A rehearsal that is happening right now is no longer offered as the previous rehearsal: only a rehearsal whose scheduled end has passed supplies the day, time and participant defaults for the next round."
    - "A rehearsal the chat marked as booked counts as scheduled when the bot chooses a target week, so planning rolls to the following week rather than proposing a second rehearsal for the same one."
    - "A cancelled slot is never read as a rehearsal that happened: it supplies no day marker, no time marker and no participant defaults."
    - "A cancelled rehearsal's week is offered again for planning, and if that week has no still-selectable day left, planning rolls to the next week rather than offering a card whose every button refuses."
    - "Being asked confers standing: anyone who was snapshotted into any round keeps previous-participant standing whatever that round's status later became, so cancelling a chat's only rehearsal does not silently revoke planning access for every non-administrator."
    - "Someone who was never snapshotted into any round gains no standing from this widening."
    - "The past-day rule has exactly one implementation, shared by the day card's classification and the target-week roll, and the module that owns it is not imported back by the module that owns the status sets."
  prohibitions:
    - statement: "A cancelled slot must never be presented as a rehearsal that happened — it must supply no usual-day marker, no last-time marker, and no participant default, because the band did not rehearse."
      status: resolved
      verification: judgment
    - statement: "Cancelling a rehearsal must not revoke planning access from people who were already asked to it — standing comes from having been invited by an administrator-curated roster, and a slot the band later called off does not un-invite anyone."
      status: resolved
      verification: judgment
    - statement: "Releasing a week must not hand the band a week it cannot use — a week whose every day has already passed is rolled past rather than offered as a card whose seven buttons all refuse."
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/domain/planning/planning-service.ts"
      provides: "previousRehearsal filtered on the scheduled end; wasPreviousParticipant decoupled from the week-claiming status set; isPastDay re-exported from its new home"
    - path: "src/domain/planning/target-week.ts"
      provides: "isPastDay relocated here, weekHasSelectableDay, and targetWeekStart's selectable-day roll"
    - path: "tests/unit/target-week.test.ts"
      provides: "Coverage for the booked-week roll (LIFE-02) and the no-selectable-day roll (LIFE-06)"
    - path: "tests/integration/planning-round.test.ts"
      provides: "Coverage for the scheduled-end boundary, cancelled exclusion, and the previous-participant widening in both directions"
  key_links:
    - "isPastDay's module location -> the absence of an import cycle: the status-set module must not import the service module, which already imports it"
    - "The cancelled position's absence from the week-claiming set -> previousRehearsal excluding it for free"
    - "wasPreviousParticipant with no status filter -> planning access: this is an authorization input, not a display one"
    - "targetWeekStart's roll predicate -> the single production caller: the predicate lives inside the function so the eight existing call sites compile unchanged"
---

<objective>
Deliver LIFE-02, LIFE-05 and the "when appropriate" half of LIFE-06 — the four surgical read edits that
make the rehearsal lifecycle's defaults tell the truth once cancellation and supersession exist.

Purpose: these are the reads every other plan in the phase changed the write side of. Doing them last
means the cancelled and superseded positions are real, so each edit can be proven against them rather
than against a fixture that could not occur yet. Without D-19 in particular, plan 04-03's cancellation
introduces a silent regression in which cancelling a chat's only rehearsal revokes planning access for
every non-administrator with no message anywhere explaining it.
Output: four read edits, one module relocation, and the coverage that pins all of them.
</objective>

<execution_context>
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/workflows/execute-plan.md
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-replanning-and-rehearsal-lifecycle/04-CONTEXT.md
@.planning/phases/04-replanning-and-rehearsal-lifecycle/04-RESEARCH.md
@.planning/phases/04-replanning-and-rehearsal-lifecycle/04-PATTERNS.md
@.planning/phases/04-replanning-and-rehearsal-lifecycle/04-01-SUMMARY.md
@.planning/phases/04-replanning-and-rehearsal-lifecycle/04-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See the full phase-wide list in `04-01-PLAN.md` under the same heading. This plan creates:

- `isPastDay` relocated into `src/domain/planning/target-week.ts` and re-exported from
  `src/domain/planning/planning-service.ts` so existing importers are unchanged
- `weekHasSelectableDay()` in `src/domain/planning/target-week.ts`

It modifies, without renaming, `previousRehearsal()`, `wasPreviousParticipant()` and
`targetWeekStart()`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: previousRehearsal reads the scheduled end, and previous-participant standing stops asking what the round became</name>
  <reversibility rating="costly">D-19 widens an authorization input; widening and then narrowing it again would revoke planning access people already have.</reversibility>
  <precondition>Docker Engine is running and `docker info` succeeds — both edits are read queries proven against a real PostgreSQL container via Testcontainers.</precondition>
  <files>src/domain/planning/planning-service.ts, tests/integration/planning-round.test.ts, tests/integration/planning-booking.test.ts</files>
  <read_first>
    - `src/domain/planning/planning-service.ts` — `previousRehearsal()` and its whole doc comment, `wasPreviousParticipant()` and its doc comment including the two sentences asserting the coupling D-19 removes, `dayStepProjection` and `timeStepProjection` (the two internal call sites feeding the usual-day and last-time markers), `rehearsalDate` and `rehearsalStartMinute` and their null guards, `confirm()`'s write of the scheduled end alongside the confirmed status, the `planningParticipant.createMany` that is the only place participant rows are written, `RECOVERABLE_ROUND_STATUSES` and its comment forbidding a shared status list
    - `src/domain/planning/target-week.ts` — `WEEK_CLAIMING_STATUSES` and its comment naming this phase's requirements as its next editor
    - `src/domain/auth/planning-access-service.ts` — how previous-participant standing feeds the planning-access policy
    - `prisma/schema.prisma` — the `PlanningRound` index on chat and start, and the schedule columns
    - `tests/integration/planning-round.test.ts` — the existing previous-rehearsal cases and the existing positive/negative previous-participant pair
    - `tests/integration/planning-booking.test.ts` — the previous-rehearsal assertions and the comment naming the Phase 3 widening they assert
    - `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-PATTERNS.md` — the `previousRehearsal()` / `wasPreviousParticipant()` section, which quotes both functions verbatim
    - `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-RESEARCH.md` — Patterns 7 and 9, and assumption A2
  </read_first>
  <behavior>
Write these first and watch them fail.

`tests/integration/planning-round.test.ts` (extend):
- A CONFIRMED round whose scheduled end is in the future and whose start is in the past — a rehearsal
  in progress — is NOT returned as the previous rehearsal.
- A CONFIRMED round whose scheduled end has passed IS returned.
- A BOOKED round whose scheduled end has passed IS returned.
- A round at the cancelled position whose scheduled end has passed is NOT returned, whatever its start.
- A SUPERSEDED round is NOT returned.
- Given two rounds where the one that started earlier has a longer duration and therefore ends later,
  the one that started LATER is returned — the ordering stays on the start, because both derived
  markers read the start and a long rehearsal that began earlier must not beat a short one that began
  later.
- A member snapshotted into a round that was later cancelled still has previous-participant standing.
- A member snapshotted into a round that was later superseded still has previous-participant standing.
- A member of the chat who was never snapshotted into any round has no standing.
- A member snapshotted into a CONFIRMED round still has standing — the widening removes nothing.

`tests/integration/planning-booking.test.ts` (adjust existing):
- The existing previous-rehearsal expectations move to the scheduled-end boundary rather than the start
  boundary; the comment naming the Phase 3 widening they assert is updated to say what they now assert.
  </behavior>
  <action>
Per D-16, D-17 and D-19 — two one-line query edits and two doc comments that must be rewritten because
they assert facts these edits falsify:

1. **`previousRehearsal()`.** Change the time filter from the start column to the scheduled-end column.
   Add nothing else. Do NOT add a not-null guard on the end column: a SQL comparison against a null
   evaluates to null, which is not true, so null rows are already excluded, and adding the guard would
   imply the null case is reachable when the confirm transaction writes the end alongside the confirmed
   status in the same statement. Do NOT add a fallback to the start column: that would re-admit a
   rehearsal currently in progress, which is exactly the state this edit removes. Keep the status filter
   on the week-claiming set — narrowing it to booked-only would blank the usual-day and last-time
   markers the first time a chat skips marking a rehearsal as booked, silently, with nothing on screen
   to say why; the cancelled position is excluded for free by its absence from that set, which is the
   correction the requirement actually needed. **Keep the ordering on the start column** and record why
   in the doc comment: duration is a per-round snapshot, so two rounds can order differently by end than
   by start, and both derived values read the start.
2. **`wasPreviousParticipant()`.** Drop the status filter entirely so the round condition is the chat
   alone. That is the whole edit. It is safe because participant rows are created in exactly one place —
   the snapshot inside the confirm transaction — so a draft round never has participants and the filter
   was already vacuous for drafts; dropping it admits only the superseded and cancelled snapshots, which
   is precisely the intent. The rows stay bounded by the administrator-curated active roster at confirm
   time.
3. **Rewrite both doc comments in the same edit.** `previousRehearsal`'s must state the scheduled-end
   rule, why no null guard and no fallback are present, and why the ordering stays on the start.
   `wasPreviousParticipant`'s currently asserts that its status set is read from the week-claiming
   constant rather than restated so a member's standing can never drift from what counts as a
   rehearsal, and that the two questions share a status filter and nothing else. Both sentences become
   false: state instead that standing comes from having been asked, that it is an authorization input
   rather than a display one, and that the two questions now share nothing.
4. **Touch neither status constant.** The week-claiming set and the recoverable set are unchanged by
   this task; a set that answers a distinct question keeps its own name.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run --project integration tests/integration/planning-round.test.ts tests/integration/planning-booking.test.ts tests/integration/planning-recovery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior listed above passes under the command in `<verify>`.
    - `npm run build` passes.
    - `previousRehearsal`'s query filters on the scheduled-end column, keeps the week-claiming status filter, orders by the start column, and contains no not-null guard and no fallback branch.
    - `wasPreviousParticipant`'s query has no status condition of any kind.
    - Both doc comments state the new rules and no longer contain the falsified sentences.
    - `npx vitest run --project unit && npx vitest run --project integration` are both green — the planning-access policy tests still pass with the widened standing.
  </acceptance_criteria>
  <done>A rehearsal in progress is no longer the previous one, a cancelled slot is never read as a rehearsal that happened, and cancelling a chat's only rehearsal no longer revokes standing from anyone who was asked to it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The target week rolls past a week with no selectable day, reusing the one past-day rule</name>
  <files>src/domain/planning/target-week.ts, src/domain/planning/planning-service.ts, tests/unit/target-week.test.ts, tests/unit/planning-day-card.test.ts</files>
  <read_first>
    - `src/domain/planning/target-week.ts` — the whole module: `WEEK_CLAIMING_STATUSES` and its comment, `weekIsClaimed`, `MAX_WEEK_LOOKAHEAD` and its rationale, `targetWeekStart` and its whole doc comment including why the predicate must be asked about every candidate, `weekDates`, `mondayOf`, `addDays`, `isoDate`, `parseCivilDate`
    - `src/domain/planning/planning-service.ts` — `isPastDay` and its doc comment, `classifyDay` and its use of it, `buildDayStepProjection`, `startOrResume`'s single call to the target-week function and its week-taken mapping, and the import line that already pulls the week-claiming set from the target-week module
    - `src/infrastructure/time/civil.ts` — where the civil-date helpers live, as the alternative home for the past-day rule
    - `tests/unit/target-week.test.ts` — all eight existing two-argument call sites and the assertion that the booked position claims a week
    - `tests/unit/planning-day-card.test.ts` — the existing importers of the past-day rule
    - `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-RESEARCH.md` — Pattern 8, Pitfall 7, and the recorded residual limitation about a today-only week
  </read_first>
  <behavior>
Write these first and watch them fail.

`tests/unit/target-week.test.ts` (extend):
- A chat whose current week has a BOOKED round for it is offered the FOLLOWING week — the booked
  position claims a week for target selection (LIFE-02).
- A chat whose current week has a CONFIRMED round for it is offered the following week.
- A chat whose current week's round was CANCELLED is offered the CURRENT week again — the cancelled
  position claims nothing (LIFE-06).
- On a Sunday, a chat whose current week is unclaimed but has no still-selectable day left is offered
  the NEXT week rather than the current one.
- On a Tuesday, a chat whose current week is unclaimed is offered the current week — Wednesday through
  Sunday are still selectable.
- On a Monday, a chat whose current week is unclaimed is offered the current week.
- A week that is both claimed and exhausted is rolled past exactly once, not twice.
- The lookahead cap still bounds the search: a chat with every week in the window claimed gets the null
  answer rather than a hung loop.
- The function still takes exactly two arguments — all eight existing call sites compile unchanged.

`tests/unit/planning-day-card.test.ts` (extend):
- The day card's past/selectable classification is unchanged after the past-day rule moves modules —
  the existing import path still resolves and every existing assertion still holds.
  </behavior>
  <action>
Per D-18 and LIFE-02/LIFE-06:

1. **Move the past-day rule to the module that needs it.** The rule currently lives in the service
   module, which already imports the status set from the target-week module; importing it back would be
   a cycle that typechecks and then fails at runtime as an undefined-at-module-init error, because ESM
   evaluates the cycle before the binding exists. Move the function into the target-week module (or into
   the civil-date module where the date-formatting helper lives, if that reads better) and **re-export
   it from the service module** so the day classification and every existing test importer are
   unchanged. This relocation is a prerequisite of step 2, not an optional tidy-up.
2. **Add the roll predicate inside the function, not at the call site.** Add a small named helper —
   `weekHasSelectableDay(weekStart, nowCivil)` — that maps the week's seven civil dates through the
   past-day rule and answers whether any remains. Then make the target-week search accept a candidate
   only when it is both unclaimed AND has a selectable day, rolling forward otherwise. Add no parameter
   to the exported signature: the predicate goes inside, so the eight existing two-argument call sites
   compile unchanged and only the expectations for an exhausted current week move. Keep the function
   pure and total — no clock read and no locale machinery — and keep the null answer for an exhausted
   lookahead window, because an exhausted window is a real state of the chat the caller has to be able
   to distinguish from an infrastructure fault.
3. **Extend the doc comment** to state both reasons a week is rolled past, and to record that the
   predicate is asked about every candidate rather than only the first, for the same reason the claim
   predicate already is.
4. **Touch no status constant.** LIFE-02 needs no implementation: the booked position is already in the
   week-claiming set. Prove it with coverage rather than re-implementing it.
5. **Record the residual limitation rather than fixing it.** Day-level selectability is what D-18 names.
   A week whose only remaining day is today, where every generated hour has already passed, is still
   offered; the time card then shows every hour unavailable. That is pre-existing Phase 2 behaviour, not
   a regression introduced here, and widening the roll to hour granularity would require the chat's
   configured daily window inside a function whose purity is load-bearing. Note it in the plan summary
   for UAT.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; npx vitest run --project unit tests/unit/target-week.test.ts tests/unit/planning-day-card.test.ts tests/unit/planning-time-card.test.ts &amp;&amp; npx vitest run --project integration tests/integration/planning-round.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every behavior listed above passes under the command in `<verify>`.
    - `npm run build` passes.
    - `grep -c 'from "\./planning-service\.js"' src/domain/planning/target-week.ts` outputs `0` — the relocation left no cycle.
    - The exported target-week function's parameter list is unchanged; no existing call site was edited to add an argument.
    - The past-day rule has exactly one implementation, and the day classification imports it from the service module's re-export without an import-path change in any existing test.
    - `npx vitest run --project unit && npx vitest run --project integration` are both green.
  </acceptance_criteria>
  <done>Planning offers a week the band can actually use: a booked or confirmed week is skipped, a cancelled week is offered again, and a week with no selectable day left rolls forward.</done>
</task>

</tasks>

<flagged_assumptions>
Three of the phase's edge-probe rows landed in the `unclassified` category and stay unresolved. Reviewed
manually here rather than treated as edge-free; each is recorded, not dropped.

- **LIFE-02, `unclassified` — unresolved.** The edges it actually carries are the three positions that do
  and do not claim a week for target selection. Covered by the Task 2 behaviors, but no probe-derived
  criterion was authored for it.
- **LIFE-05, `unclassified` — unresolved.** Its real edges are the scheduled-end boundary (a rehearsal in
  progress, one that has just ended), the cancelled and superseded exclusions, and the ordering tie
  between two rounds with different durations. All covered by the Task 1 behaviors; recorded as
  unresolved because the classifier produced no category.
- **LIFE-06, `unclassified` — unresolved.** Its real edges are the day-of-week boundaries of the
  selectable-day roll (Monday, Tuesday, Sunday) and the interaction between a claimed week and an
  exhausted one. Covered by the Task 2 behaviors.

Other assumptions carried:

- **A2 (research):** the previous-rehearsal ordering stays on the start column after the filter moves to
  the scheduled end. With mixed per-round durations a long rehearsal that started earlier could
  otherwise be reported as the previous one over a short one that started later. Pinned by the
  differing-duration behavior in Task 1.
- **D-18 residual limitation:** a week whose only remaining day is today, with every generated hour
  already past, is still offered. Pre-existing Phase 2 behaviour; belongs in UAT, not in code.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning-access policy -> previous-participant standing | An authorization input; widening it changes who may start planning |
| Application -> PostgreSQL | Two read queries whose filters decide what the chat is told about its own history |
| Civil-date arithmetic | Pure, injected inputs; no clock read and no locale machinery inside the week search |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-31 | Elevation of Privilege | Previous-participant standing widened to any snapshotted round | medium | accept | Decided and rated in D-19. The widening is bounded by "was on the administrator-curated active roster at some confirm", because participant rows are written in exactly one place from the active-membership list. It is strictly narrower than the failure it prevents: silently revoking access for every non-administrator when a chat cancels its only rehearsal, with no message anywhere explaining it. Required coverage: the positive case (a cancelled or superseded round still confers standing) and the negative case (a never-snapshotted member gains none), both in Task 1. Note for `04-SECURITY.md`: roster removal does not retroactively revoke prior-participation standing — already true today for confirmed rounds and unchanged here. |
| T-04-32 | Elevation of Privilege | Denial of standing to people who have it | medium | mitigate | The negative case is asserted alongside the positive one, so a future narrowing of this query fails a test rather than silently revoking access. |
| T-04-33 | Tampering | A cancelled slot seeding future defaults | high | mitigate | The cancelled position is absent from the week-claiming status set the previous-rehearsal query filters on, and the exclusion is proven by an integration case rather than inferred from the constant. |
| T-04-34 | Denial of Service | An unbounded or cyclic week search | medium | mitigate | The lookahead cap is unchanged and still asserted; the relocation of the past-day rule removes the import cycle that would otherwise fail at module init rather than at typecheck, proven by a source assertion. |
| T-04-35 | Repudiation | A rehearsal in progress reported as the previous one | low | mitigate | The filter moves to the scheduled end, which the confirm transaction writes alongside the confirmed status, so no null case and no fallback branch is reachable. |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs no external package; the research records an empty Package Legitimacy Audit with no candidate name. |
</threat_model>

<verification>
- `npm run lint && npm run build && npm run test:unit && npm run test:integration` all green.
- `npx vitest run --project unit tests/unit/target-week.test.ts` proves the booked-week roll, the
  cancelled-week release and the no-selectable-day roll on Monday, Tuesday and Sunday.
- `npx vitest run --project integration tests/integration/planning-round.test.ts` proves the
  scheduled-end boundary, the cancelled and superseded exclusions, and the standing widening in both
  directions.
- The target-week module contains no import from the service module.
</verification>

<success_criteria>
- A rehearsal in progress is not the previous rehearsal; one whose scheduled end has passed is.
- A cancelled slot supplies no marker and no default anywhere.
- A booked rehearsal makes planning roll to the following week.
- A cancelled rehearsal's week is offered again, and a week with no selectable day left is rolled past.
- Anyone who was ever snapshotted into a round keeps previous-participant standing; anyone who never was
  gains none.
</success_criteria>

<output>
Create `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-05-SUMMARY.md` when done.
</output>
