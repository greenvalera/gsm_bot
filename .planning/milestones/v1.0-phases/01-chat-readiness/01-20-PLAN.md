---
phase: 01-chat-readiness
plan: 20
type: execute
wave: 20
gap_closure: true
gap_ids: [G-01-3, G-01-15, G-01-19]
depends_on:
  - 01-19
files_modified:
  - src/telegram/renderers.ts
  - tests/unit/schedule-settings.test.ts
  - .planning/phases/01-chat-readiness/01-UI-SPEC.md
  - .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md
  - .planning/phases/01-chat-readiness/01-UAT.md
  - .planning/phases/01-chat-readiness/01-13-SUMMARY.md
  - .planning/phases/01-chat-readiness/01-05-SUMMARY.md
  - .planning/WINDOWS.md
autonomous: true
requirements:
  - CONF-02
  - CONF-03
  - ROST-03
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Wizard steps 3, 5 and 6 each name the value being entered before stating the required format."
    - "The verbatim time-format hint still renders unchanged in all five time prompts."
    - "The empty-roster expectation in the runbook and the UAT matches the Copywriting Contract, which states its instruction sentence once."
    - "Every SUMMARY coverage block in the phase parses against the schema with zero validation errors."
    - "The broken-windows ledger points every remaining row at the module that actually owns it."
  artifacts:
    - path: "src/telegram/renderers.ts"
      provides: "Subject sentences on the three ambiguous time-entry wizard steps"
      exports:
        - renderSetupStep
    - path: ".planning/phases/01-chat-readiness/01-05-SUMMARY.md"
      provides: "A schema-valid coverage block, replacing the silent legacy prose fallback"
  key_links:
    - from: "src/telegram/renderers.ts"
      to: ".planning/phases/01-chat-readiness/01-UI-SPEC.md"
      via: "the schedule-input-prompt surface row requires each time prompt to name its value, and the shared hint stays contract-fixed"
      pattern: "24-hour format"
    - from: ".planning/phases/01-chat-readiness/01-13-SUMMARY.md"
      to: "tests/integration/chat-readiness.e2e.test.ts"
      via: "every coverage ref must still resolve to a test that exists after the gate rewrites in plans 01-16, 01-17 and 01-19"
      pattern: "chat-readiness.e2e.test.ts"
---

## Gap Closure Context

This plan closes three gaps and two newly-discovered items.

**G-01-3 / F-1 / broken window 9** — `renderSetupStep` in `src/telegram/renderers.ts` interpolates
the shared time-format hint as the ENTIRE message body for the three time-entry branches: `:255`
(step 3, default start), `:263` (step 5, daily start), `:266` (step 6, daily end). Those prompts
state the required input FORMAT but never the SUBJECT. Only the two step-7 branches (`:276`, `:281`)
prepend a subject sentence, and only because two prompts there share an identical `Step 7 of 8`
header. Six of the eight steps already carry a self-contained subject sentence.
`01-UI-SPEC.md:157` says the step header identifies progress, not content.
**Window 9's file attribution is wrong — the owner is `renderers.ts`, not `setup-handlers.ts` (N-3).**
`01-UI-SPEC.md:128-146` has no Copywriting Contract row for any wizard step prompt; the
disambiguation requirement exists only as behavioural prose.

**G-01-15 / F-8 / broken window 10 — NOT A DEFECT.** `src/telegram/roster-renderers.ts:99-106`
already renders the Copywriting Contract byte-for-byte. `01-UI-SPEC.md` states the empty-state
instruction sentence TWICE — normatively at L134 and as an inline paraphrase in the Surface-inventory
row at L97 — and the spec's own authority note at L152 assigns empty-state copy to the Copywriting
Contract, which has no "final line" element. The runbook-authoring plan promoted the paraphrase to a
distinct third required line, which propagated into the runbook, UAT test 15, the gap, and the
window. **Correct the EXPECTATION, not the renderer. Do not change `roster-renderers.ts`.** Three
independent tests assert the two-line form with exact-match matchers; appending a third line would
fail all three. Close window 10 as MISFILED using `windows waive`, never `windows fixed`.

**G-01-19** — `01-13-SUMMARY.md:127` declares `kind: manual`, a truncation of `manual_procedural`
that is not in the classifier's valid set. **Owner decision 2026-08-24: set the kind to
`manual_procedural` AND `human_judgment: true` with a rationale, keeping D8 a human checkpoint by
design. It must NOT auto-pass** — no automated assertion backs a hand-inspected document.

**N-5** — `01-05-SUMMARY.md` has no `coverage:` block and silently runs in legacy prose fallback, so
its deliverables are never deterministically classified. Included here because it is the same
artifact class as G-01-19 and fits the wave cleanly.

**N-3** — ledger file attributions are wrong for windows 6, 9 and 10.

<objective>
Say which time is being entered, correct the empty-roster expectation that was never a defect, and
make every phase coverage block and ledger row parse and point where it should.

Purpose: The ambiguous prompts were the owner's original complaint on the live run. The empty-roster
finding is a false positive that will keep re-appearing until the runbook, the UAT and the spec agree
with the code. One malformed enum token demotes a deliverable to an undeclared human checkpoint, and
one summary is silently unclassified.
Output: Three prepended subject sentences with the rendering assertions that never existed, a
de-duplicated empty-state contract, two repaired coverage blocks, and a ledger whose file pointers
are true.
</objective>

<execution_context>
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/workflows/execute-plan.md
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/debug/ambiguous-time-hints.md
@.planning/debug/empty-roster-missing-final-line.md
@.planning/debug/malformed-coverage-block-01-13.md
@src/telegram/renderers.ts
@src/telegram/roster-renderers.ts
</context>

<interfaces>
- `renderSetupStep(draft)` (`renderers.ts:242-306`) owns all wizard step copy. The shared hint is a
  module constant at `renderers.ts:43-44` and renders verbatim today.
- The step-7 pattern to mirror is a leading imperative sentence naming the value, followed by the
  shared hint on the same line (`renderers.ts:276`, `:281`).
- Field names to derive the subjects from are the wizard-sequence entries in `01-UI-SPEC.md:108`,
  `:110` and `:111`: default rehearsal start time, daily start boundary, daily end boundary.
- The coverage schema accepts kinds `unit`, `integration`, `e2e`, `automated_ui`,
  `manual_procedural`, `other`, and statuses `pass`, `fail`, `unknown`. When `human_judgment` is
  `true`, a non-empty `rationale` string on the same entry is REQUIRED or the entry fails validation.
- `node .claude/gsd-core/bin/gsd-tools.cjs uat classify-coverage --summary <path>` reports `errors`,
  `auto_passed` and `present` with a reason per entry.
- `node .claude/gsd-core/bin/gsd-tools.cjs windows waive <id> "<reason>"` requires a reason;
  `windows fixed <id>` takes none.
- `.planning/WINDOWS.md` carries the ledger TWICE — a markdown table and a fenced JSON array. Both
  must stay byte-consistent for the same row.

## Explicitly out of scope

- `src/telegram/roster-renderers.ts` — it is correct; changing it would break three exact-match tests.
- The third wording of the same sentence at `roster-handlers.ts:41-42` (the `/roster_add`-without-reply
  usage error), which the Copywriting Contract does not document at all. Recorded as an observation
  in the debug session; do not conflate it with the empty-state copy.
</interfaces>

## Multi-Source Coverage Audit

| SOURCE | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| UAT | G-01-3 `missing[0]` | Prepend a subject sentence to steps 3, 5 and 6 in the step-7 form | 01-20 | COVERED | Task 1. |
| UAT | G-01-3 `missing[1]` | MUST PREPEND, never replace — the hint is contract-fixed | 01-20 | COVERED | Task 1 acceptance criterion pins verbatim survival of the hint. |
| UAT | G-01-3 artifacts[3] | Steps 5 and 6 have no rendering assertion; step 3 checks only the header | 01-20 | COVERED | Task 1. |
| UAT | G-01-3 `missing[2]` / N-3 | Correct the file attribution on window 9 | 01-20 | COVERED | Task 3. |
| UAT | G-01-15 `missing[0]` | Correct the runbook line and UAT test 15 | 01-20 | COVERED | Task 2. |
| UAT | G-01-15 `missing[1]` | De-duplicate the empty-state sentence in the UI-SPEC | 01-20 | COVERED | Task 2. |
| UAT | G-01-15 `missing[2]` | Close window 10 as MISFILED, not fixed | 01-20 | COVERED | Task 2 uses `windows waive`. |
| UAT | G-01-19 | 01-13 D8 kind is out of enum | 01-20 | COVERED | Task 3, with the owner's `human_judgment: true` decision. |
| UAT | N-5 | 01-05-SUMMARY has no coverage block | 01-20 | COVERED | Task 3. |
| UAT | N-3 | Ledger attributions wrong for 6, 9, 10 | 01-20 | COVERED | Task 3. |
| REQ | CONF-02 | Configure the default weekday and start time | 01-20 | COVERED | Step 3 now names the value it collects. |
| REQ | CONF-03 | Configure duration and daily boundaries | 01-20 | COVERED | Steps 5 and 6 now name their boundaries. |
| REQ | ROST-03 | View the current band roster | 01-20 | COVERED | The empty-roster contract is restated once and matches the shipped renderer. |
| CONTEXT | D-01 | Guided `/setup` wizard | 01-20 | COVERED | Every step is self-describing. |
| CONTEXT | D-05 | Alphabetical safe roster projection | 01-20 | COVERED | Unchanged; only the expectation document is corrected. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Say which time each wizard step is asking for</name>
  <read_first>
    <file>src/telegram/renderers.ts</file>
    <file>tests/unit/schedule-settings.test.ts</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
    <file>.planning/debug/ambiguous-time-hints.md</file>
  </read_first>
  <files>src/telegram/renderers.ts, tests/unit/schedule-settings.test.ts, .planning/phases/01-chat-readiness/01-UI-SPEC.md, .planning/WINDOWS.md</files>
  <behavior>
    - Step 3's body begins with an imperative sentence naming the default rehearsal start time, then the unchanged shared hint.
    - Step 5's body begins with an imperative sentence naming the daily start boundary, then the unchanged shared hint.
    - Step 6's body begins with an imperative sentence naming the daily end boundary, then the unchanged shared hint.
    - The shared hint constant still appears verbatim in all five time prompts, including both step-7 branches.
    - Each of the three step bodies still carries its own progress header.
  </behavior>
  <action>In `renderSetupStep`, prepend a subject sentence to the three time-entry branches, using exactly the form the two step-7 branches already use: an imperative sentence naming the value, a period, a space, then the interpolated shared hint. Derive the three subjects from the wizard-sequence field names in the UI-SPEC — the default rehearsal start time, the daily start boundary, the daily end boundary. PREPEND ONLY: the hint constant is contract-fixed and renders verbatim today, so it must not be reworded, split, or replaced, and the constant itself must not be edited. Leave the five non-time branches untouched. Extend the existing step-3 rendering assertion in `tests/unit/schedule-settings.test.ts`, which currently checks only the progress header, so it also asserts the subject sentence and the verbatim hint, and add the equivalent assertions for steps 5 and 6, which have none at all. Assert the hint by importing or reconstructing its exact text so a future reword of the constant fails the gate rather than silently passing. In the UI-SPEC, the schedule-input-prompt row of the Surface inventory already fixes the time-prompt format text but not the requirement to name the value; extend that row so each time prompt must state which value it is collecting before stating the format, closing the contract silence that the debug session identified as the contributing cause. Then close broken window 9 with `node .claude/gsd-core/bin/gsd-tools.cjs windows fixed 9`.</action>
  <acceptance_criteria>
    <criterion>Steps 3, 5 and 6 each begin their body with a distinct subject sentence and end it with the unchanged shared hint.</criterion>
    <criterion>The shared hint constant is byte-identical to its pre-change form.</criterion>
    <criterion>Rendering assertions exist for steps 3, 5 and 6 and check the subject as well as the header.</criterion>
    <criterion>The UI-SPEC schedule-input-prompt row requires each time prompt to name its value.</criterion>
    <criterion>Broken window 9 is marked fixed.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run --project unit tests/unit/schedule-settings.test.ts</automated>
  </verify>
  <done>No wizard time prompt is ambiguous, the contract now requires that, and the rendering gate covers all three previously untested steps.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the empty-roster expectation instead of the renderer</name>
  <read_first>
    <file>.planning/debug/empty-roster-missing-final-line.md</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
    <file>.planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md</file>
    <file>.planning/phases/01-chat-readiness/01-UAT.md</file>
    <file>src/telegram/roster-renderers.ts</file>
  </read_first>
  <files>.planning/phases/01-chat-readiness/01-UI-SPEC.md, .planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md, .planning/phases/01-chat-readiness/01-UAT.md, .planning/WINDOWS.md</files>
  <action>Do not modify `src/telegram/roster-renderers.ts` — it already renders the Copywriting Contract byte-for-byte, and three exact-match tests would fail if a third line were appended. Instead, de-duplicate the contract: in the UI-SPEC Surface-inventory row for the empty roster, drop the inline restatement of the instruction sentence so the row carries only layout and behaviour and defers copy to the Copywriting Contract, exactly as the spec's own authority note already assigns it. Leave both Copywriting Contract empty-state rows untouched. Then correct the two documents that inherited the mis-transcription: in the live-verification runbook, rewrite the empty-roster expectation so it names the heading, the body and the absence of removal controls, with no third required line, and annotate the recorded result to state that the earlier failure was a mis-transcribed expectation rather than a product defect, citing the debug session path. In `01-UAT.md`, correct test 15's `expected` line the same way, change its `result` from issue to pass, and add a `note` recording that the missing line was never a contract element and that the renderer is correct. Update the `## Summary` block arithmetic so the passed and issues counts reflect that change and still total 21. Finally close broken window 10 with `node .claude/gsd-core/bin/gsd-tools.cjs windows waive 10 "MISFILED — filed against src/telegram/roster-renderers.ts, which renders the Copywriting Contract byte-for-byte; the defect was a duplicated sentence in 01-UI-SPEC.md promoted to a third required line by the runbook transcription. Expectation corrected in plan 01-20; see .planning/debug/empty-roster-missing-final-line.md."</action>
  <verify>
    <automated>npm test &amp;&amp; test "$(grep -c 'then send /roster_add' .planning/phases/01-chat-readiness/01-UI-SPEC.md)" = "1" &amp;&amp; node .claude/gsd-core/bin/gsd-tools.cjs windows status --pick waived_count</automated>
  </verify>
  <done>The empty-state sentence appears once in the spec, the runbook and the UAT agree with the shipped renderer, the roster tests still pass untouched, and window 10 is waived as misfiled.</done>
</task>

<task type="auto">
  <name>Task 3: Make every coverage block parse and every ledger row point at its owner</name>
  <read_first>
    <file>.planning/phases/01-chat-readiness/01-13-SUMMARY.md</file>
    <file>.planning/phases/01-chat-readiness/01-05-SUMMARY.md</file>
    <file>.planning/WINDOWS.md</file>
    <file>.planning/debug/malformed-coverage-block-01-13.md</file>
  </read_first>
  <files>.planning/phases/01-chat-readiness/01-13-SUMMARY.md, .planning/phases/01-chat-readiness/01-05-SUMMARY.md, .planning/WINDOWS.md</files>
  <action>In the 01-13 summary's coverage block, change the out-of-enum kind on the D8 entry's second verification to the schema's procedural value, and — per the owner decision — also set that entry's human_judgment flag to true and add the rationale string the schema requires when it is true, stating that the COVERAGE.md half of D8 is a hand-inspected document with no automated assertion behind it and must stay a human checkpoint by design. While in that block, reconcile every coverage `ref` against the tests as they now exist: plans 01-16, 01-17 and 01-19 rewrote assertions and titles in the end-to-end file, so any ref naming a test that no longer exists must be re-pointed, and the D3 entry describing the old acknowledge-first callback ordering must be reworded to the superseding contract recorded in PROJECT.md — exactly one answer per callback query carrying the outcome, with the current-role lookup still preceding every parse and durable read. Then author a coverage block for the 01-05 summary, which currently has none and silently runs in legacy prose fallback: derive one entry per deliverable from that summary's own Accomplishments and key-files sections, give each a requirement id drawn from the phase requirement set, and cite the real verifications that exist in `tests/unit/setup.test.ts` and the integration suite with their current statuses; where a deliverable genuinely has no automated assertion, mark it human_judgment true with a rationale rather than inventing a reference. Finally correct the three wrong file attributions in the ledger (N-3): the daily-boundaries row belongs to the keyboards module, the ambiguous-prompt row belongs to the renderers module, and the empty-roster row belongs to the UI-SPEC and runbook rather than any source file. `.planning/WINDOWS.md` stores the ledger twice — a markdown table and a fenced JSON array — so edit BOTH representations for each corrected row and keep them byte-consistent, changing only the file field and leaving ids, statuses, timestamps and reasons exactly as the tooling wrote them.</action>
  <acceptance_criteria>
    <criterion>classify-coverage on the 01-13 summary reports zero validation errors and places D8 in the human path for the human_judgment reason, not for validation_failed.</criterion>
    <criterion>classify-coverage on the 01-05 summary reports coverage mode with zero validation errors instead of the legacy fallback.</criterion>
    <criterion>Every coverage ref in the 01-13 block names a test that exists in the current suite.</criterion>
    <criterion>The ledger's markdown table and JSON array agree row-for-row after the attribution corrections, and the tooling can still parse it.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node .claude/gsd-core/bin/gsd-tools.cjs uat classify-coverage --summary .planning/phases/01-chat-readiness/01-13-SUMMARY.md --pick errors &amp;&amp; node .claude/gsd-core/bin/gsd-tools.cjs uat classify-coverage --summary .planning/phases/01-chat-readiness/01-05-SUMMARY.md --pick mode &amp;&amp; node .claude/gsd-core/bin/gsd-tools.cjs windows status</automated>
  </verify>
  <done>Both coverage blocks parse cleanly, D8 stays a declared human checkpoint, 01-05 is deterministically classified, and no ledger row sends a fixer to the wrong module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram text input → wizard prompts | Only rendered copy changes; no new input is accepted |
| Planning artifacts → release gates | `/gsd-ship` gates on the windows ledger and coverage classification |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-20-01 | Information disclosure | New wizard subject sentences | low | mitigate | The three sentences are static module strings naming a field, with no interpolation of draft values, identifiers or user data. Task 1 asserts them verbatim. |
| T-01-20-02 | Repudiation | Waiving window 10 rather than fixing it | medium | mitigate | `windows waive` requires a reason; the reason records the misfiling, the true owner, the plan that corrected the expectation, and the debug-session path, so the audit trail survives the closure. |
| T-01-20-03 | Tampering | Hand-editing the ledger's dual representation | medium | mitigate | Only the file field changes; ids, statuses, timestamps and reasons are left as the tooling wrote them, and `windows status` must still parse the result. Pinned by a Task 3 acceptance criterion. |
| T-01-20-04 | Repudiation | Marking D8 auto-passable | high | mitigate | Explicitly prevented: the entry is set to human_judgment true with a rationale, so a hand-inspected document can never be recorded as deterministically covered. |
| T-01-20-SC | Tampering | npm installs | high | accept | No package-manager install task exists in this plan; no dependency is added, removed or upgraded. |
</threat_model>

<verification>
- `npm run typecheck` passes.
- `npm test` passes, including the three new wizard rendering assertions and the untouched roster empty-state tests.
- `uat classify-coverage` reports zero errors for both the 01-13 and 01-05 summaries.
- `windows status` parses and reports window 9 fixed and window 10 waived with a reason.
- `git diff --name-only src/telegram/roster-renderers.ts` is empty.

<human-check>
Open `/setup` in the test group and walk to steps 3, 5 and 6. Each prompt should name the value it is
asking for before repeating the time format, in the same voice step 7 already uses.
</human-check>
</verification>

<success_criteria>
- Steps 3, 5 and 6 are self-describing and the shared hint is unchanged.
- The empty-state instruction sentence exists once in the spec, and the runbook and UAT agree with the code.
- Both repaired coverage blocks parse with zero errors; D8 remains a declared human checkpoint.
- Windows 9 fixed, 10 waived as misfiled, and every remaining row names its true owner.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-20-SUMMARY.md` when done.
</output>
