---
phase: 01-chat-readiness
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - Dockerfile
  - .dockerignore
  - compose.yaml
  - src/app/main.ts
  - package.json
autonomous: true
requirements:
  - CONF-01
  - AUTH-02
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The proven `/setup` tracer and PostgreSQL start through one Docker Compose command using the same non-root image intended for later deployment."
    - "Exactly one chat-key-sequentialized long-poll process starts after the committed migration succeeds and shuts down cleanly."
    - "The bot cannot start against an unhealthy or unmigrated PostgreSQL service, and runtime secrets remain environment-injected."
    - "The production image retains `node_modules/geo-tz/data` and fails its build/smoke gate when `geo-tz/dist/find-now` cannot resolve a known coordinate from packaged boundary data."
  artifacts:
    - path: "Dockerfile"
      provides: "Pinned multi-stage non-root production image with geo-tz runtime-data and known-location lookup gates"
    - path: ".dockerignore"
      provides: "Bounded image build context without local secrets or dependency trees"
    - path: "compose.yaml"
      provides: "Healthy PostgreSQL, one-shot migration, and single bot development deployment"
    - path: "src/app/main.ts"
      provides: "Single-runner composition and graceful shutdown"
  key_links:
    - from: "compose.yaml"
      to: "src/app/main.ts"
      via: "the bot waits for PostgreSQL health and one-shot migration success before starting one runner"
      pattern: "migrate"
    - from: "src/app/main.ts"
      to: "src/app/create-bot.ts"
      via: "composition root starts the already-proven bot with chat-key sequentialization and graceful shutdown"
      pattern: "createBot"
    - from: "Dockerfile"
      to: "node_modules/geo-tz/data"
      via: "the runtime dependency copy retains boundary files and executes a `geo-tz/dist/find-now` lookup before the image can pass"
      pattern: "geo-tz/data"
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Package the migrated `/setup` tracer as a reproducible local full-stack runtime.

Purpose: Prove the real application composition, migration gate, image, and single-worker operational topology before feature expansion.
Output: Multi-stage image, Docker Compose PostgreSQL/migration/bot services, and graceful long-polling entrypoint.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-chat-readiness/01-RESEARCH.md
@.planning/phases/01-chat-readiness/01-VALIDATION.md
@.planning/phases/01-chat-readiness/01-SKELETON.md
@.planning/phases/01-chat-readiness/01-03-SUMMARY.md
@package.json
@src/app/config.ts
@src/app/create-bot.ts
@src/infrastructure/db/prisma.ts
</context>

<interfaces>
- `main.ts` composes config, Prisma, Telegram membership gateway, clock, `createBot`, runner sequentialization, error boundary, and signal shutdown exactly once.
- Compose starts PostgreSQL 18.4, runs committed `prisma migrate deploy` once, and starts the bot only after both health and migration success.
- The image uses the audited lockfile and compiled production output while running as a non-root user.
- The runtime stage keeps `geo-tz` as an unbundled production dependency, retains its `data/` directory, and proves the `geo-tz/dist/find-now` entry point can read those files.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Package the proven tracer as the reproducible development deployment</name>
  <reversibility rating="reversible">The Docker image and Compose topology are provider-portable and can be replaced without changing domain or database contracts.</reversibility>
  <read_first>
    <file>.planning/phases/01-chat-readiness/01-SKELETON.md</file>
    <file>package.json</file>
    <file>src/app/config.ts</file>
    <file>src/app/create-bot.ts</file>
    <file>src/infrastructure/db/prisma.ts</file>
  </read_first>
  <files>Dockerfile, .dockerignore, compose.yaml, src/app/main.ts, package.json</files>
  <action>Create a multi-stage `node:24.19-bookworm-slim` image that installs from the Plan 01-15-approved committed lockfile, generates Prisma Client, builds the TypeScript service, and runs as a non-root user. Keep `geo-tz@8.1.8` unbundled in production dependencies, retain `node_modules/geo-tz/data` when copying/pruning the runtime tree, and add a Docker build-stage assertion that both checks the directory and executes `find` from `geo-tz/dist/find-now` for a known coordinate; a missing/unreadable data product must fail the image build. Do not set `GEO_TZ_DATA_PATH` unless the image layout intentionally moves the package data, in which case set it to the copied absolute directory and run the same lookup gate. Compose PostgreSQL 18.4 with a named development volume and health check, plus exactly one long-polling `bot` service that waits for database health and runs `prisma migrate deploy` as an explicit one-shot dependency before startup. `main.ts` constructs configuration, Prisma, membership gateway, clock, bot, `bot.catch`, and chat-key runner sequentialization, then stops gracefully on SIGTERM/SIGINT. Keep secrets environment-injected and document `docker compose up --build bot` in package scripts or Compose comments without embedding credentials.</action>
  <acceptance_criteria>
    <criterion>The image builds from the lockfile, runs as a non-root user, and contains compiled production output rather than the development dependency tree.</criterion>
    <criterion>Compose declares PostgreSQL health, a one-shot committed-migration service, and exactly one bot long-poll process; the bot cannot start before migration success.</criterion>
    <criterion>`docker compose up --build bot` is the documented full-stack development command when `BOT_TOKEN` is supplied.</criterion>
    <criterion>Process signals close the runner and Prisma pool without starting a second poller.</criterion>
    <criterion>The build and post-build container smoke test both fail if `node_modules/geo-tz/data` is absent or `geo-tz/dist/find-now` cannot return at least one IANA candidate for the known coordinate.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm run format:check &amp;&amp; docker compose config -q &amp;&amp; docker build -t gsmbot:phase-01 . &amp;&amp; docker run --rm gsmbot:phase-01 sh -lc 'test -d node_modules/geo-tz/data &amp;&amp; node --input-type=module -e "import { find } from \"geo-tz/dist/find-now\"; const zones=find(47.650499,-122.350070); if (!Array.isArray(zones) || zones.length === 0) process.exit(1)"' &amp;&amp; docker compose up -d postgres &amp;&amp; docker compose exec -T postgres pg_isready -U gsmbot &amp;&amp; docker compose run --rm migrate</automated>
  </verify>
  <done>The production `/setup` tracer has a reproducible non-root image and a local full-stack deployment path backed by the same committed migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| committed source/lockfile → runtime image | Only audited dependencies and generated artifacts may cross into executable production code. |
| Compose services → bot process | Database readiness and committed migration success gate the single long-poll worker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-07 | Denial of service | duplicate bot processes | medium | mitigate | Compose declares one bot service and `main.ts` starts one chat-key-sequentialized runner with graceful shutdown. |
| T-01-28 | Tampering | migration/runtime startup | high | mitigate | Bot startup depends on healthy PostgreSQL and successful committed `migrate deploy`; schema push is absent. |
| T-01-35 | Tampering | geo-tz runtime boundary data | high | mitigate | Docker build and post-build smoke gates require the package data directory and a successful known-coordinate lookup through `dist/find-now`. |
| T-01-SC | Tampering | runtime dependency install | high | mitigate | The image uses `npm ci` from the Plan 01-15 human-approved lockfile. |
</threat_model>

<verification>
Validate Compose, apply committed migrations to clean PostgreSQL 18, build the non-root image, prove geo-tz boundary data is readable inside it, and prove the single-worker runtime topology.
</verification>

<success_criteria>
- The same committed migration gates Compose startup.
- One non-root long-poll bot process starts and shuts down cleanly.
- The full-stack local command exercises the already-proven Telegram-to-database tracer.
- The image cannot pass while geo-tz's runtime boundary data is missing or unreadable.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-04-SUMMARY.md` when done.
</output>
