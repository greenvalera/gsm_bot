---
phase: 01-chat-readiness
plan: 24
type: execute
wave: 23
depends_on:
  - 01-22
files_modified:
  - src/telegram/setup-handlers.ts
  - tests/integration/walking-skeleton.test.ts
  - tests/integration/chat-readiness.e2e.test.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-05
  - AUTH-02
gap_closure: true
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The first authorized `/setup` in a chat with no committed configuration renders the existing bold readiness prompt and its single `Start setup` action."
    - "Authorized `/setup` with an active actor-bound draft immediately renders `Setup in progress` and the draft's current `Step X of 8` prompt instead of restarting or claiming the chat is unconfigured."
    - "Authorized `/setup` in a configured chat with no draft creates a revision-bound draft and immediately renders `Setup in progress` / `Step 1 of 8`; it never emits `This chat is not configured yet.`"
    - "Authorized `/setup` after the actor's setup draft expires reports the existing setup-expiry copy and starts no mutation."
    - "An unfilled step always renders its current prompt and required input format; completed draft values remain actor-bound, `Step X of 8` identifies progress, and no partial draft is presented as active configuration."
  artifacts:
    - path: "src/telegram/setup-handlers.ts"
      provides: "State-aware `/setup` entry projection over missing, active, expired, and configured states"
      exports:
        - handleSetupCommand
    - path: "tests/integration/walking-skeleton.test.ts"
      provides: "Real-PostgreSQL command-level state matrix for first entry, resume, configured entry, and expiry"
    - path: "tests/integration/chat-readiness.e2e.test.ts"
      provides: "Regression proving `/setup` after committed configuration starts a revision-safe wizard without false copy"
  key_links:
    - from: "src/telegram/handlers.ts"
      to: "src/telegram/setup-handlers.ts"
      via: "the existing command route reauthorizes first, then handleSetupCommand classifies durable setup/configuration state"
      pattern: "handleSetupCommand"
    - from: "src/telegram/setup-handlers.ts"
      to: "src/domain/chat/setup-service.ts"
      via: "requireActive distinguishes active/expired/missing before beginOrResume creates a replacement draft"
      pattern: "requireActive"
    - from: "src/telegram/setup-handlers.ts"
      to: "prisma.chatConfiguration"
      via: "a positive committed-row read decides whether a newly created draft skips the unconfigured readiness claim"
      pattern: "chatConfiguration.findUnique"
  prohibitions:
    - requirement_id: CONF-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT claim that a chat is unconfigured when a committed ChatConfiguration row exists, or hide a resumable actor-bound draft behind the first-entry readiness card."
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Close F-11 / broken window 15 by making `/setup` project durable state truthfully.

Purpose: Honor D-01 and D-02 on every setup entry while preserving the unconfigured readiness focal point and D-03's atomic activation boundary.
Output: A state-aware setup command plus real-PostgreSQL and composed-bot regressions for first entry, resume, expiry, and configured recovery.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-UAT.md
@.planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md
@.planning/phases/01-chat-readiness/01-18-SUMMARY.md
@src/telegram/setup-handlers.ts
@src/domain/chat/setup-service.ts
@tests/integration/walking-skeleton.test.ts
@tests/integration/chat-readiness.e2e.test.ts
</context>

<interfaces>
- `SetupService.requireActive(chatId, actorId, now)` returns `missing | expired | active` and deletes an expired setup draft before returning `expired`.
- `SetupService.beginOrResume(chatId, actorId, now)` creates a draft with `expectedRevision` copied from the active configuration, or extends a still-active draft without changing its original revision.
- `replyWithStep(...)` is the existing text-emission sibling of callback `editWithStep(...)`; it renders the exact current draft step and mints only the actions that step needs.
- The `command:setup` route in `registerChatReadinessHandlers` already performs current-role authorization before calling `handleSetupCommand`; keep that boundary unchanged per AUTH-02.
</interfaces>

## Gap Disposition

- F-11 / window 15: covered exactly once by this plan.
- The owner-deferred N-6 text-input card replacement and UAT test 16 pagination check are not prerequisites and remain excluded.
- No schema push applies: the configured recovery path already uses `SetupDraft.expectedRevision`.

## Artifacts this phase produces

- No new file or exported symbol.
- No package, Prisma model, column, migration, callback kind, or new Telegram surface.
- Existing `handleSetupCommand` gains the durable state matrix described above.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive configured `/setup` directly into a revision-bound Step 1 card</name>
  <precondition>A Docker-compatible container runtime is available for the PostgreSQL integration test.</precondition>
  <read_first>
    <file>src/telegram/setup-handlers.ts</file>
    <file>src/domain/chat/setup-service.ts</file>
    <file>tests/integration/walking-skeleton.test.ts</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
    <file>.planning/phases/01-chat-readiness/01-UAT.md</file>
  </read_first>
  <files>src/telegram/setup-handlers.ts, tests/integration/walking-skeleton.test.ts</files>
  <behavior>
    - "Given a valid committed configuration and no setup draft, `/setup` creates one draft whose expectedRevision equals the committed revision."
    - "The only Telegram projection begins with `Setup in progress`, contains `Step 1 of 8`, and does not contain the unconfigured-chat sentence or a `Start setup` button."
    - "Current-administrator authorization still precedes every draft/configuration read."
  </behavior>
  <action>Start RED in the walking-skeleton integration suite with a valid ChatConfiguration and an authorized `/setup` update. Refactor `handleSetupCommand` to call `requireActive` before creating anything: render an active draft through the existing step renderer; return the existing DRAFT_EXPIRED copy for `expired`; and only on `missing` call `beginOrResume`. For a newly created draft, read whether ChatConfiguration exists. When it does, render Step 1 immediately; when it does not, retain the readiness prompt and single Start action. Reuse existing renderers/token factories and the 01-18 expectedRevision behavior rather than adding a setup-mode flag or schema field. Keep the command route's fresh role check first per AUTH-02 and do not expose configuration values before D-03 final confirmation.</action>
  <acceptance_criteria>
    <criterion>The configured-chat integration test sees one revision-bound setup draft and one Step 1 projection with no false unconfigured copy.</criterion>
    <criterion>No START_SETUP action is minted on the configured direct-entry branch.</criterion>
    <criterion>The existing first unconfigured entry still mints exactly one Start action and displays the original readiness focal point.</criterion>
    <criterion>A non-administrator still creates no draft and sees only the existing command denial.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project integration --no-file-parallelism tests/integration/walking-skeleton.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>A configured administrator can enter a real setup wizard end to end without being told the chat is unconfigured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the complete `/setup` entry-state matrix across resume, expiry, and restart</name>
  <precondition>A Docker-compatible container runtime is available for the PostgreSQL integration tests.</precondition>
  <read_first>
    <file>src/telegram/setup-handlers.ts</file>
    <file>src/domain/chat/setup-service.ts</file>
    <file>tests/integration/walking-skeleton.test.ts</file>
    <file>tests/integration/chat-readiness.e2e.test.ts</file>
    <file>.planning/phases/01-chat-readiness/01-18-SUMMARY.md</file>
  </read_first>
  <files>src/telegram/setup-handlers.ts, tests/integration/walking-skeleton.test.ts, tests/integration/chat-readiness.e2e.test.ts</files>
  <behavior>
    - "A second `/setup` by the owner of a live draft renders that draft's exact current step without resetting values or expectedRevision."
    - "A `/setup` attempt after the draft TTL returns the existing setup-expiry sentence, deletes the lapsed row, and creates no replacement row in that update."
    - "After setup is saved and the bot is reconstructed, `/setup` starts Step 1 against the committed revision and never renders the unconfigured readiness card."
  </behavior>
  <action>Expand the walking-skeleton test from its stale repeated-readiness expectation into a table of first-unconfigured, live-resume, and expired-draft states, asserting exact copy, buttons, row counts, and durable draft state. Extend the existing full migrated readiness e2e after committed setup/restart: send `/setup`, assert direct `Setup in progress` / `Step 1 of 8`, assert absence of a Start button in the rendered keyboard by positive keyboard-shape checks, and inspect the new draft's expectedRevision against the committed row. Do not change D-01 wizard ordering, D-02 TTL, D-03 activation, any user-owned UAT result, or the owner-deferred N-6 behavior.</action>
  <acceptance_criteria>
    <criterion>Four durable states—unconfigured/missing, active draft, expired draft, and configured/missing—each have one deterministic projection under integration coverage.</criterion>
    <criterion>Resume preserves already collected values and the original expectedRevision.</criterion>
    <criterion>Expiry shows the setup-specific Copywriting Contract sentence and starts no mutation.</criterion>
    <criterion>A fresh bot instance reproduces the configured direct-entry behavior from PostgreSQL.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project integration --no-file-parallelism tests/integration/walking-skeleton.test.ts tests/integration/chat-readiness.e2e.test.ts &amp;&amp; npm run format:check &amp;&amp; npm run build</automated>
  </verify>
  <done>The setup command truthfully and durably handles every entry state that F-11 exposed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram `/setup` → command route | Untrusted actor identity must be revalidated before setup/configuration state is read. |
| Setup handler → PostgreSQL | Durable draft and committed configuration state select the user-visible branch. |
| Draft state → Telegram projection | The bot must not misrepresent committed state or disclose partial draft values as active configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-24-01 | Elevation of privilege | `command:setup` | high | mitigate | Retain the existing current-role authorization ahead of every handler read and assert denied actors create no state. |
| T-01-24-02 | Tampering | `/setup` state classification | high | mitigate | Test the four-state matrix against real PostgreSQL and bind newly created drafts to the committed revision. |
| T-01-24-03 | Information disclosure | setup projection | medium | mitigate | Render only step prompts from actor-owned drafts; never project partial values as committed configuration. |
| T-01-24-04 | Repudiation | configured/unconfigured copy | medium | mitigate | Make a positive ChatConfiguration existence read the branch oracle and pin exact copy under composed tests. |
</threat_model>

<verification>
- `npm run build`
- `npx vitest run --project integration --no-file-parallelism tests/integration/walking-skeleton.test.ts tests/integration/chat-readiness.e2e.test.ts`
- `npm run format:check`
- Confirm no Prisma migration or package manifest changed.
</verification>

<success_criteria>
- F-11 is closed in a real-database command path and after bot reconstruction.
- First setup, resume, configured recovery, and expiry each render the contractually correct card.
- UAT test 22 remains pending until the final live Telegram re-run.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-24-SUMMARY.md` when done.
</output>
