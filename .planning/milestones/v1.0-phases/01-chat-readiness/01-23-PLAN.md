---
phase: 01-chat-readiness
plan: 23
type: execute
wave: 23
depends_on:
  - 01-22
files_modified:
  - src/domain/chat/settings-service.ts
  - src/telegram/settings-handlers.ts
  - src/telegram/handlers.ts
  - tests/unit/update-route-ownership.test.ts
  - .planning/phases/01-chat-readiness/01-UI-SPEC.md
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-05
  - AUTH-02
gap_closure: true
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "An administrator whose settings-edit draft expires receives `This settings change expired after 30 minutes of inactivity. Open /settings to start again.` on the next owned text or location update, and the expired draft is discarded without changing committed configuration."
    - "An expired settings-edit row still establishes route ownership before the fresh current-administrator check; an ordinary message after the row is discarded returns to the existing silent no-in-flight path."
    - "The expired settings-edit path never falls through to SetupService.requireActive and never emits setup-specific expiry copy."
    - "Validation, location, permission, expiry, save, duplicate, and stale-action failures use the corresponding Copywriting Contract row and preserve the last authoritative state."
  artifacts:
    - path: "src/telegram/settings-handlers.ts"
      provides: "Discriminated active, expired, and missing settings-draft lookup plus settings-specific expiry projection"
      exports:
        - SettingsDraftLookup
        - findSettingsDraft
        - handleExpiredSettingsDraft
    - path: "src/domain/chat/settings-service.ts"
      provides: "Actor/chat/id/expiry-bound deletion of a lapsed settings-edit draft"
      exports:
        - SettingsService
    - path: "tests/unit/update-route-ownership.test.ts"
      provides: "Composed-route regressions for expired settings text and location attempts"
  key_links:
    - from: "src/telegram/handlers.ts"
      to: "src/telegram/settings-handlers.ts"
      via: "both carrier routes dispatch the `expired` lookup result to handleExpiredSettingsDraft instead of the setup wizard"
      pattern: "handleExpiredSettingsDraft"
    - from: "src/telegram/settings-handlers.ts"
      to: "src/domain/chat/settings-service.ts"
      via: "the expiry projection discards only the actor/chat-bound lapsed draft before replying"
      pattern: "discardExpiredDraft"
    - from: "tests/unit/update-route-ownership.test.ts"
      to: "src/telegram/handlers.ts"
      via: "registerChatReadinessHandlers drives real grammY text/location updates through ownership, authorization, classification, cleanup, and reply"
      pattern: "registerChatReadinessHandlers"
  prohibitions:
    - requirement_id: CONF-02
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT silently consume an update that the router claimed as an expired settings-edit attempt, or tell the administrator to restart the unrelated setup wizard."
---

## Phase Goal

**As a** Telegram chat administrator, **I want to** prepare a persistent, access-controlled chat for rehearsal coordination, **so that** the band can coordinate rehearsals without manually chasing members.

<objective>
Close F-10 / broken window 14 by making lapsed settings edits a first-class routed outcome with their own exact expiry copy.

Purpose: Preserve D-02 expiry semantics and AUTH-02 ordering without routing a settings-owned update into the setup wizard or mutating committed configuration.
Output: Tri-state settings-draft routing, bound cleanup, exact Telegram projection, unit coverage, and the missing settings-expiry Copywriting Contract row.
</objective>

<execution_context>
@/home/pogorelov/.codex/gsd-core/workflows/execute-plan.md
@/home/pogorelov/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-chat-readiness/01-CONTEXT.md
@.planning/phases/01-chat-readiness/01-UI-SPEC.md
@.planning/phases/01-chat-readiness/01-UAT.md
@.planning/phases/01-chat-readiness/01-LIVE-VERIFICATION-RUNBOOK.md
@.planning/quick/260826-e62-fix-cr-01-authorization-service-swallows/260826-e62-SUMMARY.md
@src/telegram/handlers.ts
@src/telegram/settings-handlers.ts
@src/domain/chat/settings-service.ts
@tests/unit/update-route-ownership.test.ts
</context>

<interfaces>
- `hasInFlightAction(services, context)` deliberately treats an existing row as route ownership even when its `expiresAt` has passed; keep this read-only probe before authorization.
- `findSettingsDraft(deps, context, now)` currently collapses both missing and expired rows to `null`; replace that ambiguity with a discriminated result while retaining the exported entry point.
- `SettingsService.selectValue(...)` already deletes an expired draft on a selection attempt; the new cleanup method must use the same durable expiry policy but remain explicit to the carrier-route path.
- `AuthorizationService.requireCurrentAdministrator(...)` now denies lookup failures without deleting drafts and deletes both draft types only for a positively observed non-administrator. Do not undo quick task 260826-e62.
</interfaces>

## Gap Disposition

- F-10 / window 14: covered exactly once by this plan.
- CR-01 from `01-VERIFICATION.md`: already resolved by quick task 260826-e62; current authorization tests prove fail-closed denial, bound error logging, and zero draft deletes when membership lookup throws. No new CR-01 task is created.
- UAT test 16 and N-6 remain owner-deferred and are not reopened.

## Artifacts this phase produces

- New exported type `SettingsDraftLookup` in `src/telegram/settings-handlers.ts`.
- New exported handler `handleExpiredSettingsDraft` in `src/telegram/settings-handlers.ts`.
- New `SettingsService.discardExpiredDraft(...)` method in `src/domain/chat/settings-service.ts`.
- No new file, dependency, Prisma model, column, or migration.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Route one expired settings text attempt through authorization, cleanup, and exact feedback</name>
  <read_first>
    <file>src/telegram/handlers.ts</file>
    <file>src/telegram/settings-handlers.ts</file>
    <file>src/domain/chat/settings-service.ts</file>
    <file>tests/unit/update-route-ownership.test.ts</file>
    <file>.planning/phases/01-chat-readiness/01-UAT.md</file>
  </read_first>
  <files>src/domain/chat/settings-service.ts, src/telegram/settings-handlers.ts, src/telegram/handlers.ts, tests/unit/update-route-ownership.test.ts</files>
  <behavior>
    - "An expired settingsEditDraft row makes message:text an owned route, then current-administrator authorization runs before draft cleanup."
    - "The expired row is deleted only when id, chat, actor, and expiresAt <= now all match; committed ChatConfiguration is untouched."
    - "The reply is exactly `This settings change expired after 30 minutes of inactivity. Open /settings to start again.` and SetupService.requireActive is not called."
    - "A second ordinary text update after cleanup produces no chat reply and no current-role lookup."
  </behavior>
  <action>Start RED in `tests/unit/update-route-ownership.test.ts` with a mutable settings-draft fake and an administrator path that drives a real grammY `message:text` update through `registerChatReadinessHandlers`. Then implement `SettingsDraftLookup` as `missing | expired | active`, make `findSettingsDraft` return it without mutating, and branch on all three outcomes in the text router. Add `SettingsService.discardExpiredDraft(chatId, actorId, draftId, now)` using one bound `deleteMany` predicate that can delete only a lapsed row. Add `handleExpiredSettingsDraft` to call that service method and emit the settings-specific copy. Keep the existing ownership-before-authorization order per AUTH-02, keep an unanswerable membership lookup fail-closed and non-destructive per the resolved CR-01, and keep positively observed demotion cleanup per D-14. Do not change Prisma schema, callback tokens, configuration revision, setup expiry copy, or the silent behavior of ordinary messages.</action>
  <acceptance_criteria>
    <criterion>The focused test observes draft read → membership lookup → lapsed-draft delete → one exact reply in that order.</criterion>
    <criterion>The test proves zero SetupService.requireActive calls and zero committed-configuration writes on the expired settings path.</criterion>
    <criterion>After deletion, another ordinary message has zero outbound Telegram calls and zero membership lookup calls.</criterion>
    <criterion>A throwing membership lookup still denies and leaves the expired draft intact, matching the already-landed CR-01 contract.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project unit tests/unit/update-route-ownership.test.ts tests/unit/authorization.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>The text carrier has one production-quality end-to-end expired-settings path and cannot silently fall into the setup wizard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the same expiry contract to location edits and make the copy authoritative</name>
  <read_first>
    <file>src/telegram/handlers.ts</file>
    <file>src/telegram/settings-handlers.ts</file>
    <file>tests/unit/update-route-ownership.test.ts</file>
    <file>.planning/phases/01-chat-readiness/01-UI-SPEC.md</file>
  </read_first>
  <files>src/telegram/handlers.ts, src/telegram/settings-handlers.ts, tests/unit/update-route-ownership.test.ts, .planning/phases/01-chat-readiness/01-UI-SPEC.md</files>
  <behavior>
    - "An expired timezone settings edit answered with a location follows the same authorization, cleanup, and settings-specific expiry projection as text."
    - "Missing settings draft still routes to setup; active settings draft still routes to its existing settings handler."
    - "The Copywriting Contract owns one settings-expiry sentence distinct from the setup-expiry sentence."
  </behavior>
  <action>Add the `expired` branch to `message:location` before either active-settings or setup dispatch and extend the route-ownership suite across missing, active, and expired outcomes for both carrier routes. Add the exact `Expired settings edit` row to the UI-SPEC Copywriting Contract: `This settings change expired after 30 minutes of inactivity. Open /settings to start again.` Clarify the general expiry bullet so setup uses its existing setup row and settings uses this new row. Preserve the UI Consideration exactly: validation, location, permission, expiry, save, duplicate, and stale-action failures use their corresponding copy row and preserve authoritative state. Do not broaden privacy-mode behavior, add a reply keyboard, or change any owner-deferred text-input card behavior.</action>
  <acceptance_criteria>
    <criterion>Both text and location lapsed-settings attempts emit the same exact settings-specific sentence once and delete no active draft.</criterion>
    <criterion>Active settings and setup carrier paths retain their existing destination and user-facing behavior.</criterion>
    <criterion>The UI-SPEC contains distinct setup-expiry and settings-expiry rows with no ambiguous generic fallback.</criterion>
    <criterion>The task introduces no package or schema change.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project unit tests/unit/update-route-ownership.test.ts &amp;&amp; npm run format:check &amp;&amp; npm run build</automated>
  </verify>
  <done>Every settings-edit input mode reports expiry from the settings surface and preserves the last authoritative configuration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update → carrier router | Untrusted text/location becomes a protected action only when an actor-bound draft row owns it. |
| Carrier router → Telegram membership gateway | Current role evidence must be refreshed before lapsed-draft cleanup or projection. |
| Settings handler → PostgreSQL | Cleanup must be bound to the observed actor/chat/draft/expiry and must not touch committed configuration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-23-01 | Elevation of privilege | `handlers.ts` carrier routes | high | mitigate | Preserve ownership probe → fresh administrator lookup → settings lookup/cleanup ordering and assert it in the composed-route test. |
| T-01-23-02 | Tampering | `discardExpiredDraft` | high | mitigate | Use one id/chat/actor/expiry-bound delete predicate; assert committed configuration and active drafts are unchanged. |
| T-01-23-03 | Repudiation | expired settings route | medium | mitigate | Replace the silent fallthrough with an exact user-visible outcome while retaining the existing bounded route log. |
| T-01-23-04 | Denial of service | membership lookup failure | medium | mitigate | Reuse the CR-01 fail-closed, zero-delete path so a transient Telegram failure cannot erase a current administrator's draft. |
</threat_model>

<verification>
- `npm run build`
- `npx vitest run --project unit tests/unit/update-route-ownership.test.ts tests/unit/authorization.test.ts`
- `npm run format:check`
- Confirm `git diff -- prisma package.json package-lock.json` is empty for this plan.
</verification>

<success_criteria>
- F-10 is closed in code and automated route coverage for both carrier input modes.
- Settings expiry uses its own exact English copy, deletes only the lapsed actor-bound row, and preserves committed configuration.
- AUTH-02 ordering and the resolved CR-01 non-destructive lookup-failure behavior remain green.
- UAT test 23 remains pending until the final live Telegram re-run.
</success_criteria>

<output>
Create `.planning/phases/01-chat-readiness/01-23-SUMMARY.md` when done.
</output>
