---
phase: 02-weekly-rehearsal-proposal
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - PLAN-01
  - AVAIL-01
  - AVAIL-05
  - REM-01
gap_closure: true
gap_ids:
  - G-02-6
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every phase section in ROADMAP.md that declares `**Mode:** mvp` has a Goal written as a User Story, and every phase whose Goal is declarative declares no mvp mode."
    - "The owner chose the remedy — corrected mode fields or rewritten goals — rather than the planner choosing it for them."
    - "Phase 1's already-rewritten User Story goal and its mode line are left exactly as they are."
    - "No phase's Requirements line, Depends on line, Success Criteria, plan list or completion marks are altered: the roadmap still shows 37 checked items after the edit."
  artifacts:
    - ".planning/ROADMAP.md — phases 2, 3, 4 and 5 with mode and goal format in agreement"
  key_links:
    - "ROADMAP `**Mode:** mvp` -> the verifier's MVP path: the mode line is what makes the verifier demand a User Story goal, so the two must be decided together"
---

<objective>
Close G-02-6: phases 2, 3, 4 and 5 of ROADMAP.md each pair `**Mode:** mvp` with a
declarative goal, while phase 1's goal has since been rewritten as a User Story.
Under MVP mode the verifier is instructed to refuse verification and demand a User
Story goal; it did not refuse, so the mismatch has been silently absorbed for four
of five phases.

Purpose: the mode line is not decoration — it selects the verification path. Leaving
it disagreeing with the goal format means every future phase either gets refused
verification for a reason that predates it, or gets verified on a path nobody chose.

Output: the four phase sections brought into agreement by whichever remedy the owner
picks, and nothing else in ROADMAP.md touched.

**This plan does not gate Phase 2.** G-02-6 is project-wide ROADMAP bookkeeping, not
a Phase 2 code defect — the UAT records that explicitly (`scope: project-wide — not a
Phase 2 code defect; do not gate Phase 2 on it`). It is planned here only because
that is where the gap was found. It carries no code change and no test, it depends on
no other plan in this phase, and no other plan depends on it.

Not in scope: rewriting phase 1 (already a User Story), running `/gsd mvp-phase` for
phases 3-5 (that is a per-phase planning action taken when those phases start), and
changing any Requirements, Success Criteria, plan list or dependency line.
</objective>

<execution_context>
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/workflows/execute-plan.md
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-weekly-rehearsal-proposal/02-UAT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: choose the remedy for the ROADMAP mode/goal mismatch</name>
  <decision>How should ROADMAP.md phases 2-5 resolve the mode/goal format mismatch?</decision>
  <context>
    ROADMAP.md line 24 carries phase 1's goal as a User Story and line 25 carries
    `**Mode:** mvp` — those agree. Lines 165-166, 209-210, 225-226 and 241-242 pair
    `**Mode:** mvp` with a declarative goal for phases 2, 3, 4 and 5 — those do not.

    The mode line is what makes the GSD verifier take the MVP path and demand a User
    Story goal. With no `**Mode:**` line the phase takes the standard path, with no
    behavioural change and no mismatch. Both remedies are therefore legitimate, and the
    difference is about how this project wants its phases framed — the owner's call, not
    the planner's.

    Phase 2 is already built and verified against five explicit Success Criteria, so
    nothing here changes what Phase 2 delivered either way.
  </context>
  <options>
    <option id="drop-mode">
      <name>Remove the `**Mode:** mvp` line from phases 2, 3, 4 and 5</name>
      <pros>Smallest possible edit — four deleted lines. The goals stay exactly as they were reviewed and executed against. Phases 3-5 can still be reframed later by running `/gsd mvp-phase N` when each phase starts, which writes the mode line back alongside a User Story it helps the owner author. Phase 1 keeps both its mode line and its User Story, so the pairing rule then holds everywhere.</pros>
      <cons>The project stops declaring MVP intent for four of five phases, even though vertical-slice planning is a recorded roadmap decision ("Use vertical MVP slices; reliability work is embedded in the capability that makes it observable"). The declaration and the practice drift apart in the other direction.</cons>
    </option>
    <option id="rewrite-goals">
      <name>Rewrite the four goals as User Stories, keeping `**Mode:** mvp`</name>
      <pros>Keeps the recorded MVP intent across the whole roadmap and matches what was already done to phase 1 by hand on 2026-08-30. Every phase then reads in one voice.</pros>
      <cons>Rewrites the goal text of three phases that have not been discussed or planned yet, fixing a framing before their context exists. Phase 2's goal would be reworded after the fact, so its plans and summaries would cite a goal line that no longer matches word for word.</cons>
    </option>
    <option id="rewrite-phase-2-only">
      <name>Rewrite phase 2's goal as a User Story now; drop `**Mode:** mvp` from phases 3, 4 and 5</name>
      <pros>Resolves the mismatch everywhere with the least speculation: phase 2 is fully known so its User Story can be written accurately, while phases 3-5 stay undeclared until `/gsd mvp-phase N` frames each one at the moment it is planned, with its own context available.</pros>
      <cons>Two different remedies live in one roadmap, so the file reads inconsistently until phases 3-5 are planned.</cons>
    </option>
  </options>
  <resume-signal>Reply with one of: `drop-mode`, `rewrite-goals`, or `rewrite-phase-2-only`. If you pick a rewrite, the drafts below are offered as starting text and you may replace any of them outright. Each is derived only from that phase's existing Goal and Success Criteria — no new scope is introduced.

- Phase 2 — As an authorized planner, I want to create and recover a single week-aware rehearsal proposal with the chat's defaults already applied, so that the band has exactly one clear proposal to answer.
- Phase 3 — As a band member in the confirmed lineup, I want to answer whether I can make a proposed rehearsal, so that the group can see a unanimous result and mark it booked.
- Phase 4 — As the planning author or a chat administrator, I want to replan, change or cancel a rehearsal after someone cannot attend, so that the band recovers from a conflict without losing the week.
- Phase 5 — As a band member, I want to be reminded only about what still needs my answer, so that the band agrees on a rehearsal without anyone chasing people manually.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply the chosen remedy to ROADMAP.md phases 2-5</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 20-30 — phase 1's section, already correct and the shape
      the other four are measured against. Do not edit it.
    - `.planning/ROADMAP.md` lines 163-170, 207-212, 223-228 and 239-244 — the four phase
      headers to change. Each is a `**Goal:**` line immediately followed by a `**Mode:**`
      line, then `**Depends on:**`, `**Requirements:**` and `**Success Criteria**`.
  </read_first>
  <action>
    Apply exactly the remedy selected at the checkpoint above, closing gap G-02-6, using
    scoped `Edit` calls only. Never rewrite `.planning/ROADMAP.md` with `Write` — a
    whole-file write would destroy every phase entry outside the edit window, including
    the 30 completed plan lines of phase 1 and the 6 of phase 2.

    For `drop-mode`: delete the mode line from each of the four phase sections and change
    nothing else in them.

    For `rewrite-goals`: replace the text after `**Goal:**` in each of the four sections
    with the User Story the owner approved at the checkpoint, and leave every mode line in
    place.

    For `rewrite-phase-2-only`: replace phase 2's goal text with the approved User Story and
    leave its mode line in place; delete the mode line from phases 3, 4 and 5 and change
    nothing else in them.

    In every case: touch no `**Requirements:**`, `**Depends on:**`, `**Success Criteria**`,
    `**Plans:**`, `**UI hint:**`, wave heading, or plan checkbox line. The completion marks
    on the executed plans must survive byte for byte.

    Record the chosen remedy and the owner's exact reply in the plan summary, so a later
    reader can see this mismatch was decided rather than absorbed a second time.
  </action>
  <verify>
    <automated>awk '/^\*\*Goal:\*\*/{goal=$0} /^\*\*Mode:\*\* mvp/{if (goal !~ /As a/) {print "MISMATCH: " goal; bad=1}} END{exit bad?1:0}' .planning/ROADMAP.md &amp;&amp; test "$(grep -c '^- \[x\]' .planning/ROADMAP.md)" = "37"</automated>
  </verify>
  <acceptance_criteria>
    - The awk gate above exits 0: no `**Mode:** mvp` line in ROADMAP.md is preceded by a
      Goal that is not a User Story. It exits 1 today and prints the four offending goals,
      which is the positive existential proving the gate is not vacuous (G-02-6).
    - `grep -c '^- \[x\]' .planning/ROADMAP.md` returns 37 — no completion mark was lost.
    - `grep -c '^\*\*Requirements:\*\*' .planning/ROADMAP.md` returns 5 — every phase still
      declares its requirement set.
    - `grep -n '^\*\*Goal:\*\* As a chat admin' .planning/ROADMAP.md` still matches at line 24 —
      phase 1 was not touched.
    - `git diff --stat .planning/ROADMAP.md` shows changes confined to the four phase header
      regions and no other file is modified.
  </acceptance_criteria>
  <done>ROADMAP.md declares mvp mode only where the goal is a User Story; the owner's chosen remedy is applied and recorded; every requirement line, success criterion, plan list and completion mark is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning documents -> future agent behaviour | ROADMAP fields are read by GSD tooling to select execution and verification paths, so a field edited by an agent changes what later agents do. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-27 | Tampering | whole-file rewrite of ROADMAP.md | high | mitigate | The action forbids `Write` on ROADMAP.md and requires scoped `Edit` calls; the acceptance criteria assert the checked-plan count is still 37 and that all five Requirements lines survive, so a truncating rewrite fails the gate rather than shipping. |
| T-02-28 | Repudiation | an unattended run deciding the owner's framing question | medium | mitigate | The decision checkpoint carries `gate="blocking-human"`, so auto-mode cannot select the first option on the owner's behalf; the chosen remedy and the owner's verbatim reply are recorded in the summary. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan touches one markdown file and installs nothing. An empty `git diff package.json package-lock.json` is an acceptance criterion. An executor that concludes an install is required must stop and route back to planning for a blocking human checkpoint. |
</threat_model>

<verification>
- The awk mode/goal agreement gate exits 0.
- `grep -c '^- \[x\]' .planning/ROADMAP.md` returns 37.
- `git diff --name-only` lists `.planning/ROADMAP.md` and nothing else.
</verification>

<success_criteria>
- G-02-6 closed: no phase declares `**Mode:** mvp` over a goal that is not a User Story.
- The remedy was chosen by the owner at a blocking-human checkpoint and is recorded in the summary.
- Phase 1's section and every requirement, criterion, dependency and completion mark in the file are unchanged.
</success_criteria>

<output>
Create `.planning/phases/02-weekly-rehearsal-proposal/02-08-SUMMARY.md` when done.
</output>
