---
context: phase
phase: 02-weekly-rehearsal-proposal
task: 4
total_tasks: 6
status: paused
last_updated: 2026-09-04T06:59:36.766Z
head: 5ce527c
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through reproduced failures. A resuming agent must acknowledge them before changing code.

- [ ] **Schema state must agree with migration history.** Both planning tables being absent is safe only when the applied ledger ends strictly before `20260831100411_planning_rounds`. A ledger that records that migration or any successor is inconsistent and must fail closed.
- [ ] **No-ledger is not automatically fresh.** When `_prisma_migrations` is absent, accept deployment only for a genuinely empty application schema; an arbitrary nonempty database must not receive migration authority.
- [ ] **A fixer report is not convergence.** After the blocker is fixed, an independent standard-depth Phase 02 review must return clean before security or phase completion can be trusted.

**Do not proceed until all boxes are explicitly acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention mechanism |
|---------|-------------|----------|----------------------|
| Schema inference from table absence alone | The guard previously treated two missing planning tables as a safe prefix even when the ledger said Phase 2 had already run; Prisma then exited 0 without repairing drift | blocking | Cross-check observed tables against an exact committed ledger cutoff and reproduce the drift state in PostgreSQL |
| Declaring convergence from implementation tests | Two review iterations passed their authored suites but fresh adversarial review found additional migration states | blocking | Re-run the independent reviewer after every final fix; accept only `status: clean` |
| Runtime/isolation mismatch | Project config reports Claude `harness-worktree`, while this Codex subagent schema cannot carry that flag | advisory | Use the GSD dispatch-isolation gate and explicitly record a sequential fallback when the host cannot express the configured primitive |
| Treating usage exhaustion as a code failure | The iteration-3 fixer returned before editing because its account usage window was exhausted | advisory | Retry from committed HEAD; do not reconstruct or revert the already completed fixes |

<current_state>
Phase 02 implementation and all five original gap plans are complete. The first review's seven application findings are fixed. T-02-30 received a guarded migration runner and two hardening iterations: valid Phase-1 prefixes, composite participant checks, an atomic locked migration, rollback proof, concurrency proof, and bounded/redacted Prisma output are complete.

The latest standard-depth review is committed at `5ce527c` and contains exactly one Critical finding, zero Warnings, and zero Info findings. The third fixer dispatch failed on account usage before making changes. The tracked tree was clean at pause time.
</current_state>

<completed_work>

- Gap plans 02-07 through 02-11 merged; original Phase 02 UAT/review defects fixed.
- Nyquist audit committed as `064a732`; it found no missing requirement coverage.
- First security audit committed as `dbe488d`; it found T-02-30 open.
- Seven application review findings fixed in `3fa377e` through `5b0109b`; report committed as `c63d3e8`.
- Quick task `260903-e9e` implemented the executable migration preflight in `5540bf5` and `6a725a3`; artifacts committed as `15725f5`.
- Four migration-path review findings fixed across `497f53f` through `e780bd9`; iteration-2 report committed as `b7612c0`.
- Latest review report committed as `5ce527c` with one remaining blocker.
- Latest green gates: 267 unit tests, 129 integration tests, 11 focused migration tests, typecheck, Prisma validation, scoped formatting, Docker migrate image/replay, and zero schema diff.
</completed_work>

<remaining_work>

1. Fix the sole current blocker in `prisma/migrate-deploy.mjs` and add real PostgreSQL cases to `tests/integration/migration-preflight.test.ts`:
   - fully migrate, drop both planning tables, run supported deploy, and require a non-zero inconsistent-baseline result;
   - when the Prisma ledger is absent, reject a nonempty application schema.
2. Commit/update `02-REVIEW-FIX.md`, then run a fresh standard-depth Phase 02 review until `status: clean`.
3. Re-run `$gsd-secure-phase 2`; update `02-SECURITY.md` only from the auditor verdict and require `threats_open: 0`.
4. Refresh `$gsd-validate-phase 2` so its observed counts include the current 267 unit and 129+ integration tests.
5. Re-run Phase 02 goal verification. Advance Phase 02 only if review, security, validation, regression, and verification all pass.
</remaining_work>

<decisions_made>

- Fix all Critical and Warning findings; do not accept or defer them.
- Keep preflight diagnostics bounded and credential/identifier-safe.
- Enforce authoritative participant-integrity checks under table locks inside one PostgreSQL transaction.
- Preserve the supported `npm run db:migrate:deploy` path used by CI and Compose.
- Require independent convergence review after the final fix.
</decisions_made>

<blockers>

- **Technical:** `hasValidMigrationPrefix` accepts an applied post-planning ledger when both planning tables are missing. Reproduced against disposable PostgreSQL; current review has exact file/line evidence and fix shape.
- **External:** the iteration-3 fixer agent hit its usage limit before making changes. Safe recovery is a direct retry from HEAD `5ce527c`.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — authoritative machine-readable resume state.
2. `.planning/phases/02-weekly-rehearsal-proposal/02-REVIEW.md` — sole current blocker and reproduction.
3. `.planning/phases/02-weekly-rehearsal-proposal/02-REVIEW-FIX.md` — completed iterations 1 and 2; do not redo them.
4. `prisma/migrate-deploy.mjs` — classifier that needs the final cutoff invariant.
5. `tests/integration/migration-preflight.test.ts` — existing 11-case real PostgreSQL matrix.
6. `.planning/phases/02-weekly-rehearsal-proposal/02-SECURITY.md` — stale blocked audit; re-audit rather than hand-closing.
7. `.planning/phases/02-weekly-rehearsal-proposal/02-VALIDATION.md` and `02-VERIFICATION.md` — compliant-but-stale closing artifacts.
8. `.planning/METHODOLOGY.md` if present — apply project analytical lenses.

## Critical Anti-Patterns (do NOT repeat these)

- Do not treat a valid migration-name prefix as sufficient when observed schema objects contradict its claimed endpoint.
- Do not accept an absent Prisma ledger as proof that the database is empty.
- Do not mark security closed from implementation intent; rerun the auditor on the final tree.
- Do not rerun or revert completed application fixes—the latest review explicitly verifies them closed.

## Infrastructure State

- Git HEAD at pause entry: `5ce527c`; tracked tree clean before handoff creation.
- Docker: no task containers or background services running.
- Async external jobs: none.
- Untracked `.agents/`, `.claude/`, `.codex/`, `.gsd/`, milestone lock, and research cache files are runtime/user state; preserve and do not commit them.
- The previous fixer failure was account usage exhaustion, not a repository or test failure.

<context>
The final fix should be small but must be tested at the release entry point. The classifier already validates committed migration names. Add the missing state-dependent upper bound: for a pre-planning schema, every successfully applied migration must precede the planning-round migration. Separately prove that a database without `_prisma_migrations` contains no application schema objects before calling it fresh. Keep the established bounded output and child-process redaction behavior unchanged.
</context>

<next_action>
Start with: run `$gsd-resume-work`, acknowledge the three blocking constraints, then retry `$gsd-code-review 2 --fix` from committed HEAD `5ce527c`. After the fix report is committed, re-run the standard Phase 02 review before security validation.
</next_action>
