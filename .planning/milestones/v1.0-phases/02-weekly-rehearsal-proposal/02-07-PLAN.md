---
phase: 02-weekly-rehearsal-proposal
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/telegram/planning-renderers.ts
  - src/telegram/roster-renderers.ts
  - src/telegram/callbacks.ts
  - src/telegram/planning-handlers.ts
  - tests/unit/planning-time-card.test.ts
  - tests/unit/callback-authority.test.ts
  - tests/unit/planning-ownership.test.ts
  - tests/unit/roster-rendering.test.ts
  - .planning/WINDOWS.md
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-08
gap_closure: true
gap_ids:
  - G-02-2
  - G-02-3
  - G-02-4
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The review card's lineup heading reads correctly for a roster of exactly one person, and so does its closing sentence — both count-dependent strings have a singular arm, not just the heading."
    - "A roster of two or more still reads with the counted plural heading and the plural closing sentence, unchanged from what ships today."
    - "A non-member who taps a button on a route-resolved planning card is told they are not in this chat, not that the surface is administrator-only."
    - "Every current-admin route still answers the verbatim Phase 1 administrator refusal for every non-administrator role."
    - "A private callback alert names an owner whose stored display name contains an ampersand or an angle bracket as the characters they actually stored, with no HTML entity in the text."
    - "The HTML card path is still escaped exactly once: the HTML label is literally the escaped plain label, so the two renderings can never disagree about who someone is."
    - "A stored identity with nothing readable still renders as the masked `Telegram user ••••NNNN` form on both the plain and the HTML path; a complete numeric Telegram id never reaches chat text or an alert."
    - "Broken window 20 is closed in the ledger with a reason naming the change that closed it, and the ledger reports zero open windows."
  artifacts:
    - "src/telegram/planning-renderers.ts — renderReviewStep with a singular arm on BOTH count-dependent strings"
    - "src/telegram/roster-renderers.ts — plainMemberLabel (one identity precedence, no escaping) with memberLabel defined as its escaped form"
    - "src/telegram/callbacks.ts — CallbackRoute as a discriminated union on authority, carrying nonMemberText only where the non-member branch is reachable"
    - "src/telegram/planning-handlers.ts — PLANNING_NON_MEMBER_DENIAL, and planningNotAuthorText rendered from the plain label"
    - "tests/unit/planning-time-card.test.ts — review-card cases for rosters of 0, 1 and 2"
    - "tests/unit/callback-authority.test.ts — the two refusals proven to answer different text"
    - "tests/unit/planning-ownership.test.ts — the ampersand alert asserted as plain text"
    - "tests/unit/roster-rendering.test.ts — memberLabel proven equal to the escaped plain label"
  key_links:
    - "planningNotAuthorText -> plainMemberLabel: the alert's only identity path, plain because answerCallbackQuery text is plain"
    - "memberLabel -> escapeHtml(plainMemberLabel(...)): the single escaper still runs exactly once, and only on the HTML path"
    - "callback boundary deniedNonMember branch -> route.nonMemberText: copy now follows the route's declared authority instead of a boundary-wide constant"
---

<objective>
Close the three copy defects UAT found on the planning surface: the review card
that reads as broken English for a one-member band (G-02-2), the non-member
refusal that claims an administrator-rights problem on a surface that admits any
member (G-02-3), and the private alert that shows an HTML entity where the owner's
name should be (G-02-4).

Purpose: all three are text a band member actually reads, and each one sends them
somewhere wrong — to re-read a sentence, to ask for an admin promotion they do not
need, or to wonder who "Ben &amp;amp; Jo" is. The owner elected to fix all seven
deliberately-unfixed defects on UAT test 5; these are the three that live in the
Telegram layer and need no schema change.

Output: three corrected strings paths, a route-carried refusal copy, one plain-text
identity renderer beside the existing HTML one, unit coverage for every arm, and
broken window 20 closed.

Not in scope, deliberately: `renderConfirmedStep` ("Asked to confirm availability:")
is count-neutral and has no singular defect — do not add arms to it. The zero-roster
arm of the review card keeps its existing dedicated line; only the one-vs-many
distinction is in this gap.
</objective>

<execution_context>
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/workflows/execute-plan.md
@/home/pogorelov/projects/bots/gsmbot/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-weekly-rehearsal-proposal/02-UAT.md

@src/telegram/planning-renderers.ts
@src/telegram/roster-renderers.ts
@src/telegram/callbacks.ts
@src/telegram/planning-handlers.ts
@tests/unit/planning-time-card.test.ts
@tests/unit/callback-authority.test.ts
@tests/unit/planning-ownership.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give both count-dependent strings on the review card a singular arm</name>
  <files>src/telegram/planning-renderers.ts, tests/unit/planning-time-card.test.ts</files>
  <read_first>
    - `src/telegram/planning-renderers.ts` lines 375-400 — `renderReviewStep`. The
      `lines` array holds two count-dependent strings. The heading ternary at line 386
      swaps only the NOUN; its determiner sits outside the ternary. The sentence at
      line 391 is a plain constant with no ternary at all. G-02-2's diagnosis is that
      fixing the heading alone leaves the card wrong.
    - `src/telegram/planning-renderers.ts` lines 403-428 — `renderConfirmedStep`. Read
      it to confirm it is count-neutral and must NOT be changed.
    - `tests/unit/planning-time-card.test.ts` lines 1100-1250 — the `LINEUP` fixture,
      `reviewProjection`, `reviewCard` and the existing `describe("the review card")`
      block. New cases go in that block and reuse `reviewCard({ members: [...] })`.
  </read_first>
  <behavior>
    - Roster of exactly 1: the lineup heading names one member with a singular
      determiner and a singular noun and carries no count digit.
    - Roster of exactly 1: the closing sentence still says the availability round is
      next and refers to that one person in the singular; the plural "each of them"
      phrasing does not appear on the card.
    - Roster of exactly 2: the heading carries the digit 2 with the plural determiner
      and plural noun, and the closing sentence uses the plural phrasing — both
      byte-identical to what ships today.
    - Roster of 0: the existing dedicated "Nobody is on the band roster yet." line and
      its `/roster_add` instruction render unchanged; no heading is emitted.
    - Every arm still ends the card with a sentence containing the word "availability"
      (D-04: the phase ends at a real proposal and says what comes next).
  </behavior>
  <action>
    Rewrite the lineup block of `renderReviewStep` so the heading and the closing
    sentence are BOTH selected by `members.length`, per UAT gap G-02-2.

    Heading, three arms: zero keeps the existing bold "Nobody is on the band roster
    yet." line and its instruction verbatim; one renders the bold heading with the
    singular determiner `this` and the singular noun `band member` and NO count; two
    or more renders the existing counted plural heading unchanged. Each arm is a whole
    heading string — the plural arm must stop selecting its noun with an inline ternary,
    because that expression is the defect, not merely its symptom.

    Closing sentence, two arms: one member gets a sentence that commits the rehearsal,
    starts the availability round, and refers to that single person in the singular
    ("where they answer whether they can make it"); zero or many keep the sentence that
    ships today unchanged. Compute both from the same `members.length` the heading
    reads, in the same expression region, so a future editor cannot move one arm
    without seeing the other.

    Do NOT reproduce the retired heading expression in a code comment. The absence gate
    below greps every line carrying the plural heading for the singular quoted noun, so a
    comment showing the old inline ternary would keep that gate red for an expression the
    card no longer renders.

    Leave `renderConfirmedStep` alone — its heading is count-neutral by construction.

    Then extend `describe("the review card")` in `tests/unit/planning-time-card.test.ts`
    with three cases driven through the existing `reviewCard` helper: a one-member
    roster, a two-member roster, and the empty roster. Assert the rendered TEXT, not a
    helper in isolation — the defect was visible only in the assembled card. Each case
    asserts both the heading and the closing sentence, and the singular case
    additionally asserts the plural phrasing is absent from the whole card.
  </action>
  <verify>
    <automated>npm run test:unit -- --project unit tests/unit/planning-time-card.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` passes with no skipped or todo tests (G-02-2).
    - `grep -c 'Asking these' src/telegram/planning-renderers.ts` returns at least 1 — the
      counted plural heading is still in the file, which is the positive existential that
      keeps the absence gate below scoped to a line that really exists.
    - `grep 'Asking these' src/telegram/planning-renderers.ts | grep -c 'band member"'`
      returns 0 — no line carrying the plural heading also carries the singular quoted noun.
      This returns 1 TODAY (line 386 selects the noun with an inline ternary), so it fails
      before the fix and passes only after the singular noun moves into its own heading arm.
      It replaces an earlier gate on the literal `Asking these band member`, which was
      vacuous: that string never appears in the file because the noun is interpolated.
    - `grep -c 'Asking this band member:' src/telegram/planning-renderers.ts` returns at
      least 1 — the singular arm exists as a whole string. This also returns 0 today.
    - `grep -c 'where they answer whether they can make it' src/telegram/planning-renderers.ts`
      returns at least 1, and `grep -c 'where each of them answers whether they can make it' src/telegram/planning-renderers.ts`
      returns at least 1 — both arms exist in the source.
    - `grep -c 'Asked to confirm availability:' src/telegram/planning-renderers.ts` returns 1 —
      the confirmed card was not touched.
    - `npm run typecheck` and `npm run format:check` pass.
  </acceptance_criteria>
  <done>A review card for a one-member roster reads as correct English in both the heading and the closing sentence; rosters of two or more and of zero render exactly as they do today; all three arms are asserted against the rendered card text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the non-member refusal copy follow the route's declared authority</name>
  <files>src/telegram/callbacks.ts, src/telegram/planning-handlers.ts, tests/unit/callback-authority.test.ts</files>
  <read_first>
    - `src/telegram/callbacks.ts` lines 60-100 — `CallbackAuthority`, `CallbackActorBinding`
      and `CallbackRoute`. `staleText` is already carried per route; the refusal copy is not.
    - `src/telegram/callbacks.ts` lines 290-302 — the `denyNonAdministrator` closure that
      hard-codes `CALLBACK_DENIAL`, and lines 385-395 — the `deniedNonMember` branch that
      calls it. Note the `route.authority === "current-admin"` branch above returns first,
      so ONLY a route-resolved route can reach the non-member branch.
    - `src/telegram/callbacks.ts` lines 458-495 — `rosterCallbackRoute` and
      `planningCallbackRoute`, and 496-528 — `registerChatReadinessCallbacks`, which is where
      the four route rows are declared.
    - `src/telegram/planning-handlers.ts` lines 58-70 — `PLANNING_STATUS_DENIAL`, the command
      -path copy. The new constant goes beside it.
    - `tests/unit/callback-authority.test.ts` lines 140-165 — the `route(kind)` builder inside
      `createHarness`, and lines 259-330 — the two refusal cases that must now differ.
  </read_first>
  <behavior>
    - A `member` or `restricted` actor still dispatches the planning kind (unchanged).
    - A `left`, `kicked` or `unknown` actor tapping the planning kind is answered with the
      planning non-member copy, `show_alert: true`, and the boundary still logs outcome
      `denied` with reason `not-a-current-chat-member`.
    - The text that non-member receives is NOT the administrator refusal string.
    - Every non-administrator role on every Phase 1 kind still receives the verbatim
      administrator refusal string, and still triggers the draft-discard side effect on the
      admin-only branch exactly as before.
    - A route-resolved actor who is not a member still has no draft of theirs deleted.
  </behavior>
  <action>
    Per UAT gap G-02-3: carry the non-member refusal text on the route row, the way
    `staleText` already is, and answer it from the non-member branch.

    In `src/telegram/planning-handlers.ts`, add an exported copy constant beside
    `PLANNING_STATUS_DENIAL` named `PLANNING_NON_MEMBER_DENIAL` with the text
    "Only people in this chat can use this rehearsal card." Document why it is a second
    constant rather than a reuse of `PLANNING_STATUS_DENIAL`: that one answers a COMMAND
    asking to see the plan, this one answers a TAP on a control, and Phase 1's D-13 already
    keeps command replies and button alerts worded for the gesture they answer. Leave
    `PLANNING_STATUS_DENIAL` and its two `handlers.ts` call sites untouched.

    In `src/telegram/callbacks.ts`, convert `CallbackRoute` from a single readonly object
    type into a discriminated union on `authority`: the `current-admin` variant keeps
    exactly the fields it has now, and the `route-resolved` variant adds a required
    `nonMemberText: string`. Do it this way rather than as an optional field with a
    fallback — an optional field that silently falls back to the administrator copy is
    precisely the shape that produced this defect, and the reachable-branch analysis above
    proves a `current-admin` route can never show a non-member alert, so a `nonMemberText`
    on those rows would be copy no user can ever see.

    Replace the hard-coded text in the shared denial closure with a text parameter, keeping
    its logging behaviour identical: the three administrator call sites pass
    `CALLBACK_DENIAL`, and the non-member branch passes `route.nonMemberText`. Update the
    closure's doc comment, which currently asserts that every non-administrator refusal
    answers the identical alert — that statement is what the fix retires. Set
    `nonMemberText: PLANNING_NON_MEMBER_DENIAL` on `planningCallbackRoute`.

    In `tests/unit/callback-authority.test.ts`, split the `route(kind)` builder so the
    planning kind returns the route-resolved shape with `nonMemberText` and the Phase 1
    kinds return the current-admin shape (the union no longer accepts one object whose
    `authority` is a union of literals). Change the "refuses the planning kind for anyone
    no longer in the chat" case to expect the imported planning constant, and add a case
    asserting the two refusal strings are different values — importing both constants from
    their modules rather than retyping either, so a copy edit breaks the assertion at its
    source. Leave the Phase 1 refusal matrix asserting the administrator constant verbatim.
  </action>
  <verify>
    <automated>npm run test:unit -- --project unit tests/unit/callback-authority.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` passes with no skipped or todo tests (G-02-3).
    - `grep -c 'PLANNING_NON_MEMBER_DENIAL' src/telegram/planning-handlers.ts` returns at least 1
      and `grep -c 'PLANNING_NON_MEMBER_DENIAL' src/telegram/callbacks.ts` returns at least 1.
    - `grep -c 'nonMemberText' src/telegram/callbacks.ts` returns at least 3 — the type
      variant, the route row, and the branch that answers it.
    - `grep -c 'PLANNING_STATUS_DENIAL' src/telegram/handlers.ts` returns at least 2 — the
      command path was not repurposed.
    - `grep -c 'CALLBACK_DENIAL' src/telegram/callbacks.ts` returns at least 2 — the
      administrator copy still exists and is still passed at its call sites.
    - `npm run typecheck` and `npm run format:check` pass.
  </acceptance_criteria>
  <done>A non-member tapping a planning control receives copy that names presence rather than administrator rights; the three administrator-only kinds answer the unchanged Phase 1 string; the boundary's outcome and reason vocabulary is unchanged; the route type makes it impossible to add a route-resolved route without deciding its non-member copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the owner-naming alert as plain text, and close window 20</name>
  <files>src/telegram/roster-renderers.ts, src/telegram/planning-handlers.ts, tests/unit/planning-ownership.test.ts, tests/unit/roster-rendering.test.ts, .planning/WINDOWS.md</files>
  <read_first>
    - `src/telegram/roster-renderers.ts` lines 14-52 — `escapeHtml` (documented as the ONE
      escaper in the Telegram layer) and `memberLabel`, which escapes on every branch and
      carries the `Telegram user ••••NNNN` mask required by threat T-01-21.
    - `src/telegram/planning-handlers.ts` lines 120-142 — `planningNotAuthorText` and its doc
      comment, which currently names `memberLabel` as the one identity function.
    - `tests/unit/planning-ownership.test.ts` lines 55-80 — the `NAMED_AUTHOR`, `MASKED_AUTHOR`
      and `HOSTILE_AUTHOR` fixtures — and lines 386-442, the three identity cases. The
      hostile-author case currently pins the ESCAPED form and is the assertion this gap
      proves wrong.
    - `.planning/phases/02-weekly-rehearsal-proposal/02-UAT.md` — the G-02-4 diagnosis. Note
      that 02-05 rejected a second escaper and a second identity path; the fix below adds
      neither.
  </read_first>
  <behavior>
    - An owner whose stored first name contains an ampersand is named in the alert with a
      literal ampersand; no HTML entity appears anywhere in the alert text.
    - An owner with a readable name and a username is named in the alert with the same
      name/username precedence the card uses.
    - An owner with nothing readable stored is named in the alert by the masked form, and the
      complete numeric Telegram id appears nowhere in it.
    - For every fixture identity, the HTML label equals the escaped plain label — one
      precedence, two renderings, and the escaper runs exactly once.
    - The review card and the planning card still escape identities: an owner name carrying
      angle brackets renders escaped inside the HTML card, unchanged from today.
  </behavior>
  <action>
    Per UAT gap G-02-4 and the owner's override of note 02-05: split the identity PRECEDENCE
    from the identity ESCAPING inside `src/telegram/roster-renderers.ts`.

    Extract the existing name / name-and-username / username / masked-fallback precedence out
    of `memberLabel` into a new exported `plainMemberLabel(member: RosterIdentity): string`
    that applies no escaping, and redefine `memberLabel` as `escapeHtml` applied to
    `plainMemberLabel`'s result. This is exactly what 02-05 asked for and not what it
    rejected: there is still ONE escaper and still ONE identity precedence, and the HTML
    label is now derived from the plain one rather than maintained beside it. It is
    behaviour-preserving because the separators the precedence joins with contain no
    character `escapeHtml` touches. Say all of that in the doc comments on both functions,
    and state on `plainMemberLabel` that it is for text sent WITHOUT a parse mode only.

    In `src/telegram/planning-handlers.ts`, change `planningNotAuthorText` to build from
    `plainMemberLabel`, and rewrite its doc comment: the alert is delivered through
    `answerCallbackQuery`, whose text is plain, so the escaped form was being shown to the
    reader as literal entities. The comment must still record that planning code never
    assembles a name out of the stored identity columns and that the mask still applies.

    In `tests/unit/planning-ownership.test.ts`: correct the hostile-author case, which
    currently pins the defect by expecting the escaped label in a plain-text alert. It should
    now assert the alert contains the plain label and contains no HTML entity at all. Add a
    dedicated case for an owner whose first name is an ampersand-joined pair — the exact UAT
    reproduction — asserting the alert contains the raw two-word name. Leave the masked-author
    and no-identity cases asserting the mask and the absence of the full numeric id.

    In `tests/unit/roster-rendering.test.ts`, add a case over a fixture set that includes a
    plain name, a name plus username, a username only, a masked identity and an
    HTML-significant name, asserting for each that the HTML label equals the escaped plain
    label. That is the gate that keeps the two renderings from ever disagreeing about who
    someone is.

    Finally close the ledger entry: run the GSD windows tool to mark window 20 fixed with a
    reason naming the split and the test that proves it. Use
    `node .claude/gsd-core/bin/gsd-tools.cjs query windows fixed 20 --reason "<reason>"`,
    then confirm with `query windows status`. Do not hand-edit the ledger JSON.
  </action>
  <verify>
    <automated>npm run test:unit -- --project unit tests/unit/planning-ownership.test.ts tests/unit/roster-rendering.test.ts tests/unit/planning-time-card.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` passes with no skipped or todo tests (G-02-4).
    - `grep -cE '\bplainMemberLabel\(' src/telegram/roster-renderers.ts` returns at least 2 —
      the definition and the call inside `memberLabel`.
    - `grep -cE '\bplainMemberLabel\(' src/telegram/planning-handlers.ts` returns at least 1,
      and `grep -cE '\bmemberLabel\(' src/telegram/planning-handlers.ts` returns 0.
    - `grep -cE '\bmemberLabel\(' src/telegram/planning-renderers.ts` returns at least 2 — the
      HTML card path still uses the escaped label.
    - `grep -c 'export function escapeHtml' src/telegram/roster-renderers.ts` returns 1 and
      `grep -rc 'replaceAll("&"' src/telegram/roster-renderers.ts` returns 1 — still one escaper.
    - `grep -rnE "firstName|lastName|username" src/telegram/planning-handlers.ts src/domain/planning/`
      returns nothing — the 02-06 negative grep still holds.
    - `node .claude/gsd-core/bin/gsd-tools.cjs query windows status` reports entry 20 with
      `"status": "fixed"` and a non-empty `reason`, and `open_count` of 0.
    - `npm run typecheck` and `npm run format:check` pass.
  </acceptance_criteria>
  <done>The private D-02 alert names an owner using the characters they actually stored; the HTML card is still escaped exactly once; one precedence and one escaper remain; the last open broken window is closed with cited evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored Telegram identity -> chat-visible text | A display name is attacker-controlled input that reaches both an HTML card and a plain alert. |
| Telegram group -> any planning control | Every member (and every non-member who can see the chat) can tap a button on the card. |
| Route declaration -> user-visible refusal | The copy a refusal shows must follow the authority the route declares, or it states something false about the system. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-23 | Information disclosure | `plainMemberLabel` on an unescaped path | high | mitigate | `plainMemberLabel` keeps every branch of the existing precedence including the `Telegram user ••••NNNN` mask, so a complete numeric Telegram id still cannot reach chat text or an alert (T-01-21). It is used only by `planningNotAuthorText`, whose delivery is `answerCallbackQuery` with no parse mode; a negative grep pins that planning code has no other identity path and `tests/unit/planning-ownership.test.ts` asserts the mask on both the masked and the no-identity fixture. |
| T-02-24 | Tampering | HTML injection via a display name on the card path | high | mitigate | `memberLabel` becomes `escapeHtml(plainMemberLabel(...))`, so the HTML path is escaped by construction and cannot drift from the plain path. A new equality case in `tests/unit/roster-rendering.test.ts` asserts the identity for an HTML-significant fixture, and the existing review-card case still asserts the escaped form inside the rendered card and the absence of a double encoding. |
| T-02-25 | Information disclosure | non-member refusal copy | low | mitigate | The new copy names presence only. It carries no round id, no author name, no step and no chat state, and it is shown only to an actor who can already see the card that produced it. The boundary's log line keeps its existing bounded outcome and reason and gains no new field. |
| T-02-26 | Elevation of privilege | route-type change to the shared callback boundary | high | mitigate | The change is additive on one variant of a discriminated union: the `current-admin` variant is byte-identical, the admin branch still returns before the non-member branch, and the draft-discard side effect stays on the admin branch alone. `tests/unit/callback-authority.test.ts` re-runs the full Phase 1 authority matrix (every kind against every role) as the regression gate. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan. 02-RESEARCH.md states no package installation is required for this phase; an empty `git diff package.json package-lock.json` is an acceptance criterion of the plan. An executor that concludes an install is required must stop and route back to planning for a blocking human checkpoint rather than installing. |
</threat_model>

<verification>
- `npm run test:unit` — green, no skipped or todo tests.
- `npm run typecheck` — green.
- `npm run format:check` — green.
- `git diff --stat package.json package-lock.json` — empty.
- `node .claude/gsd-core/bin/gsd-tools.cjs query windows status` — `open_count` 0.
</verification>

<success_criteria>
- G-02-2 closed: both count-dependent strings on the review card have a singular arm, asserted for rosters of 0, 1 and 2 against rendered card text.
- G-02-3 closed: the route-resolved non-member refusal answers member-appropriate copy while every current-admin route answers the unchanged Phase 1 string.
- G-02-4 closed: the owner-naming alert is plain text built from the one identity precedence, and the HTML path is still escaped exactly once.
- Broken window 20 is closed in the ledger and the ledger reports no open windows.
</success_criteria>

<output>
Create `.planning/phases/02-weekly-rehearsal-proposal/02-07-SUMMARY.md` when done.
</output>
