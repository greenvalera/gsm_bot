---
phase: 05-proactive-reliable-reminders
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: ["src/domain/reminders/reminder-service.ts","src/app/main.ts","tests/integration/reminder-tracer.test.ts","src/shared/chat-coordinator.ts","src/app/create-bot.ts","tests/integration/reminder-coordination.test.ts"]
autonomous: true
requirements: ["RELI-02","RELI-03","REM-01"]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-12: A committed reservation is sent at most once; an unknown send or crash after reservation cannot replay that occurrence, while later occurrences remain possible."
    - "D-14: Reminder failures and recovery are reported only through redacted technical logs, without group recovery notices."
    - "Duplicate jobs for one occurrence produce one record, one reservation and at most one transport call."
  artifacts:
    - path: "src/domain/reminders/reminder-service.ts"
      provides: "Prove one durable queue-to-Telegram send and shared coordination"
    - path: "src/app/main.ts"
      provides: "Prove one durable queue-to-Telegram send and shared coordination"
    - path: "tests/integration/reminder-tracer.test.ts"
      provides: "Prove one durable queue-to-Telegram send and shared coordination"
    - path: "src/shared/chat-coordinator.ts"
      provides: "Prove one durable queue-to-Telegram send and shared coordination"
  key_links:
    - from: "src/app/main.ts"
      to: "src/domain/reminders/reminder-service.ts"
      via: "Main passes bot.api transport and real queue jobs to ReminderService.dispatch"
---

<objective>
Prove one durable queue-to-Telegram send and shared coordination.
Purpose: Deliver the Phase 5 goal without weakening D-01–D-18 or the completed lifecycle.
Output: src/domain/reminders/reminder-service.ts, src/app/main.ts, tests/integration/reminder-tracer.test.ts, src/shared/chat-coordinator.ts, src/app/create-bot.ts, tests/integration/reminder-coordination.test.ts
</objective>

<execution_context>
@C:/dev/gsm_bot/.codex/gsd-core/workflows/execute-plan.md
@C:/dev/gsm_bot/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-proactive-reliable-reminders/05-CONTEXT.md
@.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md
@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md
@.planning/phases/05-proactive-reliable-reminders/05-COVERAGE.md
@.planning/phases/05-proactive-reliable-reminders/05-02-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols/contracts: ReminderService({prisma, now, logger, transport, coordinator}); reconcile(chatId?), dispatch(occurrenceId), stop(); ChatCoordinator.run(keys, action) and update middleware.

- `src/domain/reminders/reminder-service.ts`
- `src/app/main.ts`
- `tests/integration/reminder-tracer.test.ts`
- `src/shared/chat-coordinator.ts`
- `src/app/create-bot.ts`
- `tests/integration/reminder-coordination.test.ts`

<tasks>
<task type="tracer" tdd="true">
<name>05-03-01: Tracer: dispatch one real occurrence from queue through PostgreSQL to Telegram</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/domain/reminders/reminder-service.ts (if present); @src/app/main.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/domain/reminders/reminder-service.ts, src/app/main.ts, tests/integration/reminder-tracer.test.ts</files>
<behavior>One production entry path runs queue→ledger claim→Telegram transport→durable outcome; duplicate work sends once. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Start with a failing real-PostgreSQL integration check that provisions the queue through Plan02, inserts one due PLANNING_START occurrence for a ready chat with no current process, starts the real queue adapter, runs the production ReminderService dispatch and observes one injected Telegram transport call plus persisted SENT. Export ReminderService({prisma, now, logger, transport}) with reconcile(chatId?), dispatch(occurrenceId) and stop(); transport sends via the existing bot.api in main. Keep domain dispatch separate from renderer/queue through injected ports. For this one path reload canonical chat/config and current-week state; use a transaction to lock chat state, claim only PENDING/eligible known rejection, store attemptId/reservedAt before HTTP, then record SENT/messageId or terminal UNKNOWN. Duplicate jobs and a recreated service cannot resend RESERVED/SENT/UNKNOWN. A transport timeout or outcome-write failure leaves consumed reservation, never release it. Use safe log fields and finite reason codes per D-12/D-14. Main starts queue with the real service and closes clients on initial failure. Use a test transport at the external Telegram boundary only; queue and database are real.</action>
<acceptance_criteria>One production entry path runs queue→ledger claim→Telegram transport→durable outcome; duplicate work sends once.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/reminder-tracer.test.ts</automated></verify>
<done>One production entry path runs queue→ledger claim→Telegram transport→durable outcome; duplicate work sends once.</done>
</task>

<task type="auto" tdd="true">
<name>05-03-02: Coordinate worker sends with Telegram updates</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/shared/chat-coordinator.ts (if present); @src/app/create-bot.ts (if present); @src/app/main.ts (if present); @src/domain/reminders/reminder-service.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/shared/chat-coordinator.ts, src/app/create-bot.ts, src/app/main.ts, src/domain/reminders/reminder-service.ts, tests/integration/reminder-coordination.test.ts</files>
<behavior>Updates and worker use one coordinator and durable guards; different chats proceed independently; no transaction spans Telegram I/O. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Introduce ChatCoordinator.run(keys, action) and middleware using the same instance injected into createBot and ReminderService; preserve the effective migrationKeys old/new identities and acquire sorted unique keys without nested reentry. Replace the update-only sequentialize seam, keeping coordination before migrationBoundary and every handler. Hold application coordination across final reload, reservation and send/outcome handling; release DB transaction before HTTP. Acquire durable chat state lock before round lock consistently; two different occurrences for one round must contend on the same spacing row. Test a paused worker contending with settings, cancel and response updates; migration acquires both keys. A transition before reservation suppresses work; after reservation an in-flight send may complete. Retain this honest linearization boundary and single polling process.</action>
<acceptance_criteria>Updates and worker use one coordinator and durable guards; different chats proceed independently; no transaction spans Telegram I/O.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/reminder-coordination.test.ts tests/integration/reminder-tracer.test.ts</automated></verify>
<done>Updates and worker use one coordinator and durable guards; different chats proceed independently; no transaction spans Telegram I/O.</done>
</task>

</tasks>

<threat_model>
ASVS L1; unresolved high or critical findings block completion. No credentials or participant payloads in diagnostics.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram/queue to application | External actions and stale wakeups are untrusted; durable identity and current state authorize effects. |
| Application to PostgreSQL/Telegram | Database commits precede external effects; unknown delivery is consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-03-01 | Tampering | src/domain/reminders/reminder-service.ts | high | mitigate | Apply the task's explicit identity, migration or state guards and run its negative cases. |
| T-05-03-02 | Information disclosure | Logging and presentation | high | mitigate | Use redacted SafeLogger and authoritative snapshot rendering; record no credentials or raw participant payloads. |
| T-05-03-03 | Denial of service | Repeated updates and wakeups | medium | mitigate | Bound reconciliation, preserve occurrence uniqueness and use focused duplicate/race verification. |
</threat_model>

<verification>
Run each task's automated command on Node >=24.19 &lt;25 or the existing Docker toolchain. New test paths are creation deliverables, not existing passing evidence. Real-DB suites use reviewed migrations plus queue provisioning first. Record failures and fix within the owning scope; do not substitute typechecking for migration/behavior verification.
</verification>

<success_criteria>
All listed truths and task criteria hold; automated evidence and any explicit checkpoint disposition are recorded. These plans do not claim guaranteed exactly-once Telegram delivery.
</success_criteria>

<output>Create `.planning/phases/05-proactive-reliable-reminders/05-03-SUMMARY.md` with changed files, test evidence, decisions and residual risks.</output>
