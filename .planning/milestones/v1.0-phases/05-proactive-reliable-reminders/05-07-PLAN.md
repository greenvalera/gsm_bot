---
phase: 05-proactive-reliable-reminders
plan: "07"
type: execute
wave: 7
depends_on: ["05-06"]
files_modified: ["src/telegram/planning-handlers.ts","src/domain/planning/planning-service.ts","tests/integration/reminder-publication.test.ts","src/domain/reminders/reminder-service.ts","src/telegram/reminder-renderers.ts","tests/unit/reminder-renderers.test.ts","tests/integration/reminder-followups.test.ts"]
autonomous: true
requirements: ["REM-03","REM-04","REM-05"]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-06: Each fresh followup includes date/time, real mentions of only current pending snapshot members and current-card navigation; answer buttons remain exclusively on the availability card."
    - "D-07: Blocked rounds are silent; removing the block resumes scheduled eligibility, and successor rounds use their own roster snapshot."
    - "D-08: Followups stop at rehearsal start and after all-available, booked, cancelled, completed or superseded state."
    - "D-09: A due occurrence inside 30 minutes of acknowledged publication is permanently skipped, including after downtime."
    - "D-10: Exactly 30 minutes since the last potentially delivered same-round attempt is eligible; less is skipped."
    - "REM-04 empty/adjacency/ordering edges: zero pending means no message; A/C pending around answered B mention only A/C once, in snapshot order regardless of response or roster query order."
  artifacts:
    - path: "src/telegram/planning-handlers.ts"
      provides: "Send pending-only followups after acknowledged card publication"
    - path: "src/domain/planning/planning-service.ts"
      provides: "Send pending-only followups after acknowledged card publication"
    - path: "tests/integration/reminder-publication.test.ts"
      provides: "Send pending-only followups after acknowledged card publication"
    - path: "src/domain/reminders/reminder-service.ts"
      provides: "Send pending-only followups after acknowledged card publication"
  key_links:
    - from: "src/telegram/planning-handlers.ts"
      to: "src/domain/planning/planning-service.ts"
      via: "successful availability publication records guarded first/current-anchor acknowledgement"
---

<objective>
Send pending-only followups after acknowledged card publication.
Purpose: Deliver the Phase 5 goal without weakening D-01–D-18 or the completed lifecycle.
Output: src/telegram/planning-handlers.ts, src/domain/planning/planning-service.ts, tests/integration/reminder-publication.test.ts, src/domain/reminders/reminder-service.ts, src/telegram/reminder-renderers.ts, tests/unit/reminder-renderers.test.ts, tests/integration/reminder-followups.test.ts
</objective>

<execution_context>
@C:/dev/gsm_bot/.codex/gsd-core/workflows/execute-plan.md
@C:/dev/gsm_bot/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-proactive-reliable-reminders/05-CONTEXT.md
@.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md
@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md
@.planning/phases/05-proactive-reliable-reminders/05-COVERAGE.md
@.planning/phases/05-proactive-reliable-reminders/05-06-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols/contracts: recordAvailabilityPublication guarded acknowledgement; renderFollowupReminder and current-card navigation; publication/spacing eligibility.

- `src/telegram/planning-handlers.ts`
- `src/domain/planning/planning-service.ts`
- `tests/integration/reminder-publication.test.ts`
- `src/domain/reminders/reminder-service.ts`
- `src/telegram/reminder-renderers.ts`
- `tests/unit/reminder-renderers.test.ts`
- `tests/integration/reminder-followups.test.ts`

<tasks>
<task type="auto" tdd="true">
<name>05-07-01: Acknowledge availability publication and usable reanchors</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/telegram/planning-handlers.ts (if present); @src/domain/planning/planning-service.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/telegram/planning-handlers.ts, src/domain/planning/planning-service.ts, tests/integration/reminder-publication.test.ts</files>
<behavior>Only a reliably acknowledged current availability card enables followups; failure/recovery has an explicit durable grace boundary. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Extend editAnchor/repostAnchor result handling so acknowledged successful availability edits/sends (including recognized already-identical API responses) persist publication after the external success using guarded anchor identity. Confirmation/anchor creation is not evidence. Record firstAvailabilityPublishedAt once and availabilityAnchorAcknowledgedAt for the current availability anchor, never announcement slot. Failed first publication leaves reminders disabled; successful status recovery starts grace then. Same-round ordinary reanchor preserves first grace; group migration sets reminderGraceRestartAt and recovered card establishes fresh required boundary. Legacy rounds stay silent until acknowledged /plan_status recovery; never synthesize historical publication from confirmedAt. Ensure lost pointer persistence cannot mark an unrelated card usable. Test initial edit failure, success, same-round repost, displaced announcement, post-migration recovery and legacy round.</action>
<acceptance_criteria>Only a reliably acknowledged current availability card enables followups; failure/recovery has an explicit durable grace boundary.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/reminder-publication.test.ts</automated></verify>
<done>Only a reliably acknowledged current availability card enables followups; failure/recovery has an explicit durable grace boundary.</done>
</task>

<task type="auto" tdd="true">
<name>05-07-02: Deliver exact pending snapshot and current-card navigation</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/domain/reminders/reminder-service.ts (if present); @src/telegram/reminder-renderers.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/domain/reminders/reminder-service.ts, src/telegram/reminder-renderers.ts, tests/unit/reminder-renderers.test.ts, tests/integration/reminder-followups.test.ts</files>
<behavior>Only relevant pending snapshot members are mentioned once with a usable current-card navigation affordance. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>At claim reload exact round snapshot in its established display order, derive availabilityOutcome, require at least one pending and CONFIRMED status before fixed startsAt; apply D-06–D-10. Test grace at original due time so a 09:59 publication permanently skips 10:00 even after recovery, not deferred to 10:29. Atomically reserve per-round spacing using actual send instant; exactly 30 minutes passes. Render escaped real tg://user?id mentions only for pending snapshot identities, date/time and current availability navigation, no answer keyboard. Public supergroups use actual username/message ID, private supergroups documented channel link conversion, basic groups native reply-to-current-card navigation with concise /plan_status advice; this is a research assumption requiring live verification, never fabricate a channel URL from basic-group ID. Require acknowledged current anchor before sending and refresh anchor under shared coordination. Bound 4096-character content by shortening labels while retaining every pending mention; if even minimal IDs exceed Telegram capacity, mark known unsendable and log rather than silently omit participants or split one logical occurrence. Cover empty pending, A-pending/B-answered/C-pending adjacency, stable snapshot ordering, live-roster divergence, escaped malicious labels, unavailable block, startsAt equality and reanchor.</action>
<acceptance_criteria>Only relevant pending snapshot members are mentioned once with a usable current-card navigation affordance.</acceptance_criteria>
<verify><automated>npm test -- tests/unit/reminder-renderers.test.ts &amp;&amp; npm run test:integration -- tests/integration/reminder-followups.test.ts</automated></verify>
<done>Only relevant pending snapshot members are mentioned once with a usable current-card navigation affordance.</done>
</task>

</tasks>

<threat_model>
ASVS L1; unresolved high or critical findings block completion. No credentials or participant payloads in diagnostics.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram/queue to application | External actions and stale wakeups are untrusted; durable identity and current state authorize effects. |
| Application to PostgreSQL/Telegram | Database commits precede external effects; unknown delivery is consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-07-01 | Tampering | src/telegram/planning-handlers.ts | high | mitigate | Apply the task's explicit identity, migration or state guards and run its negative cases. |
| T-05-07-02 | Information disclosure | Logging and presentation | high | mitigate | Use redacted SafeLogger and authoritative snapshot rendering; record no credentials or raw participant payloads. |
| T-05-07-03 | Denial of service | Repeated updates and wakeups | medium | mitigate | Bound reconciliation, preserve occurrence uniqueness and use focused duplicate/race verification. |
</threat_model>

<verification>
Run each task's automated command on Node >=24.19 &lt;25 or the existing Docker toolchain. New test paths are creation deliverables, not existing passing evidence. Real-DB suites use reviewed migrations plus queue provisioning first. Record failures and fix within the owning scope; do not substitute typechecking for migration/behavior verification.
</verification>

<success_criteria>
All listed truths and task criteria hold; automated evidence and any explicit checkpoint disposition are recorded. These plans do not claim guaranteed exactly-once Telegram delivery.
</success_criteria>

<output>Create `.planning/phases/05-proactive-reliable-reminders/05-07-SUMMARY.md` with changed files, test evidence, decisions and residual risks.</output>
