---
phase: 05-proactive-reliable-reminders
plan: "06"
type: execute
wave: 6
depends_on: ["05-05"]
files_modified: ["src/domain/chat/setup-service.ts","src/domain/chat/settings-service.ts","src/domain/reminders/reminder-service.ts","tests/integration/reminder-settings.test.ts","tests/fakes/chat-readiness.ts","src/domain/planning/planning-service.ts","tests/integration/reminder-lifecycle.test.ts"]
autonomous: true
requirements: ["REM-01","REM-03","REM-05","RELI-02","RELI-03"]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-03: Cancelling the current week durably silences planning reminders for its remainder while manual planning is immediately available."
    - "D-15: A saved reminder-time change invalidates old future work immediately for current and future rounds."
    - "D-16: Settings changes create only strictly future occurrences; adding 14:00 at 15:00 cannot trigger recovery."
    - "D-17: A timezone change preserves reminder local wall-clock times while leaving the rehearsal instant fixed."
    - "D-18: Settings changes preserve publication grace and the round's last potentially delivered attempt."
  artifacts:
    - path: "src/domain/chat/setup-service.ts"
      provides: "Apply settings and cancellation atomically to reminder eligibility"
    - path: "src/domain/chat/settings-service.ts"
      provides: "Apply settings and cancellation atomically to reminder eligibility"
    - path: "src/domain/reminders/reminder-service.ts"
      provides: "Apply settings and cancellation atomically to reminder eligibility"
    - path: "tests/integration/reminder-settings.test.ts"
      provides: "Apply settings and cancellation atomically to reminder eligibility"
  key_links:
    - from: "src/domain/chat/settings-service.ts"
      to: "src/domain/reminders/reminder-service.ts"
      via: "saveChange transaction updates reminder generation and effectiveFrom atomically"
---

<objective>
Apply settings and cancellation atomically to reminder eligibility.
Purpose: Deliver the Phase 5 goal without weakening D-01–D-18 or the completed lifecycle.
Output: src/domain/chat/setup-service.ts, src/domain/chat/settings-service.ts, src/domain/reminders/reminder-service.ts, tests/integration/reminder-settings.test.ts, tests/fakes/chat-readiness.ts, src/domain/planning/planning-service.ts, tests/integration/reminder-lifecycle.test.ts
</objective>

<execution_context>
@C:/dev/gsm_bot/.codex/gsd-core/workflows/execute-plan.md
@C:/dev/gsm_bot/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-proactive-reliable-reminders/05-CONTEXT.md
@.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md
@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md
@.planning/phases/05-proactive-reliable-reminders/05-COVERAGE.md
@.planning/phases/05-proactive-reliable-reminders/05-05-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols/contracts: activateReminderSchedule(tx, chatId, now), changeReminderSchedule(tx, chatId, now); cancellation quietWeekStart/quietUntil and obsolete-round invalidation.

- `src/domain/chat/setup-service.ts`
- `src/domain/chat/settings-service.ts`
- `src/domain/reminders/reminder-service.ts`
- `tests/integration/reminder-settings.test.ts`
- `tests/fakes/chat-readiness.ts`
- `src/domain/planning/planning-service.ts`
- `tests/integration/reminder-lifecycle.test.ts`

<tasks>
<task type="auto" tdd="true">
<name>05-06-01: Commit activation and schedule generation with setup/settings</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/domain/chat/setup-service.ts (if present); @src/domain/chat/settings-service.ts (if present); @src/domain/reminders/reminder-service.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/domain/chat/setup-service.ts, src/domain/chat/settings-service.ts, src/domain/reminders/reminder-service.ts, tests/integration/reminder-settings.test.ts, tests/fakes/chat-readiness.ts</files>
<behavior>Saved settings immediately govern eligibility with no invented backlog or reset of protective clocks. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>Expose small transaction-accepting activation/change helpers from reminder-service, extending narrow persistence delegates and test fakes. In successful initial setup transaction write effectiveFrom and generation; in successful reminderMinutes or timezone save increment generation, set effectiveFrom to save instant and mark old pending/rejected future occurrences obsolete. Unrelated settings preserve generation and real missed work. All changes and callback consumption commit/roll back together. Reconciliation repairs a lost postcommit wake without extending old eligibility. Per D-15–D-18 generate only strictly future new-generation occurrences, preserve first publication and lastReminderAttemptAt, and leave startsAt/endsAt fixed. For timezone edits retain quietUntil no-earlier-than original next Monday and suppress the cancelled civil week in the new timezone as the explicitly recorded assumption. Test rollback, duplicate save, past-time insertion, exact-save equality, timezone/week crossing and grace/spacing persistence.</action>
<acceptance_criteria>Saved settings immediately govern eligibility with no invented backlog or reset of protective clocks.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/reminder-settings.test.ts</automated></verify>
<done>Saved settings immediately govern eligibility with no invented backlog or reset of protective clocks.</done>
</task>

<task type="auto" tdd="true">
<name>05-06-02: Persist quiet cancellation and invalidate obsolete lifecycle work</name>

<read_first>@.planning/phases/05-proactive-reliable-reminders/05-PATTERNS.md; @.planning/phases/05-proactive-reliable-reminders/05-RESEARCH.md; @src/domain/planning/planning-service.ts (if present); @src/domain/reminders/reminder-service.ts (if present); @prisma/schema.prisma; @src/infrastructure/time/zoned-clock.ts; @src/domain/planning/target-week.ts; @src/shared/callback-schema.ts; @src/domain/planning/planning-service.ts (relevant named methods); @src/app/create-bot.ts</read_first>
<files>src/domain/planning/planning-service.ts, src/domain/reminders/reminder-service.ts, tests/integration/reminder-lifecycle.test.ts</files>
<behavior>Cancellation quiet period survives restart and old-round jobs cannot affect a successor. Boundary cases and failure/race schedules listed in the action must be tested before implementation; keep the focused suite non-watch.</behavior>
<action>In cancellation apply transaction per D-03 record quietWeekStart/quietUntil when cancellation frees current chat-local week; preserve manual week release and existing cancelled notification behavior. In confirmed replan/change/supersession, booking and cancellation transactions invalidate old-round pending/rejected occurrences using immutable round id. Completion/started and all-available/blocked derived states are rechecked at dispatch even when no materialized completion transition exists. New rounds never inherit old jobs and snapshot current roster through existing Confirm. If an unavailable answer flips back, only legitimate scheduled work resumes; do not manufacture an unblocked occurrence or recover slots that were originally suppressed as blocked. Prove cancellation rollback, duplicate apply, restart quiet week, next Monday, immediate manual planning, exact startsAt cutoff and every lifecycle transition.</action>
<acceptance_criteria>Cancellation quiet period survives restart and old-round jobs cannot affect a successor.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/reminder-lifecycle.test.ts</automated></verify>
<done>Cancellation quiet period survives restart and old-round jobs cannot affect a successor.</done>
</task>

</tasks>

<threat_model>
ASVS L1; unresolved high or critical findings block completion. No credentials or participant payloads in diagnostics.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram/queue to application | External actions and stale wakeups are untrusted; durable identity and current state authorize effects. |
| Application to PostgreSQL/Telegram | Database commits precede external effects; unknown delivery is consumed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-06-01 | Tampering | src/domain/chat/setup-service.ts | high | mitigate | Apply the task's explicit identity, migration or state guards and run its negative cases. |
| T-05-06-02 | Information disclosure | Logging and presentation | high | mitigate | Use redacted SafeLogger and authoritative snapshot rendering; record no credentials or raw participant payloads. |
| T-05-06-03 | Denial of service | Repeated updates and wakeups | medium | mitigate | Bound reconciliation, preserve occurrence uniqueness and use focused duplicate/race verification. |
</threat_model>

<verification>
Run each task's automated command on Node >=24.19 &lt;25 or the existing Docker toolchain. New test paths are creation deliverables, not existing passing evidence. Real-DB suites use reviewed migrations plus queue provisioning first. Record failures and fix within the owning scope; do not substitute typechecking for migration/behavior verification.
</verification>

<success_criteria>
All listed truths and task criteria hold; automated evidence and any explicit checkpoint disposition are recorded. These plans do not claim guaranteed exactly-once Telegram delivery.
</success_criteria>

<output>Create `.planning/phases/05-proactive-reliable-reminders/05-06-SUMMARY.md` with changed files, test evidence, decisions and residual risks.</output>
