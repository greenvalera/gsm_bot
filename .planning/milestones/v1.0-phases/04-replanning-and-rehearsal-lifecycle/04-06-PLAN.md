---
phase: 04-replanning-and-rehearsal-lifecycle
plan: "06"
type: execute
wave: 6
depends_on: ["04-05"]
gap_closure: true
gap_ids: [G-04-1]
autonomous: true
requirements: [AVAIL-05, AVAIL-06, LIFE-03, LIFE-04]
files_modified:
  - src/telegram/planning-handlers.ts
  - src/telegram/planning-renderers.ts
  - tests/unit/planning-availability-card.test.ts
  - tests/integration/planning-lifecycle-review.test.ts
  - tests/integration/planning-recovery.test.ts
  - .planning/phases/04-replanning-and-rehearsal-lifecycle/04-UAT.md
  - .planning/phases/04-replanning-and-rehearsal-lifecycle/04-LIVE-TEST-2026-09-09.md
must_haves:
  truths:
    - "A relocated announcement cannot continue making a current readiness, blocking or booking claim after later transitions."
    - "After command Cancel/Change and Keep/Apply, the displaced message clearly directs users to the current plan instead of asserting that the obsolete slot is available or booked."
    - "Status and resume relocation follow the same retirement rule."
    - "Retiring an announcement does not remove live answer controls from the separate availability anchor or alter announcement cooldown/authorization semantics."
  artifacts:
    - path: src/telegram/planning-renderers.ts
      provides: "Neutral retired-message copy with safe date/context and /plan_status recovery."
    - path: src/telegram/planning-handlers.ts
      provides: "Explicit retirement of successfully displaced surfaces at lifecycle and status/resume call sites."
    - path: tests/integration/planning-lifecycle-review.test.ts
      provides: "Message-history regression assertions after answer changes, supersede and cancellation."
  key_links:
    - "deliverLifecycleConfirmation -> reanchorLifecycleConfirmation -> neutral retirement of previous control surface"
    - "repostAnchor -> successful reanchor -> neutral retirement of displaced surface"
    - "announcement retirement -> live availability anchor remains separate and usable"
---

<objective>
Close live gap G-04-1: command/status relocation leaves untracked old messages claiming that a blocked, superseded or cancelled rehearsal is ready/booked. Keep fresh bottom-of-chat confirmations while making each retired copy unambiguously historical.

Output: regression tests, a scoped rendering/handler correction, and rerun Chrome evidence. This plan does not claim closure of pending multi-account or time-boundary UAT.
</objective>

<context>
Read these files before editing:
- `.planning/STATE.md`
- `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-UAT.md`
- `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-LIVE-TEST-2026-09-09.md`
- `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-CONTEXT.md`
- `src/telegram/planning-handlers.ts` (clearSupersededCard, repostAnchor, deliverLifecycleConfirmation, deliverSuccessor and cancellation effects)
- `src/telegram/planning-renderers.ts`
- `src/domain/planning/planning-service.ts` (reanchorLifecycleConfirmation, reanchorAnnouncement and reanchor)
- `tests/integration/planning-lifecycle-review.test.ts`
- `tests/integration/planning-recovery.test.ts`

Root cause is verified in live Chrome and source. The service moves the sole message pointer; the handler removes old controls but renders active outcome text there. Later effects only know the current anchor/announcement pointers. There is no registry of all historical copies. Prefer neutral retirement at successful relocation; a new database registry/migration is unnecessary for this normal successful-delivery defect.

Preserve the existing authorization, transaction guards, one-active-round/week invariant and no-repeat notification cooldown. Do not globally change clearSupersededCard to generic moved copy: deliverSuccessor passes a deliberate terminal superseded record to it, which must survive.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce stale outcome claims in the Telegram message-history harness</name>
  <files>tests/integration/planning-lifecycle-review.test.ts, tests/integration/planning-recovery.test.ts</files>
  <action>
Extend the existing real-PostgreSQL, mocked-Telegram harness. Track the latest text and keyboard for every successful send/edit by message_id; inspecting only the final API call would miss the defect.

Create failing regressions for:
1. Confirmed round: unavailable -> ready -> command Cancel -> Keep -> unavailable. Assert the old ready announcement is explicitly retired, current announcement is blocked, and both availability buttons remain usable.
2. Booked round: command Change -> Apply. Assert a fresh same-week unbooked successor with cleared answers, terminal tracked old surfaces and no unqualified booked claim on displaced copies.
3. Booked round: command Cancel -> Apply. Assert a new cancellation result notice, tracked surfaces cancelled, and no unqualified booked claim on any successfully retired old message.
4. Status/resume relocation of draft, availability and announcement surfaces, followed by an answer change/cancellation where applicable. Old copies are neutral historical notices; only intended current surfaces retain controls.
5. Reanchor failure: the original remains the active surface; the untracked new message must not misleadingly present itself as the current interactive result. Preserve existing failure compensation and cooldown behavior.

Use at least two participants in the collecting/retraction variant, so returning from blocked to collecting is distinct from immediate readiness. Keep timestamps deterministic. Preserve tests for native payload acknowledgement order, failed edits and separate anchor/announcement pointers.
  </action>
  <verify>Run the modified integration files with the repository's supported Node 24.19 and Docker/Testcontainers environment. The new normal-delivery assertions fail on the current implementation for the observed stale-text reason. Commit the RED regression tests separately.</verify>
  <done>Regression tests reproduce the original ready/blocked contradiction and the booked cancellation/change variants, examining all messages rather than just retained pointers.</done>
</task>

<task type="auto">
  <name>Task 2: Retire displaced messages without making current outcome claims</name>
  <files>src/telegram/planning-handlers.ts, src/telegram/planning-renderers.ts, tests/unit/planning-availability-card.test.ts</files>
  <action>
Add or reuse a renderer for a neutral displaced-message notice, for example a dated earlier-message heading and “The current rehearsal details are in the newer message. Use /plan_status to find them.” Do not include current ready, everyone-can-attend, blocked or booked assertions. Escape dynamic labels using established renderer helpers.

After successful lifecycle reanchor, replace the previous control surface with that neutral notice unless it remains a separately tracked live availability anchor. Preserve live answer buttons in that case and remove only displaced lifecycle controls through the existing path. Apply the same distinction to successful status/resume reposts. Keep committed superseded/cancelled renderers specific; they must not be replaced with ambiguous moved copy.

When reanchor fails, keep the old tracked message current and retire the untracked new copy with recovery advice. Follow existing logging/error policy for failed Telegram edits; do not claim guaranteed remote delivery or introduce a new retry system in this patch. Do not mutate round status, membership, cooldown stamps, callback authorization or target-week rules.

Add focused renderer assertions for escaping and absence of current booking/readiness claims. Make Task 1 regressions pass. Audit every clearSupersededCard caller so the distinction between terminal transition, successful relocation and untracked failed relocation is explicit. Commit the implementation separately.
  </action>
  <verify>Run typecheck, the focused unit renderer tests, and the modified lifecycle/recovery integration files. Confirm existing acknowledgement-order, answer-button, pointer-separation and cooldown tests still pass.</verify>
  <done>Successful relocations cannot strand active-state copy, while live availability controls, authorization and notification limits retain their behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Validate in Chrome and update the UAT evidence</name>
  <files>.planning/phases/04-replanning-and-rehearsal-lifecycle/04-UAT.md, .planning/phases/04-replanning-and-rehearsal-lifecycle/04-LIVE-TEST-2026-09-09.md</files>
  <action>
Run `npm run typecheck`, `npm run test:unit`, and `npm run test:integration` in the supported environment. Format/check only touched files; avoid unrelated tooling formatting changes. Rebuild/restart the one local test bot through `docker compose up --build -d bot`; preserve `.env` and the existing database volume. Do not expose credentials.

Use the Chrome extension and the existing GSM_bot_test_group session. Create fresh disposable attempts and repeat the three observed sequences: ready -> command Cancel/Keep -> unavailable; booked -> command Change/Apply; booked -> command Cancel/Apply. Inspect both current and displaced cards, including the oldest outcome announcement, and verify the new cancellation message only for booked cancellation. Repeat status relocation and inspect the active availability buttons. Finish by cancelling any active disposable attempts.

Historical messages from the pre-fix run are untracked and retained as evidence; distinguish them by attempt/time. Do not claim the patch retroactively repaired those copies. Record fresh post-fix evidence with actual times and distinguish Web DOM IDs from Bot API IDs. Mark G-04-1 resolved only after the fresh regressions pass. Keep H1/H3/H6/H7 and missing H2/H4/H5 subcases pending or blocked; do not invent human prohibition acceptance.
  </action>
  <verify>All required automated checks pass, fresh Chrome scenarios show no contradictory retired claims, and no active disposable rehearsal remains. Create 04-06-SUMMARY.md with gap_ids: [G-04-1] and exact test/evidence results.</verify>
  <done>The defect has a tested correction and fresh live confirmation, with remaining phase acceptance explicitly separate.</done>
</task>

</tasks>

<verification>
- RED tests fail before the patch and pass after it.
- Every successfully displaced outcome surface is neutral, dated where appropriate, and points to /plan_status.
- Live answer controls, authorization, week ownership and the shared cooldown remain protected.
- Existing terminal cancellation/replan records retain their specific meaning.
- Fresh Chrome evidence covers the same sequences that exposed the gap.
</verification>

<success_criteria>
G-04-1 has passing regression and live evidence; no unrelated source changes or fabricated UAT acceptance. Other manual cases remain visible in 04-UAT.md.
</success_criteria>

<output>
Write `.planning/phases/04-replanning-and-rehearsal-lifecycle/04-06-SUMMARY.md` after execution. This plan is prepared, not executed.
</output>

## Plan check — 2026-09-09

Reviewed inline against the observed ready/blocked, booked/change and booked/cancel sequences. All map to G-04-1 and the same lost-message-pointer cause. The plan includes RED history assertions, the successful and failed relocation branches, live-anchor preservation, status/resume call sites, unchanged cooldown/authorization, and fresh Chrome re-verification. No independent agent review is claimed.

`gsd-tools query verify.plan-structure` returned valid, three complete tasks, zero errors and zero warnings. Execution and application tests have not run for this plan.
