---
phase: 08-localized-reminders-and-bilingual-verification
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - tests/integration/localized-reminders.test.ts
  - tests/helpers/reminders.ts
  - tests/integration/reminder-followups.test.ts
autonomous: true
requirements: ["LREM-01","LREM-02","L10N-03"]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "EDGE LREM-01/adjacency and ordering: queued/recovered planning sends use the locale at rendering, with stable identity, due time and at most one attempt."
    - "EDGE LREM-02/adjacency: committed language changes affect queued and eligible recovered follow-ups without changing grace, cooldown, pending snapshot or claim ownership."
    - "Unknown external outcomes remain consumed across restart and language changes; explicit proven rejection retains existing retry eligibility."
    - "D-02, D-09, D-14: valid controls and destinations remain valid; authorization and exactly-once acknowledgement remain unchanged in both languages."
  artifacts:
    - path: tests/integration/localized-reminders.test.ts
      provides: "Expanded real-database language-switch and reliability regression coverage"
    - path: tests/helpers/reminders.ts
      provides: "Expanded real-database language-switch and reliability regression coverage"
    - path: tests/integration/reminder-followups.test.ts
      provides: "Expanded real-database language-switch and reliability regression coverage"
  key_links:
    - from: tests/integration/localized-reminders.test.ts
      to: src/app/main.ts
      via: "createPlanningReminderTransport exercised through persisted ReminderService"
    - from: tests/integration/reminder-followups.test.ts
      to: src/domain/reminders/reminder-service.ts
      via: "deterministic recovery/claim behavior"
---

<objective>
Prove language switching is presentation-only across durable delivery and recovery.
Purpose: Complete Phase 8 current-language delivery and evidence without changing scheduling or domain authority.
Output: Scoped production/test artifacts and revision-specific evidence.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-CONTEXT.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-PATTERNS.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-SOURCE-AUDIT.md
</context>



<tasks>
<task type="auto" tdd="true">
<name>Task 1: Language changes preserve durable planning delivery and recovery</name>
<files>tests/integration/localized-reminders.test.ts, tests/helpers/reminders.ts</files>
<read_first>tests/integration/localized-reminders.test.ts, tests/helpers/reminders.ts, src/domain/reminders/reminder-service.ts, src/app/main.ts, src/infrastructure/jobs/reminder-queue.ts, tests/integration/localization-tracer.test.ts</read_first>
<behavior>Both language directions preserve occurrence identity/generation/dueAt and domain state; concurrent/repeated dispatch produces at most one attempt; unknown outcome never replays; eligible explicit-rejection retry re-renders current locale; in-flight payload is not recalled.</behavior>
<action>Extend the persisted tracer using deterministic clocks and isolated preference rows; resetReminders must remove test-chat language preferences as part of scoped cleanup. For en→uk and uk→en, create pending work first, snapshot occurrence IDs, generations, dueAt and round state, commit only the language change, then dispatch via createPlanningReminderTransport. Assert unchanged durable snapshots except ordinary claim/outcome fields. Cover restart with new service instance, eligible recovery, duplicate/competing claims, explicit sendMessage rejection followed by eligible retry in the newly selected locale, and unknown outcome followed by restart with no replay. Hold a send in flight, commit a language change, release the send and prove the already-rendered payload stays fixed while the next distinct eligible occurrence uses the new locale. Existing callback data remains valid; use actual reminder-start callback routes to assert fresh authorization and exactly one acknowledgement on success/denial/duplicate. Use existing callback harness rather than inventing capabilities.</action>
<reversibility rating="reversible">Source/test changes use existing durable identities; revert the scoped commit without data conversion.</reversibility>
<acceptance_criteria>Both language directions preserve occurrence identity/generation/dueAt and domain state; concurrent/repeated dispatch produces at most one attempt; unknown outcome never replays; eligible explicit-rejection retry re-renders current locale; in-flight payload is not recalled.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/localized-reminders.test.ts</automated></verify>
<done>Both language directions preserve occurrence identity/generation/dueAt and domain state; concurrent/repeated dispatch produces at most one attempt; unknown outcome never replays; eligible explicit-rejection retry re-renders current locale; in-flight payload is not recalled.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Protect follow-up grace, cooldown, recovery and pending snapshots</name>
<files>tests/integration/localized-reminders.test.ts, tests/integration/reminder-followups.test.ts</files>
<read_first>tests/integration/localized-reminders.test.ts, tests/integration/reminder-followups.test.ts, tests/helpers/reminders.ts, src/domain/reminders/reminder-service.ts, .planning/milestones/v1.0-phases/05-proactive-reliable-reminders/05-CONTEXT.md</read_first>
<behavior>EDGE LREM-02/adjacency: queued and eligible recovered follow-ups resolve committed current locale in both directions; 30-minute rules, latest-eligible selection, pending snapshots and duplicate claims behave identically; unknown sends remain consumed.</behavior>
<action>Add bilingual follow-up scenarios after a queued locale switch and service restart, with refreshed current anchor and authoritative pending snapshot. Assert the 30-minute publication grace and same-round minimum interval, latest eligible catch-up/coalescing, blocked/ready/cancelled/started suppression, generation invalidation only for real schedule changes, and known unsendable consumption without spacing reservation. Language-only changes must not regenerate jobs or bypass any of these checks. Preserve the explicit-rejection versus unknown-outcome distinction across both locales and competing dispatch. Use existing reminder-followups cases as the base; extend parameterization rather than duplicate every fixture. Reproduce one queued en→uk and one uk→en current-language send after recovery using the production service path.</action>
<reversibility rating="reversible">Source/test changes use existing durable identities; revert the scoped commit without data conversion.</reversibility>
<acceptance_criteria>EDGE LREM-02/adjacency: queued and eligible recovered follow-ups resolve committed current locale in both directions; 30-minute rules, latest-eligible selection, pending snapshots and duplicate claims behave identically; unknown sends remain consumed.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/localized-reminders.test.ts tests/integration/reminder-followups.test.ts</automated></verify>
<done>EDGE LREM-02/adjacency: queued and eligible recovered follow-ups resolve committed current locale in both directions; 30-minute rules, latest-eligible selection, pending snapshots and duplicate claims behave identically; unknown sends remain consumed.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted chat/round and user labels to Telegram output | Locale controls presentation; names, IDs and claims retain their existing authority. |
| Source/catalog/build to verification evidence | Checks must measure actual outbound paths and the shipped image. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-03-01 | Tampering | Reminder rendering | high | mitigate | Retain HTML escaping, validated navigation paths, pending-snapshot filtering and unchanged callback destinations; assert hostile Unicode labels and all mentions. |
| T-08-03-02 | Elevation of privilege | Locale and durable delivery | high | mitigate | Use existing chat identity and current authorization; locale changes never reopen claims or change permissions; preserve duplicate and unknown-outcome tests. |
| T-08-03-03 | Repudiation | Verification evidence | medium | mitigate | Record exact revision, commands and observed outcomes; distinguish native acceptance and scoped waivers from automated evidence. |
| T-08-03-SC | Tampering | Dependencies | low | accept | No dependency additions or version changes; use the existing approved lockfile and existing CI installation. |

ASVS L1; high severity and above blocks completion until mitigated. No new schema or package installation is introduced by this plan.
</threat_model>

## Artifacts this phase produces

- `tests/integration/localized-reminders.test.ts`: Expanded real-database language-switch and reliability regression coverage.
- `tests/helpers/reminders.ts`: Expanded real-database language-switch and reliability regression coverage.
- `tests/integration/reminder-followups.test.ts`: Expanded real-database language-switch and reliability regression coverage.

<verification>
Run every task's automated command. Preserve unrelated workspace changes. Tests using PostgreSQL require the repository's isolated integration database and existing migration setup; never use the live Telegram fixture database. Record failures accurately and rerun affected checks after corrections. Research was explicitly skipped; research-derived Nyquist VALIDATION.md is not applicable. These concrete tests remain mandatory.
</verification>

<success_criteria>
All listed truths and task criteria have executable evidence. No unresolved assumption is counted as a behavioral pass. Source audit coverage remains complete.
</success_criteria>

<output>
Create .planning/phases/08-localized-reminders-and-bilingual-verification/08-03-SUMMARY.md with changes, commands, tested revision and residual acceptance status.
</output>

