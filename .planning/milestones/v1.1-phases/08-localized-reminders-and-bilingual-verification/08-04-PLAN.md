---
phase: 08-localized-reminders-and-bilingual-verification
plan: "04"
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - src/telegram/migration-handler.ts
  - src/shared/i18n/index.ts
  - src/shared/i18n/en.ts
  - src/shared/i18n/uk.ts
  - tests/integration/chat-migration.test.ts
  - tests/fixtures/outbound-surfaces.ts
  - tests/unit/outbound-surfaces.test.ts
  - tests/fixtures/catalog-samples.ts
  - tests/unit/i18n.test.ts
autonomous: true
requirements: ["L10N-02","L10N-03"]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Old-chat callbacks receive one catalog-backed alert in the migration destination's current language; old commands remain ignored and no callback action is forwarded or mutates domain state."
    - "Every bot-owned outbound message, edit, button and alert has an inventoried catalog path and concrete bilingual regression evidence."
    - "Strict catalog verification rejects missing entries, noncallable or empty output and incompatible parameters; English default cannot hide Ukrainian gaps."
    - "EDGE L10N-03/empty: missing or empty runtime catalog causes a strict failure, never a translation-completeness pass."
    - "EDGE L10N-03/encoding: bilingual checks retain escaping, astral Unicode boundaries and message/alert output budgets."
  artifacts:
    - path: src/telegram/migration-handler.ts
      provides: "Destination-locale recovery alert with unchanged old-chat refusal"
    - path: src/shared/i18n/index.ts
      provides: "Typed migration.upgraded key implemented in en.ts and uk.ts"
    - path: tests/integration/chat-migration.test.ts
      provides: "Composed-bot bilingual migrated-chat refusal and acknowledgement regressions"
    - path: tests/fixtures/outbound-surfaces.ts
      provides: "New outboundSurfaces typed maintained surface inventory"
    - path: tests/unit/outbound-surfaces.test.ts
      provides: "New AST-based outbound inventory checker and synthetic fail-first fixtures"
    - path: tests/fixtures/catalog-samples.ts
      provides: "New catalogSamples mapped representative parameter inventory"
    - path: tests/unit/i18n.test.ts
      provides: "Strict catalog validation tests and compile-only reminder contract cases"
  key_links:
    - from: src/telegram/migration-handler.ts
      to: src/telegram/presentation-locale.ts
      via: "resolvePresentationLocale(prisma, migration.newChatId) for presentation only"
    - from: src/telegram/migration-handler.ts
      to: src/shared/i18n/index.ts
      via: "renderMessage(locale, migration.upgraded, undefined) before the single acknowledgement"
    - from: tests/unit/outbound-surfaces.test.ts
      to: src/telegram
      via: "AST discovery compares outbound expressions with inventory"
    - from: tests/unit/i18n.test.ts
      to: src/shared/i18n/index.ts
      via: "typed MessageParameters samples and direct catalog invocation"
---

<objective>
Detect incomplete catalogs and untranslated outbound surfaces with fail-first checks.
Purpose: Complete Phase 8 current-language delivery and evidence without changing scheduling or domain authority.
Output: Scoped production/test artifacts and revision-specific evidence.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-CONTEXT.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-PATTERNS.md
@.planning/phases/08-localized-reminders-and-bilingual-verification/08-SOURCE-AUDIT.md
</context>

<flagged_assumption>
EDGE L10N-02/unclassified remains unresolved: the generic shape classifier supplied no observable predicate. This is a scanner limitation, not an undefined product acceptance scenario, waiver or behavioral pass. The explicit L10N-02 catalog/inventory predicates in Tasks 2–3 prove the actual requirement independently. Do not ask the user to accept an unspecified condition.
</flagged_assumption>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Localize migrated-chat recovery without forwarding old actions</name>
<reversibility rating="reversible">Existing migration identities and transfer semantics remain unchanged; source/catalog/test edits require no data conversion.</reversibility>
<files>src/telegram/migration-handler.ts, src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, tests/integration/chat-migration.test.ts</files>
<read_first>src/telegram/migration-handler.ts, src/domain/chat/migration-service.ts, src/telegram/presentation-locale.ts, src/domain/chat/language-service.ts, src/app/create-bot.ts, src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, tests/integration/chat-migration.test.ts</read_first>
<behavior>L10N-02/L10N-03: after actual migration, old-chat callbacks use the destination preference in en/uk, acknowledge exactly once, and preserve post-migration state. Later destination language changes affect the next alert in both directions. Old commands remain silent; absent destination preference defaults to English.</behavior>
<action>Start with failing composed-bot tests in the existing chat-migration suite, using its real persisted fixture and Telegram API capture. Add the parameterless typed migration.upgraded catalog key. Retain the existing English sentence exactly; use informal Ukrainian equivalent Цю групу оновлено. Відкрий /plan_status у супергрупі. This unselected copy follows established tone and semantics, not a new user-selected phrase. In migrationBoundary retain the chatMigration.findUnique result, then use its authoritative newChatId with resolvePresentationLocale only when rendering the refused old-chat callback alert. Render through renderMessage and make the existing single answerCallbackQuery call. Keep the early return for every migrated old-chat update; do not rewrite ctx.chat, forward to next, accept old capabilities, create a preference on the old ID, or change migrateChat transfer semantics. Leave service-update handling and ordinary nonmigrated routing intact. Tests must perform migrateChat first, prove the preference moved, then snapshot language rows, migration ledger, rounds/participants, callback actions, configuration and reminder state/occurrences after migration. Deliver a stale old-chat callback through createBot.handleUpdate and assert exact locale text, unchanged /plan_status, at most 200 UTF-16 units, exactly one acknowledgement, no send/edit and unchanged snapshots. Repeat after committing each destination locale direction; take snapshots after the intentional language write. Cover missing preference English fallback and ignored old commands, retaining existing transfer/conflict/rollback and ordinary destination recovery assertions.</action>
<acceptance_criteria>Both catalogs own migration.upgraded; migrated stale callbacks render the destination's current locale with one acknowledgement and no domain mutation or forwarding. Old commands stay silent and migration preference metadata remains unchanged by presentation.</acceptance_criteria>
<verify><automated>npm run test:integration -- tests/integration/chat-migration.test.ts; npm run typecheck</automated></verify>
<done>The real composed-bot migration recovery path is bilingual and read-only after migration, and its automated regression is ready for inventory registration.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Make every outbound surface accountable to bilingual evidence</name>
<read_first>src/telegram/migration-handler.ts, tests/integration/chat-migration.test.ts, tests/fixtures/outbound-surfaces.ts, tests/unit/outbound-surfaces.test.ts, src/telegram/callbacks.ts, src/telegram/planning-handlers.ts, src/app/create-bot.ts, src/app/main.ts, tests/integration/localization-tracer.test.ts</read_first>
<action>After Task 1 passes, explicitly inventory migrationBoundary's old-chat answerCallbackQuery surface as migration.upgraded, linking the exact bilingual composed-bot regression names in tests/integration/chat-migration.test.ts. Its /plan_status token is a preserved token within translated catalog prose; the alert itself receives no dynamic-only exemption. This task owns inventory/checks only; Task 1 owns the production localization.

Create a maintained outboundSurfaces inventory with stable surface ID, source file/export or call-site locator, output kind (message/edit/button/alert), catalog key families, dynamic-only exemptions with reason, and concrete bilingual test file/case. Cover setup/settings/roster, planning/availability/ownership, readiness/manual booking, change/replan/cancel, stale/permission/validation/error/recovery, both reminder streams and global callback failures. Build a TypeScript AST source-to-inventory checker using the already installed TypeScript compiler API: recursively discover production .ts files, scan Telegram output sinks and renderer/keyboard/feedback factories, follow local wrapper/import references, and compare to exact inventoried sites. Fail on a new sink, unmapped literal output expression, missing key, stale entry or nonexistent test reference. Permit justified dynamic names, command tokens, IDs and IANA values only at their declared parameter position; do not exempt whole directories/functions or all English text. Include synthetic small source fixtures inside the test: an unlisted sendMessage, reply/editMessageText wrapper, keyboard text and answerCallbackQuery alert each must produce a named uncovered-surface failure. Adding untracked literal prose to an otherwise covered renderer must fail too. Do not treat a hand-maintained list alone or passing key parity as surface completeness. Each inventory entry must identify the actual exercised branch in both languages; plan 05 supplies cross-workflow residual assertions.</action>
<files>tests/fixtures/outbound-surfaces.ts, tests/unit/outbound-surfaces.test.ts</files>

<behavior>L10N-02: actual sources reconcile with inventory and key/test references; five injected uncovered-copy/sink variants fail for the expected reason; known clean fixtures pass; dynamic exemptions are narrow and reasoned.</behavior>

<reversibility rating="reversible">Source/test changes use existing durable identities; revert the scoped commit without data conversion.</reversibility>
<acceptance_criteria>L10N-02: actual sources reconcile with inventory and key/test references; five injected uncovered-copy/sink variants fail for the expected reason; known clean fixtures pass; dynamic exemptions are narrow and reasoned.</acceptance_criteria>
<verify><automated>npm run test:unit -- tests/unit/outbound-surfaces.test.ts</automated></verify>
<done>L10N-02: actual sources reconcile with inventory and key/test references; five injected uncovered-copy/sink variants fail for the expected reason; known clean fixtures pass; dynamic exemptions are narrow and reasoned.</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Require complete typed, nonempty catalogs independently of default locale</name>
<action>Define migration.upgraded as undefined in MessageParameters, consistent with existing parameterless keys, and include its undefined sample in catalogSamples and both direct catalog checks.

Add catalogSamples as a mapped type over every keyof MessageParameters, with representative exact payloads per key rather than one cast catch-all object. Test every entry in en and uk directly: complete identical key sets, callable entries and nonempty output for each sample; render both without fallback substitution. Keep compile-only negative cases using @ts-expect-error for unknown keys, absent required reminder parameters and invalid parameter types; typecheck must consume them. Add cloned synthetic catalogs with one removed Ukrainian key, empty catalog, empty output, noncallable entry and wrong payload contract fixture; each strict check fails specifically while complete catalogs pass. Runtime checks cannot prove all TypeScript compatibility, so keep compile-time contracts and runtime samples separate. Preserve English default for absent preferences, which is unrelated to accepting an incomplete Ukrainian catalog. Assert count/duration boundary samples 0,1,2,5,11,14,21,22,25,101,111 and mixed-hour grammar via the established helpers.</action>
<files>tests/fixtures/catalog-samples.ts, tests/unit/i18n.test.ts</files>
<read_first>tests/fixtures/catalog-samples.ts, tests/unit/i18n.test.ts, src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, tests/unit/planning-format.test.ts</read_first>
<behavior>Missing/empty Ukrainian catalog or required key fails even with English present; all keys are sampled with typed parameters; typecheck rejects deliberate bad parameter calls; every required count boundary is covered in both locales.</behavior>

<reversibility rating="reversible">Source/test changes use existing durable identities; revert the scoped commit without data conversion.</reversibility>
<acceptance_criteria>Missing/empty Ukrainian catalog or required key fails even with English present; all keys are sampled with typed parameters; typecheck rejects deliberate bad parameter calls; every required count boundary is covered in both locales.</acceptance_criteria>
<verify><automated>npm run test:unit -- tests/unit/i18n.test.ts tests/unit/planning-format.test.ts; npm run typecheck</automated></verify>
<done>Missing/empty Ukrainian catalog or required key fails even with English present; all keys are sampled with typed parameters; typecheck rejects deliberate bad parameter calls; every required count boundary is covered in both locales.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted chat/round and user labels to Telegram output | Locale controls presentation; names, IDs and claims retain their existing authority. |
| Source/catalog/build to verification evidence | Checks must measure actual outbound paths and the shipped image. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-04-01 | Tampering | Reminder rendering | high | mitigate | Retain HTML escaping, validated navigation paths, pending-snapshot filtering and unchanged callback destinations; assert hostile Unicode labels and all mentions. |
| T-08-04-02 | Elevation of privilege | Locale and durable delivery | high | mitigate | Use existing chat identity and current authorization; locale changes never reopen claims or change permissions; preserve duplicate and unknown-outcome tests. |
| T-08-04-03 | Repudiation | Verification evidence | medium | mitigate | Record exact revision, commands and observed outcomes; distinguish native acceptance and scoped waivers from automated evidence. |
| T-08-04-SC | Tampering | Dependencies | low | accept | No dependency additions or version changes; use the existing approved lockfile and existing CI installation. |
| T-08-04-04 | Elevation of privilege | Migrated-chat callback boundary | high | mitigate | Destination ID supplies presentation locale only; retain old-chat early return and assert one acknowledgement, no forwarding and unchanged post-migration domain snapshots. |

ASVS L1; high severity and above blocks completion until mitigated. No new schema or package installation is introduced by this plan.
</threat_model>

## Artifacts this phase produces

- `src/telegram/migration-handler.ts`: Current destination-locale recovery alert; unchanged old-chat refusal.
- `src/shared/i18n/index.ts`, `src/shared/i18n/en.ts`, `src/shared/i18n/uk.ts`: Typed bilingual migration.upgraded entry.
- `tests/integration/chat-migration.test.ts`: Persisted migration plus composed-bot bilingual acknowledgement and state invariants.
- `tests/fixtures/outbound-surfaces.ts`: New outboundSurfaces typed maintained surface inventory.
- `tests/unit/outbound-surfaces.test.ts`: New AST-based outbound inventory checker and synthetic fail-first fixtures.
- `tests/fixtures/catalog-samples.ts`: New catalogSamples mapped representative parameter inventory.
- `tests/unit/i18n.test.ts`: Strict catalog validation tests and compile-only reminder contract cases.

<verification>
Run every task's automated command. Preserve unrelated workspace changes. Tests using PostgreSQL require the repository's isolated integration database and existing migration setup; never use the live Telegram fixture database. Record failures accurately and rerun affected checks after corrections. Research was explicitly skipped; research-derived Nyquist VALIDATION.md is not applicable. These concrete tests remain mandatory.
</verification>

<success_criteria>
All listed truths and task criteria have executable evidence. No unresolved assumption is counted as a behavioral pass. Source audit coverage remains complete.
</success_criteria>

<output>
Create .planning/phases/08-localized-reminders-and-bilingual-verification/08-04-SUMMARY.md with changes, commands, tested revision and residual acceptance status.
</output>
