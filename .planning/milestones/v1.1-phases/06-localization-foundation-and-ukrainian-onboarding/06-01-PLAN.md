---
phase: 06-localization-foundation-and-ukrainian-onboarding
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260916180000_chat_language_preferences/migration.sql
  - src/shared/i18n/index.ts
  - src/telegram/settings-handlers.ts
  - tests/integration/localization-tracer.test.ts
  - prisma/migrate-deploy.mjs
  - tests/integration/migration-preflight.test.ts
  - tests/integration/chat-language-migration.test.ts
autonomous: true
requirements: ["LANG-03","LANG-04","LANG-05","L10N-01"]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "New and existing chats with no preference render English; a persisted Ukrainian preference renders Ukrainian independently of schedule configuration and Telegram client language."
    - "Preference rows distinguish implicit English from an explicit English selection; each bigint chat identity owns its own preference."
    - "Schema verification deploys committed migrations to disposable PostgreSQL before schema-dependent behavior checks, and upgrade preserves all existing application rows."
    - "LANG-04 empty edge: absent preference resolves English without creating configuration or borrowing another chat's locale."
    - "LANG-04 adjacency edge: equal locale values in two chats remain separately owned preferences."
    - "LANG-05 empty edge: an incomplete setup has durable language without a ChatConfiguration row."
  artifacts:
    - path: prisma/schema.prisma
      provides: "Prove persisted locale rendering and the migration boundary"
    - path: prisma/migrations/20260916180000_chat_language_preferences/migration.sql
      provides: "Prove persisted locale rendering and the migration boundary"
    - path: src/shared/i18n/index.ts
      provides: "Prove persisted locale rendering and the migration boundary"
    - path: src/telegram/settings-handlers.ts
      provides: "Prove persisted locale rendering and the migration boundary"
    - path: prisma/migrate-deploy.mjs
      provides: "Prove persisted locale rendering and the migration boundary"
  key_links:
    - from: src/telegram/settings-handlers.ts
      to: src/shared/i18n/index.ts
      via: "Preference-resolved locale selects typed language-row phrase."
    - from: prisma/schema.prisma
      to: src/telegram/settings-handlers.ts
      via: "chatLanguagePreference.findUnique by bigint chatId."
---

<objective>
Prove persisted locale rendering and the migration boundary.

Purpose: Deliver the scoped onboarding localization behaviors above with existing authorization, scheduling and durable identity guarantees.
Output: ChatLanguagePreference model and chat_language_preferences table; src/shared/i18n/index.ts: Locale, MessageParameters, MessageCatalog, renderMessage, language catalog entries; tests/integration/localization-tracer.test.ts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-PATTERNS.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-VALIDATION.md
</context>

## Artifacts this phase produces

- ChatLanguagePreference model and chat_language_preferences table
- src/shared/i18n/index.ts: Locale, MessageParameters, MessageCatalog, renderMessage, language catalog entries
- tests/integration/localization-tracer.test.ts
- prisma/schema.prisma
- prisma/migrations/20260916180000_chat_language_preferences/migration.sql
- tests/integration/localization-tracer.test.ts
- prisma/migrate-deploy.mjs
- tests/integration/migration-preflight.test.ts
- tests/integration/chat-language-migration.test.ts

<assumption_delta_decision>
Primary noun: per-chat rendering locale. Decision: promote implicit English to explicit Locale input; stable bigint chat identity is unchanged. Preference state is independent of configuration readiness. First explicit English records selection once; later same-locale choices are no-write no-ops.
</assumption_delta_decision>

## Flagged unresolved assumptions

- LANG-01 / unclassified: the raw edge classifier supplied no additional predicate. Approved D-01 first-setup behavior remains the contract; this row stays unresolved, not auto-proven.
- LANG-03 / unclassified: absent preference means implicit English, and legacy configured chats retain their existing English setup route. The unclassified row remains unresolved rather than inventing a classifier-derived fact.

## Execution contract

Research was explicitly skipped. Use current source/PATTERNS as Level-0 discovery; no new package installs. Preserve unrelated workspace edits. Write English documentation and Ukrainian interface copy. The leading phase tracer is 06-01 task 1; all other tasks expand that verified production path. Run a failing behavioral test before behavior changes, then make it pass. No full reminder delivery, lifecycle translation or historical-message rewriting is included. Run targeted checks after each task and typecheck at the plan boundary; record real outputs, never inferred passes. Integration fixtures deploy committed migrations to disposable PostgreSQL before schema-dependent queries. Per-task file ownership is bounded to five paths.

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Trace /settings through persisted preference and explicit-locale rendering</name>
  <reversibility rating="costly">The typed message contract will be shared by interactive and background callers; extraction preserves its signature.</reversibility>
  <files>prisma/schema.prisma, prisma/migrations/20260916180000_chat_language_preferences/migration.sql, src/shared/i18n/index.ts, src/telegram/settings-handlers.ts, tests/integration/localization-tracer.test.ts</files>
  <read_first>prisma/schema.prisma; prisma/migrations/20260916180000_chat_language_preferences/migration.sql; src/telegram/settings-handlers.ts; tests/integration/localization-tracer.test.ts; tests/helpers/postgres.ts; src/telegram/renderers.ts; tests/integration/chat-readiness.e2e.test.ts; .planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md. Read existing targets before editing; for new paths read the named analogs.</read_first>
  <behavior>A real composed bot /settings update for a current administrator reads a persisted Ukrainian preference from disposable PostgreSQL and emits a Ukrainian language row. With no preference, the same path renders English. No configuration row is manufactured.</behavior>
  <action>Start with a failing composed-handler integration test using the existing createBot harness and intercepted Telegram payloads. Introduce independent ChatLanguagePreference keyed by chatId bigint, locale constrained to en/uk, explicitlySelected boolean default false, createdAt and updatedAt timestamps. Do not use a configuration foreign key. Backfill is unnecessary: missing rows mean English and not explicitly selected. Create a committed SQL migration with the locale CHECK constraint. Build a pure typed renderMessage(locale, key, params) contract and complete en/uk catalog objects for language selection, language row, changed confirmation and timezone introductory phrases; both objects satisfy the same MessageCatalog mapped type derived from MessageParameters. Keep these initial catalog objects in index.ts; plan 03 extracts them without changing this contract. Give /settings a localized current-language row using the explicit locale resolved from Prisma, keeping complete and incomplete schedule handling separate. Existing configured chats without an explicit preference follow their established setup path in English; unconfigured chats use the first-choice rule when plan 02 connects selection. Apply the committed migration through startPostgresTestContainer/applyCommittedMigrations before any query; generate the Prisma client. This test seeds a preference to prove the actual read/render path; administrator selection is the next slice. Verify this tracer before proceeding.</action>
  <acceptance_criteria>Composed /settings traverses actual migrated PostgreSQL and the shared explicit-locale catalog; both locales and absent preference pass, with no artificial configuration. A real composed bot /settings update for a current administrator reads a persisted Ukrainian preference from disposable PostgreSQL and emits a Ukrainian language row. With no preference, the same path renders English. No configuration row is manufactured.</acceptance_criteria>
  <verify><automated>npm run db:generate &amp;&amp; npm run test:integration -- tests/integration/localization-tracer.test.ts</automated></verify>
  <done>Composed /settings traverses actual migrated PostgreSQL and the shared explicit-locale catalog; both locales and absent preference pass, with no artificial configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [BLOCKING] Verify committed migration deployment and preflight</name>
  <precondition>docker info succeeds and tests/helpers/postgres.ts can create a disposable PostgreSQL 18.4 container.</precondition>
  <files>prisma/migrate-deploy.mjs, tests/integration/migration-preflight.test.ts, tests/integration/chat-language-migration.test.ts</files>
  <read_first>prisma/migrate-deploy.mjs; tests/integration/migration-preflight.test.ts; tests/integration/chat-language-migration.test.ts; tests/helpers/postgres.ts; prisma/schema.prisma; prisma/migrations/20260916180000_chat_language_preferences/migration.sql; .planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md. Read existing targets before editing; for new paths read the named analogs.</read_first>
  <behavior>Fresh and predecessor-schema databases deploy successfully; the second deployment is idempotent. Invalid locale rows fail. Existing configuration, memberships, drafts and reminder records remain unchanged.</behavior>
  <action>Extend expectedApplicationCatalog and ordered migration-ledger handling for the exact new table, physical column order, primary key and locale CHECK. Add a predecessor upgrade fixture with startPostgresTestContainer({mode: before, exclusiveCutoff: 20260916180000_chat_language_preferences}) and a fresh all-migrations fixture. Exercise the repository's migrate-deploy.mjs wrapper against each disposable URL, then Prisma migrate status. Assert resulting catalog and repeat-deploy behavior, legacy English resolution, and before/after domain-row equality. This is the blocking schema-gate implementation, using committed migration deployment rather than schema synthesis. Test-generated DATABASE_URL must point to the disposable container; never substitute a configured real database. Stop downstream execution on failure or unavailable Docker and record the concrete blocker; generated types alone cannot satisfy the gate.</action>
  <acceptance_criteria>Fresh/upgrade/repeat deployment and catalog-drift checks pass against disposable PostgreSQL; evidence records schema migration and tested revision. Fresh and predecessor-schema databases deploy successfully; the second deployment is idempotent. Invalid locale rows fail. Existing configuration, memberships, drafts and reminder records remain unchanged.</acceptance_criteria>
  <verify><automated>npm run test:integration -- tests/integration/chat-language-migration.test.ts tests/integration/migration-preflight.test.ts tests/integration/localization-tracer.test.ts</automated></verify>
  <done>Fresh/upgrade/repeat deployment and catalog-drift checks pass against disposable PostgreSQL; evidence records schema migration and tested revision.</done>
</task>
</tasks>

<threat_model>
ASVS level 1; block on unresolved high or critical threats.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update → controller | Actor, chat and opaque action are untrusted; current role and stored validity remain authoritative. |
| Controller → PostgreSQL | Chat-keyed preference and domain records have separate ownership and mutation paths. |
| Catalog/data → Telegram output | Dynamic member values retain existing escaping and output budgets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01-01 | Spoofing/Elevation | Onboarding controls | high | mitigate | Retain fresh role, chat/actor binding, expiry/consumption checks; assert denied writes and exactly one ack in affected route tests. |
| T-06-01-02 | Tampering | Preference/domain state | high | mitigate | Resolve by bigint chat identity, validate en/uk, isolate locale writes from revisions and schedule generation, verify state equality around switching. |
| T-06-01-03 | Information disclosure | Rendered dynamic data | medium | mitigate | Escape once at projection boundary, retain Unicode/UTF-16 budgets and scoped logs; assert hostile-name payloads. |
| T-06-01-04 | Denial of service | Telegram callback output | medium | mitigate | Preserve callback fallback and bounds; reject unsupported targets through normal controlled outcomes. |
</threat_model>

<verification>
npm run db:generate && npm run test:integration -- tests/integration/localization-tracer.test.ts
npm run test:integration -- tests/integration/chat-language-migration.test.ts tests/integration/migration-preflight.test.ts tests/integration/localization-tracer.test.ts
npm run typecheck
Use 06-VALIDATION.md for source coverage and phase-level checks. PostgreSQL suites may exceed 60 seconds during container startup; stream progress rather than replace real database proof with mocks.
</verification>

<success_criteria>
All must-have truths have direct behavioral evidence; task commands pass, English behavior remains supported, and no high/critical threat is left open within scope.
</success_criteria>

<output>
Create `.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-01-SUMMARY.md` with implementation and verification evidence.
</output>
