---
phase: 06-localization-foundation-and-ukrainian-onboarding
plan: "06"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/domain/chat/migration-service.ts
  - tests/integration/chat-language-identity.test.ts
  - tests/integration/chat-migration.test.ts
  - src/domain/chat/language-service.ts
  - tests/unit/chat-language-service.test.ts
autonomous: true
requirements: ["LANG-03","LANG-04","LANG-05"]
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "LANG-05 adjacency edge: replaying the same old/new group migration pair is idempotent and preserves the transferred preference."
    - "LANG-05 ordering edge: preference transfer, tombstone creation and existing domain transfer commit atomically; delayed old-chat updates retain existing suppression."
    - "Language selected before configuration exists survives group-to-supergroup migration, restart and repeated setup."
    - "A destination with its own preference causes a fail-closed migration conflict instead of overwriting its administrator's choice."
    - "Preference migration keeps setup ownership/values and existing migration invalidation rules for old-message callbacks."
  artifacts:
    - path: src/domain/chat/migration-service.ts
      provides: "Preserve independent language across group identity migration"
    - path: src/domain/chat/language-service.ts
      provides: "Preserve independent language across group identity migration"
  key_links:
    - from: src/domain/chat/migration-service.ts
      to: prisma/schema.prisma
      via: "Preference table participates in atomic lock/conflict/transfer."
    - from: src/domain/chat/language-service.ts
      to: src/domain/chat/migration-service.ts
      via: "Tombstone guard and compatible lock ordering prevent old-ID recreation."
---

<objective>
Preserve independent language across group identity migration.

Purpose: Deliver the scoped onboarding localization behaviors above with existing authorization, scheduling and durable identity guarantees.
Output: migrateChat preference lock/conflict/transfer extension; tests/integration/chat-language-identity.test.ts; tests/unit/chat-language-service.test.ts.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-PATTERNS.md
@.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-VALIDATION.md
</context>

## Artifacts this phase produces

- migrateChat preference lock/conflict/transfer extension
- tests/integration/chat-language-identity.test.ts
- tests/unit/chat-language-service.test.ts
- tests/integration/chat-language-identity.test.ts
- tests/integration/chat-migration.test.ts
- tests/unit/chat-language-service.test.ts

## Execution contract

Research was explicitly skipped. Use current source/PATTERNS as Level-0 discovery; no new package installs. Preserve unrelated workspace edits. Write English documentation and Ukrainian interface copy. The leading phase tracer is 06-01 task 1; all other tasks expand that verified production path. Run a failing behavioral test before behavior changes, then make it pass. No full reminder delivery, lifecycle translation or historical-message rewriting is included. Run targeted checks after each task and typecheck at the plan boundary; record real outputs, never inferred passes. Integration fixtures deploy committed migrations to disposable PostgreSQL before schema-dependent queries. Per-task file ownership is bounded to five paths.

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Transfer preference-only and configured chats atomically</name>
  <files>src/domain/chat/migration-service.ts, tests/integration/chat-language-identity.test.ts, tests/integration/chat-migration.test.ts</files>
  <read_first>src/domain/chat/migration-service.ts; tests/integration/chat-language-identity.test.ts; tests/integration/chat-migration.test.ts; src/telegram/migration-handler.ts; tests/helpers/postgres.ts; src/domain/chat/language-service.ts; prisma/schema.prisma; .planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md. Read existing targets before editing; for new paths read the named analogs.</read_first>
  <behavior>Preference-only old chat migrates to new chat with no configuration; configured Ukrainian chat preserves all domain data; duplicate pair is a no-op; destination preference conflicts rollback every mutation.</behavior>
  <action>Add chat_language_preferences to migrateChat's consistent table-lock order, destination-state conflict checks and atomic chatId transfer. Treat any destination-owned preference as destination state requiring operator review under existing fail-closed conflict semantics; a repeated completed old/new pair still returns already-migrated before conflicting-state checks. Cover absent preferences, explicit English, Ukrainian and preference-only incomplete setup. Preserve createdAt/updatedAt/explicitlySelected and all current draft, callback-expiry, planning and reminder transfer behavior. Test a forced failure for rollback, duplicate event idempotence, old-chat tombstone suppression and actual composed post-migration /settings lookup. Reuse committed migration fixtures, never create schema directly. Per D-02/D-16 the migrated actor resumes their own saved draft in saved language.</action>
  <acceptance_criteria>Language survives incomplete/configured identity transfer and duplicate events; conflicting destination state preserves both sides atomically. Preference-only old chat migrates to new chat with no configuration; configured Ukrainian chat preserves all domain data; duplicate pair is a no-op; destination preference conflicts rollback every mutation.</acceptance_criteria>
  <verify><automated>npm run test:integration -- tests/integration/chat-language-identity.test.ts tests/integration/chat-migration.test.ts</automated></verify>
  <done>Language survives incomplete/configured identity transfer and duplicate events; conflicting destination state preserves both sides atomically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden independent preference persistence and no-op semantics</name>
  <files>src/domain/chat/language-service.ts, tests/unit/chat-language-service.test.ts, tests/integration/chat-language-identity.test.ts</files>
  <read_first>src/domain/chat/language-service.ts; tests/unit/chat-language-service.test.ts; tests/integration/chat-language-identity.test.ts; src/shared/chat-coordinator.ts; src/telegram/language-handlers.ts; src/domain/chat/migration-service.ts; .planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-CONTEXT.md. Read existing targets before editing; for new paths read the named analogs.</read_first>
  <behavior>Two service instances read the same durable preference; two groups stay isolated; repeated explicit same-language selection performs no write; implicit English selection persists its marker once; tombstoned old IDs cannot acquire new preferences.</behavior>
  <action>Test and harden service operations across reconstructed clients and current identity checks. Verify language selection cannot recreate a tombstoned old chat, including a selection racing identity transfer; coordinate transaction/locking with the migration path using the existing chat coordinator and database guard. Keep accepted selection ordering deterministic and independent of unrelated configuration revisions per D-14. Assert no-op retains updatedAt and explicit selection marker per D-08, and locale-only selection leaves configuration/drafts/reminder fields unchanged. Preserve exported LanguageService signatures used by parallel plan 03. Record the concrete concurrency mechanism and test it against real PostgreSQL in the identity suite; unit tests alone cannot prove atomic transfer.</action>
  <acceptance_criteria>Durable preference service preserves default/explicit/no-op and identity invariants across client reconstruction and migration races. Two service instances read the same durable preference; two groups stay isolated; repeated explicit same-language selection performs no write; implicit English selection persists its marker once; tombstoned old IDs cannot acquire new preferences.</acceptance_criteria>
  <verify><automated>npm run test:unit -- tests/unit/chat-language-service.test.ts &amp;&amp; npm run test:integration -- tests/integration/chat-language-identity.test.ts</automated></verify>
  <done>Durable preference service preserves default/explicit/no-op and identity invariants across client reconstruction and migration races.</done>
</task>
</tasks>

<threat_model>
ASVS level 1; block on unresolved high or critical threats.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update → controller | Actor, chat and opaque action are untrusted; current role and stored validity remain authoritative. |
| Controller → PostgreSQL | Chat-keyed preference and domain records have separate ownership and mutation paths. |
| Catalog/data → Telegram output | Dynamic member values retain existing escaping and output budgets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-06-01 | Spoofing/Elevation | Onboarding controls | high | mitigate | Retain fresh role, chat/actor binding, expiry/consumption checks; assert denied writes and exactly one ack in affected route tests. |
| T-06-06-02 | Tampering | Preference/domain state | high | mitigate | Resolve by bigint chat identity, validate en/uk, isolate locale writes from revisions and schedule generation, verify state equality around switching. |
| T-06-06-03 | Information disclosure | Rendered dynamic data | medium | mitigate | Escape once at projection boundary, retain Unicode/UTF-16 budgets and scoped logs; assert hostile-name payloads. |
| T-06-06-04 | Denial of service | Telegram callback output | medium | mitigate | Preserve callback fallback and bounds; reject unsupported targets through normal controlled outcomes. |
</threat_model>

<verification>
npm run test:integration -- tests/integration/chat-language-identity.test.ts tests/integration/chat-migration.test.ts
npm run test:unit -- tests/unit/chat-language-service.test.ts && npm run test:integration -- tests/integration/chat-language-identity.test.ts
npm run typecheck
Use 06-VALIDATION.md for source coverage and phase-level checks. PostgreSQL suites may exceed 60 seconds during container startup; stream progress rather than replace real database proof with mocks.
</verification>

<success_criteria>
All must-have truths have direct behavioral evidence; task commands pass, English behavior remains supported, and no high/critical threat is left open within scope.
</success_criteria>

<output>
Create `.planning/phases/06-localization-foundation-and-ukrainian-onboarding/06-06-SUMMARY.md` with implementation and verification evidence.
</output>
