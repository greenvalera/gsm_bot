---
phase: 07-ukrainian-planning-and-lifecycle
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/shared/i18n/planning-format.ts
  - src/telegram/planning-renderers.ts
  - src/telegram/planning-handlers.ts
  - tests/unit/planning-format.test.ts
  - tests/integration/localized-planning.e2e.test.ts
  - src/shared/i18n/en.ts
  - src/shared/i18n/uk.ts
  - tests/unit/i18n.test.ts
  - tests/unit/planning-availability-card.test.ts
autonomous: true
requirements: [LFMT-01, LFMT-02, LANG-06]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Ukrainian headings use full weekday and genitive month names without a year, and compact buttons preserve their row/marker layout (D-09, D-11)."
    - "Date rendering preserves the civil date at month/year/leap-day/DST boundaries and uses the round's authoritative timezone (LFMT-01 boundary)."
    - "Integer minutes remain exact: 00:00, 23:59 and valid day-end ranges have no rounding or host-timezone shift (LFMT-01 precision)."
    - "Counts and durations cover 0, 1, 2, 5, 11, 14, 21, 22, 25, 101 and 111 with correct English/Ukrainian forms (LFMT-02 boundary)."
    - "Duration decomposition uses exact quotient/remainder in minutes, with cases 59, 60, 61, 90, 119, 120, 121; input validation and stored values stay unchanged (LFMT-02 precision)."
    - "The rehearsal summary shows a 24-hour range without repeating duration in either locale; separately displayed durations use natural hours/minutes (D-10, D-12)."
  artifacts:
    - path: src/shared/i18n/planning-format.ts
      provides: "Shared explicit-locale civil-date and duration formatting"
    - path: tests/unit/planning-format.test.ts
      provides: "Executable bilingual behavior regressions for this plan"
  key_links:
    - from: src/telegram/planning-renderers.ts
      to: src/shared/i18n/planning-format.ts
      via: "civil date and integer-minute formatting"
    - from: src/telegram/planning-handlers.ts
      to: src/telegram/planning-renderers.ts
      via: "one explicit current locale per complete render"
---

<objective>
Calendar, time-range and duration presentation.
Purpose: Deliver the Phase 7 full interactive Ukrainian rehearsal workflow without changing its durable planning, authorization or scheduling semantics.
Output: src/shared/i18n/planning-format.ts, tests/unit/planning-format.test.ts and the production presentation wiring described below.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-CONTEXT.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-PATTERNS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-VALIDATION.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-SOURCE-AUDIT.md
</context>

## Artifacts this phase produces

- New formatPlanningDate, formatPlanningDayButton and formatPlanningDuration exports in src/shared/i18n/planning-format.ts; pure formatPlanningTimeRange in planning-renderers.ts consumes already-resolved wall minutes; explicit Locale parameters on existing rendering seams.
- src/shared/i18n/planning-format.ts
- tests/unit/planning-format.test.ts

## Execution constraints

Research was explicitly skipped by the user. These instructions derive from repository patterns and locked decisions; no RESEARCH artifact or external research is assumed. Existing dependencies and schema are sufficient. Preserve unrelated working-tree edits. Scope is interactive presentation; actual reminder delivery and full outbound/runtime inventory stay in Phase 8. English documentation and existing English behavior remain supported. One polling process and current chat serialization remain deployment assumptions.

Calibration factor 1, sample count 0, confidence low. 3 tasks; no task modifies more than five files. Shared catalogs/renderers/handlers require this sequential wave ordering. New tests are written first in their owning task; no separate unimplemented scaffold is accepted as completion.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render real planning dates through a shared civil-date formatter</name>
  <reversibility rating="costly">Explicit locale parameters cross renderer/helper call sites; retaining English defaults keeps existing integrations compatible.</reversibility>
  <read_first>src/infrastructure/time/civil.ts, src/telegram/presentation-locale.ts, src/telegram/planning-renderers.ts, src/telegram/planning-handlers.ts</read_first>
  <files>src/shared/i18n/planning-format.ts, src/telegram/planning-renderers.ts, src/telegram/planning-handlers.ts, tests/unit/planning-format.test.ts, tests/integration/localized-planning.e2e.test.ts</files>
  <behavior>2026-09-21 becomes Понеділок, 21 вересня; compact day buttons use Пн 21 with existing glyphs. Dec/Jan dates never display a year. English keeps its current compact heading style.</behavior>
  <action>Create explicit-locale formatPlanningDate(locale, CivilDate) and formatPlanningDayButton(locale, CivilDate) in the shared i18n formatting module. Keep calendar lexicons in that shared module; reuse weekdayOf and parseCivilDate rather than host-local Date parsing. The Ukrainian weekday is full/capitalized and the month genitive, per D-09 and D-11. Add optional trailing Locale = en parameters to dayHeadingLabel, dayButtonLabel and the planning renderer entry points, preserving existing call compatibility. Resolve current preference at renderStep and direct interactive render boundaries and thread it through their helper chains, preserving status-before-step, the shared availability projection and explicit card-kind arguments. Format buttons from the same civil date that targets their existing tokens. Test all weekdays/months, leap day, Dec 31/Jan 1 and two host TZ settings; the composed bot's next real day-card render must use the persisted locale.</action>
  <acceptance_criteria>2026-09-21 becomes Понеділок, 21 вересня; compact day buttons use Пн 21 with existing glyphs. Dec/Jan dates never display a year. English keeps its current compact heading style. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/unit/planning-format.test.ts</automated>
  </verify>
  <done>Both locale formatters are pure, full Ukrainian dates/compact buttons agree with token dates, and the composed day-card date assertion passes without changes to date selection or timezone authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use grammatical hours and minutes wherever duration is separate</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <read_first>src/shared/i18n/index.ts, src/telegram/renderers.ts, tests/unit/i18n.test.ts</read_first>
  <files>src/shared/i18n/planning-format.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, tests/unit/planning-format.test.ts, tests/unit/i18n.test.ts</files>
  <behavior>Ukrainian outputs include 2 години and 1 година 30 хвилин; zero is 0 хвилин. English singular/plural and mixed-hour output are correct. Every specified count boundary is tested for both hour/minute units independently.</behavior>
  <action>Add formatPlanningDuration(locale, minutes) and a pure unit-form selection seam used by it. Use integer quotient/remainder, omit zero components except total zero, and handle Ukrainian one/few/many teen exceptions and English one/other. Make existing duration.value entries delegate to the helper so setup/settings separate-duration displays also meet Phase 7 acceptance, per D-10. Preserve the minutes parameter contract and positive-whole-minute input semantics; this is display-only. Update existing explicit duration output expectations to the new approved natural forms and add table-driven boundary tests for 0, 1, 2, 5, 11, 14, 21, 22, 25, 101, 111 for both units plus 59/60/61/90/119/120/121-minute totals. Keep the helper independent of catalog imports to avoid a catalog initialization cycle.</action>
  <acceptance_criteria>Ukrainian outputs include 2 години and 1 година 30 хвилин; zero is 0 хвилин. English singular/plural and mixed-hour output are correct. Every specified count boundary is tested for both hour/minute units independently. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/unit/planning-format.test.ts tests/unit/i18n.test.ts</automated>
  </verify>
  <done>Separate duration surfaces use exact natural language, both catalogs retain the same typed key contract, and count/unit boundary tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Show authoritative 24-hour rehearsal ranges</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <read_first>src/domain/planning/planning-service.ts, src/telegram/planning-renderers.ts, src/domain/chat/schedule-validator.ts</read_first>
  <files>src/telegram/planning-renderers.ts, src/telegram/planning-handlers.ts, tests/unit/planning-format.test.ts, tests/unit/planning-availability-card.test.ts, tests/integration/localized-planning.e2e.test.ts</files>
  <behavior>A Ukrainian 19:00 start with 120-minute duration renders 19:00–21:00, with no adjacent repeated duration. Later chat timezone changes do not relabel the stored rehearsal or mutate startsAt/endsAt.</behavior>
  <action>Apply D-12 to confirmed/booked rehearsal summaries in both locales: render the 24-hour range alone instead of repeating duration beside it. Retain other existing English wording unless grammar correction is directly required by LFMT-02. Implement formatPlanningTimeRange as a pure renderer helper accepting resolved start/end wall minutes and using formatLocalTime, including the existing day-end convention. In planning-handlers, derive the end minute from stored endsAt through civilNow using the round's timezone for committed rounds, and from the existing selected-slot resolution plus duration for review; do not assume adding elapsed minutes to a civil clock crosses DST correctly. Pass a presentation-only range through the affected pure renderer entry points without changing database projections or domain state. Where duration is independently displayed, call the D-10 helper. Test minute edges, a range crossing a DST change, and a confirmed round followed by a chat-timezone change; snapshot authoritative dates, timestamps and selections. Render pure fixtures with explicit resolved range inputs and update existing English summary assertions only for this authorized D-12 change.</action>
  <acceptance_criteria>A Ukrainian 19:00 start with 120-minute duration renders 19:00–21:00, with no adjacent repeated duration. Later chat timezone changes do not relabel the stored rehearsal or mutate startsAt/endsAt. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/unit/planning-format.test.ts tests/unit/planning-availability-card.test.ts</automated>
  </verify>
  <done>Ukrainian summaries show the correct 24-hour range, no duplicate duration, and date/time authority survives later settings changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update to planning authority | Callback actor/chat/token are untrusted; locale does not authorize the action. |
| Stored identities to Telegram output | Names may contain HTML metacharacters and long Unicode data; cards and private alerts have different escaping rules. |
| Committed state to external delivery | A translated payload must not create a new transition or notification entitlement. |

## STRIDE Threat Register

ASVS Level 1; unresolved high or critical findings block completion.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-02-01 | Elevation of privilege | planning callback/command paths | high | mitigate | Keep fresh role, chat/token and action-specific eligibility checks; test localized denials and unchanged transition/consumption state. Never use preference fallback or labels as authority. |
| T-07-02-02 | Information disclosure / Tampering | dynamic member labels in presentation | medium | mitigate | Use localizedMemberLabel once for HTML and localizedPlainMemberLabel plus 200 UTF-16-unit safe truncation for alerts; preserve masked fallback IDs and test hostile names. |
| T-07-02-03 | Tampering / Repudiation | no-op rendering and recovery | high | mitigate | Preserve revision/consumed-token checks, full text-and-keyboard fingerprints and durable announcement claims; distinguish uncommitted failure from committed/uncertain delivery in regression assertions. |
</threat_model>

<verification>
Run each task's named checks, then npm run build and the affected unit/integration regression set in 07-VALIDATION.md. Do not silently skip PostgreSQL coverage or replace it with catalog snapshots. No implementation tests were run during planning; record actual commands/results in the summary during execution.
</verification>

<success_criteria>
All must_haves truths and task acceptance criteria are demonstrated in both locales where applicable; current authority, domain transitions, timezone meaning and acknowledgement behavior remain intact. No high/critical threat is left unresolved. 
</success_criteria>

<output>
Create .planning/phases/07-ukrainian-planning-and-lifecycle/07-02-SUMMARY.md with changes, exact verification evidence, decision coverage and any flagged assumptions when done.
</output>
