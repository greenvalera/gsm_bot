---
phase: 07-ukrainian-planning-and-lifecycle
plan: "05"
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - src/shared/i18n/index.ts
  - src/shared/i18n/en.ts
  - src/shared/i18n/uk.ts
  - src/telegram/planning-handlers.ts
  - tests/unit/localized-planning-feedback.test.ts
  - src/telegram/callbacks.ts
  - src/telegram/handlers.ts
  - src/telegram/roster-renderers.ts
  - tests/integration/localized-planning-feedback.e2e.test.ts
  - tests/unit/onboarding-feedback.test.ts
  - tests/unit/callback-authority.test.ts
  - tests/unit/i18n.test.ts
autonomous: true
requirements: [TEXT-04, LANG-06]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Existing interactive instructions, validation, denials, stale and duplicate replies use the selected group language with unchanged command destinations (D-13 through D-16)."
    - "Adjacent/equal stale-expiry and consumed-token cases retain existing result precedence and one acknowledgement (TEXT-04 adjacency)."
    - "Missing chat/actor/token/round/identity and unavailable membership evidence follow their existing fail-closed routes with localized reachable feedback (TEXT-04 empty)."
    - "Current-role/token/route decisions retain their existing order and branch-owned single acknowledgement under multiple failure predicates (TEXT-04 ordering)."
  artifacts:
    - path: tests/unit/localized-planning-feedback.test.ts
      provides: "Executable bilingual behavior regressions for this plan"
    - path: tests/integration/localized-planning-feedback.e2e.test.ts
      provides: "Executable bilingual behavior regressions for this plan"
  key_links:
    - from: src/telegram/callbacks.ts
      to: src/shared/i18n/index.ts
      via: "route feedback descriptors resolve current locale"
    - from: src/telegram/handlers.ts
      to: src/telegram/planning-handlers.ts
      via: "command-boundary refusals use the same semantic translation contracts"
---

<objective>
Complete localized guidance, denial and stale feedback boundaries.
Purpose: Deliver the Phase 7 full interactive Ukrainian rehearsal workflow without changing its durable planning, authorization or scheduling semantics.
Output: tests/unit/localized-planning-feedback.test.ts, tests/integration/localized-planning-feedback.e2e.test.ts and the production presentation wiring described below.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-CONTEXT.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-PATTERNS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-VALIDATION.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-SOURCE-AUDIT.md
</context>

## Artifacts this phase produces

- Typed planning.feedback.* keys and Locale-aware planningNotAuthorText; key descriptors replace literal planning route feedback.
- tests/unit/localized-planning-feedback.test.ts
- tests/integration/localized-planning-feedback.e2e.test.ts

## Execution constraints

Research was explicitly skipped by the user. These instructions derive from repository patterns and locked decisions; no RESEARCH artifact or external research is assumed. Existing dependencies and schema are sufficient. Preserve unrelated working-tree edits. Scope is interactive presentation; actual reminder delivery and full outbound/runtime inventory stay in Phase 8. English documentation and existing English behavior remain supported. One polling process and current chat serialization remain deployment assumptions.

Calibration factor 1, sample count 0, confidence low. 3 tasks; no task modifies more than five files. Shared catalogs/renderers/handlers require this sequential wave ordering. New tests are written first in their owning task; no separate unimplemented scaffold is accepted as completion.

Scope justification: retain three tasks across 12 unique files for the single interactive-feedback concern. Task 1 modifies planning.feedback catalog entries and outbound literal/refusal expressions in planning-handlers, plus one focused test (five files). Task 2 changes only planningCallbackRoute feedback descriptors, handlers' interactive refusal/help/error sends, planningNotAuthorText and its plain-member-label seam, plus that same test (five files). Task 3 adds the composed boundary matrix and focused shared-helper/parity regressions (four test files). In the large handler modules preserve control flow, authorization order and state transitions; edits are bounded presentation expressions and locale parameters at the named seams. The file count includes three catalogs and five test files, not independent subsystems; splitting would duplicate authority/outcome context and fragment the complete boundary matrix.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Localize all semantic planning feedback and existing instructions</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <read_first>src/telegram/planning-handlers.ts, src/telegram/handlers.ts, tests/unit/planning-ownership.test.ts</read_first>
  <files>src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, src/telegram/planning-handlers.ts, tests/unit/localized-planning-feedback.test.ts</files>
  <behavior>Each action denial describes its real allowed roles. A superseded round points to /plan_status with exact D-14 copy; generic expired/no-round/cancelled/booked facts remain distinct.</behavior>
  <action>Inventory every outbound literal/constant and dynamic refusal in planning-handlers, including command replies and callback alert branches. Add typed semantic planning.feedback.* keys and replace call sites, retaining exported English constants as compatibility aliases if consumed by tests. Include not configured, policy denial, status/nonmember denial, no active round, week taken/no free week, start/save failure, day/time past, DST nonexistent slot, empty roster, takeover inactivity/admin refusal, not participant, already booked, lost unanimity, lifecycle ineligibility and owner-only feedback. D-13 names only the actual allowed roles; D-14 superseded copy is Планування вже змінилося. Поточний стан — /plan_status. D-15 safe retry and D-16 duplicate handling reuse Plan 01's semantic keys only where their result classifications apply. Keep /setup, /settings, /roster_add, /plan and /plan_status destinations truthful. Translate whole phrases, preserve English contracts and structured logging reasons.</action>
  <acceptance_criteria>Each action denial describes its real allowed roles. A superseded round points to /plan_status with exact D-14 copy; generic expired/no-round/cancelled/booked facts remain distinct. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/unit/localized-planning-feedback.test.ts</automated>
  </verify>
  <done>Every existing planning feedback branch has a bilingual semantic contract, including distinction among superseded, cancelled, expired and definitely failed actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close command and callback refusals before planning dispatch</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <read_first>src/telegram/callbacks.ts, src/telegram/handlers.ts, src/telegram/roster-renderers.ts, tests/unit/callback-authority.test.ts</read_first>
  <files>src/telegram/callbacks.ts, src/telegram/handlers.ts, src/telegram/roster-renderers.ts, src/telegram/planning-handlers.ts, tests/unit/localized-planning-feedback.test.ts</files>
  <behavior>Planning denials emitted before dispatch are Ukrainian. Owner alerts remain plain text, within 200 UTF-16 units and free of split surrogate pairs; HTML cards remain escaped exactly once.</behavior>
  <action>Replace planningCallbackRoute's literal stale/nonMember feedback with typed key descriptors consumed by existing resolveFeedback, preserving route-resolved authority and actor binding. Localize planning-related command-boundary membership/access refusals in handlers before planning is entered. Inventory existing help/instruction and recoverable error sends in those two boundary files and route their remaining interactive copy through the semantic keys already created; reuse onboarding catalog contracts for onboarding-owned paths. Parameterize planningNotAuthorText with Locale and localizedPlainMemberLabel from Plan 03; calculate the name budget from the actual selected-language full phrase and preserve safe Unicode truncation, masked IDs and plain alert rendering. Keep role lookup/parse/load/dispatch order, the single-shot ack guard and the non-destructive planning authorization accessor. Missing membership evidence remains fail closed; language fallback never grants authority.</action>
  <acceptance_criteria>Planning denials emitted before dispatch are Ukrainian. Owner alerts remain plain text, within 200 UTF-16 units and free of split surrogate pairs; HTML cards remain escaped exactly once. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/unit/localized-planning-feedback.test.ts tests/unit/callback-authority.test.ts tests/unit/planning-ownership.test.ts</automated>
  </verify>
  <done>Both command and callback boundaries deliver localized role-accurate feedback without weakening authorization, identity protection or acknowledgement behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove feedback reachability and all result precedence edges</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <precondition>The existing tests/helpers/postgres.ts fixture can start migrated PostgreSQL using a reachable Docker engine; assert docker info succeeds before the integration run.</precondition>
  <read_first>tests/integration/localized-onboarding.e2e.test.ts, tests/integration/planning-recovery.test.ts, tests/unit/localized-planning-feedback.test.ts</read_first>
  <files>tests/integration/localized-planning-feedback.e2e.test.ts, tests/unit/onboarding-feedback.test.ts, tests/unit/callback-authority.test.ts, tests/unit/i18n.test.ts</files>
  <behavior>Every cataloged planning feedback class is observed through its real route. Expiry equality, simultaneous stale/denied predicates, empty context and repeated action each select one truthful result and acknowledgement.</behavior>
  <action>Create a composed bilingual boundary matrix with real token rows and injected fresh roles for member, outsider, demoted admin and unavailable membership gateway. Exercise /plan, /plan_status, /plan_cancel and /plan_change denials, malformed/unknown/wrong-chat/wrong-actor/expired/consumed controls, empty roster, no round and superseded/cancelled/booked outcomes. Pin expiry at threshold minus one, equal and plus one; assert existing precedence when more than one predicate fails, with no extra domain writes or draft deletion from planning denial. Check owner missing-label and long astral/HTML-sensitive names. Assert one delivered answer per callback ID including silent fallback; test command feedback as group replies. Extend catalog parity samples and onboarding checks only to protect shared helper changes. Record any new cross-feature guidance omission in the summary; all Phase 7 interactive omissions must be fixed before completion, while reminder-worker messages remain Phase 8.</action>
  <acceptance_criteria>Every cataloged planning feedback class is observed through its real route. Expiry equality, simultaneous stale/denied predicates, empty context and repeated action each select one truthful result and acknowledgement. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- tests/integration/localized-planning-feedback.e2e.test.ts</automated>
  </verify>
  <done>Bilingual feedback is reachable across the real dispatcher, exact branch ordering and expiry edges are verified, and no unauthorized action or duplicate acknowledgement occurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update to planning authority | Callback actor/chat/token are untrusted; locale does not authorize the action. |
| Stored identities to Telegram output | Names may contain HTML metacharacters and long Unicode data; cards and private alerts have different escaping rules. |
| Committed state to external delivery | A translated payload must not create a new transition or notification entitlement. |

## STRIDE Threat Register

ASVS Level 1; unresolved high or critical findings block completion.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-05-01 | Elevation of privilege | planning callback/command paths | high | mitigate | Keep fresh role, chat/token and action-specific eligibility checks; test localized denials and unchanged transition/consumption state. Never use preference fallback or labels as authority. |
| T-07-05-02 | Information disclosure / Tampering | dynamic member labels in presentation | medium | mitigate | Use localizedMemberLabel once for HTML and localizedPlainMemberLabel plus 200 UTF-16-unit safe truncation for alerts; preserve masked fallback IDs and test hostile names. |
| T-07-05-03 | Tampering / Repudiation | no-op rendering and recovery | high | mitigate | Preserve revision/consumed-token checks, full text-and-keyboard fingerprints and durable announcement claims; distinguish uncommitted failure from committed/uncertain delivery in regression assertions. |
</threat_model>

<verification>
Run each task's named checks, then npm run build and the affected unit/integration regression set in 07-VALIDATION.md. Do not silently skip PostgreSQL coverage or replace it with catalog snapshots. No implementation tests were run during planning; record actual commands/results in the summary during execution.
</verification>

<success_criteria>
All must_haves truths and task acceptance criteria are demonstrated in both locales where applicable; current authority, domain transitions, timezone meaning and acknowledgement behavior remain intact. No high/critical threat is left unresolved. 
</success_criteria>

<output>
Create .planning/phases/07-ukrainian-planning-and-lifecycle/07-05-SUMMARY.md with changes, exact verification evidence, decision coverage and any flagged assumptions when done.
</output>
