---
phase: 07-ukrainian-planning-and-lifecycle
plan: "03"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/shared/i18n/index.ts
  - src/shared/i18n/en.ts
  - src/shared/i18n/uk.ts
  - src/telegram/planning-renderers.ts
  - tests/unit/localized-planning-cards.test.ts
  - src/telegram/keyboards.ts
  - src/telegram/planning-handlers.ts
  - src/telegram/roster-renderers.ts
  - tests/unit/planning-keyboards.test.ts
  - tests/unit/i18n.test.ts
  - tests/integration/localized-planning.e2e.test.ts
autonomous: true
requirements: [TEXT-02, LFMT-02, LANG-06]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Day/time/review/availability messages and their controls use the same current language, including owner, legends and counts (D-01 through D-04)."
    - "Equal default/previous/chosen markers retain established precedence and adjacent time slots stay separate (TEXT-02 adjacency)."
    - "Empty/single-member rosters and empty slot projections render truthful localized states without granting a confirm or answer capability (TEXT-02 empty)."
    - "Equal display names retain stable roster tie ordering and availability participants retain snapshot order across locale changes (TEXT-02 ordering)."
  artifacts:
    - path: tests/unit/localized-planning-cards.test.ts
      provides: "Executable bilingual behavior regressions for this plan"
  key_links:
    - from: src/telegram/planning-renderers.ts
      to: src/shared/i18n/index.ts
      via: "typed whole planning phrases and localizedMemberLabel"
    - from: src/telegram/keyboards.ts
      to: src/telegram/planning-renderers.ts
      via: "locale-aware action rows with unchanged opaque tokens"
  prohibitions:
    - statement: "Participant status and blocked-slot copy must not shame or blame people for being unavailable."
      status: unresolved
      reason: "Flagged-unverified spec-less fallback; end-of-phase wording judgment is pending."
---

<objective>
Complete Ukrainian day, time, review and availability cards.
Purpose: Deliver the Phase 7 full interactive Ukrainian rehearsal workflow without changing its durable planning, authorization or scheduling semantics.
Output: tests/unit/localized-planning-cards.test.ts and the production presentation wiring described below.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-CONTEXT.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-PATTERNS.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-VALIDATION.md
@.planning/phases/07-ukrainian-planning-and-lifecycle/07-SOURCE-AUDIT.md
</context>

## Artifacts this phase produces

- Typed planning.* card/control keys, locale-aware planning row factories and exported localizedPlainMemberLabel; existing English compatibility APIs remain usable.
- tests/unit/localized-planning-cards.test.ts

## Execution constraints

Research was explicitly skipped by the user. These instructions derive from repository patterns and locked decisions; no RESEARCH artifact or external research is assumed. Existing dependencies and schema are sufficient. Preserve unrelated working-tree edits. Scope is interactive presentation; actual reminder delivery and full outbound/runtime inventory stay in Phase 8. English documentation and existing English behavior remain supported. One polling process and current chat serialization remain deployment assumptions.

Calibration factor 1, sample count 0, confidence low. 3 tasks; no task modifies more than five files. Shared catalogs/renderers/handlers require this sequential wave ordering. New tests are written first in their owning task; no separate unimplemented scaffold is accepted as completion.

Scope justification: retain three tasks across 11 unique files for this single planning-card presentation concern. Task 1 owns only planning card/control catalog keys and named renderer helpers (five files); Task 2 consumes those keys and changes only row factories, their renderer calls and focused keyboard tests (three files); Task 3 closes named planning dispatch/member-label seams and composed/parity tests (four files). The large renderer and handler modules receive bounded function edits, not whole-module refactors. Catalog files and focused tests account for most of the file count; splitting would add catalog/interface handoffs without reducing implementation scope.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Translate planning projections and participant state text</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <read_first>src/telegram/planning-renderers.ts, src/telegram/roster-renderers.ts, tests/unit/planning-day-card.test.ts, tests/unit/planning-time-card.test.ts, tests/unit/planning-availability-card.test.ts</read_first>
  <files>src/shared/i18n/index.ts, src/shared/i18n/en.ts, src/shared/i18n/uk.ts, src/telegram/planning-renderers.ts, tests/unit/localized-planning-cards.test.ts</files>
  <behavior>All four card kinds have explicit en/uk output, correct current-locale owner and status text, and safe dynamic names. Empty/single/multiple projections and equal-name sorting preserve domain facts. Create the typed en/uk control keys for Task 2 in this task's three catalog files: D-01 answer labels, D-04 takeover, Back, Confirm rehearsal and Replan; Task 2 consumes these keys without further catalog edits.</behavior>
  <action>Add typed planning.* whole-phrase keys for day/time headings and prompts, empty time-window guidance, used-marker legends, chosen legend, review lineup/count/instructions, availability counts/outcomes and booked/cancelled summaries. Use shared parameters such as value, label and total where semantically appropriate and extend catalog parity samples in Task 3 for any new fields. Replace planningOwnerLine, legendLine/legendFor/timeLegendFor, lineupLines, renderDayStep, renderTimeStep, renderReviewStep and renderAvailabilityCard text with explicit-locale rendering. Per D-02 statuses are Очікуємо відповідь / Може / Не може with their existing glyphs; per D-03 owner text is Організатор: Ім’я using localizedMemberLabel exactly once. Retain existing English phrases and participant order. Translate collecting/blocked/all-available facts from the domain outcome, not a newly derived state machine. Test empty slots/roster, one member, equal labels, marker ties, adjacent slots and count boundaries in both locales.</action>
  <acceptance_criteria>All four card kinds have explicit en/uk output, correct current-locale owner and status text, and safe dynamic names. Empty/single/multiple projections and equal-name sorting preserve domain facts. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/unit/localized-planning-cards.test.ts</automated>
  </verify>
  <done>Pure planning cards cover every bot-owned sentence and legend in Ukrainian with accurate state/counts, unchanged identities and stable ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Put Ukrainian labels on live planning controls</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <read_first>src/telegram/keyboards.ts, src/telegram/planning-renderers.ts, tests/unit/planning-keyboards.test.ts</read_first>
  <files>src/telegram/keyboards.ts, src/telegram/planning-renderers.ts, tests/unit/planning-keyboards.test.ts</files>
  <behavior>Serialized Ukrainian answer buttons read 👍 Можу / 👎 Не можу and takeover reads Стати організатором. Action tokens, omitted controls and row splits are unchanged.</behavior>
  <action>First extend planning-keyboards.test.ts with failing en/uk assertions for exact answer/takeover labels, identical action-token bindings, missing-token omission, [4,3] day rows, [3,3,3,1] time rows and 64-byte callback_data. Consume the planning control keys created in Task 1. Add locale-aware planning row factories following existing setup factory conventions; keep existing exported English constants as compatibility aliases. Wire factories into day/time/review/availability renderer call sites so the locale already threaded in Plan 02 produces real translated keyboards. Per D-01 use Можу / Не можу after the existing marker glyphs, and per D-04 use Стати організатором only when a valid takeover token exists. Localize Back, Confirm rehearsal and Replan in the agreed informal voice. Keep PlanningControlAction strings, controlTokenFor lookup, row omission and marker precedence. Never derive callback targets from display labels. Make the new focused assertions pass in this task; broader composed navigation remains Task 3.</action>
  <acceptance_criteria>Serialized Ukrainian answer buttons read 👍 Можу / 👎 Не можу and takeover reads Стати організатором. Action tokens, omitted controls and row splits are unchanged. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- tests/unit/planning-keyboards.test.ts tests/unit/localized-planning-cards.test.ts</automated>
  </verify>
  <done>An ordinary authorized card update renders matching Ukrainian text and buttons, with unchanged action values and keyboard geometry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close planning dispatch, identity and count integration</name>
  <reversibility rating="reversible">Presentation changes retain the existing durable and callback contracts.</reversibility>
  <precondition>The existing tests/helpers/postgres.ts fixture can start migrated PostgreSQL using a reachable Docker engine; assert docker info succeeds before the integration run.</precondition>
  <read_first>src/telegram/planning-handlers.ts, src/telegram/roster-renderers.ts, tests/unit/planning-ownership.test.ts</read_first>
  <files>src/telegram/planning-handlers.ts, src/telegram/roster-renderers.ts, tests/unit/i18n.test.ts, tests/integration/localized-planning.e2e.test.ts</files>
  <behavior>The real bot completes day -> time -> review -> availability in both locales. Unknown identities are masked and localized, names are escaped once, counts use correct forms and every callback is acknowledged once.</behavior>
  <action>Finish explicit locale threading through renderStep, confirm's direct availability render, replaceAnchor/repostAnchor and their active-card helper calls. Expose the existing localizedPlainMemberLabel seam for later plain alerts while retaining its English compatibility wrapper and masking; use HTML-safe localizedMemberLabel only on cards. Add composed bilingual command/callback navigation using actual server-side tokens and assert D-01 through D-04, count/legend output, same selected date/time and real participant snapshots. Extend i18n parity sample data for every newly added parameter; retain compile-time misuse tests. Task 2 already owns focused keyboard labels, missing tokens, row layouts and callback-byte assertions. Verify long Unicode/HTML-sensitive names, identical labels, no double escaping and the existing message budget.</action>
  <acceptance_criteria>The real bot completes day -> time -> review -> availability in both locales. Unknown identities are masked and localized, names are escaped once, counts use correct forms and every callback is acknowledged once. Test assertions inspect both outbound payloads and relevant durable state where the task crosses transport.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- tests/integration/localized-planning.e2e.test.ts -t "planning workflow"</automated>
  </verify>
  <done>The real Ukrainian planning/availability journey and English control pass with real database state, safe identities, and working unchanged callbacks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Telegram update to planning authority | Callback actor/chat/token are untrusted; locale does not authorize the action. |
| Stored identities to Telegram output | Names may contain HTML metacharacters and long Unicode data; cards and private alerts have different escaping rules. |
| Committed state to external delivery | A translated payload must not create a new transition or notification entitlement. |

## STRIDE Threat Register

ASVS Level 1; unresolved high or critical findings block completion.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-03-01 | Elevation of privilege | planning callback/command paths | high | mitigate | Keep fresh role, chat/token and action-specific eligibility checks; test localized denials and unchanged transition/consumption state. Never use preference fallback or labels as authority. |
| T-07-03-02 | Information disclosure / Tampering | dynamic member labels in presentation | medium | mitigate | Use localizedMemberLabel once for HTML and localizedPlainMemberLabel plus 200 UTF-16-unit safe truncation for alerts; preserve masked fallback IDs and test hostile names. |
| T-07-03-03 | Tampering / Repudiation | no-op rendering and recovery | high | mitigate | Preserve revision/consumed-token checks, full text-and-keyboard fingerprints and durable announcement claims; distinguish uncommitted failure from committed/uncertain delivery in regression assertions. |
</threat_model>

<verification>
Run each task's named checks, then npm run build and the affected unit/integration regression set in 07-VALIDATION.md. Do not silently skip PostgreSQL coverage or replace it with catalog snapshots. No implementation tests were run during planning; record actual commands/results in the summary during execution.
</verification>

<success_criteria>
All must_haves truths and task acceptance criteria are demonstrated in both locales where applicable; current authority, domain transitions, timezone meaning and acknowledgement behavior remain intact. No high/critical threat is left unresolved. 
</success_criteria>

<output>
Create .planning/phases/07-ukrainian-planning-and-lifecycle/07-03-SUMMARY.md with changes, exact verification evidence, decision coverage and any flagged assumptions when done.
</output>
